# PRAGMA 생성·평가·저장 계약 정본

> 상태: 현재 정본. **이 문서는 계약(구조·불변조건·게이트)만 담는다.**
> 식별 규칙: 날짜 없는 이 경로만 현행이며, 날짜 판본은 `history/`의 역사 자료다.
> 현행 신규 생성 구조: `mission_v6` = **MPJ4 + DCT1 + 수정 재확인**. `mission_v5`는 역사적 미니 담화 계약으로 읽기 호환만 유지한다.
> 테스트 수·Edge 버전·해시 값·배포 상태 같은 날짜 종속 사실은
> `docs/handoff/ACTIVE_HANDOFF.md`와 research-trail 증거가 정본이며, 여기에 적지 않는다.
> **마지막 코드 대조: 2026-08-17 · `mission_v6` 통합 작업트리** (모델 라우트·미션 규칙·스키마·저장 계약 대조)
> 델타 재대조 `b2fde89`→`c2def56`: 관할 영역 2커밋 모두 계약 무영향 —
> `canonicalRouting.test.ts` 신설, `coreQualityAudit.ts`에 기존 15축의 한국어 라벨 상수
> 추가(축·판정 로직 불변). 본문 수정 없음
> (`08_작업관리\reviews\2026-08-07_관리자구조_델타재대조_변경안.md`, 박사님 승인)
> 델타 재대조 `35f0129`→`b2fde89`: 통역 역할 계약 3건 반영
> (`08_작업관리\reviews\2026-08-06_정본3종_델타재대조_변경안.md`, 박사님 승인)
> 2026-08-07 개정: §5.4 AI 모델 간 독립 검토(교차 벤더 방식) 신설 + §8·§10 각 1항
> (`08_작업관리\reviews\2026-08-07_4-6-3_교차벤더검토_계약개정_변경안.md`, 박사님 승인).
> 문서 개정만이며 코드·프롬프트 변경 없음 — 위 「마지막 코드 대조」는 그대로 유효하다.
> 2026-08-07(2차) 개정: §5.4 실행 형태를 실행/노출 분리로 재규정(박사님 지시·fable 판정).
> 관리자 화면에 읽기 전용 노출을 허용·요구하되 실행 트리거·DB 반입은 기존 게이트 유지.
> 이 개정에 따라 관리자 열람 화면(skeleton)이 앱 코드로 추가된다.
> 2026-08-09 로컬 개정: main `70d1eff` 기준 HSK 3.0 참고 데이터 provenance와
> 비차단 사후감사 계약을 §8.1에 추가했다. migration·seed·Edge·Railway는 미적용 상태다.
> 적용 범위: 코어 생성, Full Mission 승격, 자동검사, 사람 검수, 저장, provenance, 학습자 수행 로그
> 이전 문서: `history/legacy/PRAGMA_생성계약_v1_2026-07-23.md`는 결정 이력 보존용이며, 현재 동작과 충돌하면 이 문서가 우선한다.

### 2026-08-02 원문 분량 정책 파일럿 보정

`DEC-20260802-05`에 따라 `scenario_core_v3` 원문의 분량 통제를 다음처럼 시험 적용한다.
이 절은 분량 판정에 한해 아래의 이전 문장 수 규칙보다 우선하며, TTS 실측 전 최종 동결은
아니다.

- 정책 버전: `effective_chars_v1`
- 유효 글자: NFC 정규화 뒤 공백·문장부호를 제외한 Unicode 문자·숫자의 수
- R29 저장 차단: 수준·모드별 유효 글자 범위를 벗어난 경우
- R29 경고: 종결부호 기준 2~4문장을 벗어난 경우
- 생성층: 2~4문장 담화 형태와 유효 글자 범위를 함께 지시하고, 둘 중 하나가 어긋나면
  기존 사실을 보존하는 원문 보정을 최대 한 번 수행한다.
- 길이 정책은 클라이언트 입력을 신뢰하지 않고 서버가 `level`·`mode`에서 계산한다.
- 생성물에는 `length_policy={version, unit, min, max, actual}`을 저장한다. 생성 meta에는
  `length_policy_version`과 보정 적용 여부를 저장한다.
- `length_policy`는 정책 provenance이므로 내용 중복 판정용 `content_hash`에서는 제외하지만,
  생성 계열을 식별하는 `prompt_snapshot_hash`에는 정책 버전·범위 전체를 포함한다.

| 수준 | 번역 | 통역 |
|---|---:|---:|
| 입문·중초급 | 45~65자 | 30~45자 |
| 중급 | 60~85자 | 40~60자 |
| 고급 | 80~110자 | 55~85자 |

위 범위는 방향별 별도 규칙을 만들지 않는 파일럿 시작값이다. 통역 본배치 전 방향·수준별
표본의 TTS `provider`·`model`·`voice`·재생 시간을 함께 기록해 범위의 적절성을 확인한다.
값을 바꿀 때는 같은 버전을 덮어쓰지 않고 새 정책 버전과 새 생성 계열을 발급한다.

### 2026-08-02 통역 이중언어 장면·교차언어 인접쌍 보정

`DEC-20260802-06`에 따라 통역 코어는 단순히 `source_modality='spoken'`인 장면이 아니라,
두 언어 화자 사이에 학습자의 통역이 필요한 장면으로 생성한다.

- `situation_ko`에는 source 언어 화자, target 언어 화자, 학습자 통역의 개입을 자연스럽게
  드러낸다. 같은 언어 사용자끼리 통역 없이 대화하는 장면은 R16 저장 차단 대상이다.
- 응답 화행의 `preceding_turn`은 target 언어, `source_text`는 source 언어로 생성한다.
  두 턴의 언어가 다른 것은 통역 인접쌍의 정상 구조이며, 언어를 한쪽으로 통일하지 않는다.
- 선행 발화의 명제·인물·소유·행위 대상과 `source_text`의 응답 화행은 일관되어야 한다.
- 원문 길이·선행 발화 언어·이중언어 장면 중 하나라도 어긋나면 사실·PDR·화행을 보존하는
  통합 보정을 최대 한 번 수행한다. 보정 뒤 세 조건을 모두 통과한 경우에만 보정본을 채택하며,
  그렇지 않으면 기존 R8·R10·R16·R29가 저장을 차단한다.
- 생성 계열은 `core_v8_learner_scene_v1`로 식별한다. 길이 정책은
  `effective_chars_v1`을 유지하고 `policy_ver`는 올리지 않는다.
- 학생용 장면 정보 감량 규칙은 `core_v8` 이후 신규 생성분에 적용하며 구계열 코어를 소급 변경하지 않는다.

### 2026-08-05 통역 참여자·학생용 장면·사건 대응 보정

`DEC-20260805-05`에 따라 통역 장면과 학생용 상황문의 저장 전 경계를 다음처럼 보강한다.

- 통역 셀은 A=source 언어 원발화자, 학습자=통역사, B=target 언어 청자의 **서로 다른 세
  참여자**로 구성한다. 학습자가 A의 화행을 직접 수행하거나 자기 발화를 스스로 통역하면
  R16 fail이다.
- R16은 두 언어와 `통역` 문자열만 찾지 않고, 학습자의 통역 역할·세 참여자 분리·원발화자
  겸임 문형을 함께 검사한다.
- `situation_ko`에 정중성·완화·선택권·강도·명료성처럼 답의 방향을 알려 주는 평가 기준이
  노출되면 R30 fail이다. repair는 관찰 가능한 상대·용건·접촉 이력·실제 부담을 보존하고
  평가 기준 표현만 제거한다.
- 보조 AI 비평은 `participant_roles`, `scene_source_alignment`, `learner_scene`을 추가한
  `core_quality_v5` 15축으로 운용한다. 상황과 원문의 사건·행위자·대상을 항목별로 대조한다.
- 생성 후보는 `pragma_content_candidate_20260805_01`, 생성 계열은
  `core_v9_interpreter_roles_v1` / `core_v9_interpreter_roles_v1_repair_v1`로 식별한다.
  기존 코어·미션을 소급 수정하거나 새 후보와 같은 검수 묶음으로 취급하지 않는다.

### 2026-08-06 통역 역할 계약 확정 (정체성 수렴 — 박사님·Fable·Codex 3자)

495 배치의 통역 표시 117건이 직접 대화 장면으로 생성된 결함(자동검사 미검출)을 계기로,
번역·통역의 학습자 역할을 다음처럼 확정한다. **번역 셀과 번역 프롬프트는 불변이다.**

**역할 비대칭 (의도된 설계 — 결함 아님):**

| 모드 | 학습자 역할 | P·D·R 준거 |
|---|---|---|
| 번역 | 1인칭 발신자 관점의 번역 수행자 (자기 발신 상황의 원문 제약형 번역) | 학습자 ↔ 상대 |
| 통역 | 별도의 통역사 C | 원발화자 A ↔ 청자 B |

**통역 셀 역할 구조 (자연어 검사보다 먼저 구조로 고정):**

```text
A = 원발화자 (source speaker)      — 화행 목적의 소유자
B = 목표 청자 (target addressee)
C = 학습자 통역사 (interpreter only) — 언어적 실현자
pragmatic relation = A ↔ B
```

**구조값으로의 고정 (`context_spec.interpreter_role_contract`):** 위 역할 구조는 상황문의
자연어 표현에 맡기지 않고, 통역 셀에 한해 서버가 다음 필드를 주입해 고정한다.

```ts
interpreter_role_contract: {
  source_speaker:     "A",        // 원발화자
  target_addressee:   "B",        // 목표 청자
  learner_interpreter:"C",        // 학습자 통역사
  pdr_relation:       "A_to_B",   // P·D·R 준거
}
```

네 값은 모두 고정 리터럴이며 다른 값을 가질 수 없다. 번역 셀과 이 계약 이전의 기존
코어에는 이 필드가 없어도 된다(optional). 역할 비대칭이 문장 표현이 아니라 **데이터
구조로** 보장된다는 뜻이며, 자연어 검사는 그 위에 얹히는 2차 방어선이다.

**핵심 문장 (정본):** 통역 셀의 P·D·R은 원발화자 A와 청자 B의 관계를 나타낸다.
학습자 통역사 C는 이 관계의 당사자나 화행 목적의 소유자가 아니며, P·D·R을 근거로
A의 발화를 자의적으로 개선하는 것이 아니라 A의 의미·의도·화용적 힘을 B에게
기능적으로 등가하게 재현한다.

**등가 재현 ≠ 축자역:** 화용적 힘을 목표어로 등가하게 실현하기 위한 **형식 조정은
필수(허용)** 이고, A의 힘·태도·화행 목적의 **변경은 불허**다. 통역 셀에서 학습자의
화용 판단이 놓이는 자리는 이 등가 실현 수단의 선택이다.

**통역 셀 hard fail 조건:**

1. A 또는 B를 `학습자`라고 부름
2. 학습자가 원발화의 화행을 직접 수행함 (`학습자가 직접 요청·사과·불만을 말한다`)
3. 학습자가 목표 청자 역할을 겸함 (감사·사과 등을 받는 당사자)
4. 자기 발화를 자기가 통역함
5. P·D·R을 학습자와 B(또는 학습자와 A)의 관계로 계산함

**서술·문체 규칙:** 상황문은 학습자(통역사) 관점으로 서술하고, 원발화자 A의
1인칭(`저는`·`나는`) 서술을 금지한다. `직접`은 3단 처리 — hard fail(`학습자가 직접
[화행]한다`, `통역 없이 직접 대화한다`) / 경고(`A와 B가 직접 협의한다`류 모호 표현) /
허용(`학습자가 현장에서 직접 통역한다`). 생성 문체에서는 통역 상황문에 `직접`을
쓰지 않는 것을 기본으로 한다.

**검사 층위:** 동사 목록 정규식은 경고 보조 장치다(`듣는다` 자체는 금칙어가 아니다 —
원발화를 듣는 것은 통역사의 본질 행위이고, 화행의 수신 당사자가 되는 것이 결함이다).
결함 판정은 `누가 어떤 화행을 누구에게 하는가`의 논항 구조로 하며, 최종 의미 정합성은
카나리 사람 검수로 확인한다.

**규칙 대응 (저장 전 자동검사):**

| 규칙 | 층위 | 조건 |
|---|---|---|
| R25 | fail | 신규 통역 코어의 `context_spec`에 A/B/C 및 P·D·R=A↔B 역할 계약이 없음 |
| R16 | fail | 통역 셀인데 이중언어 화자·통역 개입 장면이 불명확함 (위 hard fail 5조건) |
| R16 | warning | 통역 역할에 사람 확인이 필요한 경계 사례 |

R16은 **확정 위반은 fail, 판단이 필요한 경계 사례는 warning**으로 2단 운용한다.
warning은 저장을 막지 않고 검수 큐에 표시한다. R25는 구조값 부재를 막으므로
자연어 검사(R16)가 통과하더라도 역할 계약이 없으면 저장되지 않는다.

PRAGMA는 교육 우선형 학습 플랫폼이며, 수업 과정에서 연구 가능한 자료를 남긴다. 생성·평가·저장 계약은 교육 경험을 검사 도구처럼 축소하기 위한 장치가 아니라 다음을 보장하기 위한 기술 계약이다.

- AI 생성물이 사람 검수 없이 학습자에게 공개되지 않는다.
- 상황 적절성, 의미 충실성, 문법·형식 문제를 서로 섞지 않는다.
- 생성물과 학습자 수행의 버전·출처·변경 이력을 식별할 수 있다.
- 현재 자료만으로 교육 효과나 실제 상호작용 능력을 인과적으로 주장하지 않는다.

허용되는 제품·논문 설명은 다음 범위다.

> 한·중 통번역 상황에서 맥락 단서를 해석하고, 화행별 핵심 특징을 판단·조절하여 표현으로 산출하는 능력을 지원하고 기록한다.

핵심 구성개념은 `상황 기반 화용적 의사결정 및 표현 조절 능력`이다. `전반적 화용 능력`이나 실제 상호작용 능력 전체를 측정한다고 주장하지 않는다.

**정체성 문장 (2026-08-06 확정 — 계약·학습자 구조·논문 공통 기준):**

> 1. PRAGMA는 학습자가 상황에 맞는 외국어 화행을 자유롭게 만들어 내도록 하는 도구가
>    아니라, 주어진 원발화의 의미와 의도를 보존하면서 특정 수신자와 상황에 적절한
>    목표어 표현을 판단·산출·수정하도록 지원하는 통번역 학습 워크플로우이다.
> 2. 번역 모드에서 학습자는 발신자 관점에서 주어진 원문을 읽고 목표어 텍스트로
>    옮기고(자기 발신 상황의 원문 제약형 번역), 통역 모드에서 학습자는 별도의
>    통역사로서 원발화자 A의 음성을 듣고 청자 B에게 목표어 발화로 옮긴다. 역할은
>    모드마다 다르지만, 두 모드 모두 원문 제약과 상황 제약을 동시에 만족해야 한다.

일반 L2 산출 교육과의 경계는 **원문 제약**이다 — "이 상황에서 무엇을 말할까"가 아니라
"이 원발화를 어떻게 옮길까"를 묻는다.

세부 정체성은 `docs/research/PRAGMA_PRODUCT_RESEARCH_IDENTITY_2026-07-28.md`를 따른다.

## 2. 데이터 층위

```text
scenario_core_v3
  └─ 선택한 코어만 Full Mission으로 승격
       └─ mission_v6 (MPJ4 + 독립 DCT1 + 수정 재확인)
            └─ 사람 검수 후 reviewed
                 └─ 학습자 수행과 feedback/revision 기록
```

### 2.1 코어

- JSON 스키마: `scenario_core_v3` (v2는 읽기 호환 — 분량 정책 `effective_chars_v1` 도입으로 v3)
- DB 외곽 `content_format='scenario_core_v1'`은 기존 행·RPC 호환을 위해 유지되는 이름이다.
- 코어는 상황, 관계, 출발문, 선행 발화와 요약 메타를 가진다.
- 코어에는 MPJ 문항과 DCT 참고안이 없다.
- 495 본 배치는 코어만 생성한다. 495개를 Full Mission으로 일괄 승격하지 않는다.

### 2.2 Full Mission

- 신규 스키마: `mission_v6` (`Scale4 → FixChoice → FixReview → MultiJudge → 독립 DCT1`)
- 런타임 피드백 schema는 `feedback_v1`을 유지한다.
- 현행 미션·피드백 `prompt_version` 문자열은 promptSnapshot·Prompt Harness가 정본이다.
  이 문서에 버전 문자열을 복사해 두지 않는다.
- 코어를 선택해 개별 승격하며, 생성 직후 상태는 `generated`다.
- `mission_v1/v2`는 legacy MPJ5, `mission_v3/v4`는 MPJ4 기준선, `mission_v5`는 역사적 미니 담화 계약으로 열람·회귀 호환용 보존한다.
- 기존 v1~v5를 v6로 백필하거나 신규 v6와 같은 생성 계열로 취급하지 않는다.

### 2.3 수행 로그

- 학습자 수행은 기존 `learner_mission_logs` 계열을 사용한다.
- 최초 산출, 제공된 피드백, 수정 산출, 완료 상태와 미션 식별자를 연결한다.
- `mission_v6` MPJ 응답은 기존 `context_judgment` JSONB에 `mission_response_v2` 봉투로 저장한다. 별도 DB 컬럼은 추가하지 않는다.
- 봉투에는 `mission_schema_version`, 문항별 선택 trace, 선택적 `production_support`와 `learner_dissent`를 넣는다.
- Scale4는 4점 원응답을 남기되 같은 적절성 방향의 두 응답을 수용한다. FixChoice는 최초 대역 판단과 선택한 교정안 2개를 함께 남긴다. FixReview는 탈락 교정본 ID와 실패 원인 ID를 남긴다.
- MultiJudge는 후보 3개의 분류를 모두 남긴다. 번역의 내용 어휘 힌트는 열람 여부와 최초 열람 시각을 `production_support`에 비채점 trace로 남기며, 통역에는 힌트 trace를 만들지 않는다.
- 이 trace는 응답 재구성과 문항 품질 점검을 위한 비채점 기록이다. AI 참고 판정과의 일치 여부를 학습자 점수로 자동 환산하지 않는다.
- 로그가 존재한다는 이유만으로 연구 이용 동의가 성립하지 않는다.
- 이후 새 로그 필드나 분석 의미 변경은 DB 스키마와 연구 주장 범위를 먼저 보고한다.

## 3. 생성 조건과 표본 층위

### 3.1 연구 구인행렬

```text
speech_act 9 × P 3 × D 3 × R 3 = 243셀
```

- `P`: 상대적 권력
- `D`: 사회적 거리
- `R`: 해당 화행의 상황 부담

P·D·R은 상황 적절성 판단의 조건이다. 직접성과 공손성을 하나의 축으로 합치지 않는다.

### 3.2 전달 커버리지

```text
speech_act 9 × learner_level 3 × task_mode 2 = 54셀
```

- `learner_level`: 입문·중급·고급의 교육 지원 수준
- `task_mode`: `translation | stt_interpreting`

54셀은 교육용 전달 커버리지이며 243셀과 같은 연구 구인행렬이 아니다. PRAGMA 수준을 CEFR·ACTFL 등급에 직접 대응시키지 않는다.

### 3.3 편성·사건 메타

- `domain`: 일상·학업·직장
- `theme`: 강좌 편성 영역
- `topic`: 구체적 사건 시드
- `industry`: 직장 장면의 분야 메타
- `business_function`: 직장 장면에서 수행하는 실제 업무 기능 메타
- `context_spec`: P·D·R과 사건 시드의 내부 호환성 해결층

이 값들은 필요하지만 P·D·R과 같은 독립 화용 구인으로 과장하지 않는다.
도메인은 일상·학업·직장 3분류를 유지하며 `전문·학술`을 네 번째 도메인으로 만들지 않는다.
전문성은 수준·산업·직무·theme/topic의 조합으로 표현한다. 별도 근거가 생기기 전 배치 계획의
수준별 일상·학업·직장 주변분포는 1:1:1을 유지한다.

## 4. 화행과 target feature

교육용 화행은 9종이다.

- 요청
- 거절
- 감사
- 사과
- 제안
- 초대
- 반대
- 칭찬
- 직접 불만

9화행 모두 사람이 작성한 기본 `target_feature`를 가진다. 칭찬하기와 칭찬 대응은 교육적으로 구분된 feature로 보존하되, 현재 `compliment` 코어의 자동 승격 기본값은 칭찬하기 feature다.

계약 원칙:

- `targetFeatures.ts`의 코드·version·band schema가 실행 정본이다.
- AI가 target feature, band code, learner label을 임의로 만들지 않는다.
- 모든 band는 인간 캘리브레이션 전 `proposed` 지위다.
- 표현 자원은 가능한 실현 수단이지 고정 정답 목록이 아니다.
- 한 화행에 복수의 기능적 head와 supportive move가 함께 나타날 수 있다.
- 상황 적절성, 직접성·공손성, 화행별 target feature를 분리해 해석한다.

### 4.1 `usable_facts` 폐쇄 목록

명제적 supportive move는 코어의 `usable_facts` 또는 출발문에 있는 사실만 사용할 수 있다.

- 최대 8개
- 목록 밖의 이유, 대안, 수리, 보상, 재발 방지 약속, 새 일정 추가 금지
- 사실의 유무를 MPJ 정답 단서로 사용 금지
- 관습화된 사과·인사, 완화어, 호칭 같은 비명제적 자원은 폐쇄 목록 대상이 아니다.

## 5. 코어 생성 계약

### 5.1 모델과 응답 형식

- 모델: `gpt-4.1-mini`
- **연구 콘텐츠(코어·미션·비평)에는 모델 교체 폴백이 없다** — 실패를 숨기고 다른 모델로
  강등하지 않는다(2026-08-04 제거). 학습자 런타임 피드백만 가용성 폴백(`gpt-4o-mini`)을
  유지하며, 폴백 발생은 호출 원장에 구분 기록된다.
- API: Chat Completions
- 코어 출력: strict Structured Outputs
- `strict: true`
- `additionalProperties: false`

필수 모델 출력 5필드:

```text
situation_ko
relation_ko
source_text
preceding_turn
brief_note_ko
```

출력 키와 DB 저장 구조는 strict 적용 전과 같다. strict schema 적용은 출력 형식 강화이며, 내용 타당성을 자동 보장하지 않는다.

### 5.2 코어 생성 계열

현재 코어 표면 해시 값은 **Prompt Harness와 ACTIVE_HANDOFF가 정본**이다. 이 문서에
해시 값을 복사해 두지 않는다. 해시에는 다음이 포함된다.

- system/user 프롬프트 템플릿
- 서버 주입 카탈로그와 조립 규칙
- 모델과 폴백 모델
- temperature
- response format
- strict JSON Schema 전문

셀별 화행·수준·도메인·P/D/R 값은 각 행의 구조화 필드로 저장하며 표면 해시에 넣지 않는다.

해시가 바뀌면 이전 계열 생성물과 같은 동결 집합으로 섞지 않는다.

### 5.3 코어 자동검사

- 결정론 검사: 스키마·축·모드·인접쌍·topic·산업 등 저장 전 규칙
- 보조 AI 비평: `core_quality_v5`, 15축
- 사람 눈검사: 최종 콘텐츠 판단

AI 비평의 `pass`는 사람 검수나 내용 타당화를 대체하지 않는다.

### 5.4 AI 모델 간 독립 검토 (교차 벤더 방식, 2026-08-07 확정)

생성·비평 호출이 단일 벤더로 묶여 있는 구조를 보완하기 위해, **실행 시점의 동결 연구
세트 코어 전수**에 대해 다른 벤더의 모델이 같은 15축으로 독립 판정하는 검토를 둔다.
표본 추출을 하지 않는다. 검토 단위는 코어이며 미션은 이 절의 범위가 아니다.

- 지위: **단일 벤더 구조에서는 원리적으로 확보할 수 없는 독립 편향 프로파일을 추가하는
  결함 탐지 절차다.** 검증이 아니다 — 두 판정이 일치한다고 해서 내용이 타당함을 뜻하지
  않으며, 내용 타당화는 별도의 사람 절차(전문가 형성 평가)에 배정되어 있다.
- 이견 처리: 두 벤더의 판정이 갈린 항목은 자동으로 조정하지 않고 **사람에게 넘긴다.**
  판정 다수결·평균·자동 채택을 두지 않는다.
- 상태 영향 없음: 이 검토는 §7.2의 상태 흐름을 바꾸지 않는다. 결과가 무엇이든
  `generated`를 `reviewed`로 올리지 못하고, 내리지도 못한다.
- 축: 코어 비평의 15축을 그대로 쓴다. 축을 바꾸면 두 판정을 비교할 수 없다.
  프롬프트 판본은 이 문서에 적지 않는다 — 배포 Edge의 값이 정본이며 결과 기록에 남는다.
- 실행 형태: **실행과 노출을 분리한다**(2026-08-07 개정 — 최초 조항은 "배치 실행에
  UI가 필요 없다"는 판단을 "화면에 노출하지 않는다"로 잘못 넓혀 적었다).
  - **실행**은 관리자·연구자 측 오프라인 배치다. §10 승인 게이트를 따르며,
    관리자 화면에 실행 트리거를 두지 않는다.
  - **노출**은 관리자 화면에 한다 — 이 단계의 지위(결함 탐지, 검증 아님)·15축·
    실행 이력·불일치와 그 사람 판정 귀결을 **읽기 전용**으로 열람한다. 설계도가
    QA 사슬의 한 층으로 표시하는 단계는 도구에서 확인 가능해야 한다. 실행 전에는
    미실행 상태를 있는 그대로 표시하고, 없는 결과를 채워 보이지 않는다.
  - 결과 반입은 배치 산출 파일 열람에서 시작한다. DB 반입(스키마 추가)은 별도 승인
    사안이다.
- **학습자 런타임에는 어떤 호출도 늘리지 않으며 학습자 화면에 노출하지 않는다**(불변).
- 이 검토 호출은 §5.1의 모델 교체 폴백이 아니다. 생성 모델은 여전히 단일이며,
  검토는 이미 저장된 산출물을 읽기만 한다.

## 6. Full Mission 생성 계약

### 6.1 현행 mission_v6

```text
1. Scale4
   매우 적절 / 다소 적절 / 다소 부적절 / 매우 부적절
   → 적절/부적절 방향이 맞으면 인정
   → reference_scale_code와의 정도 차이는 참고·수업 토론 자료

2. FixChoice 4
   상황에 맞는 정도 판단 1회
   → 수정안 4개 중 적절한 서로 다른 전략 2개 복수 선택

3. FixReview
   고치기 전 문장과 교정본 3개의 관계를 명시
   → 문제가 남은 교정본 1개 선택
   → 상황 결부형 실패 원인 3개 중 가장 큰 문제 1개 선택

4. MultiJudge 3
   과소 1 + 적정 1 + 과잉 1
   → 후보마다 상황과 상대에게 어떻게 들리는지 분류
   → BEST/WORST·순위 필드 없음

5. DCT
   번역 또는 통역 산출 → 피드백
   → 의미 / 문법·표현 / 목표 화용 특성의 3층 피드백
   → 수정 또는 근거 있는 유지
   → 첫 수정본 경량 재확인 1회
```

- `scale4`는 독립 첫인상 문항이다. `accepted_scale_codes`는 같은 방향의 두 코드이고, `reference_scale_code`는 그 안의 대표 정도 하나다.
- Scale4는 각 target feature 카탈로그의 `counter_rule_ko`가 경계하는 소박한 규칙에 대한 반례를 우선 배치한다. 특정 PDR에서는 직접성·강도·명료성·책임 범위 등 해당 자원의 다른 대역이 적절할 수 있음을 보여 주어, 더 간접적·길거나 강한 표현을 자동 상향하는 편향을 막는다.
- `fix_choice`는 `valid_a`, `valid_b`, `under_repair`, `over_repair`를 정확히 하나씩 가지며 판단 제출 뒤 교정안을 공개한다.
- `fix_review`는 자연스러운 교정본 3개 중 `pass` 2·`reject` 1을 가진다. 통과본은 서로 다른 전략이고 탈락본의 핵심 실패는 하나다. 실패 원인 선택지는 서로 다른 유형 3개다.
- 같은 미션에서 FixReview 탈락본의 실패 유형은 FixChoice 오답의 `under_repair`·`over_repair`와 겹치지 않는다(R30).
- `multi_judge` 후보 수는 모든 수준에서 3개로 고정하고 과소 1·적정 1·과잉 1을 만든다. 복수의 적절 전략 비교는 FixChoice가 담당하고 MultiJudge는 세 대역의 경계를 한 번씩 분류하게 한다.
- 선택지 수를 수준 난이도로 사용하지 않는다. 난이도는 장면과 표현의 미묘함으로 조절한다.
- DCT는 MPJ와 같은 `mission_id`, attempt와 `target_feature`에 속한다. MPJ 완료만으로 미션 완료 처리하지 않는다.
- DCT 직전 맥락 확인은 채점하지 않고 완료 조건에도 넣지 않는다. 번역은 비화용적 내용 어휘 0~2개, 통역은 힌트 없이 재생 최대 2회다.
- 첫 수정본은 목표 반영·원래 의미 보존·새 문제 발생 여부를 한 번만 재확인한다. 부분 반영 또는 새 문제 뒤 추가 수정은 1회 허용하되 두 번째 자동 재확인은 하지 않는다.
- 학습자 화면에는 `참고 판정`, `비점수 활동`, `대역`, `trace` 같은 연구 용어를 노출하지 않는다. 비점수 성격은 스키마와 저장에만 보존한다.

### 6.2 FixReview·맥락·전이 게이트

FixReview 단일 탈락본과 단일 실패 원인 선택은 다음 조건에서만 허용한다.

- 통과 2개와 탈락 1개가 명확하고, 두 통과본은 서로 다른 전략이다.
- 탈락본에는 `meaning_shift`, `relation_mismatch`, `new_language_or_pragmatic_problem` 중 하나의 핵심 실패만 성립한다.
- 실패 원인 3개는 선택한 표현과 상황에 직접 결부되고 서로 다른 `failure_type`을 가진다.
- `accepted_failure_reason_id`는 탈락본의 `failure_type`과 일치하는 하나뿐이다.
- 두 교정본이 동등하게 탈락 가능하거나 두 실패 원인이 동등하게 방어 가능하면 생성 실패 또는 AI 품질검사 fail이다.

P·D·R은 원시 코드를 학생에게 보여 주지 않고, 서로 이어진 2~3문장 상황에서 권한·접촉 이력·실제 부담의 구체적 근거로 추론 가능해야 한다.

- `situation_ko`는 역할 메타데이터를 나열하지 않고, 원문 화행의 주체가 누구에게 무엇을 하려는지 먼저 드러낸다. 번역 셀은 학습자 1인칭 과업으로 서술할 수 있지만, 통역 셀은 원발화자 A와 청자 B 사이에서 학습자가 별도 통역사로 개입한다고 서술한다. 다음 문장에 접촉 이력과 상대가 감수할 실제 부담을 자연스럽게 잇는다. 기록 목적·즉시 반응 여부 같은 매체 속성을 연구 설명처럼 풀어 쓰거나, 상대의 권리·선택권·의무와 답안에 포함할 표현 자원 같은 평가 기준을 학생용 상황문에 노출하지 않는다. 이 조건은 내부 `context_spec`·P·D·R·target feature에만 보존한다.
- AI 품질검사의 `scene_underspecified`도 같은 경계를 사용한다. 상대·용건이 없거나 관계·접촉 이력과 실제 부담이 모두 빠져 P·D·R 판단이 실제로 갈릴 때만 보고하며, 문장이 짧거나 기록 목적·즉시 반응 여부가 설명되지 않았다는 이유만으로 결함을 만들지 않는다.
- `relation_ko`의 학습자 표시값은 하나의 `상대` 칩에 들어갈 상대의 역할과 학습자와의 관계를 자연스럽게 합친다. 별도 `상대`·`관계` 칩으로 분리하지 않으며, 두 역할의 병렬 나열, 화자 역할, `A → B`, P·D·R 코드·라벨은 표시값에 넣지 않는다.
- legacy 행에 화자와 상대가 함께 저장돼 있으면 러너 UI에서 오른쪽 상대 정보만 추출한다. 저장 원문과 관리자 검수 화면은 바꾸지 않는다.

주간 미션의 맥락 배치는 `앵커+대비`다.

- Scale4: 소박한 규칙의 반례가 실제로 적절해지는 대비 PDR
- FixChoice와 FixReview: DCT와 같은 앵커 PDR, 서로 다른 사건
- MultiJudge: 앵커 P/D/R 중 정확히 한 축만 바꾼 대비 상황
- DCT: 같은 앵커 PDR의 새 사건으로 근접 전이

주간 DCT만으로 광범위한 전이를 주장하지 않는다. 학기 전체 PDR 배치표에서 주차별 조건을 순환하고, 중간·기말 통합 과제에는 P·D·R 중 하나 이상이 달라지는 전이 문항을 둔다. 배치 자체는 전이 학습의 기회이며, 실제 전이는 수행 자료로 검증한다.

### 6.3 미션 모델과 형식

- 미션 승격 모델: `gpt-4o`
- 현재 미션 출력: JSON mode
- 미션 strict JSON Schema: 미구현
- 생성 후 결정론 검사
- 실패 시 전체 미션 재생성, 최대 2회
- 필드 단위 repair와 outline 파이프라인은 사용하지 않는다.

### 6.4 R5 길이 단서 판정 해석 (2026-07-31 확정)

R5는 `multi_judge` 후보의 길이가 목표 화용 판단을 우회하는 응답 단서로 작동하는 것을 통제한다. 판정 수준은 다음과 같이 해석한다.

- **hard fail(저장 차단)**: 모든 non-within 후보의 길이가 within 후보 구간의 한쪽 밖에만 분포하는 완전 분리. 과소(짧음)–적정(중간)–과잉(긺)처럼 부적절안이 within 구간 양쪽에 나뉘는 배열은 fail이 아니다. 구현 기준은 `checkMultiJudgeLength`의 `separable` 조건이며, 이 서술이 `history/legacy/PRAGMA_생성계약_v1_2026-07-23.md`의 "완전 단조 = 길이순 정답키" 표현보다 우선한다 — 그 표현은 실제 차단 범위보다 넓다.
- **warning(인간 검수 신호)**: 과잉안 유일 최장, 과소안 최단 집중, 최장/최단 비율 3 초과. 저장을 막지 않으며 조립 단계 눈검사의 대상이다.

2026-07-31의 9화행 파일럿에서 B2 프롬프트 보강(대역과 길이를 별개 축으로 명시하고 탈상관 조합을 요구)은 미션 생성 성공률과 일부 길이 구간 중첩을 개선했다. 다만 화행별 1표본이므로 길이 단서 문제가 일반적으로 해소되었다고 보지 않는다.

길이가 일부 target feature와 자연스럽게 관련될 수 있다는 사실과, 길이가 응답 단서로 작동한다는 사실은 동시에 참일 수 있다. 학습자가 목표 화용 판단 없이 길이만으로 과소·적정·과잉 분류를 식별하거나 선택지를 현저히 좁힐 수 있다면 수정 대상으로 판정한다. 반복 표본에서도 동일 경향이 지속되면 축별 생성 전략 또는 응답 판정 방식의 분화를 재검토한다(현재는 미도입·유보).

## 7. 평가와 피드백 경계

### 7.1 분리 원칙

피드백은 최소 다음 층을 구분한다.

1. 의미·의도 충실성
2. 명백한 문법·형식 문제
3. 해당 target feature의 상황 적절성

직접성, 공손성, 의미 충실성을 하나의 총점으로 합치지 않는다. 더 길거나 더 완곡한 표현을 자동으로 더 좋은 답으로 판정하지 않는다.

MPJ 4개가 끝나면 handoff에서 실제 응답을 네 개의 한 줄 SUMMARY로 정리한다. 각 줄은 `첫인상 판단 → 판단하고 고쳐보기 → 교정본 검수 → 여러 표현 분류`의 기능과 이번 장면에서 확인한 판단 기준을 연결한다. 다른 판단을 한 행은 수업에서 이유를 비교하도록 짧게 안내하되 정답·오답 성적표는 제시하지 않는다.

학습자 피드백 화면은 `내 답 → 의미 전달·문법 정확성·상황 적절성 판정 → 다음 행동`만 우선 노출한다. 세 판정은 상태 아이콘과 `통과/수정 필요`로 결과를 분명히 하고, 통과한 층에는 구체적인 긍정 문구를 제공한다. 세 층이 모두 통과하면 수정을 강제하지 않는다. 수정이 필요한 경우 `피드백을 반영해 문장 고치기` 또는 `고치지 않고 그대로 제출하기` 중 행동을 직접 고르게 한다. 공백·문장부호·기호만 바꾼 답은 수정으로 인정하지 않는다. 최초 산출을 유지하려면 이유 또는 기존 이견이 필요하다. 여러 층이 동시에 실패한 한 번의 수정 과업은 `의미 전달 → 이번 미션의 목표 화용 축 → 문법 정확성` 순서로 최종 수정 목표를 정한다. 세 층의 독립 판정과 상세 피드백은 그대로 보존한다. 판정 근거 전문은 관리자 검수·로그에 남기며 학습자에게 반복해 읽히지 않는다. 첫 수정본은 목표 반영·원래 의미 보존·새 문제 발생 여부를 한 번만 재확인한다.

DCT 산출 화면에서는 장면 설명과 실제 수행 도구를 한 카드에 붙이지 않는다. 번역은 `지금, 직접 번역할 장면`과 메신저 입력 화면을 분리하고, 통역은 장면 카드와 듣기·녹음 프레임을 분리한다. 두 장면 카드는 고정 최소 높이를 두지 않고 같은 패딩·행간·문장 간격을 사용해 내용 길이에 맞게 줄어든다. 한→중 번역에는 산출을 막는 비화용적 내용 어휘·고유명사·전문용어만 정확히 2개 제공하며, 기본 접힘 상태에서 학습자가 요청할 때 한 줄 패널로 펼친다. target feature의 완화 표지, 문장 구조와 완성 답안은 힌트로 제공하지 않는다. 통역에는 첫 수행과 재도전을 포함해 힌트를 제공하지 않는다.

### 7.2 사람 검수

상태 흐름:

```text
core-only
→ generated
→ AI/결정론 검사
→ 사람 검수
→ reviewed
```

- 일반 학습자 공개 최소 조건: `mission_status='reviewed'`
- 연구 분반에서 band를 측정 주장에 사용할 경우: `reviewed`와 별도의 인간 캘리브레이션 상태가 필요하다.
- `generated`는 생성 성공이지 교육적 승인 상태가 아니다.

## 8. provenance와 재현성

코어와 미션은 최소 다음을 식별할 수 있어야 한다.

- model
- provider
- prompt version
- prompt snapshot hash 또는 mission content hash
- generated_at
- generation attempt
- schema version
- language direction

Edge가 계산한 `prompt_snapshot_hash`를 클라이언트가 변형하거나 로컬 코드로 재계산하지 않는다. 프롬프트 정본 화면은 배포 Edge 소스에서 생성된 저장소 스냅샷과 저장 행의 지문을 대조한다.

### 8.1 HSK 3.0 참고 데이터와 lexical audit

HSK 3.0은 PRAGMA 학습 지원 수준의 숙달도 등가표나 인증 기준이 아니다. 생성된 중국어를
검토할 때 사용하는 **누적 어휘 참고 상한**이다.

| PRAGMA 수준 | 중국어 어휘 참고 상한 | 누적 항목 수 |
|---|---:|---:|
| 입문 | HSK 1–4급 | 2,000 |
| 중급 | HSK 1–5급 | 3,600 |
| 고급 | HSK 1–6급 | 5,400 |

공식 PDF→추출 CSV→결정론 파생→연구자 mapping→DB seed의 각 층은 source ID·공식 URL·
SHA-256·변환 version으로 식별한다. 공식 L1/L2/L3 주제와 연구자 코딩은 같은 진실값으로
취급하지 않는다. 원 코더·시점·지침이 확인되지 않은 과거 코딩은
`legacy_imported_unverified`이며 PRAGMA 장면 채택 여부로 자동 변환하지 않는다.

생성 계약은 다음을 강제한다.

- 무작위 HSK 어휘 목록을 prompt에 주입하지 않는다.
- 중→한은 중국어 source를, 한→중은 mission의 중국어 target·참고안을 감사한다.
- 감사 결과는 `hsk_lexical_audit`에 policy/source ID, 참고 상한, token 수, 참고 일치율,
  범위 밖 검토 후보와 상태를 기록한다.
- lookup 실패는 `unavailable`로 기록하고 생성·저장을 막지 않는다.
- 범위 밖 후보는 고유명사·전문용어·분절 단위·상위/사전 밖 어휘일 수 있다. 자동 삭제·
  재작성·승인 차단에 쓰지 않고 사람 검수의 참고 자료로만 사용한다.
- lexical audit는 중국어 자연성·화용 적절성·숙달도를 판정하지 않는다.

§5.4 AI 모델 간 독립 검토를 실행한 경우 검토 결과에 대해서도 최소 provider·model·prompt
version·실행 시각·대상 행의 식별자를 남긴다. 이 기록은 생성 행의 provenance를 덮어쓰지
않고 별도로 보존한다.

## 9. 배포·검증 상태는 여기 적지 않는다

Edge 배포 버전, migration 적용 여부, 스모크 결과, 테스트 수, 프롬프트 스냅샷 수·해시는
날짜 종속 사실이다. 이 계약 문서에 적으면 반드시 썩는다.

- 현재 상태의 정본: `docs/handoff/ACTIVE_HANDOFF.md` (최상단 절)
- 시점 고정 증거: `docs/research-trail/` (evidence·dev-log)
- 프롬프트·해시 대조: `/admin/prompt-harness`
- 현행 모든 코어·미션은 조건 LOCK 전 개발·테스트 콘텐츠이며, LOCK 뒤 전량 재생산한다
  (`docs/operations/CONTENT_REFRESH_RUNBOOK.md`).

## 10. 운영 금지와 승인 게이트

- 495/500 대량 배치를 임의 실행하지 않는다.
- core hash 변경 전 영향 범위와 새 생성 계열 분리 방식을 보고한다.
- 생성·평가·저장 계약, DB schema, RLS, prompt hash에 영향이 있으면 먼저 범위를 보고한다.
- migration 적용, Edge 배포, Railway 배포는 사용자 승인 없이 실행하지 않는다.
- `generated`를 `reviewed`로 자동 승격하지 않는다.
- AI 모델 간 독립 검토 배치를 임의 실행하지 않는다. 대상 표본·규모·모델을 먼저 보고한다.
- target feature의 학술적 지위를 코드 구현만으로 validated라고 부르지 않는다.
- 라운지 활동은 이 계약의 학습 수행·연구 로그에 포함하지 않는다.

## 11. 관련 정본

- 현재 작업 상태(날짜 종속 사실의 정본): `docs/handoff/ACTIVE_HANDOFF.md`
- 학습자 구조: `docs/product/PRAGMA_학습자구조_정본.md`
- 관리자 구조: `docs/product/PRAGMA_관리자구조_정본.md`
- 제품·연구 정체성: `docs/research/PRAGMA_PRODUCT_RESEARCH_IDENTITY_2026-07-28.md`
- Can-do 원칙: `docs/research/PRAGMA_CAN_DO_ALIGNMENT_PRINCIPLES_2026-07-28.md`
- 화용론 설계 원칙: `docs/research/PRAGMA_PRAGMATICS_DESIGN_PRINCIPLES_2026-07-27.md`
