# 생성계약 역사 자료

- `../PRAGMA_생성계약_정본.md`만 현행 계약이다.
- 새 이정표 스냅숏은 이 폴더에 `PRAGMA_생성계약_YYYY-MM-DD.md`로 발행하고, 발행 커밋 이후
  수정하지 않는다.
- `legacy/`는 2층 규칙 도입 전의 가변 날짜 판본이다. 그 날짜는 정확한 동결 시점을 보장하지
  않으며 현재 구현 근거가 아니다. 특정 시점의 전문은 Git 커밋에서 확인한다.
