# PRAGMA 생성 계약 v1.6 (`scenario_core_v2` JSON + `mission_v1` 2층 + 편성층 메타) — 2026-07-23 (v1.6 정본화 07-27)

> 🔐 **0-w (2026-07-28) — 9화행 target feature 카탈로그 완성. 조항 127~130.**
>
> 이 블록은 0-v까지의 계약을 폐기하지 않고 최신 우선 조항으로 보정한다. 과거의
> “승격 가능 3화행” 기록은 당시 상태를 설명하는 감사 기록이며, 현재 가용 범위는 아래가 우선한다.
>
> **127. 9화행 모두 사람 작성 기본 target feature를 가진다.**
>
> - 요청·거절·감사 기존 3종에 사과·제안·초대·반대·칭찬하기·칭찬 대응·직접 불만 7종을 추가했다.
> - 자동 승격 기본값은 화행별 1개로 총 9개다.
> - 칭찬은 상위 교육 화행군이며 `compliment_giving`과 `compliment_response`를 같은 주차의
>   별도 미션·target feature로 유지한다. 둘을 하나의 정답 대역으로 합치지 않는다.
> - `politeness`는 기존 비활성 보조축이며 9개 기본값·10개 승인 feature 수에 포함하지 않는다.
> - 모든 band는 인간 캘리브레이션 전 `proposed` 지위다. 표현 자원은 가능한 실현 수단이지
>   언어·문화별 고정 정답 목록이 아니다.
>
> **128. 명제적 Supportive Move는 서버 승인 `usable_facts` 폐쇄 목록만 사용한다.**
>
> - `usable_facts`는 core_content의 선택 필드이며 최대 8개다.
> - 목록에 없고 원문에도 없는 이유·대안·수리·보상·재발 방지 약속·새 일정은 MPJ 후보와
>   reference alternative에 추가할 수 없다.
> - 목록이 없거나 비어 있으면 명제적 Supportive Move를 정답 조건으로 요구하지 않는다.
> - 한 문항 안의 대조 후보는 같은 명제 내용을 유지한다. 사실의 유무를 band 정답 단서로 쓰지 않는다.
> - 관습화된 사과·인사, 완화어, 호칭 등 비명제적 자원은 이 폐쇄 목록의 대상이 아니다.
>
> **129. 칭찬 대응은 보존하되 7월 자동 승격 기본값으로 사용하지 않는다.**
>
> - 현재 495 코어 경로의 `compliment`는 칭찬하기 상황을 생성한다.
> - 따라서 `DEFAULT_FEATURE_BY_ACT.compliment = compliment_grounding_sensitivity`로 고정한다.
> - `compliment_response_uptake`는 카탈로그와 construct 정본에 포함하지만,
>   별도 `interactional_subtype` 코어 경로가 생기기 전에는 기존 칭찬하기 코어에서 자동 승격하지 않는다.
> - 이 제한은 칭찬 대응을 삭제하거나 축소한다는 뜻이 아니라, 서로 다른 인접쌍을 같은 코어로
>   잘못 승격해 데이터를 오염시키지 않기 위한 임시 실행 게이트다.
>
> **130. 코어 동결 지문과 미션 프롬프트를 구분한다.**
>
> - 0-w는 코어 생성 system/user 표면과 243/54/495 배치 계획을 바꾸지 않는다.
> - `usable_facts` 규칙은 미션 승격 프롬프트에만 추가한다.
> - 따라서 코어 `core_surface_hash=24adf002ee1d7ff391062445d8dbc55ba822638172aef0ede43497bbbe979b01`
>   계열은 유지한다. 미션 프롬프트 SHA와 Edge 소스 SHA는 새 버전으로 갱신한다.

> 🔐 **0-v (2026-07-27) — House & Kádár 반영, 243 구인행렬·495 본배치·상황 호환층 확정. 조항 116~126.**
>
> 이 블록은 0-u까지의 생성계약을 폐기하지 않고 **최신 우선 조항으로 보정**한다. 아래 조항과 과거 본문의 표현이 충돌하면 0-v가 우선한다. 과거 판정 기록은 감사 추적을 위해 소급 수정하지 않는다.
>
> **116. 생성 조건의 층위를 분리한다.**
>
> ```text
> 연구 구인행렬: speech_act 9 × P 3 × D 3 × R 3 = 243셀
> 과업 트랙: task_mode = translation | stt_interpreting
> 전달 커버리지: speech_act 9 × learner_level 3 × task_mode 2 = 54셀
> 교육 편성층: learner_level + domain
> 사건·검색 메타: theme + topic + work의 industry
> 내부 호환성 해결층: context_spec
> ```
>
> - 위 층들은 모두 필요하지만 동등한 연구축이 아니다.
> - 논문에서 `243개 조합`이라 할 때는 **화행×P×D×R**만 뜻한다.
> - `54셀`은 수준·모드별 콘텐츠 전달 커버리지이며 243 구인셀과 별도 감사한다.
> - `domain`, `theme`, `topic`, `industry`, `context_spec`을 화용적 적절성의 독립 원인으로 과장하지 않는다.
> - 모든 필드를 완전요인 교차하여 조합 수를 부풀리지 않는다. 495건은 243 구인셀을 채우면서 수준·도메인·모드·사건 배경의 주변분포를 확보하는 층화 생성 계획이다.
>
> **117. domain은 3개를 유지하되 한국어 표시명을 `일상·학업·직장`으로 통일한다.**
>
> | 코드(불변) | 표시명 | 범위 |
> |---|---|---|
> | `daily` | 일상 | 개인 생활, 대인관계, 지역사회·생활서비스 |
> | `school` | 학업 | 대학 수업, 교수·조교·동료 학생, 학사 행정, 교내·국제교류 활동 |
> | `work` | 직장 | 조직 내부 협업, 고객·파트너·거래처와의 업무 상호작용 |
>
> - DB·API 코드값 `school`은 바꾸지 않는다.
> - domain은 HSK의 생활·학습·직장 사용 영역과 정합하는 **교육 편성·소재 층**이다. P·D·R과 같은 화용 구인축으로 서술하지 않는다.
> - 과거 문서의 `학교` 표기는 역사 기록으로 남기되 신규 UI·프롬프트·보고에서는 `학업`을 쓴다.
>
> **118. theme과 topic은 연구축이 아니라 편성 영역과 사건 시드다.**
>
> - `theme_code` 8종은 학습자·교수자가 콘텐츠를 찾고 강좌를 편성하는 **상황 영역**이다.
> - `topic_code`는 그 영역에서 반복되는 **구체적 사건 유형**이다. 생성 입력이 곧 저장 태그가 된다.
> - 신규 코어 생성 순서는 다음으로 고정한다.
>
> ```text
> 화행 + P·D·R + task_mode + domain
> → 호환 가능한 topic 선택
> → 서버가 context_spec 확정
> → 상황·관계·원문 생성
> ```
>
> - topic은 이미 정해진 화행·domain·P·D·mode와 모두 양립해야 한다.
> - `allowedSpeechActs`, `allowedDomains`, 필요 시 `allowedPowers`, `allowedDistances`, `allowedModes`를 정적 호환 조건으로 사용한다.
> - 호환 topic이 없으면 생성 계획 단계에서 **명시적으로 실패**한다. 다른 화행 또는 domain의 topic을 조용히 재사용하는 폴백은 금지한다.
> - topic 시드는 원칙적으로 P·D와 전달 매체를 미리 고정하지 않는다. 추천서 부탁·낯선 사람에게 길 묻기처럼 사건 성립 자체가 특정 관계를 요구하는 경우만 제한을 선언한다.
> - 현재 카탈로그의 최소 topic 수와 빈 셀 없음은 **구조적 커버리지**만 뜻한다. 학부생 요구에 대한 실증적 타당화로 주장하지 않는다.
>
> **119. `industry`의 학습자·관리자 표시 용어는 `업무 분야`로 한다.**
>
> - 내부 DB·API 필드명 `industry`·`industry_sector`와 기존 enum 코드는 호환성을 위해 유지한다.
> - 업무 분야는 `work` domain에서만 채우는 편성·검색·상황 배경 메타데이터다. 연구축이나 학습자 진로 판정값이 아니다.
> - 업무 분야가 지정된 코어는 실제 업무 대상·행동·어휘에 그 배경이 드러나야 한다. 단, 화행·P·D·R·topic·context_spec을 덮어쓸 수 없다.
> - 단순 균등 순환은 콘텐츠 뱅크의 배경 커버리지일 뿐 학부생 요구 충족의 증거가 아니다.
> - **495 본배치 전 게이트**: 공유 카탈로그 하나에서 생성기·배치·아카이브·편성기의 코드↔라벨 매핑을 통일한다. 현재 구형 아카이브의 5분류와 최신 7분류, 의미가 달라진 legacy enum 키를 그대로 병존시킨 채 본배치를 시작하지 않는다.
> - `business_function`(해외영업·마케팅·고객응대·물류·프로젝트 운영·번역·로컬라이제이션·대외협력 등)은 “어느 분야인가”와 다른 “무슨 업무를 하는가”의 층이다. 495 배치에서 필수로 결합할지와 호환표는 별도 승인 전까지 미확정이며, 임의의 새 생성축으로 추가하지 않는다.
>
> **120. `context_spec`은 새 실험축이 아니라 서버 고정 상황 호환층이다.**
>
> ```ts
> interface ContextSpec {
>   standard_situation_code: string;
>   role_pair: {
>     speaker_ko: string;
>     addressee_ko: string;
>   };
>   speaker_entitlement: string;
>   addressee_obligation: string;
>   decision_authority: string;
> }
> ```
>
> - 신규 코어에서는 필수다. 도입 이전 legacy 코어와의 읽기 호환을 위해 JSON 스키마 파싱에서는 optional일 수 있으나, 신규 저장 검사에서는 누락을 fail 처리한다.
> - 클라이언트가 보낸 `context_spec`을 신뢰하지 않는다. 서버가 화행·domain·P·D·topic을 바탕으로 확정하여 프롬프트와 반환 코어에 동일하게 넣는다.
> - 역할명만으로 P·D를 추정하지 않는다. 누가 무엇을 요구·결정·수리·전달할 권리와 의무가 있는지를 함께 고정한다.
> - 학습자 화면에 다섯 필드를 체크리스트처럼 노출하지 않는다. 생성·감사 내부에서 사용하고, 학습자에게는 자연스러운 상황·관계 설명으로 실현한다.
>
> **121. 교육용 단일 화행은 `Head Act 정확히 1개`를 뜻하지 않는다.**
>
> - 미션의 **초점 화행은 하나**로 유지한다.
> - 같은 초점 화행을 실현하는 multiple heads, 앞뒤의 supportive moves, 여러 문장으로 된 extended speech-act set을 허용한다.
> - supportive move가 독립적인 두 번째 학습 목표로 승격되거나 다른 화행이 장면을 지배하면 실패다.
> - 거절은 PRAGMA의 교육용 9화행 중 하나로 유지한다. 인접쌍의 반응 위치라는 사실은 `preceding_turn`과 adjacency 검사로 처리하며 화행 지위를 삭제하지 않는다.
> - 과거 계약의 “중심 화행 1개”는 유지하지만 이를 “표면 Head Act 문장 1개”로 해석하지 않는다.
>
> **122. 직접성·공손성·격식·완화·적절성을 분리한다.**
>
> - 직접성의 높고 낮음을 공손성의 높고 낮음으로 자동 환산하지 않는다.
> - 중국어 상위자 상황에서도 명료한 원망 진술과 충분한 완화가 함께 나타날 수 있다. 직접형을 곧바로 무례·오답으로 판정하지 않는다.
> - formality는 관계·제도·담화 관습의 층이며 directness와 별개다.
> - mitigation은 전략을 유지한 채 부담과 관계 효과를 조절하는 자원이며 directness와 직교할 수 있다.
> - 최종 적절성은 위 속성 하나가 아니라 해당 Standard Situation, 권리·의무, 화행 목적에 비추어 판단한다.
> - R은 전 화행 공통의 추상 “부담감”만으로 운용하지 않는다. 요청의 비용, 거절하는 유발행위의 중요도, 사과의 위반 심각도, 감사의 호의 크기, 칭찬의 민감성 등 **화행별 조작적 정의**를 따른다.
>
> **123. 본배치는 명목상 500이 아니라 정확히 495건으로 실행한다.**
>
> | 항목 | 고정값 |
> |---|---|
> | 대상 방향 | `ko_zh` |
> | 화행당 번역 | 42건 (`13+15+14`) |
> | 화행당 통역 | 13건 (`4+5+4`) |
> | 화행당 합계 | 55건 |
> | 전체 | 9화행 × 55 = **495건** |
>
> - 243 구인셀은 빈 셀 0, 셀당 최소 2건이어야 한다.
> - 54 전달셀은 빈 셀 0, 셀당 최소 3건이어야 한다.
> - 대규모 실행은 `ko_zh`이면서 정확히 495건이고 위 두 게이트를 모두 통과할 때만 허용한다.
> - `zh_ko`는 요청·거절·감사 × 수준3 × 모드2의 별도 18셀 검증 범위다. 495 본배치와 섞지 않는다.
> - 모든 생성물은 단일 배포 프롬프트 지문 아래에서 생성한다. 다른 지문의 코어를 섞어 495를 채우지 않는다.
>
> **124. 코어 JSON·외곽 저장 포맷·프롬프트 버전을 구분한다.**
>
> - 신규 반환 JSON의 `schema_version`은 `scenario_core_v2`다.
> - DB의 외곽 `content_format='scenario_core_v1'` 값은 기존 RPC·행 호환을 위한 이름이며 JSON 스키마 버전과 동일한 개념이 아니다.
> - 코어 프롬프트 메타 버전은 `core_v3`, 코어 품질 비평 프롬프트 버전은 `core_quality_v4`다.
> - 프롬프트 문자열이 바뀌면 스냅샷을 다시 생성하고 배포본 지문을 재확인한다. 변경 전후 생성물을 같은 동결 집합으로 취급하지 않는다.
>
> **125. 코어 품질 비평기는 12축 보조 감사이며 동결 게이트를 대체하지 않는다.**
>
> ```text
> speech_act · power · distance · burden · domain
> · industry · mode · context_spec · referents
> · decision_authority · topic_seed · adjacency
> ```
>
> - 각 축은 `pass | warning | fail + reason_ko`를 반환한다.
> - `referents`는 화자·상대·문제 책임자·행위 대상의 지시 일관성을, `decision_authority`는 화행별 수행·수락·승인 권한을 별도로 판정한다.
> - 직장 산업이 지정되었는데 실제 장면·원문에 분야 고유 단서가 전혀 없으면 결정론적 R26에서 저장 전에 차단한다. 구체성의 최종 판정은 비평기와 인간 검수가 맡는다.
> - 비평기는 생성 저장과 분리된 보조 안전망이다. 누락 축을 조용히 pass 처리하지 않는다.
> - 동결의 최종 선행조건은 동일 18셀 재스모크에 대한 연구자 눈검사다.
> - 비평기 파일럿이 사전 정의한 BLOCKER 검출 기준에 미달하면 비평기 없이 진행하고, 495 생성 후 무작위 50건 눈검사로 대체한다.
>
> **126. 0-v 구현·배포 상태를 구분한다.**
>
> - ✅ 로컬 구현: 27개 P·D·R 전조합, 243/54 이중 감사, 495 프리셋·하드 게이트, topic 호환 선택·폴백 차단, 서버 `context_spec`, topic별 역할 쌍, 산업 프롬프트·R26, 12축 비평기 v4, 프롬프트 스냅샷.
> - ✅ 로컬 검증: 타입 검사·단위 테스트·프로덕션 빌드 통과.
> - ✅ edge 재배포(2026-07-27): Supabase `generate-scenario`에 5개 강화 기준(행위자·대상, 산업 실현, topic, 반대 인접쌍, 결정 권한)과 `core_quality_v4`를 함께 반영했다. 최종 배포·저장소 지문은 `24adf002ee1d7ff391062445d8dbc55ba822638172aef0ede43497bbbe979b01`이다.
> - ✅ 동일 18셀 재스모크 실행(2026-07-27): `core_ko_zh_1785163033842`, 중급 18건(9화행 × 번역·통역), 저장 성공 18·실패 0.
> - ✅ 비평기 파일럿 실행(2026-07-27): `core_quality_v4` 기준 pass 18·warning 0·fail 0·호출 실패 0.
> - ✅ Codex 수동 감사(2026-07-27): 강화 대상 5축 BLOCKER 0. 호텔 담당자·호스트 가족·공식 버디·콘텐츠 이견의 topic/역할 정합을 확인했다. `#13 반대×일상×통역` 원문의 한국어 자연성 warning 1건은 495 표본 검사 항목으로 남긴다.
> - ⏳ 최종 연구자 눈검사: 위 run의 18셀 내용 승인 전에는 동결로 간주하지 않는다.
> - ⏳ 미실행: 495 본배치.
> - 🔴 새 run 18셀의 연구자 눈검사 전에는 495 본배치를 실행하지 않는다.

> 🔐 **0-u (2026-07-26 저녁) — 재현성 provenance 구현 + 프롬프트 정본 소재 확정. 조항 112~115.**
> 배경: 지도교수 구두 보고(7/26) 준비 중 **"어떤 프롬프트로 만든 데이터인지 증명할 수 없다"**는 공백이 드러났다. 0-g·48이 선언한 *versioned provenance* 노선을 실제 저장 경로로 구현한 라운드다. **스키마 변경·migration 0건.** 구현·배포 완료(커밋 `8ff15ca`·edge 재배포, 배포본 해시 일치 확인).
> ⚠️ **배포 주소 정정**: 라이브는 `https://l2-pragmatics.up.railway.app` 하나뿐이다. 과거 기록의 `pragma.up.railway.app`은 **404이며 존재하지 않는다.**
>
> **112. 프롬프트 지문(`prompt_snapshot_hash`) — 코어 생성 provenance의 저장 규칙.**
> 기존 `generation_prompt_version`은 `'core_v2'` **고정 리터럴**이라 프롬프트를 고쳐도 값이 변하지 않았다 — 즉 7월 초 생성분과 동결 후 생성분이 **기록상 구별 불가**였다. 이제 엣지가 **모델에 전송하기 직전의 문자열**로 지문을 계산한다.
> - **해시 입력** = 코어 생성 표면 전체: system 프롬프트(방향 2종) + user 프롬프트 템플릿(방향2 × 모드2 × 인접쌍2 × 산업 유무2 = 16분기, 값 자리는 고정 센티넬 `PROBE_*`) + 서버 주입 카탈로그(P/D/R 표기·산업 표기·역할 쌍·화자 권리/책임·상대 의무/선택권·결정 권한·조립된 `context_spec` 행렬) + `model` + `model_fallback` + `temperature` + `response_format` + (Structured Outputs 사용 시) **strict JSON Schema 본문 전체**. 키 정렬 canonical JSON → SHA-256.
> - 🔴 **셀별 입력값(화행·수준·도메인·P/D/R·장면시드)은 해시에 넣지 않는다.** 넣으면 500행이 전부 다른 해시가 되어 **"같은 프롬프트로 만들었다"는 그룹핑 판정 자체가 불가능**해진다(= 동결 증명 실패). 그 값들은 이미 행 컬럼(`speech_act`·`learner_level`·`domain`·`scenario_p/d/r`·`topic_code`·`mode`·`language_direction`)에 저장되므로, **템플릿이 확정되면 최종 user 프롬프트는 100% 복원된다.**
> - 🔴 **엣지가 계산하고 클라이언트는 무변형 전달한다.** 클라이언트가 로컬 코드로 재계산하면 **배포본과 어긋나 기록이 거짓**이 된다(로컬 코드 ≠ 배포본).
> - 🔴 **소급 backfill 금지.** 지문 도입 이전 생성분(162건)은 `NULL`로 둔다. 채우면 확인되지 않은 사실을 기록하는 것이 된다.
> - 저장 경로: 컬럼·RPC(`save_generated_core`)는 **2026-07-23 migration에 이미 존재**했고 값만 비어 있었다. **migration 불요.** 단건 생성(`AdminGenerator`)도 같은 규칙을 따른다.
> - 부수 효과: 배포본이 자기 지문을 보고하므로 **"배포본 = 이 커밋의 프롬프트"를 기계적으로 증명**할 수 있다. 지문이 코드 기준값과 다르면 배포가 낡았거나 프롬프트가 바뀐 것이다.
> - ⏳ **미완**: DB 저장 왕복이 아직 실증되지 않았다(`save_generated_core` 권한 오류 — 0-u 하단 참조). **500 배치 전 필수 확인.**
>
> **113. 프롬프트 정본의 소재 = 엣지 소스 단일. 화면은 빌드 시 자동 스냅샷(읽기 전용).**
> - **모든 AI 프롬프트의 정본은 `supabase/functions/generate-scenario/index.ts` 하나다.**
> - ⚠️ DB 테이블 `prompt_templates`는 **생성 파이프라인이 조회하지 않는다.** 2026-07-06에 틀만 만들고 `content`가 계속 비어 있었으며, 이를 채워도 **생성에 아무 효과가 없다.** 관리자 화면에서 「문서 보관함(생성에 사용되지 않음)」으로 분리 표기한다. 마감 전 삭제하지 않는다.
> - 프롬프트를 **손으로 옮겨 적은 문서는 금지**한다. 코드가 바뀌는 순간 조용히 거짓이 되기 때문이다(구 `question-designer` 전례). 대신 `prebuild`가 엣지 소스에서 **모델에 실제로 가는 문장**을 렌더해 스냅샷으로 굳히고, 화면은 그것만 읽는다 → **화면이 코드보다 낡을 수 없다.**
> - 화면에는 **편집 경로를 두지 않는다.** 프롬프트를 고치려면 코드를 고쳐야 한다. 표시 항목 = 프롬프트별 SHA-256 + 코어 표면 지문 + git 커밋 + 캡처 시각, 그리고 **DB 집계로 "이 지문으로 생성된 시나리오 N건"**(정본과 다른 지문이 섞이면 동결 위반 경고).
> - 스냅샷 파일은 **자동 생성물**이다. 직접 수정하지 말고 프롬프트 수정 후 재생성한다.
>
> **114. 미션 승격 가용 범위 = 9화행 중 3화행. "500 코어 ≠ 500 미션"의 두 번째 이유.**
> `target_feature` 카탈로그(코드 정본, **AI 생성 금지·사람이 쓴 것만** — R14)에 현재 **요청·거절·감사 3종만** 정의돼 있다. 나머지 6화행은 화용 초점이 없어 **미션 승격 자체가 불가능**하다.
> - 코어 생성은 9화행 전부 가능하므로 **500 코어 목표에는 영향이 없다.** 영향은 **학습 미션 수**에만 있다.
> - 이는 코드 결함이 아니라 **연구자가 판정 축을 확정해야 하는 사안**이다. 나머지 6종 작성 전까지 해당 화행 주차는 교안·미션 내용이 비어 있다.
> - 대외 보고 시 **"코어 몇 개"와 "학습 가능한 미션 몇 개"를 반드시 구분**한다(0-q·101 분리 계수의 연장). 7/26 구두 보고에서 이 구분을 적용했다.
>
> **115. 시연 편의 변경은 학습 경로를 침범하지 않는다 (구인 보호 원칙).**
> 지도교수 시연 편의를 위해 **번역 산출을 마친 뒤 통역으로 이어질 때 1부(MPJ 판단 연습)를 건너뛰는** 경로를 넣었다. 이때 지켜야 할 원칙:
> - 🔴 **학습 미션 단위는 여전히 MPJ 5문항 + DCT 1회다. 이 정의는 바뀌지 않았다.**
> - 🔴 단축 경로는 **샘플 미션 + 데모 플래그에서만** 작동하도록 코드에서 격리한다. 실제 학습 세션에서 1부를 건너뛰면 **"판단 → 적용"이라는 미션 구인과 완료 조건이 깨진다.** 이 가드를 푸는 변경은 계약 개정 사안이다.
> - 일반화: **시연·데모 편의를 위한 변경은 실 학습 경로에 절대 침범해서는 안 된다.** 데이터 오염·구인 훼손이 조용히 일어난다.
>
> **⚠️ 0-u 미해결 이슈 — `permission denied for function save_generated_core`**
> 관리자 화면에서 코어 1건 생성 시 저장 단계에서 실패한다(현재 세션이 `is_admin()`을 통과하지 못함). **provenance 코드 문제가 아니며**(클라이언트 전달 경로는 코드 검토로 정상 확인), `profiles.role='admin'` 확인이 필요하다. **이 때문에 112의 저장 왕복이 미실증 상태다 — 500 배치 전 반드시 해결.**
>
> 📌 **부수 수정(계약 무관·기록)**: 개별 생성 화면이 **기본 상태에서 곧바로 R1c 실패**하던 버그를 고쳤다(기본 도메인 `work` ↔ 기본 주제 `campus_study`(school 전용) 불일치). 지도교수 "개별 생성 오류 발생" 리포트의 원인. theme·topic 파생이 domain을 존중하도록 3중 교정.

> 📚 **0-t (2026-07-26) — 문헌 라운드 판정 (사용자 전부 승인).** 입력 = 선행연구 **8편 전문 독해**(폴더 `md file\웹앱 적용 논문`) + GPT 설계변경안 A~E + GPT 학습설계 후보 A·B·C. 성격 = **대부분 기존 설계의 사후 정당화**이며, 실질 신규는 아래 🔴 1건과 프롬프트 층 3건뿐. **스키마·미션 골격·화면 무변경.**
>
> ### 🔴 t-1. 고P 셀 직접성 편향 — 코어 동결(7/29) 전 필수 검사 [유일한 일정 결합 항목]
> **Yu(1999)**: 중국어 화자는 **상위자(P=higher) 상황에서 동등 상황보다 직접 전략을 *더* 많이** 쓴다. 단 직접형은 **명령형이 아니라 원망 진술(我希望·我想)** + **다량 완화**(완화어·보조 이동). 근거 = 연대 공손(solidarity politeness), 직접성이 성실성·명료성의 표지. 저자 명시: *"직접적인 것이 공손할 수 있다는 함의는 간접성=공손이라는 보편성 주장에 도전한다."*
> ⚠️ 현재 `directness_level` 1~5(5=가장 직접/명령)가 서구 기본값("P 높으면 간접이 적정")으로 작동하면 **고P 셀 기준안이 체계적 편향** — 243셀 중 P=상위 약 1/3 → 500의 상당분이 걸림. **동결 후 발견 = 500 재생성.**
> → **7/27 스모크에 눈검사 항목 추가.** 절차·판정 기준·교정 문구 초안 = `md file\PRAGMA_스모크_체크리스트_2026-07-27.md`. **판정 전까지 프롬프트 변경 없음.**
> 부수 효과: 이 발견은 **"중국어의 공손 레버는 간접성이 아니라 완화량"**을 뜻하므로, 아래 t-2(완화 축 직교)의 **Su&Ren과 독립된 두 번째 실증 근거**가 된다.
>
> ### t-2. 완화(mitigation) 축 — directness와 직교. **7월=라이트 / 8월=풀 GATE**
> **Su&Ren(2017)**: 학습자는 전략 레퍼토리는 갖췄으나 **내적 완화에 전반적 무능**하고, 숙달도가 올라도 **양은 안 늘고 종류만 는다**(NS vs 전 학습자군 유의차, 학습자군 간 무차). 🔴 **모든 수준에서 완전 미사용 = understater(一下·一点)와 appealer(好吗·行吗·可以吗)** — 오답 정조준점. 공손표지만 수준 지표(Lv2·3 请 일변도 → Lv4·NS 帮·请问 분화). 저자 교육 함의 = *"이미 레퍼토리에 있으므로 언제·어떻게 쓰는지의 noticing 강화가 핵심"* — PRAGMA MPJ 판단 과제와 동일 논리.
> - **7월 라이트**(승격·피드백 프롬프트 어휘, 스키마 무변경, 코어 동결 무관): ①닫힌 완화 인벤토리 주입(통사 S1~S4·어휘 L1~L6·강화 U1~U2·호칭 A1~A4) ②오답 1종 = **"一下만 빠진 한 끗 차이"** ③피드백이 **빠진 장치의 이름**을 말함("帮我改 → 帮我改一下") ④**신규 오답 유형 = 축자역**(아래 t-4 ①) ⑤화행별 semantic formula 표(아래 t-3). **4유형 회귀 필수.**
> - **8월 풀 = HIGH-RISK GATE**: C1~C5 후보 5종 세트 · `mitigation_band`(M0/M1/M2/M3) 정식 축 · `modifiers_used` 태깅 · 인벤토리 **외부 버전관리 JSON**(0-g·48 versioned provenance 노선과 정합, 개정 이력=논문 재현성 재료). **미션 후보 구조 재설계 사안**(후보 수는 현재 수준 변수 3/5/7)이라 계약 개정+승인 필요. 9월 실증 전 확정이면 데이터 손실 없음.
> - **직교성 자동 검증 = 가능**: ⓐ완화 밴드 = 닫힌 표층형이라 문자열/정규식 → **완전 결정론** ⓑ전략 동일성 = C2·C3를 **C1의 편집본으로 생성**(인벤토리 항목만 가감)하면 diff로 기계 검증. 독립 생성 후 LLM 자기 태깅은 신뢰 불가 ⓒ정보 단위 = 불변항 체크리스트(§4 백신3) + 최종 교강사 검수.
> - ⚠️ 사실 정정: GPT안의 "L6 동사중첩=학습자 미사용"은 원문과 다름 — Lv3·4는 사용(Table 10). 미사용은 **understater·appealer 2종**.
>
> ### t-3. 화행별 semantic formula — 판정 어휘 확장(신규 축 아님)
> **Li, Taguchi & Xiao(2019)**: 같은 "부적절" 판정이 화행마다 전혀 다른 결핍이었다 — **거절=감사·이유·대안 누락 / (고부담)요청=정당화 누락 / 칭찬응답=겸양 회피(哪里哪里) 부재**. generic 척도 실패의 주원인. 해법 = Youn(2015)식 **화행별 하위 기술어**. 저자 강조: 자기 과제가 롤플레이보다 **더 통제된 CODCT였는데도** 발생.
> → 화행 9종 × 3~4개 "기대 formulae 표"를 **검증②·feedback-lite 프롬프트에 주입**. 판정 스키마 불변(directness·formality·imposition 3축 유지 + 화행 고유 결핍 진단 추가).
>
> ### t-4. Qin & van Compernolle(2021) C-DA — 설계 차용 (PRAGMA 쌍둥이)
> ① 🟢 **오답 유형학**: 5지선다 중 1개는 **축자적 의미**(말은 알아들었으나 의도 추론 실패), 3개는 "부분만 이해 시 그럴듯한 것". 축자 선택은 부분점수 없음(구인=의도 추론). → PRAGMA `failed_challenge`는 화용 실패 *방향*만 태깅하고 **"의미는 정확하나 화용 의도를 놓친 직역"** 유형이 없다. **통번역 과제에 가장 자연스러운 오답 유형 → t-2 라이트에 1종 추가.**
> ② 비표적 문항 삽입(표적12+비표적4로 무엇을 재는지 은폐) — 1부 MPJ 5문항 고정이라 8월 검토감.
> ③ 점층적 매개 3단(①힌트 없이 재시도 ②관련 내용 강조 ③강제 선택) + 시도 회차 기록. ⚠️ **learning potential score는 점수 체계라 0-q·95(점수 산출 금지)와 충돌 — 차용은 힌트 3단 구조와 시도 회차 기록까지.** C-1 로깅에 **시도 회차 필드**만 추가하면 확보됨.
>
> ### t-5. 관계 효과형 feedback-lite — **7월 수용. 신규 축 아님**
> GPT 후보 B. **계약 0-q·95가 이미 판정 순서에 "화용적 인상" 층을 규정**하고 있으므로 **새 층이 아니라 기존 층의 서술 구체화** — 계약 개정 불요, 비용 최소.
> 🔵 **이론 정박(오늘 발견)**: **Haugh & Hinze(2003)** 메타언어 = face·politeness의 공통 요소는 "타인에 대한 외적 평가"이며 **"A가 B를 어떻게 생각하는지 A가 *보이는* 것" / "A가 B를 어떻게 생각한다고 B가 *생각하는* 것"** 두 항으로 분해된다. GPT 후보 B의 두 관점(**표현이 보여주는 관계적 메시지 / 수신자가 받아들일 인상**)이 이 두 항과 **일대일 대응**. Chen 외(2024)도 같은 편 — 평정 대상을 화자가 아니라 **대화(상호 교환)**로 둔 근거가 "적절성은 일방적 의도가 아니라 상호 교환에서 달성".
> **조건 2개**: ⑴**주 카드 2문장 상한**(0-r·103·0-j·72 화면 감량 유지, 확장 서술은 [자세히] 안으로) ⑵**4유형 회귀 필수** — 화용층 강화가 **의미층을 침범**해 0-r·108 실패(완화 소실을 의미 손실로 이중 계산)가 재발할 수 있음. 어조는 기존 비단정 유지(Youn 백신4)와 이미 정합.
>
> ### t-6. 학습자 의도 기록 — P1 유지, 단 **"9월 실증 개시 전 필수"로 데드라인 고정**
> GPT 후보 C. 근거는 최상급 — **Taguchi & Li(2021)**가 대조언어학적 접근·평정척도 접근 **둘 다 학습자 행위성(agency)을 담지 못한다**고 결론짓고 해법으로 **회고적 코멘트 수집**을 명시.
> ⚠️ **이견 채널과 중복**: 어제 배포한 `context_judgment`(조건 4종+한 줄 이유)가 같은 구조다. 둘 다 넣으면 한 미션에서 **자기 이유를 두 번** 적게 되어 화면 감량 원칙 위반 + 순수 피로 증가.
> → **통합 설계**: 같은 칩 목록·같은 저장 필드를 쓰되 **시점만 분리**(제출 시=선택 이유 / 피드백 후=판정 이견), 또는 둘 중 하나만. **C-1(문항별 MPJ 선택률 로깅)과 한 라운드로 묶는다.** 7/31 완료 기준 무관, 그러나 **학기 시작 후 소급 수집 불가**.
>
> ### t-7. 인용 정박 (코드 0줄, defensibility만 상승)
> - **Shin & Lee(2023)**: CSAT vs ChatGPT 블라인드 50인. 지문 자연스러움은 **차이 없음**(d=.24/.14 ns)이나 **오답 매력도 d=.64\*·문항 완성도 d=.42\*로 유의하게 열등**. 자유응답 최빈 지적(n=22)="매력적 오답 부재"(말 안 되는 선택지·지문에 없는 내용·**복수정답 위험**). CSAT 측 부정 코멘트는 n=3. → **검증②의 `implausible_distractor`·`answer_cue`와 교수자 승인 게이트의 실증 근거.** 심사 대사: *"AI 생성 문항의 알려진 결함은 지문이 아니라 오답이다(d=.64)."* 부수: Shin(2023) **모델 문항 > 프롬프트 단독** → 참조 미션(골든 3) 방식이 이 원칙의 구현.
> - **Chen, Li & Ye(2024)**: ①**"one-prompt-fits-all은 없다 — 프롬프트가 바뀌면 human-resemblance 재평가 필요"** → **7/29 코어 동결이 일정 관리가 아니라 연구문제 ③의 방법론적 요구**임을 정당화. 재현성 정의는 기존 0-g·48("재호출 결정성 비보장 병기 + versioned provenance + 저장 산출물 수준 재현성")이 이미 정답 — temperature·seed 고정은 동일 출력을 보장하지 않음 ②**"ChatGPT는 영어만큼 다른 언어를 못한다 — 그 언어로 훈련된 LLM 선택이 첫 단계"** → 검증②(비평 모델 분리)와 인간 캘리브레이션 필수의 근거 ③문어 대화만 다룸 → **통역 모듈에 그대로 적용 불가**(병기 필수).
> - **Li, Taguchi & Xiao(2019) 척도 실패 4양상**: ⑴평정자 2인이 **같은 L1·같은 나라·동일 교육과정·같은 직종·척도 공동 개발·표준화 절차까지 거쳤는데도** 해석 불일치 ⑵generic 척도가 화행별 뉘앙스 불포착 ⑶숙달도별로 척도의 다른 구간만 사용 ⑷**6점이 실질 4개로 붕괴 — 기술(descriptive) 치중의 결과.** 저자 결론: *"척도는 묘사가 아니라 해석을 위해 만들어야 한다."* → **MPJ 3대역 유지의 방어 근거**("왜 더 세분하지 않나" → *세분한 6점조차 4개로 무너졌다*). 9월 캘리브레이션 보정: 대역별 **최소 표본 수** 명시(저빈도 범주가 disordering 원인), 불일치는 실패가 아니라 **경계 문항 탐지 신호**, **평정자 훈련 절차 명문화**.
> - **Taguchi & Li(2021)**: NS 동질성 가정 붕괴의 계보 확정(Taguchi 2011b 문화배경 상이 → Li 외 2019 배경 동일) + **평정자 훈련 중요성 + 비원어민 평정자 포함 가능성**. **링구아 프랑카 맥락에서는 NS 규범이 아니라 "지역적으로 협상된 규범"이 적절** → 한국 대학 한중 통번역 수업이 정확히 이 맥락. **proposed 채점키(0-e·38)와 측정 주장 경계(0-q·100)의 최상 인용.**
> - **Haugh & Hinze(2003)**: 面子(사회적 명성·지위) vs 脸(도덕적 인격) 구분, **脸은 잃을 수만 있고 얻을 수 없음**. → 교수 단원 이론 콘텐츠 + 논문 "중국어 특수성" 절. ⚠️ **GPT 변경 C-2의 `face_consideration` 태그 경고**: 面子와 脸을 한 태그로 뭉뚱그리면 이 논문이 반증 근거가 된다 — 만들려면 최소 2종(8월 사안).
>
> ### t-8. GPT 설계변경안 A~E 판정 (2026-07-26)
> | 변경 | 판정 | 사유 |
> |---|---|---|
> | A-1 문법 분리 | ❌ **기각(기구현)** | feedback-lite가 이미 의미/문법/화용 층 분리(0-r·108 4유형) — 재구현은 퇴행 |
> | A-2 화행별 앵커 | ✅ 수용 | = t-3 |
> | A-3 척도 범주 가변 설계 | ❌ 기각 | **존재하지 않는 척도**의 마이그레이션 대비 = 과잉 설계. 산출 점수 자체가 금지(0-q·95) |
> | B 완화 축 | 🔧 분할 수용 | = t-2 |
> | C-1 선택률 로깅 | ✅ 수용·**지위 격상** | 이미 0-r 8월 ①에 존재. GPT 기여는 **프레이밍** — Li(2018)식 오답 기능 분석("아무도 안 고르는 오답=기능하지 않는 오답=제거")이 **RQ③의 정량 증거**. 8월 임의 → **9월 개시 전 필수** |
> | C-2 중국어 태그 3종 | ⏸️ 8월 보류 | 전제 문헌 미확보(Ye 2004·Chen Xinren 2019 폴더에 없음) + 面子/脸 분리 필요(t-7) |
> | D 生人/熟人 경계 변인 | ❌ 생성축 기각 / ✅ 축소 수용 | 매트릭스 LOCK·코어108·500 배치와 충돌. 대체 = D대역 **파생 읽기전용 태그** + 교안·논문 서술. (부수 통찰: D=지인 셀이 경계 통과 지점이라 교육적으로 가장 풍부 — 교안 재료) |
> | E 검수 게이트 4항 | ❌ **대부분 기구현** | 정보 등가→0-r·108+검증② / 자연성→`unnatural_language`·`implausible_distractor` / 직교성→B-풀 채택 시에만 유의미 / **호칭 적합성만 신규**(B-풀과 함께). GPT가 E를 제안하고 후보 A-2로 재제안한 것은 **검증②의 존재를 모르기 때문** — 이후 GPT 입력 시 감안 |
>
> ### t-9. GPT 학습설계 후보 A·B·C 판정
> - **후보 A(생성 품질 패키지)** = 🔧 **해체 후 부분 수용.** ⚠️ **패키지로 묶으면 안 됨** — A-3(직접성/완화 분리)은 7/29 동결 전 필수인데 A-1(유형별 골든 견본)은 **교수자 검토 시간**이 필요하고 그 시간은 500 배치·admin 검증에 이미 묶여 있다. 묶으면 **가장 급한 것이 가장 느린 것에 발목잡힌다.** → A-3=t-1/t-2로 즉시, A-1=8월(코어에 MPJ 없으므로 500 품질 무관 — 0-g·45), A-2=기구현.
> - **후보 B(관계 효과 피드백)** = ✅ **7월 수용** = t-5.
> - **후보 C(의도 기록)** = ✅ P1 동의 + **데드라인 고정** = t-6.
>
> ### t-10. 확정 실행 순서 (사용자 승인 2026-07-26)
> 1. **provenance-lite**(완료 기준 ④의 빠진 절반) → 2. **7/27 스모크 + 🔴 고P 셀 검사**(t-1) → 3. **7/29 동결 → 500 배치**(7/30 초과 금지) → 4. **t-5 + t-2 라이트 한 라운드**(4유형 회귀 필수) → 5. 표현 고착도 감사(배치 후, Chen quantile 차용) → 6. **8월**: t-6+C-1 통합 로깅(9월 전 필수) · A-1 골든 견본 · **t-2 풀 GATE** · C-2 재검토 → 상시: t-7 인용 정박.
> **미결 이월**: 수준별 통역 비율 A/B/C (2026-07-25 GATE, 판정 대기 — 판정 전 코드·프리셋 변경 금지).

> 📘 **0-s (2026-07-25 심야 3차) — 구 문항설계 프로토타입 자산 회수 + 후보 생성 원칙의 정당화 문장 확보. 계약 변경 없음(기록).** 회수 전문 = `md file\PRAGMA_문항설계_회수자산_2026-07-25.md`.
> **① 후보 생성 원칙 = "좁은 축 × 눈금"(방법론적 정당화 — 연구문제 ③ 재현성 통제에 대한 직접 답).**
> 최초 프롬프트는 추상적이었다(*"P·D·R과 rapport management를 고려해서 번역해" vs "그냥 번역해"*). 결함 = 후보 간 차이가 **어느 변수에서 왔는지 통제되지 않아**, 학습자가 A 대신 B를 골라도 그것이 직접성 판단인지 격식 판단인지 알 수 없다 → **선택이 측정값이 되지 못한다.**
> 현재 방식 = **화용 변수 하나(directness)만 좁게 고정 + 1~5 강도의 눈금으로 후보 생성.** 후보 간 차이가 통제된 단일 변수에서만 발생하므로 학습자의 선택은 **그 변수 하나에 대한 판단값**이 된다. 이것이 research robustness의 실제 근거이며, **논문 방법 절에 그대로 쓸 문단**이다.
> 코드 확인(2026-07-25): `generate-scenario/index.ts` — `directness_level` 1~5(:240,255) · 후보 개수 = 수준 변수 `candidateCount`(:254) · `appropriateness_label` + `failed_challenge: directness|formality|imposition`, **한 후보당 primary failure 하나만**(:257-258). **이미 계약대로 구현돼 있음 — 조항 신설·변경 불요.**
> **② distractor 정의 재확인**: distractor는 문법 오류가 아니라 **의도적으로 심은 화용 실패**다(구 프로토타입 문구, 현재 `failed_challenge` 필드로 실현). 이 정의는 검증②의 `implausible_distractor` 판정과 같은 뿌리다.
> **③ 방어 스크립트 추가**: *"문항 설계는 시나리오 생성 후의 단순 후처리가 아니라, 학습자 수준에 따라 후보 수·힌트·수정 범위·산출 부담을 조정하는 핵심 설계 단계다."* — 심사의 "그냥 GPT로 문제 뽑은 것 아닌가"에 대한 한 줄 답.
> **🟠 미결 안건(HIGH-RISK GATE, 사용자 판정 대기 — 2026-07-26 재개 시 첫 안건)**: **수준별 통역(Oral DCT) 비율**. 회수 표(입문 0·중급 0·고급 0.4)와 현행 프리셋 값(입문 0.40 · 고급 0.35 — **수준과 역행**)이 충돌. 선택지 A(수준 파생)·B(현행 테마 우선 유지)·C(수준은 상한만, 그 안에서 테마가 결정 — **Claude 권고**). 상세 = 학습구조md·관리자구조md 같은 날짜 블록. **판정 전까지 코드·프리셋 변경 금지.**

> 🔴 **0-o (2026-07-25) — `channel`(매체) 축 폐기 (사용자 확정, 상위 구조 결정)**
> `channel = messenger | email | facetoface | phone`을 **조합축·생성조건·판정축·학습분기에서 폐기**한다. 근거: ①매체가 말투를 결정하지 않음 ②화용 조절은 이미 P·D·R + speech act + target_feature가 담당(channel은 P/D/R과 중복·"매체=격식 자동결정" 오모델링) ③이메일/메신저 차이는 화용 대역이 아니라 텍스트 외형·담화 조직.
> **활성 조합축 = 화행9 × P3 × D3 × R3 = 243 기본 셀** (구 729 폐기). **수행 방식 = `task_mode`(translation | interpreting)** — channel 대체 변수가 아닌 순수 수행 속성. 1부 MPJ 공통, 2부 DCT만 번역/통역 분기.
> **폐기 처분(물리 삭제 아님, legacy 격리)**: ChannelSchema `optional`화(읽기 호환) · 신규 생성 채널 미요구 · 생성 프롬프트의 "매체 관습(문체·격식)" 강제 제거 · **R23의 channel 계승검사 삭제** · batchPlan 회전축 channel→task_mode 1차 승격 · 러너 매체 배지 제거 · genre 행태그는 task_mode 파생(DB 무변경) · 기존 저장 데이터 무수정. `genre`/`discourse_format` 등 대체 metadata **신설 금지**.
> 상황 자연어의 매체 표현("메시지 보낸다·서면 요청·회의 중 답한다")은 **자유 서술로 허용**(enum·조합축·평가변수·자동 말투 규칙·필수 metadata 아님).
> 영향: 아래 R16(모드↔채널 독립)·R23(계승)·§7-0 배분축·§2b 행태그·0-k·79(채널 담화형식) 조항은 이 0-o가 상위 규정 — 매체를 축으로 읽는 서술은 무효.
> ⚠️ edge `generate-scenario` 프롬프트 수정은 코드 반영됨, **재배포는 승인 후 활성**(그 전까지 live 엣지는 mode 파생 legacy channel로 호환).

> 🟢 **0-p (2026-07-25 오후) — 학습 미션 러너 실구현 (0-k·74~79 실현, 계약 자체 변경 없음).** 커밋 `b252aac`→`021d9d1`→`09910ad`. 러너 정본 경로 = `/learner/practice`(MissionRunV1), 구 `/scenario` 목업 은퇴. **배포 = Railway `main` 브랜치 자동배포**(이 서비스는 main 연동 — feature push는 배포 안 됨).
> 계약 관점 실현: ①2부 분절·인계·**상황 확인 = 판단형**(relation_ko·pdr 결정론 파생, 무채점) ②**통역 실동작**(§0-b·16 실현 — TTS 듣기→MediaRecorder 녹음→SpeechRecognition STT 초안→학습자 전사 확인→§4 3층 피드백. 폴백=수동 전사. 발음·성조·유창성 평가 제외 유지) ③**대화 스킨 = 채널 상표 무표기**(0-k·79 실현 — 위챗 문법 유지·"微信" 등 상표 비표기, `preceding_turn`을 상대 말풍선으로 렌더) ④B1 판정 대역 = **proposed 지위 1회 고지**(0-e·38).
> 스키마·R검사·프롬프트 불변. `preceding_turn`은 러너 대화 스킨의 상대 말풍선(R8/R10 = 선행발화가 방향 target 언어 — 샘플·zh_ko 미러 픽스처 이 규칙으로 보정). feedback-lite 실판정 = 여전히 비범위(정직 배너). 데모 도구(데모 채우기·2부 점프)는 **IS_DEMO(VITE_ENABLE_DEMO) 전용** — 생성·실행 콘텐츠와 무관, 실 학습 세션 미노출.

> **지위**: fable 설계 → GPT 교차검증 5라운드(33건→8건→5건→5건→감량 5건) → 외부 검토 4인(0-g) → **GPT 후속 단독 검토 5 BLOCKER 판정·머지(v1.5, 0-h — 07-24)** → opus 구현 입력.
> 구현 중 임의 변경 금지 — 변경 필요 시 이 문서를 먼저 고친다.
> v1.2 개정 근거: **지도교수 요구 2건 사용자 확인** — ①통번역 시나리오 DB **최소 500개**(여러 번 직접 언급) ②통역 7월 최소기능 필수(TTS·STT, "API 연동 불요 수준". 발음·성조·유창성 평가 제외는 사용자가 반박해 확정). 감사 기록 §11.
> **마감 구조**: 7/26 저녁 = 중간보고(공격 목표 = 전체 루프 시연) / **7/31 = "개발연구 프로토타입" 완료 실마감**(0-g·46 — "플랫폼 완성" 명칭 폐기. 피드백 반영 7/27~31).
> v1.1 판정 근거: GPT 33건(수용 26·부분 6·기각 5, §11). 근거 정본: 학습구조md·관리자구조md §7-9·문헌적용맵 1~3차.

---

## 0. v1 → v1.1 핵심 변경

1. **축·대역은 화행 공통이 아니라 화용 초점별** — `target_feature` 카탈로그가 `band_schema`를 정의, 문항·피드백 모두 그 코드를 쓴다 (A1·D21)
2. **PDR 값 개명**: `p: speaker_lower|equal|speaker_higher` · `d: close|acquaintance|distant` (A2)
3. **mission_v1 = 서면 번역 전용.** 통역은 태그만(편성·보고용), 학습자 실행 제외 (A4)
4. **모든 판정은 `accepted_band_codes` 배열** — 후보 포함. 경계 = 배열 길이>1로 계산, `is_boundary` 필드 삭제 (A6)
5. **`summary` 필드 삭제** — 문항별 `recommended_example_zh`를 코드가 모아 렌더 (B13)
6. **행 태그 의미론 규칙**: scenarios 행의 p/d/r·genre = **production_task 기준**(편성 필터 대상), MPJ 문항별 값은 JSON이 정본. `week_no` 생성 시 기록 금지 (B10·B11)
7. **멱등성**: `generation_run_id`·`generation_item_key`(UNIQUE)·`content_hash` (B16)
8. **파이프라인**: outline(mission_outline_v1) → final(structured output) → 결정론 검사 → **필드 단위 repair** → 재검증. temperature: outline 0.6 / final 0.3 / repair 0.1 (C17~19)
9. **승인 분리**: `approval_basis`(item_manual_review | sampled_batch_release | rules_only_release). **fail은 승인 불가, warning은 사유 기록 후 override** (E27·28)
10. **7월 승인 범위**: 골든 3 = 개별 인간 검토 / 파일럿 27셀 = 표본 인간 검토 후 sampled release / 나머지 = rules_passed 대기. **135 전량 approved 금지** (E29 수정 수용)
11. 규칙검사 R1~R21로 확장, R5·R9 강화 (F31~33)
12. `mission_attempt_v1` 학습자 수행 로그 계약 신설 (D26)

## 0-b. v1.1 → v1.2 핵심 변경 (2026-07-23 저녁 — 2층 분리판. **opus는 이 절부터**)

13. **2층 분리**: 500개 구축 단위 = `scenario_core_v1`(검색·편성용 코어, §2b) / **완전 미션은 편성·데모 선별분만** `mission_v1`로 승격. "500 = 완전 미션 500"이 아니다
14. **같은 행 미션화 + 병렬 상태 보존**: `content_format`은 승격 후에도 `'scenario_core_v1'` 불변. 미션은 `mission_content`(NULL 가능)·`mission_status`(NULL|generated|reviewed)로 별도 관리, 미션 provenance는 mission_content 내장. "500개 코어 중 하나"라는 신원·코어 provenance가 절대 흐려지지 않음
15. **`source_modality`**(written|spoken) 신설 — 통역 코어 = spoken + **구두 담화체 원발화**(이메일 문어체를 TTS로 읽는 위장 금지)
16. **통역 7월 필수**(지도교수 지시): TTS 재생 → **MediaRecorder 녹음과 SpeechRecognition 실시간 인식 병렬 실행**(SpeechRecognition은 라이브 마이크 전용 — Blob 재전사 불가) → **학습자 전사 확인·수정** → 확인 텍스트로 §4 3층 피드백. 발음·성조·속도·유창성·음질 평가 제외. 폴백: 병렬 실행 실패 환경 = 녹음+직접 입력. 9월: Edge Function 경유 OpenAI STT 스왑(키 브라우저 비노출)
17. **학습자 실행 게이트**: `mission_status='reviewed'`(개별 인간 검토)만 실행. 코어·미검토 미션은 검색·편성까지만
18. **feedback_v1-lite**: 숫자 offset 삭제(`anchor_text` 존재 검증만) · 문법 카드 최대 1 · **uncertainty_flags는 로그 전용 유지**(7/22 잠금 — 캘리브레이션 표본용, 삭제 금지) · 교차 필드 검증·루브릭 주입 유지
19. **axis_feature = unit.target_feature 고정** — politeness 보조 축은 이후 버전(단일 초점 원칙 준수). scale4는 전반 판정 척도로 별개 유지
20. **Wu&Roever 완화**: 유발행위별 구조 = 고정 규칙이 아니라 **선택적 자원**(집단 경향≠생성 공식)
21. **R16 재작성**: 모드↔채널 독립(대면·전화는 통역의 대표 환경). 통역 정합 = source_modality='spoken'
22. **멱등키 복합화**: `UNIQUE(generation_run_id, generation_item_key)` — 셀 재생성·supersedes와 충돌 해소
23. **단계 배치**: 20 smoke → 100 분포 검산 → 500 본 배치. 표본 검수 27 = **화행9×수준3 전수 + 모드·도메인 균형**

## 0-c. v1.2 → v1.3 핵심 변경 (2026-07-23 밤 — 편성층 메타 + GPT 4라운드 반영)

24. **`theme_code`(8종 통제값)·`topic_code`(장면 시드 코드) 신설** (§2b) — 편성·검색용 교차 축, domain 불대체. **논문 서술 경계: theme·topic은 화용 판정 변인이 아니라 운영 메타데이터** — 새 화용 변인으로 서술 금지
25. **`scenarioTopics.ts` 카탈로그 계약 명시** (§2b) — topic은 AI 임의 생성 금지(카탈로그 원칙), 시드 선택이 곧 태그. 배치 전 최소 요건 = theme당 topic ≥3
26. **15주 프리셋 5종** = 최소 필터 조건을 가진 TS 상수 (§2b)
27. **RPC payload 소유권 잠금** (§2b·§6): 검색 메타 = 행 컬럼 / 상황·원문 = core_content(p/d/r·channel의 정본 — RPC가 행 태그로 파생)
28. **DB CHECK 강화** (§6): 코어는 theme_code 8값 + topic_code NOT NULL 필수
29. **원문 분량 사다리 정정**: ~~고급=화행 연쇄~~ → 고급 = 긴 담화 맥락·복합 관계 조건이되 **중심 화행 1개·화용 초점 1개 유지**(번역 3~5문장 / 통역 = 기억 과부하 없는 짧은 구두 담화). 화행 연쇄는 미션이 아니라 **12주차 통합 과업 유형**(기존 weekType 설계와 정합) (§7-1)
30. **확신도 범위 확정** (§6b): MPJ **④ reason_conf만** confidence 보유 — ①~③·⑤·DCT는 없음. GPT의 전 문항+DCT 확장안은 **기각**(7/22 피로 원칙 잠금 + response set 오염 방지). 9월 파일럿 후 재검토

## 0-d. v1.3 → v1.4 핵심 변경 (2026-07-23 밤 — 7/31 실마감 감량 라운드)

> 판정 전제 교정: GPT 감량안 5건 중 1·3(일부)·4(일부)·5는 **이미 구현 완료**(스키마·R검사·카탈로그 typecheck 통과) — "미구현 전제" 불성립. GPT 자신의 단서("이미 완성된 것은 삭제 비용 재계산") 적용.

31. **미션 승격 파이프라인 단순화(수용 — 유일한 미구현 = 진짜 절감)**: ~~outline→final→필드 repair~~ → **structured output 1회 → R검사 → 실패 시 미션 전체 재생성(최대 2회, temp 상향+실패 사유 주입)** → 인간 검토. outline 스키마·필드 repair 엔진 삭제. 코어 파이프라인(§7-0)은 원래 단일 호출 (§7 개정)
32. **reference_alternatives 정확히 2 → 1~2 완화**(R11). `recommended_example_zh`는 유지 — 완료 화면(요약=표현 정리) 잠금의 재료 + 이미 구현
33. **feedback grammar 카드의 `error_type`·`anchor_text` = 선택 필드**(있으면 검증). uncertainty_flags·교차 필드 검증·루브릭 주입 유지(**3차 기각** — 7/22 잠금 + 저비용)
34. **MPJ 5유형 사전 축소 기각 → 조건부 강등 경로 채택**: 골든 게이트에서 특정 유형이 화행당 3회 시도에도 품질 미달이면 **폴백 = scale4 1 + judge3 4(마지막에 이유 선택+확신도)**로 강등, 사유를 §11에 기록. 기각 근거: ①5유형 흐름 = 지도교수 승인 잠금 ②판정+교정(fix_choice) = 사용자 최고 평가 유형 ③union·R검사 구현 완료(매몰) (§10 개정)
35. **errorPatterns.ts 삭제 기각**: 이미 구현·문헌 근거 지불 완료. 프롬프트 지위만 명확화 = **선택적 시드**(의무 주입 아님)

## 0-e. v1.4 정합 라운드 (07-23 밤 — GPT 3문서 정합 점검 14건 반영)

36. **골든 게이트 상태 정정**: "통과" → **콘텐츠 검증 통과(생성·R1~R23·눈검사 2라운드) + DB 저장→재조회 렌더 대기**(RPC 라운드트립, 7/24 배선 시). 동결 = **프롬프트 동결(조건부)** — 라운드트립 통과 후 완전 동결·reviewed 승인 가능
37. **CHECK 강화**: reviewed에 `mission_reviewed_by` 필수 + 미션에 `target_feature_version` 필수 (§6 개정 + 추가 마이그레이션)
38. **band 지위 명시**: accepted_*_codes = proposed 채점키(§3-2). validated 표시는 9월 캘리브레이션 스키마
39. **R2 하향**: 반례 슬롯 구조 검사임을 명시 — 내용 타당성은 눈검사(§8)
40. §8 제목 v1.2→v1.4 정정. 구형 outline→final 경로·전이(transfer)·숙달 확인의 문서 잔존 정리는 학습구조·관리자구조md에 반영(전이·숙달 확인 폐지, 통역=정규 미션 모드, 7월 검증 가동 = ①+④만)

## 0-f. 리포트·프로필 범위 라운드 (07-23 심야 — 사용자 확정)

41. **개인 화용 프로필 = 7월 미구현.** 7월은 수행 로그(§6b: MPJ 판단·최초 산출·3층 피드백 결과·수정안·완료/시간)만 보존. 새 프로필 스키마·자동 집계 로직·추천 엔진·리포트 화면의 **상세 설계 및 구현은 하지 않는다 — 후속 기능의 목적·주장 경계·필요 로그만 명시한다**(0-f·42~43). 후속 프로필 정의 = **능력 진단·숙달 판정이 아니라 관찰된 수행 패턴의 기술적 요약**(예: "최근 거절 미션 5회 중 완충 부족 피드백 3회")
42. **리포트 2종 = 후속 기능**(지도교수 장기 요구): 개인 = 논문 표기 **"주차별 수행 추이 리포트"**(화면명 '성장 리포트' 허용) / 학급 = **"학급 수행 경향 리포트"**(40~50명 익명 집계). **단일 "화용 성장 점수" 금지 · 능력 향상 입증 주장 금지.** 자동 집계·요약·화면 = 7월 범위 아님
43. **로그 연결성 확인**: learner(profile_id·auth_user_id)·cohort_id·started/completed_at·speech_act·target_feature(feature_id)·mode(task_type: translation|interpreting)는 현 `learner_mission_logs` 구조로 확보 ✓ — 로그 JSON에 중복 저장하지 않는다. **후속 보강 필요 2건**: ①배정 식별자(`curriculum_week_scenario_id`) 링크 — 테이블 자체가 7/24 신설이라 로그와의 연결은 후속(week_no는 이 조인으로 파생, 중복 저장 불요) ②`post_revision_feedback` 또는 `revision_resolved` — 수정 후 문제 해소 판정(수정 성공 추이 리포트의 전제). **지금 신규 필드·RPC·분석 엔진을 구현 범위로 확정하지 않음**

## 0-g. 외부 검토 라운드 (07-23 심야 — 4인 적대 검토: Claude Code·Claude Chat·GPT PRO·Gemini PRO, **전원 조건부 GO**. 패킷 = `PRAGMA_외부검토패킷_2026-07-23.md`, 원답변 = `외부검토_4인답변_0723.txt`)

44. **"골든 미션" 명칭 폐기 → "파이프라인 참조 미션(채점키 proposed)"** (4인 수렴). 학습자 화면에 "AI 제안 기준(검증 예정)" 배지. 모어 화자 3인 캘리브레이션 일정 고정 = 9월 실증 전(가능하면 8월) — validated 문항만 실증 공개
45. **MPJ 판정 후보 = 게이트1(불변항) 통과 후보만** (3인 수렴 — 참조 미션 request③ 반증: 의향 묻기가 사라진 "周末一起去看电影吧"를 too_direct 처리 = 화용 대역 라벨을 의도 소실에 오용, 吧는 완화 표지 문헌 존재. **fable 눈검사 2차의 "번역 비평 옹호"는 오판 — 철회**). 부적절성은 오직 화용 대역에서만, 의도 소실·의미 이탈 예문은 MPJ 판정 대상 금지(그건 피드백 semantic_fidelity 층) → **승격 프롬프트 수정+눈검사 항목, 다음 승격 전 필수**. ⚠️ 코어 500 배치는 MPJ 없음 — 이 결함과 무관("500 복제 임박" 주장은 검토자 사실 오류)
46. **7/31 완료 기준 동결 = "개발연구 프로토타입"**("완성 제품" 명칭 금지): ①코어 500 + 감사표(54셀 커버리지·중복률·규칙 통과율·재생성률) ②프리셋 5종이 실제 15주 표 생성+주차 교체 ③번역 1·통역 1 완전 수직 경로(로드→판단→산출→피드백→수정→로그) ④reviewed 게이트·provenance 배포 환경 작동. **7/26 보고 필수**: 분리 계수(코어/생성/검토/실행가능) + 교수에게 "500의 단위" 명시 확인 + 승격 라이브 데모
47. **15주 편성표 출력에 target_feature·검토 상태 열 필수** + 프리셋별 반복 원칙 1문장 선언(RQ2 증명 장치 — 미지정 항목은 RQ2 구현으로 계산 안 함) → 태스크 D 스펙에 반영
48. **용어 정정**: ~~"재현성 보장"~~ → "versioned provenance + 저장 산출물 수준 재현성"(재호출 결정성 비보장 병기) · 통역 논문 표기 = "화용적 적절성과 의미·의도 유지를 다루는 한→중 통역 산출 모드"(UI '통역하기' 유지) · "HSK5·6≈L1" 압축 표현 정밀화(해당 9과업 시험 한정)
49. **ChatGPT 방어 v2** (4인 수렴): ~~"학습 도구 vs 수행 도구"~~ 이분법 폐기(Study Mode 존재) · ~~"판단력을 기른다"~~ 삭제(**효과 주장 누출 — 자체 주장 금지 목록과 모순**). 차별 = **교수자 통제층**(답 보기 전 판단·산출 강제·승인 게이트·구인 반복 감사·수정 이력) + **이중제약 구조 강제**(의미 게이트×화용 대역) + **스스로 요청할 수 없는 오개념 반례**(counter_rule). 허용 표현 = "연습 기회를 체계적으로 제공"까지. **정밀 문구(v1.5 — 0-h·58, ~~"범용 LLM은 아첨·역번역 왜곡 통제 불가"~~ 단정 삭제)**: *차이는 범용 LLM이 그런 학습을 "할 수 없다"는 데 있지 않다 — 교수자가 승인한 과제·화용 초점 아래에서 판단 전 제출, 의미·화용 이중제약, 반복 배치, 수정 이력·검수 provenance를 **일관되게 강제하고 감사할 수 있다**는 데 있다* (반박 가능한 불가능성 주장 제거)
50. **MPJ 5유형 서사 재정식화**: 매몰비용 논리 금지 → 유형별 교수 기능 1줄 정의 · 유형 간 점수 합산 금지 · "형식 다양성은 연구문제 아님(운영 선택)" · valid=2 강제가 자연 키를 왜곡하면 문항 재생성(구조 규칙 < 내용 타당성)
51. **문헌 재배치(집필 지침)**: 서론 필요성 축 IC→메타화용 판단·적절성(Dai&Roever는 배경·페르소나 근거로 강등, **중심 구인 = "번역 제약 아래의 상황별 화용 의사결정"**) · Wu&Dai **전문 입수**(공개 확인됨) 후 요청 화행 설계 가설로 하향+Taguchi&Li 이중화 · Taguchi&Li 하중 분산(Li et al. 2019 분리 인용) · 강병규·이해영 각주/국내 배경 하향 · Byon = 9월 수업 설계 장 이동 · **추가 필수: 번역학 기능주의(Nord·PACTE)+DDR 방법론(Richey&Klein)+논증 기반 타당도(Kane) 표** · Schmidt/Swain = 제한 인용("주의 기회 제공"까지 — 효과 암시 금지)
52. 기타 수용: "고급=경제성 회복은 채점 대역이 아니라 산출 표적·피드백 층에서만"(경계 문장) · 통역 승격 시 MPJ 후보 구두체 강제(프롬프트) · 참고안 호칭(负责人) 등 실물 결함 = 개별 검토에서 수정
**기각(사유 기록)**: 코어 500→150 축소(교수 본인의 숫자·단계 배치로 리스크 관리) · 편성기 목업 대체(교수 지시의 본체 — P0-A 자멸) · 프리셋 5→2(TS 상수라 비용 미미) · 백영경 장식 판정(3인 PASS — R8 국소 인용 적정) · "500 배치 전 동결 해제 선결"(사실 오류)

## 0-h. v1.4 → v1.5 (07-24 — GPT 후속 단독 검토 5 BLOCKER, fable 판정·머지. 종합: "외부 의견 추가 수집 단계 아님, 0-g를 활성 조항에 머지할 단계" — 동의)

> 판정 방식: 각 지적의 **사실 전제를 계약 원문·구현 코드와 대조**(편성 테이블·missionSchema·batchPlan·RPC 호출 실태는 07-24 opus 세션에서 코드로 확인됨) 후 수용/부분/기각.

53. **v1.5 정본화 (B1 수용)**: 0-g 결정을 활성 조항에 직접 머지 — 용어 통일: ~~골든 미션~~→**참조 미션**(§2·§4·§5·§10) · ~~"콘텐츠 검증 통과"~~→**"구조·규칙검사+연구자 눈검사 통과"**(내용 타당성 함의 제거 — proposed 채점키와 정합) · ~~"플랫폼 완성"~~→**"개발연구 프로토타입 완료"**(헤더). **단 GPT의 "이전 용어는 부록에서만" 부분 기각**: §11·0-a~0-g 판정 기록은 역사 문서 — 원문 유지+구칭 주석(판정 기록 소급 수정 = 감사 추적 자체 오염)
54. **게이트1 명문화 (B2 수용 + 사실 정정)**: §7-1에 불변항 문장 신설(모든 판정 후보·교정지·다중 후보·참고안 대상) + §10 눈검사에 H1·H2 추가. **단 request③ supersedes 처분은 사실 오류로 기각** — 해당 참조 미션은 **DB에 저장된 적 없음**(save_generated_mission RPC 미배선·골든 하네스는 메모리 검증+md 출력만, 07-24 코드 확인). 처분 = 리뷰 파일 폐기 표시 + **수정 프롬프트(게이트1 반영)로 재생성·재눈검사 = 승격 UI 배선의 선행 조건**(0-g·45 재확인)
55. **편성 초점 데이터 흐름 (B3 부분 수용)**: 지적 자체는 사실(코어에 target_feature 없음 → 편성 시점에 계획 초점 미정의). **7월 해법 = 파생 규칙**: 계획 초점 = 주차 화행 → `DEFAULT_FEATURE_BY_ACT`(카탈로그 기본 초점, 코드 상수 — 결정론·카탈로그 버전과 함께 감사 가능). 편성표 표기 = **계획 초점(기본)** vs **확정 초점(미션 승격분)** 구분. 승격 입력 = 계획 초점 명시 전달 → `unit.target_feature` 일치 검사(**R24 신설**). **`curriculum_week_scenarios.planned_target_feature` 컬럼(GPT 권장안)은 화행당 복수 초점이 되는 시점(9월 카탈로그 확장·교강사 초점 선택 UI)에 추가** — 7월은 화행당 초점 1이라 파생으로 충분, 마감 앞 스키마 변경 회피
56. **미션 provenance 객체 신설 (B4 수용)**: §3-1에 `provenance` 필드(model·prompt_version·prompt_snapshot_hash·mission_content_hash·generated_at·generation_attempt) + **R20 확장**(존재·필수값 검사). "JSON 내장" 선언(0-b·14)과 스키마 실체의 불일치 해소 — RQ③ 근거. 기존 승격분 0건이라 백필 없음. 구현 = 승격 경로 배선 시(edge function이 채움)
57. **54셀 쿼터·중복률 정밀화 (B5 수용)**: §7-0 개정 — **1차 쿼터 = 화행9×수준3×모드2 = 54셀, 셀당 최소 3**(현 배치 러너의 전역 순번식 통역 배분은 셀 공백 허용 — 코드 확인. **500 본 배치 전 러너 수정 필수**) / 2차 주변 분포 = theme·topic·domain·industry·P/D/R·channel. **중복률 2종**: 완전 중복률 = R19 자동 / **근접 중복률 = 표본 검수 27의 점검 항목으로 통합**(유사도 엔진 신설 없음 — §9 유지). 감사표(0-g·46)에 **theme·topic별 건수** 포함
58. **기타 수용 2건**: ①ChatGPT 방어 문구 정밀화(0-g·49 개정 — "통제 불가" 단정 삭제) ②**9월 인간 캘리브레이션 = 2단계 강제**: 1단계 후보 자격(불변항 유지·중국어 자연스러움·실사용 가능성) → 통과분만 2단계 화용 대역(과소·적정·과잉·경계 여부·이유). 자격 미달 문장에 대역 라벨 부여 금지(게이트1 논리의 캘리브레이션 확장 — validated 지위의 전제)

## 0-i. 학습자 경험층 라운드 (07-24 — 사용자 발제 + GPT 의견 3건, fable 판정. 원장 = auto-memory `pragma-ux-round-0724.md`)

> 목적: 학부생 만족·참여·교수 운영 어려움을 경험층(UI·수업 연계)에서 흡수. **대원칙 = 7/31 완료 기준 4묶음(0-g·46 동결)에 아무것도 추가하지 않는 별도 트랙** — 7월 버퍼(②검수 리스킨·④수정 지도만) / 8월(생각벽 MVP·표현함·선택권·설문·AI Trace 로그·통역 원발화 영상 경로) / 9월(분포·대시보드·펄스·차등 지원·AI 군집).

59. **MPJ "AI 번역 발송 전 검수" 리스킨 = 프레임만**: target_zh를 "AI 번역 초안" 서사로 제시(실제로 AI 생성물이라 정직 + MPJ=번역 비평 정체성 + 0-g·49 방어 정합). **선택지 대체 기각** — "그대로/수정 후/보류" 3지선다는 band 방향(과소/과잉) 소실+기권 오염 = 구인 변경. 채택 = band 1:1 유지 문구("이대로 보내도 됨(알맞음)"/"수정 필요—너무 직접적"/"수정 필요—지나치게 우회적"). 30초 배지=서사만(타이머·감점 금지). **accepted_band_codes·explanation_ko·recommended_example_zh 데이터 계약 무변경**. ⚠️ **재기각 기록(의견4)**: GPT가 "그대로 사용/수정해서 사용/사용하지 않음"으로 문구만 바꿔 재제안 — 동일 사유 재기각. multi_judge "발송 가능한 것 모두 고르기" 이진화도 기각(후보별 band 판정 유지 — 발송 서사는 안내문에만)
60. **Human-first AI Trace**: ③피드백의 alternatives(=AI 제안)에 **수용/부분 수용/거부 1클릭(+이유 체크 = 선택** — 7/22 피로 원칙) → §6b 로그 확장(`ai_alternative_response`). **AI 제안은 최초 산출 후에만 노출**(판단 전 제출 원칙 명문화). 미션 4단계 골격 불변(③ 내부 상호작용). AI 탐지 도구·탐지율 지표 = 도입 금지
61. **확신도 3차 판정**: 정규 MPJ 확장 **재기각**(0-c·30 잠금 유지 — reason_conf ④만). **수업용 즉석 폴(펄스·생각벽 계열)에서만 허용**, 그 데이터는 수업 운영용 — 캘리브레이션 표본과 분리 저장
62. **차등 지원량(9월)**: 같은 구인·같은 채점 기준 + 지원량만 차등. **힌트 = 카탈로그 relevant_resources·불변항 체크리스트(§4 백신3)에서 파생만 — 신규 생성 파이프라인 금지**
63. **통역 원발화 영상(8월 계약 개정 예고)**: §3-3 통역 흐름 ①에 "검수된 원발화 영상이 있으면 TTS 대체" 경로 추가 예정. 영상 대사 = **reviewed 콘텐츠에서만**(생성 모델이 대사 재작성 금지) · "AI 제작 장면" 표시 · 표정·제스처 채점 금지 · MPJ 5문항 영상화 금지(구인이 비언어 해석으로 확장되는 것 차단) · 주 2개 상한 · 영상=필수 아닌 확장 자산
64. **경험층 부속 스키마 = 생성 계약 밖 운영층**: `learner_saved_attempts`(표현함 — 로그 참조·복제 금지·카드에 상황 한정 주의 문구 필수) · `class_notes`+`class_note_reactions`(생각벽 — 본인 제출 후 공개·"나도 궁금해요"만·AI는 군집만+교수 승인 후 공개·자동 답변 금지) · `media_assets`(rights_status·reviewed_by 필수) · 학기 설문/주차 펄스(능력 진단 문항 금지·집단 분포만) · `get_cohort_mpj_distribution` RPC(n<5 숨김·제출 후 공개·순위/비교점수 금지·**다수 의견≠기준 판정 표시 규칙**). **주장 경계 추가**: 참여·만족 = 형성평가 운영 지표만(효과 주장 금지) · 메모·설문 = 능력 진단·개인 프로필 사용 금지(0-f 연장) · "Z세대 본성" 서술 금지(참여 방식 설계로 서술) · GPT 인용 연구 전건 실물 확인 전 논문 인용 금지 · 순위·총점·스트릭·타이머·콤보·승패 대결 금지(기존 금지 명문화)
65. **평가 계약 바(의견4 — 7월 버퍼 가능)**: 미션 시작 화면에 **제출 전 공개 = 평가 3차원(의미·문법·화용 초점)·이번 초점(learner_label·operational_definition)·"확인하지 않는 것"(excluded_confounds)·완료 조건·예상 시간**만 표시. **accepted 코드·참고안·해설 = 제출 후에만 공개**(독립 판단 보존 — "모호함 해소"를 정답 선노출로 풀지 않는다). 전부 카탈로그 렌더(AI·DB 0). 노출 시점 규칙은 이 조항이 정본
66. **이견 버튼 + 교수자 예외 큐(의견4 — 8월/9월)**: 피드백 화면에 "이 판정에 이견이 있어요" — reason_code 5종+150자, class_notes 재사용(`note_type=judgment_disagreement`), **점수·완료 상태 불변**. 교수자 집계(같은 문항 5건→우선 검토). **지위 = proposed 채점키의 캘리브레이션 보조 자료**(9월 validated 전환 입력·경계 문항 탐지 — SJ 질문의 운영 답변. 능력 판정·성적 사용 금지). 예외 큐 = **결정론 필터만**(이견 3+·uncertainty_flag 존재·의미 왜곡 판정·분포 분산·수정 후 동일 이견 — AI 분석 아님. **uncertainty_flags의 실용처 확정**). 표시 규칙: AI 피드백을 최종 판결처럼 표시 금지(proposed 병기) · 이견=오답 저항 취급 금지 · AI 의존형/소극형 학생 자동 분류 금지

## 0-j. Lean 감량 + 정합 라운드 (07-24 저녁 — GPT 정합 점검 5 BLOCKER 전건 사실 확인 후 fable 판정. **0-i와 충돌 시 0-j가 우선**)

> 배경: 경험층 4건을 하루에 수용하며 로드맵 과밀 위험(사용자 노파심 = 타당). 원칙 재확인 — **핵심 4단계·백엔드 품질 장치(2층·게이트·불변항·provenance·54셀·3층 판정)는 손대지 않고, 학생 클릭 수와 교수자 관리 화면만 줄인다.**

67. **경험층 최종 감량 (0-i 시점·지위 대체)**: **즉시(7월 버퍼) 4** = 평가 계약 바 **축소형**(기본 표시 = 이번 핵심·예상 시간·완료 조건, 평가 3차원·"확인하지 않는 것"은 서랍) · AI 초안 프레임(0-i·59 그대로) · 피드백 근거 서랍 · 수정 지도 / **핵심 수직 경로 후 2** = 학급 분포(공통 미션 전제) · 이견 버튼 **단순형**("피드백이 맞지 않는 것 같아요"+선택 메모 — 사유 5종·예외 큐는 후속. 캘리브레이션 보조 지위는 유지) / **자동 1** = 표현함 → **"내 최종 표현 이력"으로 축소**(새 테이블 없이 완료 로그 조회 — 저장 버튼·메모·검색·인쇄 후속) / **백로그 이동** = 미션 선택권·AI Trace 수용/거부 클릭(§6b 필드는 선택 스펙으로 존치)·생각벽 풀버전(공개범위 3종·반응·핀·군집 = 미니 SNS 비용 — **8월 대체 = 단일 익명 질문창**, class_notes 최소형·교수자 전달만)·주차 펄스(설문은 학기초 1회만)·AI 군집·media_assets 플랫폼(**대체 = curriculum_weeks에 media_url·start_sec·end_sec 3필드**, 대표 주차 시범만)·AI 제작 영상·weekly_lesson_pack·프롬프트 시험대
68. **주차 완료 정본 통일 (BLOCKER 1·2 해소)**: **공통 필수 미션 1개 + 선택 복습 미션 0~1개(당주 배정분 내·진도 무관)**. 근거: 3중 1택은 학급 분포·공통 토론 전제 붕괴 + reviewed 콘텐츠 3배(15주 45미션 = MPJ 225문항 개별 검토) — **의견1 수용 판정을 번복**(선택권 경험은 선택 복습으로 보존). **IntroArc = 앱 완료 게이트 아님**(수업 운영 요소 — [B] 기본 경로와 정합, "최소 미션은 150분 수업을 대체하지 않는다" 원칙 재확인)
69. **실행 게이트 2겹 명문화 (BLOCKER 4 해소)**: 개발·데모·형성 연습 = `reviewed` / **9월 연구 분반 = `reviewed` AND `band_validation_status='validated'`**(0-g·44 실행형). 학습자 배지 = 학습자 언어로: **"현재 수업 기준 · 다른 적절한 표현도 가능"**(proposed/validated 등 내부 상태 용어 학생 화면 비노출 — 0-g·44 배지 문구의 학습자화)
70. **15주 초점 정책 하향 (BLOCKER 5 해소)**: 동일 화행 재등장 시 v1.5 = **화행별 기본 초점 동일 적용**(0-h·55 파생 규칙이 정본). "같은 화행은 다른 화용 초점으로 재등장"(학습구조 구 서술)은 **카탈로그 확장(화행당 복수 초점) 이후로 하향** — RQ2 서술 범위도 "동일 초점의 상황 변주 반복"으로 정합
71. **통역 오디오 기본 비저장 (7월)**: §3-3 ③ 수정 — 오디오는 **세션 내 재생·확인용만, 서버 업로드 안 함**. 로그 보존 = raw/confirmed/edited **텍스트 3필드만**(attempt 로그). 근거: 발음·유창성 미평가 확정이라 저장 이득 < 음성 개인정보·IRB·스토리지 비용. 9월 실증 저장 여부 = IRB 판단에 위임
72. **MPJ UI 단순화 + UX 게이트**: 데이터 5유형 불변, 화면은 **2그룹 공통 레이아웃**(A 판단형 = scale4·judge3·reason_conf / B 수정·대안형 = fix_choice·multi_judge). 피드백 화면 = **revision_scope 기반 주 카드 1개 펼침**(나머지 상태 칩+서랍) · 완료 화면 = 기본(최초/최종/핵심)+접힘(적절안 5) · 수업 연계 기능(분포·질문)은 개인 미션 화면 밖. **내부 UX 게이트**(공개 주장 아님): 번역 중앙 완료 ~12분·통역 ~15분·피드백→수정 필수 행동 ≤2 — 파일럿 초과 반복 시 §10 강등 경로 가동
73. **문서 분리 = 후속(7/26 이후)**: GPT 6문서 체계 제안은 방향 수용하되 지금 재편은 마감 리스크 — 대신 각 정본의 경험층 블록에 "백로그 = 구현 지시 아님" 지위 명시로 대체. 27칸 브라우저 = 유지 + **칸 내 번역/통역 수 분리 표기**(감사 출력만 54셀)

## 0-k. 구버전 이식 + 2부 세션 분절 라운드 (07-24 밤 — 사용자 **전부 승인**, 범위 명세 사용자 확정. fable 판정 원장 = `pragma-ux-round-0724.md`)

> 출처: 구버전 슬라이스 목업(위챗 UI·상황 확인·수신자 대조) revisit + 세션 분절 발제. **구현 게이트 = 현재 P0 E2E green 이후**(기존 MPJ5·DCT1·3층 피드백·reviewed 게이트 회귀 통과가 선행).

74. **불변 조건(사용자 확정)**: 하나의 mission_id/attempt/target_feature 유지 · 순서 MPJ5→번역·통역→피드백→다듬기 불변 · MPJ/DCT를 별도 미션·별도 로그로 분리 금지 · 새 테이블·새 AI 호출·9단계 진행바 금지. **범위 밖** = 교수자 시점 잠금 · DB 기기 간 재개 · 대시보드 3계수 · AI 이미지 · 추가 미션 유형 · 새 분석 점수
75. **A. 2부 세션 분절**: 상위 = **1부 판단 연습(MPJ 1/5~5/5) | 2부 실전 적용**(내부 라벨 = 기존 확정어 번역하기/통역하기→피드백→다듬기 — 7/22 라벨 잠금 유지, GPT의 재명명 기각). MPJ 5 완료 전 2부 접근 차단(URL 직접 접근 포함) · **판단 연습 완료 ≠ mission_completed**(3단계 상태) · 7월 재개 = localStorage(key = user_id+scenario_id+mission version, 완료 시 삭제) · 인계 화면 = **전략 범주만**(중국어 모범 문장 노출 금지 — 인출 보호) + [바로 실전 적용]/[저장하고 나중에] · `resume_stage`(mpj|production|feedback|revision|complete)·`mpj_completed_at`은 §6b 명세만(DB 배선 = 수행 로그 작업 때). 교실 토론 후 DCT 수행 해석 = mpj_completed_at↔first_response 시각차로 파생(새 필드 불요). 서술 경계: "숏폼 세대라서"가 아니라 **세션 분절·재개 설계**로 서술
76. **B. ChannelSceneFrame + C. 상황 핵심 확인**: B = 기존 channel 값만, messenger=브랜드 중립 메신저 문법(회색 배경·초록 발신·흰 수신), email=from/to/subject/body, phone/facetoface=기존 카드 유지. **상황 제시·산출 입력만**(피드백·수정 = 공통 UI). 로고·상표·AI 생성 이미지 금지. C = 2부 시작점 무채점 1문항 — production_task의 relation_ko·pdr에서 **결정론 생성**, P/D/R 기호 비노출, 점수·정오율·MPJ 수 불포함, 오답 시 관계·부담 자연어 설명만 주고 진행 허용, 새 AI 호출·새 DB 필드 금지
77. **D. 수신자 최소대조 피드백(feedback-lite와 동시)**: `revision_scope==='feature'`일 때만 주 카드 — "받는 사람 입장에서는…" → 학습자 최초 문장 → 최소 조정안 → **변경 span만 highlight**. **§4 프롬프트 1줄 개정**: alternatives[0] = 가능한 한 학습자 문장 보존 + 목표 화용 지점 하나만 최소 수정(불변항 유지). meaning/grammar/clear에서는 강제 표시 안 함. 새 단계·새 스키마 금지
78. **E. counter_rule 예외 한 줄**: 완료 화면 closing_ko 아래 1문장 — 카탈로그 `counter_rule_note` 고정 템플릿 렌더(결정론). 새 카드·새 AI 호출·장문 원리 금지. **완료 조건 8항(사용자 확정)**: ①MPJ 완료 후 새로고침 시 2부 재개 ②미완료 상태 2부 URL 차단 ③1부 완료만으로 미션 완료 처리 안 됨 ④messenger/email 동일 콘텐츠 채널별 렌더 ⑤상황 확인에 P/D/R 원시 코드 비노출 ⑥feature 범위에서만 최소대조 카드 ⑦예외 문장 = 카탈로그 결정론 ⑧기존 MPJ5·DCT1·3층·reviewed 게이트 회귀 통과
79. **76-b. 채널 스킨·렌더러 라운드 (07-25 사용자 전부 동의 — 목업 논의 반영)**: ①**스킨 = 위챗 문법 유지 + 상표 문자 비표기** — 회색 배경·초록 발신·흰 수신(0-k·76의 "브랜드 중립형 위챗 스타일" 확정 유지, GPT의 "초록 제거·완전 중립화"는 기각 — 현장감 손실). 화면에 "微信" 등 상표 문자는 쓰지 않음("메신저 · 상대명") ②**2부 렌더러 = 결정론 파생, 새 DB 필드 금지**: 1부 MPJ = 통일 맥락 스레드(위챗 문법) / 2부 = mode=interpreting→오디오 프레임(듣기→녹음, "전화로/현장에서"는 자연어만) · channel=email→이메일 컴포저(from/to/subject/답장) · 그 외→메신저 입력창. **DCT "항상 이메일" 고정 기각** — R23이 production_task의 코어 channel 계승을 강제하므로 화면·채점 조건 모순 발생. 이메일 DCT는 편성에서 email 채널 코어 선택으로 달성 ③**채널 삭제 기각**(R23·행 태그·브라우저 필터·54셀 감사·CHANNEL_TO_MODE의 모드 파생·매체 다양성 디펜스 축). "생성 텍스트에 채널 영향 없음" 관찰 = 프롬프트 미활용 → §7-1에 담화 형식 1줄 추가(아래) ④**카톡 UI 기각(방향 무관)**: 중→한이라도 위챗 통일 — 카톡 노랑=PRAGMA 액센트 충돌 + 목표 언어(중국어) 환경 몰입 정합. ~~"중→한 없어서"~~ 근거는 사용자 정정으로 폐기 ⑤관리자 생성 폼 채널 자동 배정(topic 기반+고급 설정) = 8월 백로그 · 선행 발화 = 계약 유지(응답형 필수·그 외 선택 — 목업의 전 문항 선행발화는 연출)
80. **방향 스코프 정정 (사용자 확정 — 구 기록 폐기)**: **한→중·중→한 모두 7월 말 구현 목표.** 7/22 이월 목록의 "중→한 방향 9월"은 폐기. ⚠️ **단 중→한은 현 계약이 전제하지 않음** — 스키마 필드(source_text_ko·target_zh·candidates.zh)·R10(source=한국어/target=중국어 문자 검사)·카탈로그 대역(중국어 자원 기반)·피드백 층(중국어 산출 판정)·통역 TTS ko/STT zh 방향이 전부 한→중 전제. **zh_ko 확장 계약(0-l) = 다음 fable 설계 라운드에서 확정 후 구현**(명세 없이 opus 구현 금지). 0-g·46 완료 기준(동결)과 방향 2종의 관계(수직 경로가 방향별 요구인지)도 그 라운드에서 확정
81. **테마 라운드 2차 (07-25 — GPT 후속 검토 판정·반영)**: **수용** — ①`allowedDomains` = domain 호환성 장치(교차 테마 노출 아님 — fable 선행 서술 정정). 정본: *테마는 생활권 기반 운영 분류(MECE 주장 안 함), topic은 주된 의사소통 목적에 따라 primary theme 정확히 1개, 복수 생활권 조합은 프리셋 담당*(0-c·24 보충) ②용어 = **"8 시나리오 테마 + 5 강좌 프리셋"**("8개 독립 교과목" 서술 금지, UI 라벨 "강좌 테마"→"시나리오 테마") ③digital_content 정의 축소 = 콘텐츠 제작·게시·커뮤니티 운영·크리에이터 협업(메신저 사용만으로 digital 분류 금지) — group_chat_coordination 삭제→work_team_chat·friend_group_plan **2분할**(campus는 기존 group_work_coordination이 이미 담당 — GPT 3분할안의 campus 중복 정정) + content_reuse_permission 신설(theme당 ≥3 유지) ④국제교류 보강 = **프로그램 고유 장면 3종**(버디·기숙사 배정·오리엔테이션). **비자·정부기관 topic 기각 — §7-1 소재 배제와 충돌(fable 선행 제안 철회).** 은행·통신은 daily 소속이 원칙 ⑤의료 1종(병원·약국, daily) · 경계 규칙(친구 관계 초점=relationship / 이웃·비인격=daily) ⑥selectTopic에 **min-topic 커서 추가**(같은 테마 내 최소 사용 topic 우선 — 반복 방지) ⑦**프리셋 5×수준 3 = 15주 dry-run을 감사표 항목에 추가**(0-g·46 감사표 구체화 — "빈 주차 없음·주차별 교체 후보 존재"가 교강사 편성 요구의 직접 증거. 500 배치 후 실행) ⑧보고 서술 경계: 0-l 확정 전 언어쌍 ×2를 풍부함 근거로 계산 금지. **기각** — ⓐ사용량 카운트=저장 성공 기준(buildBatchPlan=실행 전 계획기·순수 함수 — 실행 감사는 실분포 감사표 몫) ⓑTHEME_TARGET_WEIGHTS(가중 근거 부재 = 과설계, 500 실분포 후 백로그). 이미 충족 — 유효 후보 필터 선행·54셀 유지·폴백

## 0-l. 양방향 일반화 라운드 (07-25 — fable 설계. 조사 결과 = 레포 전수 하드코딩 지도 기반. **상태: 초안 — 사용자 확인 시 확정·계약 v1.6 승격**)

> 원칙: **저장은 그대로, 읽기에서 정규화.** 기존 ko_zh 데이터(코어 108·참조 미션)는 1바이트도 다시 쓰지 않는다. 방향은 "값"으로 승격하고, 값이 없으면 ko_zh로 읽는다.

82. **방향 축 신설**: `direction: 'ko_zh' | 'zh_ko'` — 코어·미션 JSON 최상위 필드 + `scenarios.language_direction` 행 태그(둘 다 기록 — JSON이 정본, 행 태그는 필터용). **부재 = ko_zh**(기존 데이터 자동 호환). 코딩 정본 = `ko_zh`/`zh_ko`(승격 파이프라인 계열). entryGate의 `ko_to_zh`·legacy `ko-zh`(하이픈)는 각자 영역에 동결 — 신규 저장에 사용 금지, 마이그레이션도 하지 않음.
83. **중립 스키마 v2** (`scenario_core_v2`/`mission_v2`): **언어가 뒤집히는 필드만** 개명 — `source_text_ko`→`source_text` · `source_ko`→`source` · `target_zh`→`target` · `highlights_zh`→`highlights` · `preceding_turn_zh`→`preceding_turn` · `recommended_example_zh`→`recommended_example` · `corrections[].zh`→`text` · `candidates[].zh`→`text` · `reference_alternatives[].zh`→`text`. **메타언어 필드는 개명 안 함**(situation_ko·relation_ko·explanation_ko·note_ko·reasons[].text_ko·brief_note_ko·closing_ko — 학습자 UI 언어 = 항상 한국어, 양방향 공통). **preceding_turn의 언어 = 대화 상대의 언어 = 타깃 언어**(장면 정의: 원발화자는 source 언어로 말하고 상대는 target 언어 화자, 학습자가 중개 — ko_zh는 중국어 선행 발화, zh_ko는 한국어 선행 발화).
84. **읽기 정규화 호환**: `normalizeCore`/`normalizeMission`이 v1 JSON을 읽기 시점에 v2 형태 + `direction:'ko_zh'`로 변환. 모든 소비자(러너·미리보기·편성·승격·규칙검사)는 정규화 형태만 본다. **신규 저장 = v2만.** DB CHECK(`scenarios_core_ck`·`mission_ck`)는 schema_version v1 OR v2 허용으로 완화(상위집합 — 기존 행 위반 불가능). 백필 = **코어 행 한정** `language_direction='ko_zh'` UPDATE(`content_format='scenario_core_v1' AND language_direction IS NULL`). ⚠️ 전 테이블 CHECK/NOT NULL 금지 — legacy 행에 하이픈 값 존재 가능(실데이터 감사 전).
85. **R 규칙 방향화**: **R10** = direction 기준 스왑 — zh_ko는 source=중국어(CJK 있고 한글 없음)·target=한국어(한글 필수), 한국어 산출 내 한자 혼입은 warning(현대 한국어 관용 범위). **R9** NATIONALIZE = 기존 중국 패턴 + **"한국인은/한국에서는/한국 문화에서는" 패턴 추가, 양방향 공통 검사**(해설·note는 항상 한국어이므로 두 패턴 다 적용). **R5** 길이 규칙 불변 — 단 zh_ko(교착어 산출)의 3배 비율 기준 적정성은 표본 검수 27의 관찰 항목. 게이트1·R14(카탈로그 복사)·R24(계획 초점)·나머지 R 규칙 = 언어 중립으로 불변.
86. **카탈로그 방향 변형**: `TargetFeature`에 **선택 필드 4종** 추가 — `operational_definition_zh_ko`·`relevant_resources_zh_ko`·`excluded_confounds_zh_ko`·`counter_rule_note_zh_ko`. band code·within_band_code·learner_label·closing_principle_ko = **방향 공용**(구인 동일, 자[尺]만 타깃 언어가 바뀜). **version 불변**(bump 시 기존 미션이 R13 위반 — 필드 추가는 비파괴 확장). 내용 초안 = `PRAGMA_카탈로그_zh_ko_변형_2026-07-25.md`(요청·거절·감사 3종), 승인 후 targetFeatures.ts 이식.
87. **구인 대칭 선언**: zh_ko의 판정 구인 = ko_zh와 동일한 **상황(P·D·R) 화용 적절성** — 산출 언어만 한국어. 원문 화용 강도의 보존은 별도 판정 축이 아니라 게이트1(불변항)+피드백 semantic_fidelity 층 소관(이중제약 구조 불변). 학습자 L1(한국어) 산출을 판정하는 정당성 = 통번역 TT 규범·화역(register) 조절은 L1 화자에게도 훈련 대상(기능주의 근거 — 번역투 회피·화용 등가는 산출 언어 모어 여부와 독립).
88. **수준 사다리 방향화**: zh_ko 수준 = **원문(중국어) 난도 기준** 동일 HSK 사다리(입문=HSK4 이하 원문 1~2문장 / 중급=HSK5 / 고급=HSK6·복합 담화). 산출(한국어) 측 등급 정책 없음 — 화용 초점만. LEVEL_POLICY 문구는 방향별 2벌(승격 프롬프트).
89. **쿼터 감량안 + 완료 기준 관계(0-k·80 유보 해소)**: 1차 = **ko_zh 54셀 체계 불변**(0-h·57 그대로). zh_ko는 **스모크 쿼터만** — 승격 가능 3화행(request·refusal·thanks) × 수준3 × 모드2 = **18셀, 셀당 ≥1(18~27 코어)**. 500 본 배치는 ko_zh 중심 유지, zh_ko 확장 쿼터(54셀 대칭 여부·비율)는 스모크 눈검사 후 별도 결정. **7/31 완료 기준(0-g·46 동결)은 방향×2로 확장하지 않는다** — ko_zh 수직 경로 완성 + zh_ko 스모크 3종(코어 생성·승격 1건·러너 렌더) 통과 = 충족. 감사표에 방향별 칸 분리, **셈에 넣는 것은 zh_ko 실저장분만**(0-k·81⑧의 계획 ×2 계산 금지 유지).
90. **통역 방향화**: `DIRECTION_LANGS = { ko_zh: {tts:'ko-KR', stt:'zh-CN'}, zh_ko: {tts:'zh-CN', stt:'ko-KR'} }` — §3-3 흐름(TTS→병렬 녹음·전사→전사 확인→3층 피드백) 불변, lang 파라미터만 방향화. ⚠️ 스모크 확인 항목: 브라우저 speechSynthesis의 zh-CN 보이스 존재·품질(부재 시 해당 셀 보류 여부는 스모크 후 결정). SpeechRecognition ko-KR은 광범위 지원.
91. **승격·편성 게이트 방향화**: zh_ko 승격 = 카탈로그 방향 변형 이식된 3화행만(그 외 화행 코어 = 편성·검색용). 편성기 자동 채우기 = **outline.language_direction 일치 코어만 배정**(이 필터 패치 전 zh_ko outline 사용 금지 — 오배정 창). zh_ko 참조 미션 게이트 = 3개(요청·거절·감사×중급) 동일 절차 + H1·H2 + **H3 신설: 한국어 후보가 번역투·기계투 아닌 자연스러운 화역인가**(연구자 = 한국어 모어 화자 직접 검수 — ko_zh보다 검수 신뢰 우위). 중국어 원문 품질은 원어민 검토 목록으로.
92. **구현 절차**: opus 4라운드 — ①스키마·규칙(enums·coreSchema·missionSchema·missionRules + v1 회귀) ②DB·edge(**migration push·deploy = 실행 전 보고**) ③파이프라인(batchPlan·coreBatchRun·promoteMission·targetFeatures 이식) ④조회·편성·학습자 UI(composer·AdminComposer·AdminBrowser·AdminBatch·missionDb·MissionRunV1·MissionPreview). smoke 4경로 = ko_zh 회귀(최우선) → zh_ko 코어 → zh_ko 승격·미리보기 → 편성→실행.

## 0-m. 범위 확정 라운드 (07-25 — 사용자 확정: 수업연계형 단일화)

93. **PRAGMA = 수업연계형 웹앱으로 단일화(이번 박사논문·프로토타입 범위 확정)**: 학생이 500개 DB에서 직접 검색·추천·선택해 자유롭게 연습하는 **자율학습 경로는 공식 범위 제외** — 단순 구현 연기가 아니라 범위 제외이며, 향후 확장은 **후속 연구로만** 남긴다. ①**500 코어는 그대로 유지** — 용도 = 학생 자유학습이 아니라 **교강사의 수준별·주제별 15주 수업 편성**(교수 지시와 정합) ②학습자 실행 = 교강사가 주차에 배정하고 `mission_status='reviewed'`인 미션만(0-b·17 불변) ③"공통 필수 미션 1 + 교강사 배정 선택 복습 0~1"은 유지 가능 — 이는 배정이지 자율학습이 아니다 ④코드 반영 = 학습자 홈 자율/수업연계 토글·"약점 기반 추천" 카피 삭제(구현체는 이 목업 토글뿐이었음 — 자유 검색·추천·즐겨찾기·자율 풀은 전부 미구현 스펙) ⑤§6b `entry_mode`는 `course` 단일값으로 운용(self_study 값은 후속 연구 예약어 — 스키마 변경 불요) ⑥§9의 "자율학습 UI"는 "9월 이월"이 아니라 **범위 제외**로 재분류. 학습구조·관리자구조의 자율학습 서술(9월 구현 예정 표기)은 범위 제외 표기로 최소 수정, 역사 기록은 보존.

## 0-n. 검증② 7월 복원 라운드 (07-25 — 사용자 확정: 콘텐츠 품질관리는 미룰 수 없음)

94. **생성모델 분리형 AI 품질·일관성 점검(검증②)을 7월 P0로 복원**(구 9월 표기 무효화 — **이 조항이 0-e·40·§9·§11의 "검증② 9월"보다 우선**). ①**정의**: 생성된 `mission_content`를 **생성 모델과 분리된 다른 모델·비평 프롬프트**로 재검토해 **pass/warning/fail**을 판정하는 **관리자 품질관리 장치**(규칙검사가 못 잡는 의미·자연성·후보 자격·단일 초점 오염·게이트1 위반 후보를 2차 선별). 학습자에게 노출되지 않음 — 검토/승인 파이프라인 내부. ②**7월 검증 가동 재정의**: **①규칙검사 + ②AI 품질·일관성 점검 + ④교수자 승인**(③인간 캘리브레이션 패널은 9월 유지). ③**⚠️ B3(학습자 산출을 분석하는 동적 3층 feedback-lite)와는 별개 작업** — 그것은 후속 보류(런타임 피드백). 검증②는 **생성물(미션) 품질 게이트**이지 학습자 수행 분석이 아니다. ④**구현 = 별도 작업**(이번 라운드는 범위 복원만): 승격 파이프라인(`promoteMission`)에 규칙검사 후 AI 비평 호출 1단 추가 → 결과를 미션 행에 기록 → 관리자 검토 UI에 pass/warning/fail 표시. 새 미션 골격·학습자 UI 변경 없음. ⑤B1(proposed 지위 라벨)·B2(counter_rule 완료 화면)와 독립.

## 0-q. 신규 설계안 판정 라운드 (07-25 밤 — GPT·Gemini 검토 의견 + **Roever 원문 4장 클러스터 조사** + 레포 코드·DB 전수 조사 후 fable 판정. 이 라운드는 **범위·원칙 확정**이며 구현은 별도 작업)

> 입력: ①학생 관점 위험 가설 4종(이론 피로·AI 판정 불신·작위적 중국어·방향별 난이도 불균형) ②3층 구조 가설(Scenario Core / Executable Mission / Instructional Unit) ③Roever *Teaching and Testing L2 Pragmatics* 원문(§3.1 맥락·§3.2 MPJ·§3.4 DCT·§4 커리큘럼·§5.3 레슨 단계·§5.4 수준별 초점·§6.7 판단문항 검증·§6.8 산출 채점) ④GPT의 Roever 해석 비평.
> **사실 확인(코드 조사 결론)**: 위험 2(AI가 학습자 산출을 특정 표현 유무로 오답 판정)는 **코드에 기능 자체가 없음**(산출 채점 0·정직 "예시" 배너, MPJ는 순서형 밴드, `不好意思`·`抱歉` 카탈로그 동급, B1 캡션 노출) · 위험 4는 **실재 구조 공백**(ko_zh 산출 = 방향·수준 무관 빈 자유입력, 스캐폴딩 개념 스키마·DB·러너 전무) · 위험 1은 최소 미션에선 방어됨(P/D/R 원시코드 비노출)이나 **교수 단원 미구현**이 잠재 위험 · 위험 3은 Authentic Import 작동하나 **출처 provenance 저장 0**(`applyAuthentic`가 source_ref·source_original 폐기).
> **GPT 비평 판정**: 4수용(①Roever의 90% 합의 기준은 *이진 문항×인간 벤치마크* 전용 — AI 검증②의 보편 게이트로 이식 금지 ②"Roever가 실제성 불요를 명시하므로 실자료 우선순위 하향"은 절반만 맞음 — 타당성 필수조건은 아니나 자연스러움·차별화의 **가치 추가 요소** ③PRAGMA 입문/중급/고급 ≠ CEFR A1/B1/C1 — **과제 내 상대 지원량**으로 변환 ④DCT는 상호작용 능력을 측정하지 않는다는 함의 누락 → 100항으로 신설) · 1부분기각("교수 단원 실렌더링을 7월 P0 필수로" — 0-g·46 동결 기준 4항에 없고 마감 6일 전 범위 확장이라 기각, 시점은 96항으로 판정).

95. **7월 P0 순서 확정 + feedback-lite 지위**: 순서 = **①검증②(0-n·94 구현) → ②provenance-lite(98항)·기록 탭(소형, 병행) → ③feedback-lite(§4 feedback_v1 그대로) → ④500 본 배치**. 근거: 검증②는 미션당 +1콜로 작고 **500 배치 전 품질 게이트**라 선행이 논리적이며, feedback-lite는 스키마·백신 6제약이 §4에 이미 설계 완료라 남은 것은 구현뿐. **feedback-lite 지위 확정**(Roever §6.8 정합) = **학습 지원용 질적 피드백** — 조건부·비단정 어조(화용층), 복수 대안 제시, 불확실성 플래그 유지, **총점·효과측정과 분리**(점수 산출 금지). 판정 순서 = ①앞뒤 발화·상황에 기능적으로 맞는가 → ②의도·핵심 의미 전달 → ③이해 가능·자연스러움 → ④관계·상황에서의 화용적 인상 → ⑤다른 가능한 선택. **특정 표현의 유무로 판정하지 않는다**(§4 백신 1·4의 실행 형식).

96. **Instructional Unit(교수 단원) = 3층 구조 확정, 단 7월은 문서+시범 1단원 · 데이터 정규화는 8월**: 구조 자체는 **채택**(Roever §5.3~5.4 = 초점 1개 제시 → 명시 설명 → 수용 연습 다수 → 산출 연습 다수 → 상위 수준 재심화 = "교수 단원 1개 → 관련 미션 복수"와 동형. 폐기된 `feature_packages`가 실제로 이 구조였음). **정의** = ①**학생 공개 교수 단원**(hook·사례·해당 화행 원리·표현) + ②**교수자 전용 교안**(이론·예상 Q&A·운영). **키** = speech act + target_feature + level + language_direction (+필요 시 theme). **7월 범위** = 위 정의를 문서로 확정 + 기존 `IntroArc`(요청 주차)를 시범 1단원으로 정돈. **7월 금지** = 신규 테이블·신규 라우트·신규 생성 파이프라인. **8월** = 독립 Instructional Unit 테이블로 정규화(1단원→복수 미션 FK). 7/26 보고 표기 = "구조 확정 + 시범 1단원 + 8월 로드맵"(구현 완료로 표기 금지).

97. **산출 지원량 차등 원칙 (위험 4 처분 — 0-i·62 시점 개정 후보)**: **같은 구인·같은 판정 기준, 지원량만 변수**(0-i·62 원칙 유지) — **새 미션 유형·새 미션 플로우를 만들지 않고 기존 산출 단계의 지원 수준만 조절**한다(Roever §5.4가 동일 초점을 수준별 지원 차등으로 가르치는 구조 = 이 원칙의 문헌 근거. §3.4 DCT 변형(선행발화·rejoinder·제공 턴)도 "산출 개방도를 낮추는" 확립된 장치). **채택 형식 = 담화 슬롯 골격**(예: 인사·배경 → 이유 → 핵심 화행 → 조정·마무리) — 단어은행보다 PRAGMA의 화용 초점에 정합. **PRAGMA 변환**(CEFR 절대수준 이식 금지) = 입문: 슬롯 골격 + 참고 표현 + 일부 작성 / 중급: 슬롯 힌트 **또는** 참고 표현 택1 / 고급: 자유 산출, 필요 시 힌트 열기. **생성 금지 제약 유지** = 힌트·골격은 카탈로그 `relevant_resources`·화행별 결정론 상수에서 **파생만**(신규 AI 호출·신규 생성 파이프라인 금지, 0-i·62). **원칙은 이 조항으로 확정. 구현 시점 = 조건부** — 학습자 가시 UI 변경이므로 **UI 영향 브리프 승인 후** 착수하며, 7월 여력이 없으면 8월 초로 이월하되 설계는 논문에 기술한다.

98. **provenance-lite (위험 3 처분)**: 실자료 기반 생성은 **타당성의 필수조건이 아니다**(Roever §3.1 — 요구는 *맥락 확립*: 물리적 상황·사회적 관계(P/D/R)·목표와 이유, 약 1문단. 구성된 시나리오도 정당). 그러나 자연스러움·몰입도·차별화의 **가치 추가 요소**이므로 폐기하지 않는다. **7월** = `core_content`에 optional 5필드(`source_type`·`source_ref`·`source_original`·`ai_adapted`·`anonymized`) + `applyAuthentic` 저장 배선(**DB migration 불요** — 지금은 이미 수집한 값을 버리고 있음). 근거 = 저작권 방어·감사 가능성. **기각** = 500 전량 실자료화. **후속(8월)** = 대규모 유튜브 마이닝 파이프라인. **권장** = 핵심 샘플과 교수 단원 hook에 선택적 활용.

99. **검증② 역할 경계 (0-n·94 실행 세칙)**: ①**범위 = 미션(`mission_content`) 전체 대상 통합 비평 1콜** — pass/warning/fail + 사유. **per-facet 8~9콜 전면 검수는 기각**(비용 8~10배 + 피드백·교수단원·교안은 생성기 자체가 없어 검수 대상 부재). 8월에 교수 단원·교안이 생기면 **동일 방식으로 1콜에 흡수**(코어+미션+학생 단원+교수자 교안 → 분리 모델 1회 통합 품질검사 → 교수자 최종 승인). ②**AI가 검사하는 것** = 문항 내부 모순·답 단서·과장된 오답·밴드 정합·게이트1(불변항) 위반 후보·단일 초점 오염·중국어 자연성. ③**AI가 대체할 수 없는 것** = 인간 벤치마크와 교수자 최종 승인. **AI 한 모델의 판정을 인간 합의 분포처럼 취급하지 않는다.** ④**Roever의 벤치마크 합의 분포·이진 문항 90% 합의 기준이 착지하는 곳 = 9월 인간 캘리브레이션 2단계(0-h·58)** 이며, 90% 기준은 그 안의 **명시적 이진 문항에만 제한 적용**. 과소·적정·과잉·경계·복수 판단 문항은 **판단의 분산 자체가 정보**이므로 단일 정답 수렴을 품질 기준으로 삼지 않는다(Roever §3.2 — 교실에서 판단 차이는 토론의 출발점).

100. **측정 주장 경계 조항 (PhD defensibility — 신설)**: PRAGMA의 미션은 본질적으로 **서면 DCT · 구두 DCT · 통제된 화용적 의사결정 연습**이다. Roever는 DCT가 실시간 상호작용을 재현하지 못하며 실제 대화 행동 예측에 제한이 있음을 명확히 경고한다(선행 발화·다중 턴을 넣어도 상호작용과 같아지지 않음). 따라서 **논문·UI·보고에서 다음을 주장하지 않는다**: ~~"상호작용 능력을 측정한다"~~ · ~~"실제 의사소통 수행을 예측한다"~~ · ~~"통역 상호작용 능력을 평가한다"~~. **허용 문구 정본** = *"맥락화된 번역·통역 과제에서 학습자의 화용적 판단, 표현 선택, 수정 과정을 지원하고 기록한다."* 기존 **향상 주장 금지**(연구 정위)·**구인 대칭 선언**(0-l·87)·**0-g·49 정밀 문구**와 같은 계열의 방어선. 구현 시 학습자 UI·관리자 UI 문구에 "상호작용 능력" 류 표현이 없는지 점검 1회 포함.

101. **분리 계수 5종 + 범위 확정표**: ①**보고 계수(0-g·46 확장)** = **코어 수 / 미션 생성 수 / AI 검수(검증②) 수 / 교수자 검토(reviewed) 수 / 실행 가능 미션 수** — 5계수. 교수 단원·완결 학습 패키지는 **존재할 때까지 0으로 정직 표기**(미구현을 구현처럼 집계 금지). ②**7월 P0** = 검증② · provenance-lite · 기록 탭 · feedback-lite · 500 본 배치. ③**7월 조건부(컷라인 아래)** = 산출 지원량 차등 구현(97항 — UI 브리프 승인 선행). ④**문서만(7월)** = 교수 단원 정의(96항) · **이론 노출 규칙**(1주차에 공통 개념 P·D·R·사회화용/화용언어를 정식 설명 / 매 화행 주차는 **그 주차의 새 원리만 짧게 명시적으로** / **공통 이론 전체 강의 반복 금지** / 실제 사례·중국어 표현이 항상 이론보다 먼저 또는 가까이 — Roever §5.3의 메타화용 설명 = 경계된 1단계, 귀납적 발견으로 대체 가능) · 측정 주장 경계(100항) · 나선형 커리큘럼 근거 1문장(Roever §4.2 spiral — 동일 초점의 수준별 재순환) · 5계수. ⑤**8월 이후** = Instructional Unit 정규화 · 스캐폴딩 본격판 · 유튜브 마이닝 · **target_feature 편성축 승격**(0-h·55의 9월 유예 유지 — 7월은 화행당 초점 1개라 파생으로 충분) · 9월 인간 캘리브레이션. ⑥**기각** = per-facet 전면 AI 검수 · 500 전량 실자료화 · 한→중 별도 미션 플로우 · 신규 테이블 7월 신설 · "교수 단원 실렌더링 7월 P0 필수"(GPT안). ⑦**학습구조·관리자구조 동기화는 각 항목 구현 라운드에서 수행**(이 라운드는 계약 단독 기록 — 두 문서를 읽는 세션은 이 0-q를 상위 규정으로 볼 것).

## 0-r. 학습 경험 라운드 (07-25 밤 — 외부 의견 4건 병합 판정. 사용자 "conflict 없는 선에서 전부 적용" 승인)

> 입력 4건: ①**가상 에타 후기**(GPT 작성, 학부생 관점 10대 불만 · 3.0/5.0) ②**Gemini 개선안 5종** ③**대학원 실제 토론 과제 자료**(원 과제·학생 토론·원어민 튜터 코멘트 요약 — 가상이 아닌 실증 기록) ④**GPT 변주 스튜디오 안**(150분 배치·후반부 활동 레퍼토리).
> **핵심 진단(수용)**: PRAGMA의 아이디어가 틀린 게 아니라, **초기 토론 과제에서 가장 강했던 '판단 차이를 이용한 집단 학습'이 개인 화면으로 압축되며 빠졌다.** 처방은 미션에 기능을 더 얹는 것이 **아니라**, 미션의 역할을 줄이고 집단 층을 별도 모듈로 세우는 것.
> **관통 경고**: "장치를 더 얹지 마라 — 학생은 이미 지친다." 이 라운드의 조치 중 둘은 **빼는 개선**이다.
> **기존 조항과의 관계**: 0-j·72(주 카드 1개·분포는 미션 화면 밖)·0-k·74(미션 골격 불변)·0-j·68(주차 필수 미션 1개)와 **정합**하며 폐기하지 않는다. 103·104는 그 원칙의 **확장**이다.

102. **미션의 역할 재정의 + 개인/집단 2층 선언**: PRAGMA = *"모든 학생의 독립적 화용 판단과 번역안을 수업 전에 확보하고, 그 차이를 수업 토론과 번역 의사결정으로 연결하는 수업연계형 웹앱."* 미션은 **공통 데이터 수집 엔진**이며 수업 전체가 아니다(주차 완전 미션 1회 = 0-j·68 재확인, 2회 반복 금지 — 두 번째부터는 상황 판단이 아니라 풀이 요령을 익힌다). **개인 층(미션+feedback-lite) = 바닥, 집단 층(분포+토론) = 차별화. ⚠️ 집단 층을 이유로 feedback-lite를 격하하지 않는다** — 결석·과제·복습은 교실 토론 없이 돌아가므로 개별 반응이 유일한 채널이고, "AI가 답 4유형을 구분 못 하면 전부 무너진다"는 경고가 여전히 유효하다.

103. **학습자 화면 감량 (빼는 개선 — 0-j·72 주 카드 원칙의 확장)**: ①**시작 화면** = 상황 1줄 + 목표 1줄 + 예상 시간만 노출, 평가 계약 바의 나머지(완료 조건·확인하는 것·확인하지 않는 것)는 **서랍 1개로 접기**(삭제 아님 — 0-j·72가 의도한 장치이므로 보존하되 기본 접힘) ②**완료 화면** = **최초→최종 비교 + 핵심 1줄만 펼침**, 나머지(참고 표현·예외 규칙·오늘의 도구·생생 표현)는 전부 접기 ③**점수·수치 카드 금지**(0-q·95 — 성과 카드의 내용은 내 문장 비교이지 숫자가 아니다) ④적시 학습 툴팁의 시점은 ~~"틀렸을 때"~~가 아니라 **기준 판정 공개 시점**(MPJ는 정오답이 아니라 기준 대조이므로). 근거: 후기 1·10 — "시작도 전에 연구 설명문 읽는 느낌", "완료 화면이 또 공부 시작하는 느낌".

104. **이견 채널 신설 (판정에 반응할 수 있게)**: B1 캡션("다른 표현도 가능")은 **행동이 없으면 면책 문구일 뿐**이고 결국 "교수님 기준 외우기"로 수렴한다. 조치 = 기준 판정 화면에 **「내 판단을 남길게요」 원탭** — 학생의 선택 + **판단이 달라질 조건 체크**(더 친하다면 / 처리 부담이 더 크다면 / 기록으로 남는 요청이라면 / 거절하기 어려운 관계라면 / 기타 한 줄) + 선택적 한 줄 이유를 로그에 기록하고 "기록했어요 — 수업에서 다룰 수 있어요"로 끝낸다. **판정을 바꾸지 않고, 새 화면도 만들지 않는다.** 소비처 = 관리자 목록(교수자가 수업 토론 안건으로). ⚠️ **정규 MPJ 문항으로 만들지 않는다** — 생성 스키마·R규칙·로그·채점 상태·UI가 전부 늘어난다. ⚠️ 0-j·72("분포·질문은 미션 화면 밖")와 충돌하지 않는다: 이것은 **분포 표시가 아니라 개인 이견 기록**이다. 부수 가치 = 9월 인간 캘리브레이션 표본 + 경계 사례 지도.

105. **문항 품질 조준 (뻔한 오답 제거)**: 극단 문장(`你必须…`)·노골적 오답은 중국어 지식 없이 소거되어 화용 훈련을 "나쁜 표현 찾기"로 격하시킨다(Roever §3.3과 동일 논리). 조치 = ①**검증②(0-q·99) 비평 프롬프트의 `implausible_distractor` 기준을 명문화**: *"이 후보를 중국어 초급자가 화용 지식 없이 소거할 수 있는가"* ②**§10 눈검사에 '경계 사례 비율' 항목 추가** ③단, 경계 사례를 늘리면 판정 논란이 커지므로 **104(이견 채널)와 반드시 함께** 간다.

106. **완화 편향 시정 — 3면 정렬**: 앱이 깨려는 오해가 "길고 공손할수록 좋다"인데, 실제 활동(문항·산출 힌트·피드백)이 계속 완화 자원을 더하라고 하면 학생의 결론은 *"方便的话·麻烦您·能不能 넣으면 됨"*이 되어 **가르치려던 것의 정반대**를 가르친다. 조치 = ①**ProductionGuide(0-q·97) 문구를 P·D·R 조건부로** — R(부담) 낮음 또는 D(거리) 가까움이면 첫 줄이 *"이 상황은 짧고 직접적인 쪽이 자연스러울 수 있어요 — 빼는 것도 조절입니다"* ②**배치·승격 감사에 "적정 = 직접형"인 미션 쿼터** 추가(주차마다 최소 1개는 '덜어내는' 연습) ③피드백의 격식 상향 편향 금지는 §4 백신 2로 이미 존재 — 문항·힌트 쪽만 맞추면 세 면이 정렬된다.

107. **상황 서술 완결성 5요소 (channel 복원 아님)**: 학생들이 매체를 계속 문제 삼은 이유는 매체 축이 필요해서가 아니라 **어떤 장면인지 덜 그려져서** 서로 다른 상황을 상상했기 때문이다(실증 자료 §3 — 이메일/대면/전화로 갈림). 0-o(channel 폐기)는 유지하되, `situation_ko` 한 문장이 다음을 분명히 하도록 **코어 생성 프롬프트에 체크리스트 주입**: ①직접 말하는가 / 문장을 작성하는가 ②즉시 반응을 기대하는가 ③기록으로 남는 요청인가 ④선행 대화가 진행 중인가 ⑤상대 혼자 처리하는가 / 내부 보고가 필요한가. **새 조합축이 아니라 서술의 완결성**이며, Roever §3.1의 맥락 명세 요건(물리적 상황에 소통 방식 포함)과 정합. 검증② 항목·눈검사에도 "장면 미명세" 추가. **500 본 배치 전에 반영한다.**

108. **feedback-lite 수락 기준 = 4유형 분기 (계약 §4 구현 검수 기준)**: 학습자 답을 ⓐ핵심 의미 누락 → `meaning` ⓑ이해를 막는 문법 → `grammar` ⓒ의미·문법은 맞으나 너무 직접적 → `feature` ⓓ이미 적절 → `clear` 로 **서로 다르게 진단해야 통과**. 배포 후 이 4케이스 실호출로 검수한다. ⚠️ **프롬프트 체인 금지** — 단일 호출 + 코드 결정론 분기(`deriveRevisionScope`)로 이미 이 4갈래가 나오며, 체인은 학습자가 기다리는 런타임의 지연·비용만 늘린다. ⚠️ ⓓ의 문구로 **"완벽합니다" 류 절대 판정 금지**(B1 위반) — "이 상황에 충분히 적절합니다"까지.

109. **집단 학습 층 로드맵 (8월~9월) + 기각 목록**: ①**8월 첫 작업 = 문항별 MPJ 응답 로깅**(현 `learner_mission_logs`는 최초/최종 산출·완료만 저장 — 문항별 답이 없으면 **분포를 그릴 수 없다**. 이 구상 전체의 실제 선행 조건이며 DB 변경이라 승인 게이트) ②**판단 광장 v1** = 제출 전 열람 잠금 → 학급 판단 분포 → 익명 산출 표본 → **교수자 정리 카드(고정 템플릿 우선, AI 자동 생성 아님)**: `판단이 갈린 핵심[친분/권력/부담/처리 책임/자연스러움/의미 유지] · 이번 수업 강조 한 줄 · 예외 조건`. **⚠️ 앱 내 댓글 스레드는 v1 범위 제외** — 학부 주 1회 수업은 토론이 교실에서 라이브로 일어나므로 앱 게시판은 죽는다(원 과제는 대학원·비동기라 댓글이 살았던 것). ③**혼합 공개 방식 실험 = P1**(문항 1~3 즉시 판정 / 4~5는 선택만 저장 → 분포 공개 후 상세 해설). 러너 동작 변경이라 7월 P0 제외. 지금도 **1부 → 교실 토론 → 2부** 분할 운영은 인계 화면+localStorage 재개로 이미 가능하다. ④갈린 축 자동 정리·분포 군집 = 9월(데이터 축적 후). **기각(되살리지 말 것)**: ~~점수·감점 프레임~~(0-q·95 위반) · ~~"실제 중국인 80%가 선택" 등 존재하지 않는 데이터 표시~~(9월 캘리브레이션 산출물이며 지금 표시하면 조작) · ~~학급 분포의 7월 표시~~(코호트 데이터 부재) · ~~2축(의미/화용)으로 축소~~(문법층 삭제 = 후기 ⓑ케이스 진단 불가, **3층 유지**) · ~~앱 내 자유 게시판~~ · ~~모든 문항 장문 이유 강제~~ · ~~원어민 1인 코멘트의 절대화~~(9월 패널이 복수·2단계인 이유와 동일) · ~~랭킹·포인트·배지 등 게임화~~(구인 오염 — 학습자가 화용 판단이 아니라 점수 최적화를 학습한다. 0-q·95 점수 금지의 연장) · ~~기업 커뮤니케이션 시뮬레이터로의 확장~~(3층 판정 축을 늘리지 않는다 — 110②의 '현장 메모'는 채점 밖 정보 카드로만) · ~~자유 검색 학습~~(0-m·93에서 이미 범위 제외).

110. **수업 층 문서화 (코드 아님 — 교수 단원 문서 소관, 0-q·96)**: ①**변주 스튜디오 4종**(주차마다 후반부 활동을 바꿔 같은 미션 엔진으로도 매주 다른 경험을 만든다) — 우선순위 **1위 번역 의사결정 회의**(선택 이유·버린 안의 위험·최소 수정·유지한 불변항을 기록 → **PRAGMA의 통번역 정체성을 가장 잘 방어**하며 14주 "통번역 의사결정 리포트" 백로그와 연결), 2위 G안 제작소(초기 토론에서 참여도 최고), 3위 수신자 레드팀(0-k·77 "받는 사람 입장" 카드의 교실 확장), 4위 한 조건만 바꾸기 ②**현장 메모** = "자연스러운 중국어"와 "회사에서 안전한 행동"은 다르다 — 후자는 **채점 축을 늘리지 않고**(3층 유지) 교수 단원·완료 화면 접힘 구역의 정보 카드로만 ③**내 표현집** = 학기 누적 최초→최종 쌍 + G안 + 받은 반응(에타 후기의 유일한 강한 칭찬 지점) ④150분 배치 예시와 15주 활동 순환표 ⑤**역할 분담 3표**: 웹앱=독립 판단 확보·산출 확보·3층 피드백·수정안 저장·분포 집계·익명 표본 / 교수자=갈린 문항 선택·상황 가정 확인·언어 적절성과 업무 전략 구분·활동 진행·기준과 예외 정리 / **AI 금지**=최종 규범 독점 선언·토론 자동 요약을 정답처럼 제시·실무상 거짓 명분 생성·전 산출 점수화·자연스러움과 업무 성공을 한 점수로 합산 ⑥**통역 미션은 과제(집)로 배정 권장** — 교실 30~40명 동시 녹음은 운영 문제이지 앱 문제가 아니다(코드 변경 없음).

111. **실소요 시간 실측 (선언에서 측정으로)**: "약 12분"은 근거 없는 선언이다. `learner_mission_logs`의 started/completed로 **실소요 분포를 관리자 화면에 표시**하고, 내부 UX 게이트(0-j·72: 번역 ~12분·통역 ~15분) 초과가 반복되면 **§10 조건부 강등 경로**(특정 MPJ 유형 → scale4 1 + judge3 4 폴백)를 증거 기반으로 가동한다. 측정 인프라는 이미 있으므로 표시만 붙인다.

## 1. 정찰 결과 (v1과 동일 — 재사용/동결/신설)

scenarios 테이블·enum 2종·[생성 요청] 라벨 사전·HSK 어휘 주입·outline→final 골격 = 재사용. scenario_candidates/feedback = 동결. 기존 RPC 무변경, 신설 RPC 별도. legacy 29건 = `content_format='legacy_v1'` 자동 분류, 자동 편성·배치 제외.

## 2. `target_feature` 카탈로그 (`src/lib/pragma/targetFeatures.ts` — 코드 정본, AI 임의 생성 금지)

```ts
interface TargetFeature {
  code: string;                 // "request_mitigation_optionality"
  version: string;              // "1.0" — 문항에 함께 저장(R13)
  speech_act: string;
  learner_label: string;        // 화면 "이번 주 핵심" — AI가 생성하지 않고 여기서 복사(R14)
  operational_definition: string; // 이 초점이 무엇이고 무엇이 아닌지 (프롬프트 주입)
  band_schema: BandDef[];       // 판정 축 — 화행·초점별로 다름! (A1)
  relevant_resources: string[]; // 이 초점을 실현하는 장치 목록 (예: 포석·能不能·선택권 맺음)
  excluded_confounds: string[]; // 이 초점이 아닌 것 (예: 격식체 어휘 선택)
  closing_principle_ko: string; // 완료 화면 핵심 한 줄 — AI 복사(R14)
  counter_rule_note: string;    // judge3 반례가 깨야 할 "소박한 규칙" (A1 일반화)
}
interface BandDef { code: string; label_ko: string; }  // 순서 = 척도 순서
```

**v1 시드 3종** (참조 셀용(구칭 골든) — 나머지 화행은 생성 전에 추가):
```text
request_mitigation_optionality (요청·완화와 선택권)
  bands: too_direct / within_band / too_indirect
  counter_rule: "직접적이면 나쁘다" — 반례 = 친밀·저부담의 직접형 적절 사례
refusal_softening (거절·완충과 대안)
  bands: too_blunt / within_band / over_elaborate
  counter_rule: "거절은 길수록 공손하다" — 반례 = 친한 사이의 간결한 거절
gratitude_calibration (감사·강도 조절)
  bands: insufficient / within_band / excessive
  counter_rule: "감사는 강할수록 좋다" — 반례 = 가벼운 호의에 간단한 감사
  ※ excessive 대역의 실증: 감사 과잉은 정중이 아니라 거리감을 만든다
    ("Thanking too much can be alienating" — Dai 2023 요구분석의 실제 사례)
politeness (공손성 — 호칭·대인 배려)
  bands: impolite / within_band / overpolite
  ※ v1.2 미사용(0-b·19): axis_feature=unit.target_feature 고정. 공손성 중심 단원의 중심 초점으로만 쓸 것
```
scale4(적절성 4점: very_appropriate ~ very_inappropriate)는 전 화행 공통의 전반 판정 척도로 유지 — feature band와 별개.

## 2b. `scenario_core_v1` — 500개 구축 단위 (0-b·13~15)

저장: `scenarios.core_content` JSONB + `content_format='scenario_core_v1'`(불변). **target_feature 없음** — 카탈로그 미완성 화행도 코어 생성 가능 → 9화행 전체를 500개로 커버.

```jsonc
{
  "schema_version": "scenario_core_v1",
  "situation_ko": "…", "relation_ko": "…",
  "source_modality": "written",        // written=번역 | spoken=통역. 행 컬럼에도 복사
  "source_text_ko": "…",               // spoken이면 실제 말로 전달할 법한 짧은 구두 담화체
  "preceding_turn_zh": null,           // 거절·응답류 필수 (R8)
  "pdr": { "p": "…", "d": "…", "r": "…" }, "channel": "…",
  "brief_note_ko": "…"                 // 편성 화면용 한 줄 (선택)
}
```

- 행 태그 = speech_act·learner_level·domain·industry(work만)·mode(translation|stt_interpreting)·source_modality·**theme_code·topic_code(07-23 신설)** + 코어의 p/d/r·channel→genre
- 코어 검사 서브셋 = R1c(코어 스키마 + **theme/topic 통제값·theme↔domain 허용 매핑**)·R8·R9·R10·R15·R16·R17·R19 (+R22 warning)

**편성층 메타데이터 2종 (07-23 사용자 확정 — GPT 5종 중 2종만 수용, role_pair·channel_subtype 기각)**
- `theme_code` 8종(통제값): `campus_study·daily_living·travel_mobility·relationship_social·career_workplace·commerce_customer·digital_content·international_exchange` — **강좌 기획 단위**("초급 여행 중국어"의 그 "주제별"). **domain을 대체하지 않는다**: domain(일상/학교/직장) = 연구·생성 통제 축(난이도 정합·소재 방어·R15/R17·주차 골격 참조) 그대로, theme = 편성·검색·프리셋용 **교차 축**. theme↔domain 허용 매핑은 코드 상수(예: campus_study⊂school / travel_mobility⊂daily / career_workplace⊂work / commerce_customer⊂daily|work / international_exchange⊂school|daily)
- `topic_code` = **장면 시드 목록의 형식화** — Dai 2023 기반 시드에 코드를 부여하고 각 시드가 theme·허용 domain에 소속. 배치 생성이 시드에서 상황을 뽑으므로 **생성 입력이 곧 태그(태깅 비용 0)**. AI 임의 topic 생성 금지(카탈로그 원칙)
- **15주 프리셋 4~5종** = 저장된 필터 조합(theme+level+화행 비율 — 편성기 config, 스키마 아님): 캠퍼스·유학 / 취업·직장 / 여행·생활 / 디지털 콘텐츠·커머스 / 고객응대

**topic 카탈로그·프리셋·RPC payload 계약 (v1.3 — 0-c·25~27)**
```ts
// src/lib/pragma/scenarioTopics.ts — 코드 정본, AI 임의 생성 금지 (연구자 승인 시드)
interface ScenarioTopic {
  code: string;                 // "deadline_extension"
  labelKo: string;
  themeCode: ThemeCode;         // 8종 중 1
  allowedDomains: Domain[];     // theme↔domain 허용 매핑의 실체
  allowedSpeechActs?: SpeechActUI[];  // 비우면 전 화행 허용
  situationSeedKo: string;      // 생성 프롬프트 주입용 장면 시드 (Dai 2023 기반 + 재설계)
  sourceNote?: string;          // 시드 출처 (문헌/설계)
}
// 배치 전 최소 요건: theme당 topic ≥3 (쿼터 배분·프리셋 선반이 비지 않게)

// 15주 프리셋 — TS 상수 (편성기 config)
interface CoursePreset {
  preset_code: string; label: string;
  target_level: LearnerLevel;
  included_themes: ThemeCode[];
  speech_act_distribution?: Partial<Record<SpeechActUI, number>>;
  translation_interpreting_ratio: number;
}

// save_generated_core payload — 소유권 잠금 (0-c·27):
// 검색 메타 = 행 컬럼 / 상황·원문 = core_content (p/d/r·channel의 정본 — RPC가 행 태그로 파생)
interface SaveGeneratedCorePayload {
  speech_act: string; learner_level: string; domain: string;
  industry_sector?: string;
  mode: "translation" | "stt_interpreting";
  source_modality: "written" | "spoken";
  theme_code: string; topic_code: string;    // 시드 선택이 곧 태그
  core_content: ScenarioCoreV1;
  generation_run_id: string; generation_item_key: string;
  content_hash: string; prompt_snapshot_hash?: string;
}
```
- **미션화(승격)** = 편성·데모 선별분만: 코어를 시드로 mission_v1 생성 → **같은 행** `mission_content` 기록, content_format·코어 provenance 불변 (0-b·14). production_task는 코어의 상황·원문·PDR·채널을 그대로 계승(R23), MPJ 5문항은 평행 신규 상황

## 3. `mission_content` JSONB 스키마 (mission_v1)

### 3-1. 상단
```jsonc
{
  "schema_version": "mission_v1",
  "unit": {
    "target_feature": "request_mitigation_optionality",
    "target_feature_version": "1.0",
    "learner_label": "완화와 선택권",        // 카탈로그 복사값 (R14)
    "closing_ko": "…"                       // 카탈로그 복사값 (R14)
  },
  "mpj_items": [ /* 5개 union, 아래 */ ],
  "production_task": { /* 아래 */ },
  "provenance": {                            // v1.5 신설 (0-h·56) — 미션 provenance는 JSON 내장(0-b·14), 코어 provenance 컬럼과 별개·불변
    "model": "gpt-4o",
    "prompt_version": "mission_v1.1",        // 승격 프롬프트 버전
    "prompt_snapshot_hash": "…",             // 승격 프롬프트 스냅샷 해시 (코어의 컬럼과 혼동 금지)
    "mission_content_hash": "…",
    "generated_at": "2026-07-25T…Z",
    "generation_attempt": 1                  // 재생성 루프(§7 ③) 몇 회째 산출인지
  }
  // summary 없음 — 코드가 recommended_example_zh 5개를 모아 렌더 (B13)
}
```
- provenance 채움 주체 = 승격 edge function(모델 응답이 아니라 서버 주입 — id·axis_feature와 동일 원칙). R20이 존재·필수값 검사(0-h·56)

### 3-2. MPJ 공통 필드 + 유형별 완전 분리 union (B12)
```ts
interface MpjCommon {
  id: 1|2|3|4|5;
  axis_feature: string;          // 판정 축 = unit.target_feature와 동일 고정 (0-b·19, R1 검사)
  situation_ko: string; relation_ko: string;
  channel: "email"|"messenger"|"facetoface"|"phone";
  pdr: { p: "speaker_lower"|"equal"|"speaker_higher",
         d: "close"|"acquaintance"|"distant",
         r: "low"|"mid"|"high" };
  source_ko: string;
  preceding_turn_zh?: string;    // 거절·응답류 필수(R8)
  explanation_ko: string;        // 기준 판정 해설 — 어조 규칙 §7-2
  recommended_example_zh: string; // 이 상황의 적절안 예시 1개 (완료 화면 재료, R21)
}
type Scale4Item = MpjCommon & { type:"scale4"; target_zh:string; highlights_zh:string[];
  accepted_scale_codes:string[] };                      // 연속 구간(R7), 1~2개
type Judge3Item = MpjCommon & { type:"judge3"; target_zh:string; highlights_zh:string[];
  accepted_band_codes:string[] };                       // 세트당 1개는 within_band 정답(R2)
type FixChoiceItem = MpjCommon & { type:"fix_choice"; target_zh:string; highlights_zh:string[];
  accepted_band_codes:string[];                         // 반드시 부적절 계열(R18)
  corrections: { zh:string; is_valid:boolean; note_ko:string }[] };  // 4개, valid 정확히 2
type ReasonConfItem = MpjCommon & { type:"reason_conf"; target_zh:string; highlights_zh:string[];
  accepted_band_codes:string[];                         // 반드시 부적절 계열(R18)
  reasons: { id:string; text_ko:string }[];             // 4개
  accepted_reason_ids: string[] };                      // 1~2개 허용 (A7 — 유일 정답 강제 해제)
type MultiJudgeItem = MpjCommon & { type:"multi_judge";
  candidates: { zh:string; accepted_band_codes:string[]; // 후보도 배열 — 경계=길이>1 (A6)
                note_ko:string }[] };                    // 5개. target_zh 없음
```
- 유형 순서 고정: scale4 → judge3 → fix_choice → reason_conf → multi_judge (형식은 공통, **판정 방향·분포는 초점별 정책** — A8)
- multi_judge 분포 `부적절2·적절2·과잉1`은 **기본 권장값**(계약 강제 아님). 불변 규칙은: within_band ≥1 · 분포 예측 불가 · 길이 단서 금지(R5)
- reason_conf(4번)의 pdr = production_task와 같은 조건대(난이도 브리지, R4)

**🔴 band 지위 (0-e — 07-23 정합 라운드)**: `accepted_band_codes`·`accepted_scale_codes`는 **AI가 제안한 현재 채점키(proposed)**다. 모어 화자 캘리브레이션(9월) 전에는 validated가 아니며, `mission_status='reviewed'`(교수자 검토)도 **모어 화자 검증과 동일하지 않다**. 캘리브레이션 결과 반영용 `band_validation_status(proposed|validated)` 필드는 9월 스키마에 추가 — 논문·화면에서 "검증된 정답"으로 서술 금지(proposed_band/validated_band 용어 정본과 연결).

### 3-3. production_task — 번역·통역 (0-b·16, A4 개정)
```jsonc
{
  "mode": "translation",                  // "translation" | "interpreting" — 코어 mode에서 결정
  "source_modality": "written",           // 코어에서 계승 (interpreting이면 "spoken")
  "situation_ko": "…", "relation_ko": "…", "channel": "email",
  "pdr": { "p": "speaker_lower", "d": "distant", "r": "high" },
  "source_text_ko": "…",                  // 코어 계승 (R23)
  "preceding_turn_zh": null,
  "replay_limit": 2,                      // interpreting만 (선택)
  "reference_alternatives": [ { "zh": "…", "note_ko": "…" } ]   // 1~2개, 서로 다른 전략 (v1.4 완화 0-d·32)
}
```
**통역 실행 흐름 (7월 필수 — 0-b·16)**:
① 원발화 TTS 재생(`speechSynthesis` ko-KR, 외부 API 0) → ② 녹음 시작 시 **MediaRecorder + SpeechRecognition(zh-CN) 병렬 시작**(SpeechRecognition은 라이브 마이크 전용, Blob 재전사 불가) → ③ 종료: 오디오는 **세션 내 재생·확인용만(서버 업로드 안 함 — v1.5 0-j·71, 9월 저장 여부=IRB)** + 실시간 전사 초안 → ④ **학습자 전사 확인·수정 화면**(raw 원본 보존) → ⑤ 확인 텍스트로 §4 3층 피드백.
- 폴백: 병렬 실행 실패 환경 = 녹음 + 학습자 직접 입력으로 강등
- **평가 문구 정본**: "통역 수행은 STT로 확인된 언어적 산출에 대해 의미 충실성·명백한 문법 오류·목표 화용 초점을 피드백하며, 발음·성조·유창성·음질은 평가하지 않는다."
- 전사 확인 단계 = **구인 타당성 방어 장치**(STT 오인식을 학습자 오류로 판정 금지) — 9월 OpenAI STT(`gpt-4o-mini-transcribe`, Edge Function 경유) 스왑 후에도 유지

## 4. `runtime_feedback` 스키마 (feedback_v1) — 7/25 구현

```jsonc
{
  "schema_version": "feedback_v1",
  "rubric_version": "…",                       // 카탈로그 version + 프롬프트 버전 (D22)
  "verdicts": {
    "semantic_fidelity": "preserved|minor_loss|distorted",
    "grammatical_accuracy": "clean|impeding_errors",
    "pragmatic_appropriateness": {              // 초점별 대역 재사용 (D21)
      "feature_code": "request_mitigation_optionality",
      "band_code": "too_direct"                 // 카탈로그 band_schema의 코드
    }
  },
  "revision_scope": "meaning|grammar|feature|clear",  // 코드가 유도 — 모델이 정하지 않음
  "blocks": {
    "meaning_ko": "…",
    "grammar": [                                 // 배열, v1.2 최대 1개 (lite — 0-b·18)
      { "error_type": "function_word|word_order|lexical_choice|collocation|missing_component|redundant_component|other",  // v1.4: 선택 필드 (0-d·33)
        "anchor_text": "…",                      // v1.4: 선택 필드 — 있으면 답안 내 실제 존재 검증 (숫자 offset 없음)
        "suggested_correction": "…", "explanation_ko": "…" }
    ],                                           // 명백한 오류 없으면 빈 배열
    "feature_ko": "…",
    "alternatives": [ { "zh": "…", "note_ko": "…" } ]
  },
  "uncertainty_flags": [                         // 문법+화용 통합 (D24) — 화면 비노출, 로그 전용. 7/22 잠금(캘리브레이션 표본용) — lite에서도 삭제 금지
    { "dimension": "grammar|pragmatic", "reason": "…", "internal_only": true }
  ],
  "provenance": { "model": "…", "prompt_version": "feedback_v1" }
}
```

**교차 필드 검증 (D25 — validator 강제)**: `clean` → grammar 빈 배열 / `impeding_errors` → grammar ≥1 / `within_band` → revision_scope ≠ feature / `clear` → 세 verdict 모두 무수정 계열.

**피드백 프롬프트 필수 제약 (백신 4건 + 구조 2건)**:
1. 간접·암묵 실현도 완전한 실현 — 정형 표현 부재를 감점 금지 (Youn 2025)
2. 격식 상향 편향 금지 — 과잉 방향 오교정 금지 (Youn 2025)
3. 불변항은 명시적 체크리스트로 제공 — 원문 핵심 사실 목록의 유지 여부 확인 (Youn 2025)
4. **화용 층 어조 = 비단정** — "이 상황에서는 ~하게 들릴 수 있습니다" 형 위험 방향 제시. 의미·문법은 명시 판정 (Youn 2025 층별 확신도)
5. **루브릭 = 해당 미션의 mission_content + 카탈로그에서 주입** — 화행별 루브릭 9벌을 따로 만들지 않고 구조적으로 해결. 각 verdict 코드의 정의·포함/제외 사례·참조 예시(구칭 골든) 포함, rubric_version 기록 (D22)
6. CoT 3단(grounding→adaptation→explanation), 내부 추론 비노출 (강병규 2025)
7. **통역 입력 = `confirmed_transcript`**(학습자 확인 전사)만 — raw STT 초안 직접 투입 금지(STT 오인식을 학습자 오류로 판정하면 구인 타당성 붕괴). raw/confirmed/edited 3필드는 attempt 로그 보존 (0-b·16)
8. **최소대조 (v1.5 0-k·77)**: `blocks.alternatives[0]` = 가능한 한 학습자 문장을 보존하고 **목표 화용 지점 하나만 최소 수정**한 버전(불변항 유지). revision_scope='feature'의 "받는 사람 입장" 주 카드 재료 — 진짜 최소 편집이 아니면 "한 가지만 바꾼 표현" 라벨 금지

## 5. 상태 사다리·승인 — 2층 (E27~29 + 0-b·17)

**코어 층 — 검색·편성 노출 자격**
| 단계 | 표현 |
|---|---|
| draft | `review_status='needs_review'` + `auto_check_result=NULL` |
| rules_passed(검증①) | `auto_check_result='pass'` |
| released(검증④) | `review_status='approved'` + **`approval_basis`** 기록 |

- `approval_basis`: `item_manual_review` \| `sampled_batch_release` \| `rules_only_release` — 보고 수치는 상호 배타: **개별 인간검토 A / 표본 검토 후 배치 승인 B / 자동검사 통과·미검토 C / 검수 대기 D**
- **fail = 승인 버튼 비활성** / warning = 사유 확인 후 override 가능(관리자·시각·사유 저장)
- 표본 검수 27 = **화행9 × 수준3 전수 + 모드·도메인 균형 배분** (0-b·23)

**미션 층 — 학습자 실행 자격 (코어 승인과 별도 게이트)**
| mission_status | 의미 |
|---|---|
| NULL | 코어만 (미션 없음) |
| generated | 미션 생성됨 — 관리자 열람·편성 미리보기만 |
| reviewed | **개별 인간 검토 완료(검토자·시각 기록) — 학습자 실행은 이 상태만** |

- 코어가 어떤 basis로 released됐든 무관 — sampled/rules_only 코어의 미션도 reviewed 없이는 실행 불가(아무도 안 읽은 중국어 문항이 학습자에게 노출되는 경로 차단)
- **7월 승인 계획**: 참조 미션 3(구칭 골든) + 데모 편성분 미션 = reviewed / 코어 500 = smoke·표본 검수 후 sampled_batch_release, 잔여 rules_passed 정직 보고. ~~검증②(AI 점검)는 9월~~ → **0-n·94로 7월 P0 복원**(관리자 품질관리 장치)
- 수정·반려 = **새 행 생성 + `supersedes_scenario_id` 링크**, 구 행은 `usage_assignment='excluded'`. reviewed 미션 인플레이스 수정 금지 (B14 경량 수용)

## 6. DB 마이그레이션 + RPC (opus 명세)

```sql
ALTER TABLE public.scenarios
  ADD COLUMN IF NOT EXISTS content_format text NOT NULL DEFAULT 'legacy_v1',
  ADD COLUMN IF NOT EXISTS core_content jsonb,                 -- 0-b·13
  ADD COLUMN IF NOT EXISTS source_modality text,               -- 0-b·15
  ADD COLUMN IF NOT EXISTS theme_code text,                    -- 07-23 편성층 (§2b)
  ADD COLUMN IF NOT EXISTS topic_code text,                    -- 07-23 장면 시드 코드
  ADD COLUMN IF NOT EXISTS mission_content jsonb,              -- 승격 시 채움 (0-b·14)
  ADD COLUMN IF NOT EXISTS mission_status text,                -- NULL|generated|reviewed
  ADD COLUMN IF NOT EXISTS mission_reviewed_by uuid,
  ADD COLUMN IF NOT EXISTS mission_reviewed_at timestamptz,
  ADD COLUMN IF NOT EXISTS target_feature text,
  ADD COLUMN IF NOT EXISTS target_feature_version text,
  ADD COLUMN IF NOT EXISTS prompt_snapshot_hash text,
  ADD COLUMN IF NOT EXISTS generation_run_id text,
  ADD COLUMN IF NOT EXISTS generation_item_key text,
  ADD COLUMN IF NOT EXISTS content_hash text,
  ADD COLUMN IF NOT EXISTS supersedes_scenario_id uuid REFERENCES public.scenarios(scenario_id),
  ADD COLUMN IF NOT EXISTS approval_basis text;
ALTER TABLE public.scenarios ADD CONSTRAINT scenarios_content_format_ck
  CHECK (content_format IN ('legacy_v1','scenario_core_v1'));  -- mission_v1은 format이 아니라 층 (0-b·14)
ALTER TABLE public.scenarios ADD CONSTRAINT scenarios_core_ck
  CHECK (content_format <> 'scenario_core_v1' OR (
    core_content IS NOT NULL
    AND core_content->>'schema_version' = 'scenario_core_v1'
    AND source_modality IN ('written','spoken')
    AND theme_code IN ('campus_study','daily_living','travel_mobility','relationship_social',
                       'career_workplace','commerce_customer','digital_content','international_exchange')
    AND topic_code IS NOT NULL ));
    -- topic 값 목록 검증은 카탈로그(클라이언트+RPC) — DB에 목록 미고정 (0-c·28)
ALTER TABLE public.scenarios ADD CONSTRAINT scenarios_mission_ck      -- 병렬 층 정합 (B15 개정 + 0-e 강화)
  CHECK ( (mission_content IS NULL AND mission_status IS NULL)
       OR (mission_content IS NOT NULL
           AND mission_status IN ('generated','reviewed')
           AND mission_content->>'schema_version' = 'mission_v1'
           AND target_feature IS NOT NULL
           AND target_feature_version IS NOT NULL) );
ALTER TABLE public.scenarios ADD CONSTRAINT scenarios_mission_reviewed_ck   -- 0-e: 검토자도 필수(감사 요구 정합)
  CHECK (mission_status IS DISTINCT FROM 'reviewed'
      OR (mission_reviewed_at IS NOT NULL AND mission_reviewed_by IS NOT NULL));
CREATE UNIQUE INDEX IF NOT EXISTS scenarios_generation_run_item_ux
  ON public.scenarios(generation_run_id, generation_item_key)
  WHERE generation_item_key IS NOT NULL;   -- 복합 멱등키 (0-b·22): 런 재시도 멱등 + 셀 재생성 허용
```

**RPC 3종 (기존 RPC 무변경)**

`save_generated_core(p_payload jsonb)` — 배치용 INSERT:
- 저장 전 **클라이언트 코어 서브셋 검사 통과 필수** — RPC는 위 CHECK로 최소 방어
- 행 태그 = speech_act·learner_level·domain·industry·mode·source_modality·theme_code·topic_code + **코어의 p/d/r·channel→genre** (행 태그 의미론 — B10). **week_no 기록 금지** (B11)
- provenance(run/item/content 해시) = 코어 배치 기준

`save_generated_mission(p_scenario_id uuid, p_payload jsonb)` — 승격용 UPDATE(같은 행):
- 대상 = `content_format='scenario_core_v1'` AND `mission_content IS NULL`인 행만 (재생성·수정은 supersedes 새 행)
- `mission_content`(**미션 provenance는 JSON 내장 — §3-1 provenance 객체(0-h·56)**, 컬럼 아님) + `mission_status='generated'` + `target_feature(+version)` 기록. 승격 입력 = **계획 초점 명시 전달**(주차 화행의 카탈로그 기본 초점 — 0-h·55) → R24 일치 검사
- content_format·코어 provenance 컬럼 **불변** (0-b·14) · production_task = 코어 계승 검사 R23 통과 필수

`review_mission(p_scenario_id uuid)` — `mission_status='reviewed'` + `mission_reviewed_by/at` 기록 (실행 게이트 0-b·17)

## 6b. `mission_attempt_v1` — 학습자 수행 로그 계약 (D26, 저장처 learner_mission_logs)

```jsonc
{ "schema_version": "mission_attempt_v1",
  "scenario_id": "…", "mission_schema_version": "mission_v1",
  "entry_mode": "course",                    // course 단일 운용 — self_study는 후속 연구 예약어(자율학습 범위 제외, 0-m·93)
  "mpj_answers": [ { "item_id":1, "picked_codes":["…"], "is_within_accepted":true,
                     "reason_ids":["…"],   // ④ reason_conf만 존재
                     "confidence":"…",     // ④ reason_conf만 — ①~③·⑤·DCT엔 없음 (0-c·30)
                     "elapsed_sec":12 } ],
  "first_response_zh": "…",                  // interpreting은 confirmed_transcript가 이 역할
  "interpreting": { "audio_path": "…", "raw_stt_transcript": "…",
                    "confirmed_transcript": "…", "transcript_was_edited": true,
                    "replay_count": 1 },       // mode=interpreting일 때만 (0-b·16)
  "feedback_snapshot": { /* runtime_feedback 전문 */ },
  "ai_alternative_response": { "decision": "accepted|partial|rejected",
                               "reason_codes": ["…"] },  // v1.5 경험층(0-i·60) — 이유는 선택, 8월 로그 배선 시
  "revised_response_zh": "…", "revision_scope_shown": "feature",
  "resume_stage": "mpj|production|feedback|revision|complete",  // v1.5 0-k·75 — 2부 분절 재개점. 7월=localStorage만, DB 배선=로그 작업 때
  "mpj_completed_at": "…",                   // 0-k·75 — 1부 완료 시각(교실 토론 후 DCT 판별은 first_response와의 시각차로 파생)
  "stage_elapsed_sec": { "judge":95, "produce":180, "feedback":40, "revise":120 },
  "provenance": { "feedback_model":"…", "prompt_version":"…" } }
```

**연결성 주석 (0-f·43)**: attempt는 `learner_mission_logs` 행에 저장 — learner·cohort·시각·화행·초점·모드는 행 컬럼·조인으로 확보(로그 JSON에 중복 저장 안 함). **후속 보강 필요** = ①배정 식별자(`curriculum_week_scenario_id`) 링크 ②`post_revision_feedback`/`revision_resolved`(수정 성공 판정). 7월엔 이 명세만 남기고 필드·RPC 미구현.

## 7. 생성 파이프라인 (C17~19)

```text
① 미션 전체를 OpenAI structured output(json_schema) 1회 호출로 생성 (temp 0.3)
   — 프롬프트에 산출 정합 커버리지(요구 전략 ↔ 어느 문항이 노출하는가) 지시 포함
② 결정론 검사 R1~R24
③ 실패 시 미션 전체 재생성 (최대 2회, temp 0.5 상향 + 실패 사유를 프롬프트에 주입) → ② 재검증
④ 통과 → mission_status='generated' → 인간 검토(reviewed)
```
(v1.4·0-d·31: outline 단계·필드 repair 엔진 삭제 — 7월 승격 물량 15~20개는 전체 재생성이 더 싸고,
 production_task가 코어 계승이라 outline의 상황 기획 역할이 승격 경로에선 불필요)
다양성은 temperature가 아니라 셀 조건·상황 시드로 만든다.

> ⚠️ **2026-07-28 구현 감사:** 위 ①은 계약 목표이나 감사 시점의 배포 Edge는 모든 action에
> `json_object`를 공통 적용하고 있었다. 따라서 미션 strict `json_schema`는 **미구현 상태**로
> 기록하며, 코어 선행 적용과 회귀검증 후 별도 세대로 반영한다.

### 7-0. 코어 배치 파이프라인 (500 — 기존 배치 러너 개조, 0-b·23)
단일 호출(코어는 outline 불요, temp 0.7) → 코어 서브셋 검사 → 실패 셀은 통째 재생성(코어는 repair보다 싸다).
**단계 배치**: ① **20 smoke** — 구조·중복·중국어 자연성·spoken 담화체 눈검사 → ② **100** — 셀·도메인·모드 분포 검산 → ③ **500 본 배치**. 단계마다 게이트 통과 후 진행(스키마·프롬프트 오류의 500배 복제 방지).
- **2026-07-28 로컬 정렬안:** 코어 모델 출력 5개 필드(`situation_ko`, `relation_ko`,
  `source_text`, `preceding_turn`, `brief_note_ko`)에 `strict:true` JSON Schema와
  `additionalProperties:false`를 적용한다. 출력 키와 DB 저장 구조는 유지한다.
  response format·schema 본문이 hash에 포함되므로 기존 `24ad...`와는 새 생성 계열이다.
  대표 셀 소량 회귀 전에는 Edge 배포·18셀 재생성·500 배치를 하지 않는다.
- spoken 코어 원발화 = TTS로 읽혀 자연스러운 **짧은 구두 담화체**(이메일 문어체 낭독 금지 — 프롬프트 명시)
- **배분 쿼터 축 (v1.5 개정 0-h·57)**: **1차 쿼터 = 화행9×수준3×모드2 = 54셀, 셀당 최소 3** — 모드를 주변 분포로 두면 특정 화행×수준에서 번역/통역이 비는 것을 배치 러너가 허용(전역 순번식 통역 배분 — 코드 확인됨). **500 본 배치 전 러너 수정 필수.** 2차 주변 분포 = theme·topic·domain·industry·P/D/R·channel(프리셋 선반이 비지 않게)
- **중복률 2종 (0-h·57)**: 완전 중복률 = R19 자동검사 / **근접 중복률 = 표본 검수 27의 점검 항목으로 통합**(유사도 엔진 신설 없음 — §9 유지). 감사표(0-g·46)에 **theme·topic별 건수** 포함(주제별 풍부함 방어)
- 미션 승격 파이프라인(아래 ①~④)은 편성·데모 선별분에만 가동

### 7-1. 프롬프트 v2 주입 요소
- [생성 요청] 조건 블록(기존 재사용, PDR 새 값 이름) + **화용 초점 카탈로그 전문**(operational_definition·band_schema·relevant_resources·excluded_confounds·counter_rule_note)
- **승인된 오류 패턴 카탈로그** (A5): `src/lib/pragma/errorPatterns.ts` — v1 시드는 문헌 근거 있는 것만
  ```text
  {patternId, description, evidenceSource, applicableSpeechActs, approvedExample}
  예: 직접 부정 전면 배치(Wu&Roever 2021 A2 실증) · 학습자 장황성(Blum-Kulka via Taguchi&Li) ·
      내적 완화 빈약(Taguchi 2018) · 한자어 간섭 발표→发表(관찰) · 把 명령형 과잉(관찰)
  ```
  근거 없는 오답은 "전형적 학습자 오류"가 아니라 **"교육적으로 설계한 대조 후보"**로 표기(note_ko에도)
- 통번역 경계(불변항·역번역 검사·정형 표현 단서·암시 금지 — v1과 동일)
- **채널 담화 형식 (v1.5 — 0-k·79)**: 채널에 맞는 담화 형식을 사용한다 — **이메일은 메신저 문장을 형식적으로 늘린 형태가 아니어야 하고**(수신자 호칭·맥락 재제시·구조화된 본문·마무리), **메신저는 완성된 공식 이메일 문체가 아니어야 한다**(직전 턴 반응·짧은 턴·대화체). 검증 규칙 신설 없음 — 프롬프트 지시만(표본 검수 27의 눈검사 항목)
- **게이트1 명문화 (v1.5 — 0-g·45→0-h·54)**: `target_zh`·모든 `corrections.zh`·모든 `candidates.zh`·`recommended_example_zh`·`reference_alternatives.zh`는 **먼저 불변항(명제·의도·화행 목적)을 유지해야 한다.** 의미·의도가 달라진 문장은 화용 대역 판정 후보로 **생성 금지** — 부적절성은 오직 해당 화용 초점의 과소·적정·과잉 차이로만 실현한다(의도 소실·의미 이탈 예문은 MPJ가 아니라 피드백 semantic_fidelity 층의 소관). 통역 승격 시 MPJ 후보 = **구두체 강제**(0-g·52)
- 거절 분기: preceding_turn 필수 + **유발행위별 관련 자원의 선택적 고려**(초대→사과, 제안→감사+但是, 요청→이유 선행 등 — Wu&Roever의 **집단 경향이지 생성 공식 아님**. P·D·R·부담·채널에 따라 생략·재배열 가능, 전 자원 의무 삽입 금지 — 0-b·20, 단조 "정답 거절 템플릿" 양산 방지) + 수준 사다리
- **수준 정책 `level_policy_v1`** (C20): 입문=단문 중심·종속절 제한 / 중급=복문 1~2·이유/조건 표현 / 고급=담화 조직·복합 전략. **수준 차등 = 길이가 아니라 자원 조합 수**(입문 1·중급 2·고급 선택 배열 — Wu&Roever)
  + **원문 분량 사다리 (07-23 사용자 추가 · v1.3 정정 0-c·29)**: 입문 = 1~2문장 발화 / 중급 = 2~4문장 / **고급(번역) = 맥락 포함 3~5문장 · 고급(통역) = 기억 과부하를 유발하지 않는 짧은 구두 담화**. 모든 수준에서 **중심 화행 1개·화용 초점 1개 유지** — 화행 연쇄는 미션이 아니라 12주차 통합 과업 유형. ⚠️ 늘어나는 것은 **원문의 분량·담화 단위**이고 산출 분량은 그 자연스러운 결과다 — 산출 "길이" 자체를 평가 목표로 삼지 않는다(고급 표적 = 경제성 회복, Wu&Dai U자형: "길수록 좋음"은 우리가 깨려는 장황성 오류를 가르침)
- 해설 어조: **"주어진 관계·매체·상황 속 수신자(중국어 모어 화자)가 어떻게 듣는지"** (A9 — 상황 결부형으로 수정, 모어 화자 속성은 유지: 수용성 판단 주체 필요 + Dai&Roever indigenous criteria 근거). "중국인은/중국에서는/중국 문화에서는/일반적으로 중국에서" 등 국가 단위 서술 금지(R9)
- 소재 배제: 정치·시사·정부 기관 관련 상황 생성 금지(장면 시드 원전인 Dai 2023 목록에 일부 존재하나 학부 수업 콘텐츠로 부적합 — 도메인 필터에 더해 명시 배제)
- judge3 규칙(일반화): **해당 화용 초점의 소박한 규칙(counter_rule_note)을 깨는 적절한 반례 1개** (A1)
- learner_label·closing_ko는 생성 금지 — 카탈로그 값 복사(R14)
- 패턴은 시드로만 — 문헌 예문 그대로 복제 금지, 한국어 원문·조건에 맞게 재설계

## 8. 규칙검사 v1.5 (R1~R24 — 결정론·API 0회. **코어 서브셋** = R1c·R8·R9·R10·R15·R16·R17·R19+R22)

| ID | 검사 | 판정 |
|---|---|---|
| R1 | 스키마 형태·유형 순서·필수 필드·**코드값이 카탈로그 band_schema에 존재**·**axis_feature=unit.target_feature**(0-b·19). R1c=코어 스키마 형태 검사 | fail |
| R2 | judge3의 accepted에 within_band 포함 — **구조 검사만**(반례 슬롯 존재 여부). 반례가 실제로 counter_rule을 깨는지(내용 타당성)는 자동 검증 불가 → **눈검사 항목**(§10) | fail |
| R3 | fix_choice: 4개·valid 정확히 2 | fail |
| R4 | reason_conf: 이유 4개·accepted_reason_ids 1~2·pdr=production과 같은 조건대 | fail |
| R5 | multi_judge 길이 통제: **완전 단조(길이순=정답키) = fail** · over 유일 최장문 = **warning**(v1.4 증거 강등) · under 전부 최단 = **warning**(v1.4 강등) · 최장/최단 비율 > 3 = warning | fail/warning |
| R6 | highlights_zh ⊂ target_zh | fail |
| R7 | accepted 코드가 척도의 연속 구간 | fail |
| R8 | 거절·응답류인데 preceding_turn_zh 없음 | fail |
| R9 | **확장판**: 해설·note 필드에서 "중국인(들)은·중국에서는·중국 문화에서는·중국어 화자는·일반적으로 중국" 패턴 | **fail** (해설 필드 한정) |
| R10 | source=한국어·target/candidates=중국어(문자 범위) | fail |
| R11 | reference_alternatives 1~2개(v1.4) · recommended_example_zh 전 문항 존재 | fail |
| R12 | 세트 accepted 분포 전부 동일 방향 | warning |
| R13 | target_feature 코드·version이 카탈로그와 일치 | fail |
| R14 | learner_label·closing_ko가 카탈로그 값과 동일(복사 검증) | fail |
| R15 | 전 문항이 요청된 speech_act·domain·level 범위 안 | fail |
| R16 | **mode↔source_modality 정합**: interpreting→spoken(구두 담화체)·translation→written. **채널은 모드와 독립**(대면·전화는 통역의 대표 환경 — v1.1의 "통역×대면=부정합"은 오류였음, 0-b·21) | fail |
| R17 | industry_sector는 work 도메인에서만 | fail |
| R18 | fix_choice·reason_conf의 accepted = 부적절 계열 | fail |
| R19 | 세트 내·배치 내 source/candidate 완전 중복 | warning |
| R20 | **2층 정합**: content_format='scenario_core_v1' · core_content/mission_content의 schema_version·mission_status·target_feature 상호 일치 (0-b·14) **+ mission_content.provenance 존재·필수값**(model·prompt_version·mission_content_hash·generated_at·generation_attempt — v1.5, 0-h·56) | fail |
| R21 | recommended_example_zh가 그 문항 accepted 판정·교정 선택지와 모순되지 않음(교정 valid 후보와 동일성 검사 등 기계 검사 가능 범위) | warning |
| R22 | 수준 정책: 문장 길이 상한·절 수·HSK 초과 어휘 비율(hsk_vocab 조회) | warning |
| R23 | **미션 production_task가 코어 core_content를 계승**(situation·source_text·pdr·channel 동일 — 0-b·14) | fail |
| R24 | **승격 입력 정합 (v1.5, 0-h·55)**: 승격 요청의 계획 초점(주차/코어 화행 → `DEFAULT_FEATURE_BY_ACT` 카탈로그 기본 초점) = `unit.target_feature`. 9월 복수 초점 도입 시 계획 초점은 명시 파라미터로 승격 | fail |

## 9. 7월에 하지 않는 것 (명시적 제외 — 사유는 §11. 마감: 중간보고 7/26·완성 7/31)

~~검증②(AI 품질 점검) 신설~~(⛔ **0-n·94로 7월 P0 복원 — 이 줄 무효**) · **발음·성조·속도·유창성·음질 평가**(통역은 7월 실행하되 평가는 §4 3층만 — 0-b·16) · OpenAI STT 전사(9월 Edge Function 경유 스왑 — 7월은 브라우저 병렬 STT) · `formality` 입력 축 · `mission_packages` 별도 테이블 · `content_pool`/`scenario_family`/unlock·retire(9월 노출 계약) · 유사도 스코어링 · pair(아크 전용)·pol5 변주 풀 · 자율학습 UI(⛔ 0-m·93 — 이월 아닌 **범위 제외**, 후속 연구로만) · MFRM 분석 · **경험층 구현 전건(0-i — 생각벽·표현함·media_assets·설문/펄스·분포 RPC·차등 지원·AI Trace 로그 배선. 7월은 ②검수 리스킨·④수정 지도 버퍼분만)**

## 10. 게이트 2종

**참조 미션 게이트 (구칭 '골든 미션 게이트' — 0-g·44) (7/23 저녁)** — 요청·거절·감사 × 중급 3개, **코어 생성→승격 경로로**(2층 파이프라인 전체 검증): DB 저장 → 재조회 렌더 → R1~R24 통과 + **눈 검사**: 축·대역이 화행에 맞는가 / 거절의 유발행위 구조 / 수준 사다리 사실성 / recommended_example이 해설·교정과 정합 / 산출 정합(요구 전략 사전 노출) / **H1. 모든 판정 후보(target·corrections·candidates·recommended·reference)가 게이트1(불변항)을 통과하는가 / H2. 부적절 판정의 근거가 의미 손실이 아니라 해당 초점의 과소·적정·과잉 차이인가** (v1.5 — 0-h·54) / **H4. 경계 사례 비율 — 판정 후보 중 '중국어 초급자가 화용 지식 없이 소거할 수 있는' 극단 문장이 몇 개인가. 그런 후보가 세트의 절반을 넘으면 문항이 화용 훈련이 아니라 '나쁜 표현 찾기'다** (0-r·105). ※ H3(한국어 후보의 자연스러운 화역 — zh_ko 전용)은 0-l·91.
**참조 미션 request③ 처분 (0-h·54)**: 吧 문항 오판(0-g·45)의 해당 미션은 **DB 미저장 상태**(RPC 미배선 — supersedes 불요, GPT의 "구 행 excluded" 안은 사실 오류로 기각). 처분 = 리뷰 파일에 폐기 표시 + **수정 프롬프트(게이트1·H1·H2 반영)로 재생성·재눈검사 = 승격 UI 배선의 선행 조건.**
**조건부 강등 경로 (0-d·34)**: 특정 MPJ 유형이 화행당 3회 시도에도 품질 미달이면 폴백(scale4 1 + judge3 4, 마지막에 이유 선택+확신도)으로 강등 — 사전 컷이 아니라 게이트 증거 기반 결정. 강등 시 사유를 §11에 기록.

**코어 smoke 게이트 (7/24)** — 20개 배치: 구조·중복·중국어 자연성·spoken 담화체 눈검사 통과 → 100(분포 검산) → 500 본 배치(7/25 밤 목표, §7-0).

## 11. GPT 검증 33건 판정 기록 (감사 추적)

**수용(26)**: A1 A3 A4 A6 A7 A8 B11 B12 B13 B15 B16 C17 C18 C19 C20 D21 D22 D23 D24 D25 D26 E27 E28 F31 F32 F33
**부분 수용(6)**: A2(d 개명·p 화자 기준 ✅, formality 입력 축은 보류 — 채널·관계가 격식 신호, 매트릭스 확장 회피) · A5(오류 카탈로그 ✅ 경량판 — 문헌 근거 5종 시드, 무근거는 "설계 대조 후보" 표기) · A9(상황 결부형 표현 ✅, 단 모어 화자 속성 유지 — 수용성 판단 주체 + indigenous criteria 근거) · B10(행 태그 의미론 규칙 ✅, 별도 테이블은 v1 기각 — §2-1 사유) · B14(supersedes 경량판 — 새 행+링크, 인플레이스 금지 유지) · E29(취지 ✅, 단 "경량 AI audit" 대신 표본 인간 검토 — 검증② 9월 결정과 모순 회피)
**기각/보류(5)**: formality 축 · mission_packages 테이블 · E30 노출 계약(9월 — 기존 판정, 새 논거 없음) · AI audit 신설 · 유사도 엔진
**D23 부속 수용**: 강병규 오류 분포와 "직접 비교 가능" 주장 삭제 — 코드북 상이. "참조 가능한 비교점"으로 하향(문헌적용맵 1차의 해당 표현도 이 기준으로 읽을 것)

---

### v1.2 라운드 판정 기록 (2026-07-23 저녁 — GPT 2차 8건 + 최종 5건, fable 판정)

**전제 사실 2건 사용자 확인 → 1차 기각 2건 번복**: 지도교수 요구 = ①시나리오 DB 최소 500(여러 번 직접 언급, 박사동료 A의 1000곡 대비) ②통역 7월 최소기능(TTS·STT, "API 연동 불요 수준" — 발음·성조·유창성 평가 제외는 사용자가 화용 적절성 집중으로 반박해 확정). 구 "볼륨 135 + 숫자 부풀리기 금지" 결정 폐기(500은 교수님 본인의 숫자).

**수용(GPT 2차·최종)**: 2층 분리(0-b·13 — 기존 배치 러너가 이미 시나리오 단위 생성기라 공학 비용 반론도 소멸) · 병렬 상태 보존(content_format 불변·mission_status — 0-b·14) · source_modality(0-b·15) · 녹음+STT 병렬 실행(SpeechRecognition은 Blob 재전사 불가 — fable 초안의 기술 오류를 GPT가 정정) · 전사 확인 단계(구인 타당성 방어) · 통역 피드백 3층 유지("화용만 평가" 문구는 fable 오류 — 의미 충실성은 통번역 간판) · 단계 배치 20→100→500 · 표본 27=화행×수준 전수+모드·도메인 균형 · politeness 보조축 제외(0-b·19) · feedback-lite(0-b·18) · Wu&Roever 완화(0-b·20) · R16 재작성(0-b·21) · 복합 UNIQUE(0-b·22) · 학습자 실행=reviewed만(0-b·17)

**조정(fable — GPT안 대비)**: mission_status 4값→2값(executable은 reviewed에서 파생, 상태 중복 방지) · generation_history 배열·미션 provenance 컬럼 기각→mission_content 내장 · 본 배치 시점 = 7/24 smoke→7/25 100→7/25 밤 500(GPT의 "7/24 내 본배치"는 생성기 완성 일정과 충돌. 실마감 7/31이라 반나절 지연 무리스크) · 병렬 STT 실패 환경 폴백 = 녹음+직접 입력 명시

**유지(GPT 제안 기각)**: uncertainty_flags 삭제(7/22 잠금 위반 — 캘리브레이션 표본용 로그) · 교차 필드 검증 삭제(10줄 코드로 모순 피드백 차단) · 루브릭 주입 단순화(이미 화행별 루브릭 9벌을 회피하는 경량화 장치)

---

### v1.3 라운드 판정 기록 (2026-07-23 밤 — GPT 4차 5건, fable 판정)

**수용(4)**: ①고급 "화행 연쇄" 정정(fable 표기 오류 — 사용자 의도는 "여러 문장"이었고 연쇄는 통합 과업 영역. 통역 장문 기억 과부하 제한 포함) ②scenarioTopics.ts 카탈로그 계약(인터페이스·위치·theme당 ≥3) ③RPC payload 소유권 + DB CHECK 강화(theme 8값·topic NOT NULL) ④v1.3 버저닝 + 0-c 변경록(조용한 덮어쓰기는 거버넌스 위반이 맞음)
**부분 수용(1)**: 확신도 — 로그 예시의 모호성 해소는 수용(④만 confidence 명시), 단 **GPT의 확장안(MPJ ①~④ 전 문항 + DCT production_confidence)은 기각**: 7/22 피로 원칙("맞게 판단한 학생에게 캐묻지 않기") 잠금 + 매 문항 확신도는 response set 오염으로 캘리브레이션 데이터 질 하락. 9월 파일럿 후 확대 재검토

---

### v1.4 라운드 판정 기록 (2026-07-23 밤 — GPT 감량 TOP5, fable 판정)

**전제 교정**: GPT는 "미구현 전제"로 5건을 뽑았으나 1·3(일부)·4(일부)·5는 opus가 이날 밤 구현 완료(typecheck 통과) — GPT 자신의 단서("이미 완성된 것은 삭제 비용 재계산") 적용해 재판정.
**수용(1)**: 2위 파이프라인 단순화(0-d·31) — 유일한 미구현 항목 = 진짜 절감(~1일). outline·필드 repair 삭제, 전체 재생성 ≤2회. 연구 손실 0
**부분 수용(2)**: 3위 피드백(error_type·anchor_text 선택 필드화 — 나머지 유지) · 4위(reference_alternatives 1~2 완화 — recommended_example_zh는 완료 화면 잠금 재료라 유지)
**기각(2)**: 1위 MPJ 5유형 사전 컷 — ①지도교수 승인 잠금 ②fix_choice=사용자 최고 평가 유형 ③union·R검사 매몰. 단 **조건부 강등 경로**(§10)로 리스크는 수용: 게이트 증거로 강등 결정, 시점이 7/25 UI 배선 전이라 낭비 없음 · 5위 errorPatterns — 이미 구현·문헌 비용 지불 완료, "선택적 시드"로 지위만 명확화
**uncertainty_flags 삭제 3차 기각** — 7/22 잠금(캘리브레이션 로그), 비용 = 로그 배열 하나

### 참조 미션 게이트(구칭 골든) 실행 기록 (2026-07-23 밤, opus) — **구조·규칙검사+연구자 눈검사 통과**(구 표기 "콘텐츠 검증 통과"는 내용 타당성 함의로 폐기 — 0-h·53) · 라운드트립 대기 (0-e·36)
- 3화행(요청·거절·감사)×중급 = 코어 생성→미션 승격 경로로 생성, **R1~R23 전부 fail 0**(경고만). 리뷰 파일 `바탕 화면\최근 작업\골든미션_눈검사_0723.md`
- **구현 중 발견·정정**: ①id·axis_feature = 서버 주입(모델 부담 제거) ②pdr·channel 코드값 프롬프트 명시(한국어 라벨 누출 차단) ③스키마 5문항 전개(축약 "..." 제거 — 필드 누락 해소) ④미션 모델 = gpt-4o(코어는 mini 유지, 미션은 저volume·복잡) ⑤R8 응답류 preceding_turn 스키마 반영 ⑥intensity≠length 프레이밍(R5)
- **R5 증거 기반 강등**(위 표): 자연 언어의 강도-길이 상관 때문에 "over 유일 최장문"·"under 전부 최단"을 hard fail로 두면 자연성 훼손 → warning(눈검사)으로. "완전 단조"만 fail 유지
- **눈검사 2라운드(fable)**: 1차 BLOCKER 7건(맥락 무시 대역 판정 등) → 프롬프트 수정(사용자 정제 2건 반영) → 재생성 1회 → **2차에서 체계적 BLOCKER 소멸 확인**. 잔존 WARNING(참고안 약속 추가 등)은 reviewed 개별 검토 몫
- ⚠️ **미검증(게이트 정의 §10의 일부)**: save_generated_core/save_generated_mission RPC의 실제 DB 저장→재조회 렌더(admin 인증 필요 — 브라우저/AdminBatch UI 몫, 7/24). 생성+R검사+눈검사는 검증됨 → **프롬프트 동결(조건부)** — 라운드트립 통과 후 완전 동결
