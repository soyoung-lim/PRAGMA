# 2026-08-04 · 학습자 헤더 진동·콜드 오픈 표시 검수

## 검수·보완

- Claude가 적용한 진행 레일 히스테리시스(접힘 `>160px`, 펼침 `<100px`)와 높이
  transition 제거를 검수했다. 레일 높이 변화 40px보다 임계값 간격 60px가 커서 기존
  scrollY 되먹임이 끊긴다.
- MPJ의 고정 맥락 바는 `fixed`라 문서 흐름과 높이를 바꾸지 않는다. 따라서 표시 여부가
  자기 자신의 경계값을 다시 움직이는 2차 진동 구조는 아니다.
- 콜드 오픈의 긴 첫 문장에 대한 단계별 글자 크기 완충과 DEV 경고를 유지했다. 계약 위반
  문구를 화면에서 숨기거나 원문을 표시층에서 재작성하지 않는다.
- DEV 금지 문구 검사가 두 번째 문장 이후만 보던 누락을 보완해 `situation_ko` 전체를
  검사하도록 수정했다. 판정·저장·완료 조건·정책 버전은 변경하지 않았다.

## 검증

- localhost `?preview=v5`에서 MPJ1 데모 입력 뒤 50ms 간격 16회(750ms) 측정:
  `scrollY=156`, 진행 레일 높이 `52px`로 모두 고정되어 방향 전환이나 진동이 없었다.
- `npm run typecheck` 통과.
- 전체 Vitest **259 pass / 7 skip**.
- `MissionRunV1.tsx` ESLint와 `git diff --check` 통과.

## 보류·후속 판단

- 정본 run `core_ko_zh_1785458303114`의 495건 확산 조회는 로컬 DB 읽기 권한이 없어
  완료하지 못했다. 익명 REST는 401, 로그인되지 않은 localhost 관리자 조회는
  `permission denied for table scenarios`였으며 DB 쓰기·배포는 하지 않았다.
- 명시된 금지 문구는 신규 계열의 결정론적 저장 전 규칙으로 차단하고, 동의어·우회 표현은
  `quality_check`가 보완하는 이중 게이트가 적합하다. 현 R16은 수행 모드 모순 규칙이므로
  여기에 섞지 않는다.
- `brief_note_ko`는 현재 계약상 편성 화면용이며 미션 런타임에도 전달되지 않는다. 학습자
  콜드 오픈 표제로 재사용하지 않고, 다음 계약 계열에서 별도 학습자용 표제
  `scene_title_ko` 또는 첫 문장 역할·길이를 명시하는 대안을 검토한다.
- `scene_title_ko`는 `2026-08-03-cold-open-single-screen.md`에서 `hook_ko`로 기록한 것과
  동일한 미구현 후보 필드다. 선행 기록보다 늦은 이름이지만, 학습자 장면의 **표제**라는
  용도를 정확히 드러내고 일반적인 관심 유도용 hook과 혼동되지 않아 이 이름으로 통일한다.
  필드 구현·계약 개정은 이번 범위에 포함하지 않는다.
