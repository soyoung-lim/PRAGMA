# 주차 미션·학습 노트·교과목 수업자료 진단 인계

- 날짜: 2026-08-27
- 상태: 사용자 승인으로 출력 구조를 구현하고 production에 배포했다. 현행 상태는 아래 **후속 구현·운영 배포** 절을 따른다.
- 분류: 완성 전 해결 권장 / 최초 진단은 [단독 진행 적합], 출력·공개 경계 결정은 [교차검증 권고]
- 최초 종료 요청 뒤 사용자가 `구현해`, 이어서 `배포까지 해`라고 지시했다. 승인 순서와 실제 결과를 아래에 구분한다.

## 최초 진단 시 작업 기준(후속 구현 전)

- 이 메모의 저장 위치는 `C:\Users\cnkr\.codex\worktrees\664e\l2-pragmatic-translator`다.
  현재 HEAD는 `f89937fa82d6247c4663d45c4b4d9263d06174fc`, detached HEAD이며 오래된 작업본이다.
  메모 작성 전에는 clean이었다. 이 메모를 저장했다고 코드 기준이 최신으로 바뀐 것은 아니다.
- 이번 세션에서 원격 main을 `git ls-remote`로 확인한 값은
  `871802f0f21369a1930f8aecdca47d09a02d4847`로, 인계받은 PR #25 병합 커밋과 일치했다.
  현재 체크아웃에는 해당 커밋 객체가 없어 조상 포함 여부 검사는 성립하지 않았다.
- 진단에 사용한 소스는 사용자 인계 경로
  `C:\Users\cnkr\Documents\Projects\l2-pragmatic-translator\.worktrees\mpj5-mainline-2026-08-24`다.
  브랜치 `codex/mpj5-mainline-2026-08-24`, HEAD
  `dbc092326a94d676f6086490215b35f7c2abbeb5`, clean을 확인하고 읽기만 했다.
- 기존 worktree와 오래된 로컬 main은 건드리지 않는다. 기능 수정에 앞서 병합 커밋을 포함하는
  올바른 작업 기준을 확보하고 이 메모를 이어받아야 한다. 아직 checkout·fetch·merge는 하지 않았다.
- 기존 작업본 AGENTS.md의 완성 우선 모드를 확인했다. 그 문서는 삭제된
  `docs/handoff/ACTIVE_HANDOFF.md`를 복원하지 않고 CANONICAL·관련 dev-log·evidence index에서
  인계를 시작하도록 명시한다. 현재 오래된 체크아웃의 안내와 혼동하지 않는다.

## 사용자 피드백과 확인 사실

1. **주차의 미션 카드가 DCT 과제 설명처럼 보이고 너무 길다.**
   - `LearnerCourseWeek.tsx`는 `scenario.situation_ko`를 카드 본문에 그대로 표시한다.
   - `composer.ts` → `learnerCourse.ts`를 추적하면 원천은 `core_content.situation_ko`다.
   - 시작 버튼은 `/learner/practice/:scenarioId`의 `CanonicalMissionRun`으로 연결된다.
     카드는 DCT 전용 미션을 뜻하지 않으며, 전체 MPJ5+DCT1 흐름을 안내하는 표현이 부족하다.
   - 캡처의 미션 1에는 상대가 교수님인지 학사 담당자·조교인지 혼재한다. 실제 콘텐츠 검토
     대상으로 남기며, 요약 문구로 모순을 숨기거나 이 세션에서 원본을 고치지 않았다.
2. **주차 학습 노트는 기존 예습·복습 템플릿이며 Package와 별도 경로다.**
   - `learnerNote.ts`가 주차 Can-do와 `targetFeatures.ts` 카탈로그로 노트를 조립한다.
   - `operational_definition`을 `definition`으로 전달하고 `WeeklyLearningNote.tsx`가 그대로
     출력하므로 내부 band 코드와 생성·재생성 판정 문구가 학생용 본문에 노출된다.
   - 현재 카탈로그를 읽기 때문에 고정된 옛 문서라고 단정할 수는 없다. 핵심 원리와 반례는
     활용 가능하나 학생용 설명·구체적인 상황 및 통번역 사례로 가공하는 단계가 부족하다고 평가했다.
   - 예습은 먼저 열리고 복습은 교수자 공개 또는 해당 주차 필수 미션 전체 완료로 열린다.
3. **사용자는 수업자료의 기준을 교과목·주차 계획으로 정정했다.**
   - 요청: Composer에서 만든 교과목의 주차별 수업자료를 구성해야 하며, 승인 미션을 원천으로
     수업 전체를 역으로 구성하는 방식은 의도와 다르다.
   - 현행 `AdminTeachingMaterials.tsx`는 승인된 `mission_content`를 조회하고
     `buildInstructorMissionGuide`로 자료를 구성한다. 교과목·주차 수업자료 생성기는 아니다.
   - `AdminComposer.tsx`의 개별 수업자료 링크는 `/admin/package?mission=...` 형태다.
   - 원하는 관계: 교과목·주차 계획 → 주차별 수업자료 → 교수자 자료·교실 화면 / 학생용
     예습·복습 노트. 편성된 미션 1·2는 해당 수업의 실습 활동으로 연결한다.
   - 학습 노트의 구체적인 연결 방식은 진단에서 제안한 방향이다. 상세 UI·저장 구조·생성 방식의
     구현 승인을 받은 것으로 확대 해석하지 않는다. 안내 문구만 바꾸는 것으로 해결하지 않는다.

## 확인한 증거와 한계

- 운영 `/admin/package`와 `/admin/composer`를 브라우저에서 읽기 전용으로 확인했다.
  Package의 승인 미션 선택 구조와 Composer의 교과목·주차 편성 구조를 확인했다.
- 학습자 화면은 사용자가 제공한 다음 캡처와 소스를 대조했다. 미션 실행·학습 기록 저장은 하지 않았다.
  - `C:\Users\cnkr\OneDrive\사진\스크린샷\스크린샷 2026-08-27 152152.png`
  - 같은 폴더의 `스크린샷 2026-08-27 152227.png`, `152235.png`, `152255.png`
- 이전 기록 `2026-08-25-instructor-teaching-materials.md`와
  `2026-08-26-classroom-output-integration.md`에도 승인 미션 기반 자료 구성이 명시돼 있다.
- 테스트·typecheck·build는 재실행하지 않았다. 기존 CI의 537 pass·9 skip 및 typecheck·build 성공은
  직전 인계에서 전달된 결과이며, 이번 진단의 신규 검증 결과가 아니다.
- 앱 코드·DB·학습 콘텐츠·프롬프트·운영 배포는 변경하지 않았다. 이 메모만 작성했다.

## 복귀 후 순서와 보류 사항

1. 병합본을 포함하는 작업 기준부터 확보한다. 기존 worktree·로컬 main을 임의로 정리하지 않는다.
2. 사용자와 위 진단을 이어가고, 수업자료 입력 기준 및 학습 노트 연결의 최소 수정 범위를 정한다.
   기존 교과목 선택 UI를 되돌리거나 별도 엔진·새 기능을 불필요하게 만들지 않는다.
3. 문헌 정리 전 학습 콘텐츠를 미리 생성하지 않는다. 한 미션은 MPJ5+DCT1이며 같은 화행의
   서로 다른 상황 미션 2개를 사용한다. A/B 실험쌍·효과·전이 측정 구조를 새로 만들지 않는다.
4. 적정 2·조정 필요 2와 BEST/MIDDLE/WORST의 선택은 계속 미결정이다. 이번 수업자료 피드백을
   판정 체계 변경 승인으로 해석하지 않으며, 변경 시에도 기존 학습 기록을 보존한다.

## 후속 협의 · 학생 유인물 1개와 교수자 전용 메모

- 아래는 최초 진단 이후 사용자가 구체화한 출력 방향이며, 위 예습·복습 분리 제안보다 우선한다.
  사용자는 이 구조의 구현 가능성을 확인해 달라고 했다. 앱 구현은 아직 시작하지 않았다.
- 교과목·주차 계획에 기반한 공통 학습 내용을 한 곳에서 관리한다. 학생용은 주차별 강의 유인물
  1개이며 별도 예습·복습 자료를 생성하지 않는다. 개인 미션 수행 기록은 기존 웹앱에 남긴다.
- 프로젝터 화면과 HTML은 수업 중 함께 보는 공통 내용을 사용한다. HTML은 해당 화면의
  오프라인 사본이며, 세 번째 내용을 따로 생성하는 것이 아니다.
- 교수자 전용 메모(치트시트)는 수업 지도 메모와 미션별 핵심 해설을 보여주는 관리자용 별도 보기다.
  미션 해설은 배정된 미션의 기존 MPJ·DCT 자료를 재사용하며 정답·판정 기준을 새로 만들지 않는다.
- 학생 유인물과 프로젝터용 HTML에는 교수자 전용 메모를 포함하지 않는다. 공통 수업 설명과
  공개할 사례 해설까지 모두 지우는 것이 아니라, 교수자 전용 정보와 분리하는 뜻이다.
- 별도 생성기·교안 편집기는 추가하지 않고 기존 화면·HTML 내보내기·인쇄 기능을 우선 재사용한다.
- 추가 소스 확인: `InstructorMissionGuide.tsx`에는 이미 audience 및 document/projector 구분이
  있고, `WeeklyLearningNote.tsx`에는 브라우저 인쇄 기능이 있다. 다만 모델 자체는 여전히 미션 중심이다.
- 주의: 현재 `instructorGuideHtml.ts`는 해설을 HTML 안에 넣고 CSS로 숨기며 인쇄 시 표시한다.
  기존 문서에도 교수자용 파일이라고 명시돼 있다. 새 프로젝터용 HTML에서 교수자 전용 정보를
  제외하려면 화면에서 숨기는 것만으로는 부족하고, 출력할 데이터·HTML 구성에서 실제로 빼야 한다.
- 확인한 `curriculum_weeks` 타입에는 주차 목표·편성 정보가 있으나 수업자료 본문·교수자 메모의
  저장 필드는 없다. 기존 기능을 재사용할 수 있다는 것과 공통 주차 자료 원본이 이미 존재한다는
  것은 다르다. 원본 보관 방식은 최소 구현 설계에서 정하며, DB 변경 불필요를 미리 확약하지 않는다.
- 검증: 관련 소스·타입·기존 HTML 개발 기록을 읽어 재사용 지점과 분리 필요를 확인했다.
  테스트·build·자료 생성·운영 변경은 하지 않았다. 이 후속 협의만 기존 인계 메모에 추가했다.

## 최초 진단 종료 시 기록과 논문 영향

- 갱신 dev-log: 이 파일.
- 수업자료 입력 기준에 대한 사용자 정정은 연구적으로 중요한 후속 기록 대상이다. 다만 현재
  체크아웃의 research-trail은 오래된 기준이므로 이번에는 덮어쓰지 않았다. 올바른 작업 기준에서
  관련 기존 결정(승인 미션 기반 수업자료)과 연결하여 사용자 요청·미구현 상태를 기록해야 한다.
- [논문 영향 3줄]
  1. 바뀐 수치: 없음.
  2. 바뀐 화면: 없음.
  3. 바뀐 프롬프트·계약: 없음. 동결본 재발행은 하지 않았다.

## 후속 구현 · 사용자 승인 및 작업 중 정정 반영

### 실제 기준과 범위

- 현재 작업공간은 같은 `664e` worktree이며, 원격 main fetch와 조상 포함 확인 후 PR #25 병합본
  `871802f0f21369a1930f8aecdca47d09a02d4847`에서 `codex/weekly-course-materials-2026-08-27`을 만들었다.
  기존 `mpj5-mainline-2026-08-24` worktree와 오래된 로컬 main은 변경하지 않았다.
- CANONICAL의 현행 세 경로와 논문 저장소 정본목록의 경로 일치를 확인했다.
- 사용자 승인: 공통 학습 내용 재사용, 프로젝터·HTML·학생 유인물, 관리자 전용 치트시트 분리.
  예습·복습 두 자료, 별도 생성기·교안 편집기는 추가하지 않는다.
- 작업 중 사용자 정정: **주차 자료는 미션 편성 후 정해진다.** 페이지를 먼저 열어 두는 것과
  수업자료가 완성된 것은 다르다. 따라서 `주차 계획 → 미션 편성 → 해당 미션에 맞춘 자료`를 적용했다.
  최초 구현의 “미션 편성과 별도로 자료 확인” 안내를 철회하고 계획 미리보기와 편성 반영을 구별했다.

### 변경

- `weeklyMaterials.ts`와 `WeeklyMaterialDocument.tsx`에서 공통 본문을 한 번 구성·렌더링한다.
  주차 목표·맥락·기존 원리/표현 자원에 현재 편성의 기본 상황·원문을 반영한다. 원문은 코어의
  기본 원문이며 DCT의 참고 답안으로 표시하지 않는다. 미션 교체 시 해당 본문도 바뀐다.
- 미편성·부분 편성은 계획 미리보기로 표시한다. 편성 정보가 있다는 이유만으로 상세 교안 작성이나
  교수자 검토가 완료됐다고 간주하지 않는다. OT·평가·메타화용 주차도 계획 안내임을 명시한다.
- `/admin/package`는 교과목·주차를 선택하며 Composer의 각 주차 링크로 진입한다. 승인 미션 단독
  선택/시간 프리셋 중심 화면을 대체했다. 기존 `?mission=` 주소는 주차 선택 안내로 처리한다.
- 교수자 메모는 현재 주차의 공개 가능한 미션만 조회해 기존 MPJ·DCT 해설을 재사용한다.
  프로젝터 전환 시 전용 메모를 닫으며, 종료해도 메모를 자동으로 다시 노출하지 않는다.
- 프로젝터·학생 유인물·새 공용 HTML은 허용한 공개 필드만 렌더링한다. 교수자 전용 메모를
  CSS로 숨긴 사본을 배포하지 않는다. 예전 teacher-only HTML 함수는 호환용으로 남되 새 화면에서 호출하지 않는다.
- 기존 `/note`는 한 개의 강의 유인물로 연결한다. 완료 상태 조회·예습/복습 해금과 편집기의
  복습 공개 스위치를 제거하되 `review_released` 저장값과 개인 수행 기록은 보존한다.
- 주차 카드의 상황문은 첫 문장 요약·두 줄 표시로 줄이고 MPJ5+DCT1 구성임을 명시한다.
  교과목 선택 화면의 디자인과 미션 실행·평가·로그 저장은 변경하지 않는다.
- 새 학습 콘텐츠·원문·답안·판정 기준은 생성하지 않았다. DB·migration·인증·프롬프트 변경도 없다.

### 검증과 한계

- 표적 6파일 **27 tests 통과**, typecheck 통과. 새 테스트는 공개 필드 제한·HTML 비밀정보 제외,
  미편성 미리보기·편성 원문 교체 반영, 주차 전환 시 메모 초기화, 프로젝터 종료 시 메모 미노출,
  과거 `review_released` 값과 무관한 단일 유인물·인쇄 연결을 확인한다.
- 최종 `vite build`와 `git diff --check` 통과. 프롬프트 스냅숏을 재생성하는 prebuild는 실행하지 않았다.
  빌드는 큰 청크 경고를 남겼고, diff check는 Windows 줄바꿈 안내만 출력했다.
- 초기 추가 테스트의 잘못된 화행 enum(`invitation` 대신 현행 `agreement`)과 사용자 정정 전
  안내문 기대값을 수정했다. 기존 전체 CI 537 pass·9 skip을 이번 변경의 결과로 재사용하지 않았다.
- Windows 격리 환경에서 esbuild 실행 제한이 있어 허용된 실행으로 테스트·Vite를 확인했다.
  로컬 Node는 24.18.0이며 프로젝트 권장/CI Node 22와 다르다.
- 로컬 브라우저 `/admin/package`의 화면과 빈 목록 안내를 확인했다. 해당 로컬 세션에서 조회 가능한
  교과목이 없어 **실제 인증 계정의 Composer→자료→학습자 종단은 미확인**이다. 권한을 우회하지 않았다.
- `.tmp/weekly-material-preview.html`은 “표시 검증용·실제 교과목 자료 아님”을 명시한 임시 fixture다.
  브라우저에서 기존 초대 원리의 공용 HTML 표시와 다음 페이지 이동을 확인했다. 실수업 자료 생성이나
  운영 검증을 대신하는 증거로 쓰지 않는다.
- 상세 강의 내용·사례 보강은 문헌 검토 뒤 별도 작업이다. 원본 상황문의 교수님/조교 역할 혼재,
  BEST/MIDDLE/WORST와 적정2·조정필요2의 선택은 이 작업에서 고치거나 확정하지 않았다.
- push·PR·merge·Railway 배포·운영 DB 쓰기는 하지 않았다. 최신 소스 기준의 상세 교안 완비나
  운영 배포 성공을 주장하지 않는다.

### 기록

- 학습자·관리자 구조 정본을 실제 출력 구조와 맞추고 `DEC-20260827-04`, `ITER-20260827-04`에
  사용자 정정과 채택/기각 대안을 기록한다. 생성계약·프롬프트 동결본은 재발행하지 않는다.

## 후속 운영 배포 · 2026-08-27

- 사용자 승인: `배포까지 해`.
- 배포 소스: clean 커밋 `5c8dda3c955866655ec5f5def18eb4494a854438`. 기능 브랜치를 원격에 보존하고
  `git archive`로 추출한 추적 파일만 업로드했다. `.env`, 로컬 빌드·임시 자료는 배포 소스에서 제외했다.
- 대상: 기존 Railway `PRAGMA / production`, 서비스 `3973a97b-5823-463b-a866-e4493cb84256`.
- 배포 `6d68574d-4c1b-45f8-831e-9da0de4b250d`: 빌드 후 **SUCCESS** 확인.
  image digest `sha256:e7717b9b9e8666cc2218f5921b236c48e240de2b787a559f21c7fd6252b91317`.
  도메인 `pragma.up.railway.app`과 기존 GitHub 저장소 연결은 유지됐다.
- 이전 배포 `ca8da300-e517-45bb-8b51-060805f4ae76`는 PR #25 병합본의 SUCCESS였고,
  새 배포 전환 뒤 REMOVED로 바뀌었다. 배포 상태를 GitHub CI 성공으로 대신 판정하지 않았다.
- 원격 main은 `871802f` 그대로이며 이번 작업을 병합하지 않았다. 기존 main 자동 배포 연결이
  유지되므로 다음 main 배포 전에 이 기능 브랜치를 병합해야 변경이 빠지지 않는다.

### 운영 확인(기존 인증 관리자 세션·읽기 전용)

- Composer의 실제 대표 교과목 `915fec24-cc38-4b00-a2a0-c3628abcd3f7`에서 15개 주차 자료 링크와
  각 `courseId / weekNo`를 확인했다.
- Package에서 해당 교과목 5주차를 선택해 **편성 미션 2개 반영**과 두 기본 상황·원문을 확인했다.
  미션 ID는 `6e05b84b-81b6-4c89-993e-8263dd87966a`, `622fad8c-f902-41d8-b0ce-298e383dde6f`다.
- 교수자 메모에서 미션별 해설이 로드되고 MPJ 1~5·DCT 항목이 표시되는 것을 확인했다.
- 프로젝터 첫 페이지와 다음 페이지 이동을 확인했다. 열기·닫기 모두 교수자 메모 DOM이 없었다.
- 동일 계정으로 학습자 5주차 유인물 경로를 열어 공통 본문이 Package와 정확히 일치함을 확인했다.
  인쇄 버튼·주차 복귀 링크와 교수자 오개념·지도 핵심 미포함을 확인했다. 별도 learner 역할 계정의
  권한 검증이나 실제 인쇄·다운로드 파일 재검사는 하지 않았다.
- 주차 복귀 후 짧은 미션 카드·MPJ5+DCT1 안내·기존 2/2 완료 표시를 확인했다.
  13주차의 미편성 상태는 `계획 미리보기 · 미션 0/2개 편성`으로 표시됐다.
- 새 콘텐츠 생성, 미션 수행·로그 저장, 운영 DB 쓰기, migration·프롬프트 수정은 하지 않았다.
  상세 강의 내용 보강·원본 역할 혼재와 판정 체계 미결정은 그대로 남는다.
- 이전 27 tests·typecheck·로컬 build를 배포 때문에 반복하지 않았다. Railway production build는
  통과했으며 CLI의 Config as Code 전환 경고는 후속 개선으로 남겼다. 이번에는 설정을 바꾸지 않았다.

### 배포 기록과 논문 영향

- `DEC-20260827-04`·`ITER-20260827-04`의 반영 상태와 `EVD-20260827-04`를 갱신한다.
- [논문 영향 3줄]
  1. 바뀐 수치: 테스트 수 추가 변경 없음. Railway 배포 SUCCESS.
  2. 바뀐 화면: 주차 수업자료·프로젝터·학생 유인물·교수자 메모·주차 카드가 운영에 반영됨.
  3. 바뀐 프롬프트·계약: 추가 변경 없음. 동결본 재발행 없음.
