# 2026-08-17 · MPJ4 + FixReview + 독립 DCT1 최종 통합

## 수행한 변경

- 과거 `fca7734`의 `mission_v4` 구현과 현행 MPJ5를 대조했다.
  - 과거 구현에는 4문항 골격과 후보 전수판정이 있었다.
  - 현행 분기에는 Scale4·Judge3·FixChoice·ReasonConf·5후보 BEST2/WORST1이 있었다.
- 신규 생성 계약을 `mission_v6`로 추가했다.
  - 처음에는 `mission_v5`를 고려했으나, `d84a520`과 `20260730120000_core_v3_mission_v5_minidiscourse.sql`에서 이미 미니 담화 DCT 의미로 사용된 사실을 확인해 재사용하지 않았다.
  - 과거 `mission_v1`~`mission_v5`는 읽기 호환으로 보존하고 신규 생성만 `mission_v6_fix_review_mpj4_dct1_v2`를 사용한다.
- 학습 흐름을 Scale4 → FixChoice → FixReview → MultiJudge → 독립 DCT → 3층 피드백 → 실질 수정 → 1회 재확인으로 개편했다.
- FixReview를 탈락 교정본 선택 후 핵심 실패 원인을 고르는 2단계로 구현했다.
- 사용자 실걸음 피드백에 따라 FixReview의 실패 원인은 4개에서 3개로 줄였다.
- MultiJudge는 사용자 실걸음 피드백에 따라 후보 4개에서 과소·적정·과잉 각 1개, 총 3개로 줄이고 BEST/WORST 계약을 제거했다. 복수의 적절 전략 비교는 FixChoice에만 남겼다.
- 모든 현행 MPJ에 `reference_non_scored` 프레임을 내부 계약·저장 trace로 필수화했다. 학습자 화면에서는 점수·trace·참고 대역 같은 연구 용어를 숨기고 상황과 행동을 직접 묻는다.
- Fable 마감 의견을 반영했다.
  - Scale4 1 + FixChoice 3 + FixReview 2 + MultiJudge 3 = 판단 클릭 9회를 명시하고 문항별·전체 체류시간을 이벤트에 저장한다.
  - MPJ3 탈락본이 MPJ2 미수리·과잉수리 실패 유형을 반복하면 `R30` fail로 거부한다.
  - 의미 재확인은 기존 의미 목표 미반영과 수정 중 새 의미 훼손을 구분한다.
- DCT 직전 맥락 화면은 관계·행위 부담 확인 정보만 보여 주며 별도 응답·채점·완료 조건을 제거했다.
- 번역 내용 어휘 힌트 0~2개, 통역 힌트 없음·재생 최대 2회를 계약과 UI에 맞췄다.
- 최초 산출·피드백·수정/유지 결정·재확인 snapshot·추가 수정 사용 여부를 `mission_response_v2`에 저장한다.
- 수정 선택 문구를 `피드백을 반영해 문장 고치기`와 `고치지 않고 그대로 제출하기`로 바꾸고, 유지 시에는 이유 입력과 `지금 문장 그대로 제출하기`를 직접 안내한다.
- `revision_rechecked` 이벤트와 `mission_v6` 허용을 위한 로컬 migration을 추가했다. 원격에는 적용하지 않았다.
- 날짜 없는 기존 학습자 구조·생성계약 정본에 현행 `mission_v6` 계약을 통합하고 관련 연구 기록을 갱신했다.
- 최신 `main`(`9daa62c`) 기반 격리 worktree에서 통합해, 6일 뒤처진 dirty 루트 작업트리와 기존 사용자 변경을 건드리지 않았다.

## 생성·검수 게이트

- 문항 4개와 유형 순서를 스키마와 R1에서 고정했다.
- 의미·의도 보존, 자연성, 표면/길이 단서 부재, 단일 역할 명확성의 `generation_checks`를 필수화했다.
- FixChoice의 2 valid·2 invalid와 서로 다른 전략, FixReview의 pass 2·reject 1·상황 결부 실패 원인 정확히 3개, MultiJudge의 1·1·1 역할 분포를 검사한다.
- MPJ2·3·DCT의 기준 P·D·R 동일성, MPJ3와 DCT의 사건 분리, MPJ4의 단일 P·D·R 축 대비를 검사한다.
- 역할이 길이 최단·최장만으로 드러나는 후보를 fail 처리한다.
- 서버 반환 전 corrections와 candidates 배열을 섞는다.

## 검증

- `npm.cmd run typecheck`: PASS.
- `npm.cmd test`: 71파일 PASS·3파일 skip, 420개 PASS·9개 skip(총 429개).
- 프롬프트 스냅샷: PASS, 19종 재생성. `core_surface_hash=4eb32e4fffae…`, pack hash `18cce236df6f…`.
- `npm.cmd run build`: PASS, 1,941 modules. 기존 Browserslist 노후 및 CSS `-: T` 경고는 남았지만 빌드 실패는 없었다.
- 관련 파일 `git diff --check`: PASS. Windows CRLF 변환 경고만 확인했다.
- 최신 `main` 통합본을 `http://127.0.0.1:8099/learner/practice?preview=v6`에서 실제 클릭했다.
  - MPJ1은 `이 상황에서 이 표현은 얼마나 적절한가요?`만 표시되고 점수·trace 고지가 없음을 확인했다.
  - MPJ2는 괄호 설명 없이 `너무 직접적`·`알맞음`·`지나치게 우회적`만 보여 주고, 최초 판단 뒤 `상황에 알맞게 고친 문장 2개를 고르세요`로 수정안을 공개한다.
  - MPJ3은 `처음 쓴 문장`과 `고친 문장 3개 중, 이 상황에 여전히 어울리지 않는 문장을 고르세요`만 남기고 중복 설명을 제거했다.
  - MPJ4는 괄호 설명 없는 세 대역으로 후보 3개를 하나씩 분류하게 한다.
  - 수정 단계에서 두 직접 행동 문구와 유지 이유 입력, `지금 문장 그대로 제출하기`를 확인했다.
- Fable이 최종판을 별도로 read-only 재검토해 typecheck·v6 표적 19개와 MPJ2~4 실제 gate를 재현하고 승인 유지 의견을 남겼다. 보고서의 저녁 추록은 초기 구 worktree·4후보 기술을 대체한다.

## 기존 데이터 사용 현황

- 로컬 migration과 Git 이력에서 `mission_v1`~`mission_v5`의 역사적 의미와 허용 계약을 확인했다.
- 연결된 Supabase를 공개 키로 읽어 버전별 행 수를 확인하려 했으나 `anon`에 `scenarios` SELECT 권한이 없어 `42501`로 차단됐다.
- 차단 결과를 0건으로 해석하지 않았다. 과거 행 변환·삭제·백필은 수행하지 않았다.

## 관련 연구 기록

- `TRC-20260817-02`
- `DEC-20260817-04~05`
- `ITER-20260817-01~02`
- `EVD-20260817-01~03`

## 확인 필요

- 인증된 관리자 경로에서 원격 `mission_content.schema_version`·`mission_status`별 실제 건수를 확인해야 한다.
- 판단 클릭 9회의 실제 소요시간·체감 부담을 학습자 수행으로 확인해야 한다.
- 반복 미션에서 1·1·1 고정 분포를 눈치채고 `대역마다 하나`라는 소거법으로 푸는지 오선택 이유와 함께 관찰해야 한다.
- Glaser 근거의 정확한 서지·페이지를 논문 정본과 연결해야 한다.
- `mission_v6` 생성 샘플의 중국어 자연성·실패 유형·P·D·R 대비는 인간 검수가 필요하다.
- 로컬 migration의 원격 적용과 신규 생성 smoke는 별도 승인 후 수행해야 한다.
- 관련 커밋: 없음. 커밋·push를 수행하지 않았다.

## 후속 보완 · 판단 결과와 수리 경계의 회고 가능성

- V6 handoff를 일반 표현 도구 목록에서 실제 MPJ 네 응답 기반 요약으로 교체했다. FixReview는 남은 문제와 수정 중 새 문제 검수, MultiJudge는 학습자용 세 방향 분류로 정리한다.
- DCT 피드백의 기존 필드를 `살펴볼 문제 → 상대에게 들릴 수 있는 인상 → 표현을 바꿀 때 지킬 의미` 순서로 배치했다. 새 스키마·점수·응답은 추가하지 않았다.
- 완료 화면에 기존 피드백·재확인·이견으로 만든 학습 흔적을 추가했다. 추가 수정 뒤 재확인을 다시 하지 않은 경우에는 최종 문장을 확인한 것처럼 표시하지 않는다.
- 개발 미리보기의 MPJ2~4 해설은 상대가 받을 수 있는 인상과 보존된 핵심 뜻을 더 직접적으로 말하도록 다듬었다.
- MPJ4는 후보 표시 순서를 계속 무작위화하지만 최종 분류만 저장한다. 선택 순서·변경·소거법은 현행 자료로 추론하지 않고, 세 분류를 독립 문항 3개로 해석하지 않는다.
- 2.4 근거감사에서 확인된 정본 공백을 보완해, 번역과 통역 산출을 매체·처리 조건이 다른 자료로 구분하고 하나의 점수로 합산하지 않는다는 기존 DEC-20260817-04 원칙을 학습자구조 §7.3에 명문화했다. 기능·스키마 변화는 없다.
- 검증: 전체 Vitest 72파일 424개 통과·9개 skip, typecheck 통과, 변경 파일 ESLint 오류 0건(기존 hook 경고 1건), production build 1,942 modules, localhost 전체 흐름 실걸음 통과.
- 원격·DB·migration·프롬프트·content release·배포는 변경하지 않았다.
- 관련 기록: `DEC-20260817-06`, `ITER-20260817-03`, `TRC-20260817-02`.
