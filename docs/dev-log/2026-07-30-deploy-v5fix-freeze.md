# 2026-07-30 — 배포 사슬 · v5 규칙 수정 · 표본 검수 · IA 재편 · 코어 soft freeze

담당: Claude Code(개발) / Codex(검수) — 이날 역할 반전 확정.

## 사실 기록

- 원격 migration 2건 적용(`20260729090000`, `20260730120000` — 둘 다 미적용 상태였음),
  Edge `generate-scenario` 배포. 배포본 직접 호출로 `scenario_core_v3`·focal_segments·
  코어 지문 `dc8f1494…0eb8d334`(저장소 스냅샷 일치) 실증.
- `dba866b`: missionRules의 버전 분기가 v4만 확인해 **mission_v5가 legacy(V2) 기준으로
  검사되던 결함** 수정(R1·R7·R3·R5·R8·R27을 v5에 적용). 이 결함 상태에서는 v5 승격이
  규칙검사를 통과할 수 없었다. `MissionRunV1.sequentialFix` v5 누락도 복원.
  놓친 원인 = 기존 테스트가 checkMission 결과에서 R29만 필터. 회귀 테스트 추가.
- `f916b44`+`b119534`: 9화행 표본 하네스(RUN_V5_SAMPLES·RUN_V5_RECHECK·RUN_V5_SUPPLEMENT).
  셀은 buildBatchPlan에서 추출(본 배치와 동일 규칙). 표본 결과(11건) = 코어 비-fail 9/11,
  미션 비-fail 8/11. 잔여 쟁점 = R5 길이 단서(6/11 지적, 완전 분리 1) — **미션 승격 단계
  사안으로 코어 동결과 분리**(corePromptSnapshotHash가 코어 표면만 해시함을 Codex가
  코드로 확인). 고P 셀(상대가 위) 보충 표본 2건 포함 — 기본 표본이 전부 첫 구인 셀
  (대등·지인·중부담)이라 고P 편향(계약 0-t)은 기본 9건으로 검수 불가했음.
- `4e22e43`: Codex 검수 반영 — v5 learner 로그 legacy confidence 정리, promoteMission
  429/502/503 백오프(같은 모델 유지 — 엣지 fallback 모델 전환은 배치 내 모델 혼합을
  낳아 기각), 하네스 fail 코어 승격 생략.
- `a04fd71`~`7fb5959`: admin IA 재편(A1) — 레거시 3화면 폐기, /admin/library(조회 전용),
  /admin/authentic(원자료 분석), **/admin/assembly 신설**(코어→미션 변환 작업대,
  배타 4상태 계기판·계열 혼합 경고). 개념 확정: 코어=미션 재료, 미션=학습 콘텐츠.
- 스코프 확정(사용자): A2(일괄 조립)·B1(후보 개수 강제) 폐기. 유지 = B2(R5)·C(라운지 8월).

## 코어 soft freeze 확정 (2026-07-30 밤, 사용자 승인)

- 동결 대상 = 코어 생성 표면, 지문 `dc8f149400de634a0e9e30f70c8b7e62d3c84999c044cf30fe1429100eb8d334`.
- 근거 = 표본 코어 눈검사 통과 + 배포본 지문 일치 + 코어/미션 게이트 분리(7/26 A+C안 구조).
- 495 본 배치는 dev 서버(HMR)가 아니라 **8097 고정 빌드(vite preview)**에서 실행한다 —
  배치 중 소스 편집이 HMR로 배치 탭을 리로드시키는 위험 차단(Codex 지적).
- 알려진 재생성 대상: 초대 셀(work_activity_invitation × tourism_hospitality)의 R26
  반복 실패 — 배치에서 fail은 저장되지 않고 같은 runId 재실행으로 메꾼다.

## 495 본 배치 실행 결과 (2026-07-31)

- **정본 배치 ID = `core_ko_zh_1785458303114`, 495/495 저장 완료.**
  계획과 저장분이 1:1로 일치하므로, 실행 전 계획 게이트에서 확인한 243 구인셀
  (화행9×P3×D3×R3) 전수 커버와 54 전달셀(화행9×수준3×모드2) 셀당 ≥3이 저장 데이터에
  그대로 성립한다.
- **잉여 배치 1건**: 그 앞서 다른 run ID로 486건이 저장돼 있다. 화면이 배치 ID는
  localStorage에 저장하면서 쿼터는 저장하지 않아, 새로고침 후 프리셋을 다시 누르면
  새 ID로 전량 재생성되는 구조가 원인이다. 두 배치 모두 같은 동결 프롬프트
  (`dc8f1494…`)라 계열은 동일하다. **삭제하지 않고 run ID로 격리한다** — 연구 기록에는
  정본 run ID만 기재한다.
- 이 배치가 규칙 오탐 5건과 산업 증거 공백 2건을 드러냈다(표본 11건으로는 미검출).
  전부 수정·회귀 테스트 고정, 전체 테스트 167 → 176.
  - R16 부정 표현: `남기지는 않습니다`(보조사) · `아닙니다`(활용형에 "아니" 없음) ·
    `남지 않으며`(어간 축약) · `남기진 않지만`(지+ㄴ 축약)
  - R16 `직접`이 발화 아닌 동사를 수식(`직접 수행할 … 요청하며`)
  - R26 엔터테인먼트·미디어 증거가 영상 제작 용어에 치우쳐 기획 어휘(드라마·서사·
    예능·시놉시스·출연자) 누락
- 조회 상한 결함도 함께 수정: 코어가 1000을 넘자 라이브러리·조립 큐가 최신 1000건만
  보여 주면서 전체인 것처럼 표시했다(상한 4000 + 도달 시 화면 고지).

## B2 — multi_judge 길이 단서 보강과 실측 (2026-07-31)

프롬프트 보강(`104b18a`, 엣지 배포 완료, `prompt_version` → `mission_v5_mpj4_minidiscourse_v2`):
대역과 길이가 별개 축임을 명시하고 탈상관 조합을 직접 요구했다 — ①과소 후보 중 1개는
말수 많되 알맹이 없게 ②적정 후보 중 1개는 짧지만 알차게 ③과잉은 문장 추가가 아니라
강도 표지로 ④작성 후 글자 수 자가 점검. 코어 동결 무관(`core_surface_hash` 불변).

9화행 표본 재생성으로 전/후를 측정했다(기준선 백업 = scratchpad `v5-samples-baseline-preB2.json`).

| 지표 | 전 | 후 |
|---|---|---|
| 미션 생성 성공 | 7/9 | **9/9** |
| R5 fail(적정·부적절 길이 완전 분리) | 불만 fail | **불만 통과**, 칭찬만 fail |
| 길이 비율 | 사과 2.4·반대 2.9·제안 1.8 | **1.7·1.9·1.4** |
| "과잉안이 유일한 최장문" 경고 | 4건 | **7건(증가)** |
| 극단 비율 | 감사 6.7 | 감사 6.7·거절 6.3 |

**판정**: 탈상관 지시는 치명 신호(완전 분리)를 억제하는 방향으로 작동한다. 반면
"과잉 = 최장" 경향은 프롬프트로 제거되지 않는다 — 모델 불복종이 아니라 구인과 표층의
생태적 상관이다(과잉 대역은 정의상 표현 요소가 더 얹히고, 강도 표지 자체가 글자를 늘린다).
학습 과제가 BEST 1 + WORST 1 선택이므로, **적정안 구간이 부적절안과 겹치는 한**
"가장 긴 것 = 과잉"은 과잉 1개 식별에 그치고 정답으로 직결되지 않는다.

**판정 종결(2026-07-31, Codex 교차검증 후 사용자 승인)**: **A안 보완 수용** — 규칙 코드
변경 0건, 계약 **§6.4** 신설(§8은 provenance 절이라 위치를 정정). 확정 내용은
`DEC-20260731-01` 참조.

교차검증에서 **초안 논거 하나가 코드로 반증돼 철회**했다. "적정 구간이 부적절 구간과
겹치므로 변별은 훼손되지 않는다"고 적었으나, `mpjSummary`의 방향 일치 판정은 BEST가
within이고 WORST가 non-within이기만 하면 인정한다 — 과소·과잉 어느 쪽도 유효 WORST이므로
"유일 최장 과잉안"을 길이만 보고 골라도 유효 응답이 된다. 함께 확인된 사실: 계약 v1의
"완전 단조 = 길이순 정답키" 서술은 실제 hard fail 조건(`separable` — 모든 non-within이
within 구간의 한쪽 밖에만 분포)보다 넓다. 과소–적정–과잉이 길이순으로 늘어서는 배열은
fail이 아니다. "구조적으로 불가피"도 표본 9건에는 과한 주장이라 "일부 축에서 지속 관찰된
상관"으로 완화했다.

B안(경고→fail 승격)은 감사 6.7·거절 6.3이 반복 관찰돼 강도류 화행 조립을 막으므로 기각.
C안(축별 규칙 분화)은 **기각이 아니라 유보** — 완전 분리가 전 축에서 억제된 것은 맞지만
화행별 1표본으로 영구 기각할 근거는 못 된다. 비상 대안으로 남긴다.

## 마감 후 백로그

- **커리큘럼 「게시」가 「고급 설정」 접기 안에 숨어 있다**(`CurriculumEditor.tsx:357`의 아코디언
  안 status Select). 2026-07-31 e2e에서 사용자가 편성 저장까지 마치고도 학습자 화면이 비어
  있는 이유를 찾지 못했다 — 편성 저장(주차 배정)과 게시(학습자 공개)가 다른 화면의 다른
  동작인데, 후자가 기본 접힘 상태라 존재를 알 수 없다. 게시를 표면으로 올리고, 편성 화면에서
  "이 강좌는 아직 게시되지 않았습니다" 상태를 보여 주는 편이 맞다. UI 변경이므로 브리프 후 착수.

- 배치 화면이 쿼터를 배치 ID와 함께 저장하지 않는다 — 재개하려던 사람이 프리셋을
  눌러 전량 재생성하게 되는 직접 원인. 저장 또는 재개 시 계획 복원이 필요하다.
- 통역 셀 상황문에 서면 목적("공식 기록으로 남기기 위한 것이다")이 섞여 들어오는
  패턴이 반복된다 — 규칙 오탐이 아니라 코어 프롬프트의 통역 셀 지침 문제로 보인다.
  B2(R5)와 함께 다룬다.

## v3 대역–근거 정합 표본과 검증② 계약 보완 (2026-07-31)

- `mission_v5_mpj4_minidiscourse_v3` 엣지 배포 후 직접 호출로 provenance를 확인했다.
  코어 표면 지문은 `dc8f149400de…`로 불변이다.
- 사용자가 성공 사례를 제외하고 제공한 조립 화면에서 확인된 신버전 잔여 문제:
  - 제안 `#852ac0e0`: fix_choice 오답에 `必须…`·강요 기능의 `立即…`가 남았다.
    v3 생성 지시가 명시적으로 금지한 극단형이므로 실제 생성 결함이다. 같은 건의 R29
    reference_alternatives 미완역 경고는 별도 잔여 문제다.
  - 요청: 한 건은 R4·R5로 저장 전 차단됐고, 같은 화면의 `#2174af38`은 검증②를
    통과했다. 결정론적 실패는 학습자 노출 위험이 아니라 조립 효율 문제다.
  - 사과 `#b15e3c48`: 검증②가 fix_choice의 `is_valid=false`를 "완전히 부적절한
    문장"으로 읽고, `note_ko`의 한국어 설명을 correction 표현처럼 인용했다.
    corrections의 불리언은 `true=within_band`, `false=non-within 경계 오답`이며
    false가 문법 오류·완전 무효를 뜻하지 않으므로 일부 finding은 점검기 오판이다.
  - 11:52 반대 R5 캡처는 v3 배포 전 localhost 표본이므로 신버전 효과 집계에서 제외한다.
  사용자가 실패 화면만 선별했으므로 이 캡처 묶음으로 통과율은 계산하지 않는다.
- 검증② 코드에서 추가 결함을 확인했다. 프롬프트 출력 enum에는
  `primary_reason_ambiguity`·`context_plan_mismatch`가 있으나 응답 정규화 allowlist에는
  빠져 있어 두 코드가 `internal_inconsistency`로 덮였다. 따라서 baseline 14건의
  `internal_inconsistency` 코드 중 일부는 원래 두 코드였을 수 있다. 과거 표본은 finding
  **코드 라벨이 아니라 note_ko 서술을 직접 읽어** 증거로 사용한다.
- 보완 및 배포:
  - 검증②에 fix_choice `is_valid`의 조작적 의미와 `note_ko` 오인 금지 규칙 추가
  - 누락된 finding code 2개를 allowlist에 추가
  - 결정론 규칙 범위 R1~R29로 동기화, 검증② provenance `quality_v1`→`quality_v2`
  - 프롬프트 스냅샷 재생성. 코어 표면 지문 불변 확인
- 검증: prompt snapshot 6/6, typecheck, 전체 176 pass(생성형 6 skip), `git diff --check`
  통과. 사용자 승인 후 엣지 재배포를 완료했고, DB 저장 없는 배포본 직접 호출에서
  `quality_check.prompt_version`과 `meta.prompt_version`이 모두 `quality_v2`
  (critic=`gpt-4.1`)임을 확인했다.
- 측정 한계: 다음 표본은 생성기 v3와 점검기 quality_v2가 함께 적용되므로, 단일 결함률
  전후 차이를 어느 한 수정의 효과로 귀속할 수 없다. `必须/立即` 잔존·실제 자원과 대역의
  모순은 **생성 문장 자체**, `is_valid=false`를 완전 무효로 해석하는 지적은 **점검기
  판정**으로 나누어 finding 서술을 인간이 직접 읽는다. 소수 표본 확인 전에는 해결로
  판정하지 않는다.
- quality_v2 배포 후 사용자가 제공한 실패 선별 표본에서 사과 `#20f7f45d`의
  `下次我会送您礼物`가 under_acknowledged로 붙은 실제 생성 오라벨(과도한 보상 약속이므로
  overextended), 칭찬 `#fc6a3686`의 `完美无缺/完美无瑕` 극단 오답과 reason 주원인
  불명확성이 확인됐다. 검증②가 이를 잡은 것은 안전망의 유효 증거다.
- 반면 칭찬 `#fc6a3686` finding이 correction에는 존재하지 않는
  `accepted_band_codes`를 언급해, quality_v2에서도 fix_choice 스키마 오독이 완전히
  사라지지는 않았다. 사과 표본에서는 quality_v1의 "false는 완전히 부적절해야 한다"는
  오독 문구가 재발하지 않았으나 표본 1건이므로 해결 판정은 하지 않는다.
- 운영 결정(`DEC-20260731-02`): v3+quality_v2를 유지하고 추가 프롬프트 수정은 보류한다.
  규칙검사 fail은 저장 전 차단되므로 재조립한다. AI 결함 미션은 이미 저장된 상태라 현
  화면에 재조립 경로가 없으므로 reviewed하지 않고 그대로 보류한 뒤 다른 코어로 이동한다.
  AI 통과는 ①노란 규칙 경고(R19·R29 우선, R5 warning만으로는 차단하지 않음) ②대역 정합
  ③극단 오답 ④reference 전체 번역을 인간이 확인한 뒤 reviewed 처리한다. 기존 baseline
  14건은 계속 reviewed 금지. 실패 화면만 선별돼 제공됐으므로 통과율·개선율은 산출하지 않는다.
