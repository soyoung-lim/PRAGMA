# 4.6 검증 스냅숏과 정본 495 통역 역할 감사 준비

- 날짜: 2026-08-06
- 범위: 읽기 전용 감사 SQL과 논문 검증결과표 초안

## 수행

- 정본 run `core_ko_zh_1785458303114`만 격리하는 읽기 전용 SQL을 추가했다.
- 495 계획값(번역 378·통역 117), 화행×수준별 쿼터, 모드–원문 양식, 방향과 지문을 대조하도록 했다.
- 통역 117건은 진단 힌트를 숨긴 블라인드 전수표와 정규식 선별 상세표를 분리했다.
- 정규식 탐지 건수를 오류 발생률로 쓰지 않고 사람 확정 분자와 실제 reviewed 분모를 사용하도록 해석 규율을 SQL에 명시했다.
- 2026-08-06 KST·코드 `bbf7a1f`·Edge v53 지문에 고정한 제4장 4.6 검증결과표 초안을 만들었다.

## 검증

- SQL은 CTE와 조회문으로만 구성했으며 데이터 변경문이 없음을 정적 확인했다.
- 정본 495 계획의 통역 수는 `FULL_BATCH_QUOTA_495`의 화행별 13건×9화행 = 117건과 대조했다.
- 문서의 테스트·카나리·Edge 수치는 `EVD-20260805-04`와 보존 JSON을 대조했다.
- `git diff --check`로 공백·패치 오류를 확인했다.

## 변경하지 않은 것

- DB 조회 실행, DB 쓰기, 콘텐츠 생성·삭제, 미션 조립, 상태 승격을 하지 않았다.
- Edge·Railway 배포와 프롬프트·규칙·스키마를 변경하지 않았다.
- 별도 진행 중인 C드라이브 논문 폴더·자동화 체계 점검 범위는 건드리지 않았다.

## 확인 필요

- 사용자가 Supabase SQL Editor에서 쿼리를 실행해야 정본 495의 실제 감사 수치가 생긴다.
- Fable A/B/C 내용 판정은 아직 기록되지 않았으므로 4.6.3 결과로 채우지 않았다.
