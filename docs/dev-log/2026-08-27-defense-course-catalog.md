# 디펜스용 3개 교과목 카탈로그 정렬

- 날짜: 2026-08-27
- 목표: 학습 콘텐츠를 미리 생성하지 않고, 디펜스에서 실제 작동시킬 세 교과목의 메타정보와 15주 운영 구조를 확정한다.

## 구현

- 학습자에게 다음 세 교과목만 표준 디펜스 강좌로 노출한다.
  - `AI 기반 한중 화용 통번역 실습`: 중급·한→중·번역 6주+통역 6주
  - `AI 기반 한중 비즈니스 커뮤니케이션`: 고급·한→중·번역 10주+통역 2주
  - `AI 기반 중한 실전 번역`: 중급·중→한·번역 12주
- 각 과목에 확정된 도메인·주제 메타정보를 연결하고 관리자 빠른 프리셋에도 같은 수준·방향·주제를 적용한다.
- 수행모드 분모를 화행 9주가 아니라 OT·중간·기말을 제외한 실제 학습 12주로 정정한다.
- 세 과목의 공통 15주 구조를 `1주 OT → 2~6주 화행 → 7주 중간 메타화용 → 8주 중간 점검 → 9~12주 화행 → 13주 고부담 맥락 → 14주 종합 메타화용 → 15주 기말 점검`으로 고정한다.
- 학습자 교과목 카드에 수준·방향·도메인·주제·번역/통역 주차 수를 표시한다.
- 주차 미션은 실험쌍이 아닌 같은 화행의 독립 완전 미션 `1·2`로 표시한다.
- 운영 DB 적용용 migration을 추가하되 새 미션·수업자료·주차 배정은 생성하지 않는다.

## 범위

- 기존 공통 생성기·편성·미션 수행·수업자료 경로를 재사용한다.
- 제외한 여행·생활 강좌 때문에 공용 도메인·주제 데이터를 삭제하지 않는다.
- 신규 두 교과목의 학습 콘텐츠는 문헌 정리와 후속 검수 전까지 만들지 않는다.
- 수강등록·새 점수·분석·과목별 엔진·실험 A/B 메타데이터를 추가하지 않는다.

## 검증

- `courseModePolicy.test.ts`, `composerPlanning.test.ts`, `learnerCourse.test.ts`: 3파일 24 tests 통과.
- 아래 안전 보완 후 변경한 `learnerCourse.test.ts`만 다시 실행: 7 tests 통과. 최초 sandbox 실행은 esbuild 접근 오류로 테스트 시작 전에 실패했고, 권한 승인 후 통과했다.
- `npm.cmd run typecheck`: 통과.
- `git diff --check`: 통과(CRLF 변환 안내만 발생).
- 전체 회귀·브라우저·로컬 production build는 반복하지 않았다. 운영 build·healthcheck는 아래 Railway 배포에서 통과했다.

## 운영 반영 전 안전 보완

- 기본 구현 커밋은 `dbdfebc`, 데이터 보존 보완 커밋은 `94c8e06`이다.
- 운영 DB를 읽기 전용으로 확인했다: 교과목 3개, 주차 45개, 미션 배정 23개, 시나리오 1,403개, 학습 로그 63개. 신규 두 교과목 ID는 기존 데이터와 충돌하지 않는다.
- 교과목·주차·배정 원본과 시나리오·학습 로그의 개수/다이제스트를 로컬 `.tmp/defense-catalog-before-20260827.json`에 보관했다. Git 제외 파일이며 전체 콘텐츠·학습 답안 백업은 아니다.
- migration dry-run은 `20260827093000_seed_defense_course_catalog.sql` 한 건만 표시했다.
- 기존 대표 과목의 9~12주차 배정 7개가 모두 번역 미션이라 새 통역 주차 정책과 충돌했다. 배정·미션·학습 기록은 삭제하지 않고, 학습자 주차 목록에서 수행모드가 맞는 미션만 표시하도록 보완했다. 맞는 미션이 없으면 `준비 중`으로 표시한다.
- migration의 기존 주차 갱신에서 교수자가 설정한 채널·PDR·도메인 등의 맥락값을 초기화하지 않도록 수정했다. 기존 주차 ID와 미션 배정을 보존한다.
- 생성계약·관리자·학습자 정본의 구 9주 수행모드 분모를 실제 학습 12주 정책으로 동기화했다. 생성 프롬프트와 학습 콘텐츠는 변경하지 않았다.

## 운영 반영 결과

- `[독립 검토 필수]`: 저장소 협업 프로토콜에 따라 DB migration을 Claude 읽기 전용 검토에 전달했으나 OAuth 인증 오류(401)로 검토 보고서를 얻지 못했다.
- 사용자가 이번 건의 독립 검토 생략과 Codex 안전 검토에 따른 DB 적용·푸시·배포를 명시적으로 승인했다. 일반 협업 프로토콜 자체는 변경하지 않았다.
- migration `20260827093000` 한 건을 운영 Supabase에 적용했다. seed·role 변경과 콘텐츠 생성은 없었다.
- 적용 후 교과목은 기존 초안 2개를 포함해 총 5개, 주차는 75개이며 공개 과목은 확정된 3개다. 각 과목의 15주 편성, 통역 주차 수 6·2·0, 메타화용 7·14주를 확인했다.
- 사전 스냅숏과 대조해 기존 미션 배정 23개 전체, 기존 주차 ID·교수자 맥락 설정, 시나리오 1,403개와 학습 로그 63개의 다이제스트가 보존됐음을 확인했다.
- `94c8e06`까지 `codex/mpj5-mainline-2026-08-24`에 푸시했다. main 병합은 하지 않았다.
- 해당 소스를 Railway production에 배포했다: deployment `93f15fe9-9fd9-4647-92cf-d4a0daf9518c`, 상태 `SUCCESS`, image digest `sha256:872e0e47a14551fd6a4387fe4fc53f9ffcad94114eab6b178ad15406ea9ca361`.
- 현재 완료 범위는 세 과목의 설정·편성 구조 운영 반영이다. 새 학습 콘텐츠를 생성하지 않았으므로 세 과목의 콘텐츠 완비나 전체 학습 종단을 검증한 것으로 해석하지 않는다.

## 교과목 선택 화면 미감 정리 · 2026-08-27

- 사용자 피드백: 좁은 3열 카드에서 과목명이 줄바꿈되고, 주차 수·화행 수·긴 주제 목록이 과목 선택을 방해했다. 넓은 가로형 카드 세 개를 세로로 배치하는 제안을 승인받아 적용했다.
- 기존 크림·네이비·옐로 팔레트와 Pretendard를 유지했다. 제목의 크기·자간, 여백, 작은 과목 번호, 얇은 구분선과 호버·키보드 포커스 강조로 정보 위계를 정리했다.
- 제목은 `내 교과목`, 안내는 `교과목을 선택해 주차별 학습계획과 미션을 확인하세요.`, 카드 행동은 `주차별 학습계획 보기`로 변경했다.
- 카드에는 전체 과목명·수준·언어방향·학생용 한 줄 소개만 남겼다. 도메인·주제·통번역 주차 수·화행 수는 이 화면에서만 제외했고 저장 데이터나 실제 학습계획은 변경하지 않았다.
- 과목명은 데스크톱에서 한 줄로 표시하며 모바일에서는 읽기 쉬운 크기를 유지해 자연스럽게 줄바꿈한다. 말줄임으로 과목명을 숨기지 않는다.
- 카드 전체를 기존 교과목 주소로 이동하는 링크로 구성했다. 접근 가능한 이름·설명, 키보드 포커스와 동작 줄이기 설정을 지원한다. 로딩·오류·빈 목록 처리는 유지했다.
- `[단독 진행 적합]`: DB·생성 콘텐츠·프롬프트·접근 제어·학습 경로 변경 없이 목록 화면만 수정했다.
- 검증: `LearnerCourseList.test.tsx` 5 tests 및 `npm.cmd run typecheck` 통과. 첫 테스트 실행은 sandbox 접근 오류로 시작 전에 실패했고 권한 승인 후 같은 표적 테스트만 재실행해 통과했다. 전체 회귀·로컬 production build·브라우저 시각 검증은 수행하지 않았으며, 배포 시 Railway production build와 설정된 `/` healthcheck를 거쳐 `SUCCESS`를 확인했다.
- 기록 범위: 표시·문구·미감 변경이므로 이 dev-log에만 기록한다. 연구 구성개념이나 핵심 학습 흐름은 그대로여서 research-trail·계약 동결본은 갱신하지 않는다.
- UI 변경 커밋 `667c8dc`를 기존 `codex/mpj5-mainline-2026-08-24` 브랜치에 푸시하고 Railway production에 배포했다. main 병합·DB migration·콘텐츠 생성은 하지 않았다.
- 배포 근거: deployment `2c77f855-8883-4343-92cb-a9d4024456ca`, 생성 시각 `2026-08-27T04:19:42.239Z`, 상태 `SUCCESS`. 이 배포 기록만 추가하는 후속 문서 커밋은 실행 소스를 바꾸지 않으므로 재배포하지 않는다.

## 교과목 선택 화면 밀도·비교 정보 보완 · 2026-08-27

- 후속 사용자 캡처에서 세 번째 카드가 화면 아래로 밀리는 문제가 확인됐다. 화면 높이와 여백을 줄인 가로형 목록으로 보완하고, 기존 `wide` 레이아웃을 재사용했다. 스크롤을 강제로 숨기거나 과목명을 말줄임하지 않는다.
- 목록 표시만 중급 한중 → 중급 중한 → 고급 비즈니스 순으로 정렬한다. 공유 쿼리 배열·저장 데이터·관리자 편성 순서는 변경하지 않는다.
- 학생의 선택에 필요한 수준·전체 언어방향·수업 영역·주제·번역/통역 주차 수를 복원했다. 주제와 수행 주차는 저장된 설정 및 기존 12주 정책 함수를 사용한다. 영역은 확정된 세 과목 범위를 표시하여 DB의 대표 도메인 하나를 전체 범위로 오인하지 않도록 했다.
- 통역을 하지 않는 중한 과목에는 `번역 12주 · 통역 없음`을 명시한다. 비율은 시간·과제·성적 비중과 혼동될 수 있어 사용하지 않는다. 메타화용 클리닉은 번역/통역 주차 안의 활동이며 별도 수행모드나 추가 주차로 합산하지 않는다. 이 변경에서 주차별 클리닉 명칭은 바꾸지 않았다.
- 과목 번호·소개 문장·화행 개수·중복된 학기 설명을 제외하고 공통 15주/실제 학습 12주 안내는 목록 아래에 한 번만 표시한다. 전체 카드와 `주차별 학습계획 보기`는 기존 경로를 그대로 사용한다.
- 공식 과목명의 `AI 기반`은 그대로 유지했다. 명칭 변경·DB migration·콘텐츠 생성·접근 제어 변경은 없다. `[단독 진행 적합]`.
- 검증: `LearnerCourseList.test.tsx` 6 tests 및 `npm.cmd run typecheck` 통과. 최초 표적 테스트는 sandbox의 esbuild 접근 오류로 시작 전에 실패했고 승인 후 동일 테스트만 재실행했다.
- Browser 스킬로 실제 컴포넌트와 기존 세 과목 프리셋을 사용하는 로컬 레이아웃 fixture를 확인했다. 1366×640 및 1024×640 CSS viewport에서 문서 높이 640px·가로 넘침 없음·제목 모두 한 줄이며, 마지막 카드 하단 471.2px이 하단 메뉴 시작 568.2px 위에 있다. 390×844 모바일은 제목 잘림·가로 넘침 없이 세로 스크롤을 허용한다. 운영 DB 조회·인증·학습 종단 검증으로 해석하지 않는다.
- 기록 범위: 교과목 목록의 정보 밀도·표시 변경이므로 이 dev-log만 갱신한다. 학습설계·계약·핵심 경로는 그대로이며 research-trail·동결본 재발행은 하지 않는다. 이번 변경은 커밋·푸시·배포하지 않은 로컬 상태다.

## 교과목 선택 정보·상단 정렬 보완 · 2026-08-27

- 사용자 추가 피드백에 따라 `내 교과목`을 `교과목 선택`으로 바꾸고, 헤더의 중복 제목·사용 안내문·15주/12주 설명·영역 표시를 제거했다. 언어방향과 수행 주차는 `한국어 → 중국어 | 번역 6주 · 통역 6주` 형태로 구분한다.
- 주제의 보이는 라벨은 제거하고, 저장된 선택 범위 안에서 과목별 중요도순 세 개를 짧게 표시한다. 생략한 주제가 있을 때만 `등`을 붙인다. 스크린리더용 주제 라벨, 공용 주제 정본과 저장된 선택은 유지한다.
- 읽기 폭을 최대 960px로 줄이고 카드 사이 여백을 늘렸다. 최초 세로 중앙정렬은 사용자가 헤드라인 위치가 애매하다고 지적하여 제거했다. 최종본은 헤더 아래에 제목을 고정하고 카드들도 함께 위로 배치한다. 모바일 하단 메뉴에 내용이 가려지지 않도록 바닥 여백을 유지한다.
- 세 과목의 프리셋 명칭에서 `기반`을 제거했다. 표준 과목 ID와 정확히 일치하는 구 DB 명칭만 새 표시명으로 읽는 공통 함수를 목록·주차별 계획·주차 화면·관리자 편성 표시·강의계획서에 사용한다. 교수자가 지정한 다른 이름은 보존하며 DB를 직접 수정하지 않는다. 학습자 구조 정본에는 새 과목명만 반영했다.
- 검증: 교과목 목록 8 tests와 강의계획서 1 test, `npm.cmd run typecheck`, `git diff --check` 통과. 마지막 위치 조정은 CSS 클래스만 변경하여 테스트를 반복하지 않았다.
- Browser 스킬로 실제 로컬 페이지의 세 과목 표시를 확인했다. 최종 1024×640 CSS viewport에서 문서 크기 1024×640, 제목 모두 한 줄, 마지막 카드 하단 551.2px·하단 메뉴 시작 568.2px이다. 1280×720 화면을 시각 확인했고, 390×844에서도 가로 넘침 없이 세 카드가 표시된다. 학습 수행 종단 검증을 한 것은 아니다.
- `[단독 진행 적합]`: UI 표시·명칭 보완으로 연구 구성개념·학습설계·핵심 경로는 변경하지 않았다. dev-log 및 학습자 정본의 과목명만 갱신하고 research-trail·계약·동결본은 갱신하지 않는다. DB migration·콘텐츠 생성·커밋·푸시·배포는 없으며 localhost:8080 미리보기 서버는 계속 실행한다.
- 후속 미감 조정: 카드 폭을 960px → 820px로 더 줄이고 주제 표시를 최대 네 개로 늘렸다. 한중 화용은 네 주제를 모두, 중한은 네 주제와 `등`을 표시한다. 비즈니스는 설정된 세 주제만 유지하며 새 주제를 임의로 추가하지 않았다. 제목과 카드의 세로 위치는 유지한다.
- 이번 추가 변경은 교과목 목록 8 tests 통과 및 Browser 스킬의 실제 로컬 1024×640 화면으로 확인했다. 카드 폭 820px, 과목명 모두 한 줄, 문서 크기 1024×640으로 스크롤·가로 넘침이 없다. 표시 개수와 CSS 폭만 바꿔 전체 테스트·typecheck는 반복하지 않았다. 연구 기록 갱신 대상은 아니다.
