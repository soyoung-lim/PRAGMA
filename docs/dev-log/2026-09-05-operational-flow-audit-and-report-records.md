# 생성부터 학습 리포트까지 연결 점검과 기록 집계 수정

- 날짜: 2026-09-05
- 기준: 새 `origin/main@1802382ebbf46b7d644b206db16f8843f1e10f8d` 기반
  `codex/e2e-completion-2026-09-05`. 기존 dirty 복구 작업공간은 보존했다.
- 판단: **[단독 진행 적합]** — 읽기 전용 실측과 원인이 확인된 리포트 국소 오류 수정.
- 상태: 로컬 구현·검증 완료. main 병합·운영 배포는 하지 않았다.

## 실제 운영 연결 상태

관리자 인증 후 읽기 전용으로 2026-09-05 23:06 KST에 조회했다. 앱 `docs/CANONICAL.md`가
지정한 정본 3개 경로는 논문 작업공간 `01_정본/00_정본목록.md`의 지정과 일치했다.
현행 release는 `pragma_zhko_bidirectional_candidate_20260904_02`다.

| 단계 | 확인한 사실 | 해석 |
|---|---|---|
| 생성 | 전체 scenarios 1,708행 중 현행 코어 50행·현행 미션 31행 | 현행 미션 31행은 모두 native MJT5이고 generated 상태 |
| 로컬 형식·실행기 | 실제 미션 31/31 스키마·정본 러너 변환 성공, 규칙 fail 0 | 31행 모두 warning. 교육적 타당성·교수자 승인 증거가 아님 |
| 검수 | 현행 reviewed/released 0행. 전체 검수 테이블 4행 중 승인 0행 | 현행 미션 가운데 검수 이력이 있는 대상은 1건 |
| 편성 | 정본 3개 교과목 모두 published·15주. 기존 배정 16/0/0행 | 16행은 전부 과거 release. 현행 실행 가능 배정은 세 과목 모두 0행 |
| 수행·리포트 | 모든 버전 합산 완료 로그 63행 | 현행 교과목의 인증 학습자 종단 완료 증거로 사용할 수 없음 |

원격 시나리오는 500행씩 페이지를 읽어 집계했다. 현행 코어와 미션의 release 불일치는 0건이었다.
미션별 규칙 경고는 R5·R19·R27·R29이며, 집계와 주차별 후보 목록은 아래 JSON에 보존했다.
경고만으로 자동 재생성·승인하지 않았다.

`content-review`의 읽기 전용 `inspect`에서 요청·한→중 통역 미션
`492fd2ab-47c0-48ff-8c4d-34248f561615`의 현재 source/content hash와 검수 행
`575db851-c9df-44eb-b23b-fd72da23e317`이 일치했다. OpenAI·Claude·지적별 판정 기록은 있지만
교수자 체험 확인과 승인은 없다. AI 검토 완료를 내용 적합성이나 승인 가능성으로 해석하지 않는다.

2주차 요청의 실제 수준·방향·모드·주제 조건으로 현행 생성 미션을 대조하면, 교수자 승인 전
형식 후보는 `AI 한중 화용 통번역 실습` 0개, `AI 한중 비즈니스 통번역 실습` 1개,
`AI 중한 실전 통번역` 1개다. 세 과목 모두 첫 화행 주차의 완전한 두 미션이 부족하다.
위 검토 이력이 있는 통역 미션은 2주차 번역 조건을 대신 충족하지 않는다.

## 확인한 오류와 수정

- `LearnerRecords`가 실제 완료 기록 0건일 때 계정과 분리된 로컬 mock `practiceCount`를
  완료 수로 대체했다. 이제 표시 중인 수행 기록 배열의 길이만 집계한다.
- 조회 실패를 빈 배열로 처리하고 로딩 중에도 localhost 시연 기록을 표시하던 동작을 수정했다.
  로딩·실패·정상 빈 결과를 구분하고, 실패에는 다시 불러오기를 제공한다.
- 시연 자료는 localhost에서 정상 조회한 원격 기록이 비었을 때만 명시적 배지와 함께 표시한다.
  localhost 이외의 development 실행에서도 로컬 시연 기록을 실제 기록으로 합산하지 않는다.
- 조회는 계속 현재 `auth_user_id`와 `mission_completed=true`로 제한한다.
  저장 schema·완료 조건·프롬프트·콘텐츠 승인 기준은 바꾸지 않았다.

## 검증

- 시작 기준 전체: 122 files / 727 tests pass, 3 files / 9 tests skip.
- 추가 회귀 6개: 오래된 로컬 횟수가 있어도 실제 0건, 로딩 중 시연 비노출, 본인 완료행 집계,
  조회 실패 후 재시도, 인증 오류 응답·예외 처리.
- 수정 후 전체: **123 files / 733 tests pass**, 기존 3 files / 9 tests skip.
- `npm.cmd run typecheck`, 변경한 두 파일 ESLint, `npm.cmd run build`: 통과.
  운영 빌드의 프롬프트·검수 번들 사전 검증도 통과했고 기존 CSS·큰 청크 경고는 유지됐다.
  prebuild가 만든 기준 커밋·시간 메타데이터 변화만 복원했으며 프롬프트 본문 변화는 없었다.
- worktree 안에 `npm ci`로 의존성을 별도 설치했다. 실행 Node는 24.18.0으로 저장소 지정
  22.x와 달라 설치 시 engine 안내가 있었다. CI의 Node 22 검증은 이번 단계에 포함하지 않았다.
- 개발 서버는 해당 worktree 루트에서 `127.0.0.1:8099 --strictPort`로 실행하고 HTTP 200을
  확인한 뒤 브라우저를 열었다. 실제 `LearnerRecords`를 모의 응답으로 렌더한 로컬 검증에서
  오류 안내 → 다시 불러오기 → 1건 표시, 로컬 practiceCount 7이 있어도 실제 0건 표시를 확인했다.
  오류 화면의 레이아웃도 확인했다. 이 과정은 운영 DB 저장이나 실제 학습 수행이 아니다.
- 운영 브라우저 `/learner/course`는 정상적으로 학생 로그인 화면으로 연결됐다.
  인증 학습자 배정·MJT5·DCT 제출·음성·유료 피드백 종단은 미수행이다.
- 도구의 경로·네트워크 제한은 해당 명령의 권한으로 해결했다. 운영 읽기 조회의 일시적
  `JWT issued at future`는 짧은 간격 후 재시도에서 해소됐다. 인증·Vite 보안 설정은 바꾸지 않았다.

## 우선순위와 다음 작업

- **지금 반드시 해결:** 첫 운영 주차의 조건에 맞는 완전 미션 두 건 확보 → 현재 버전 AI 검토와
  교수자 체험 감수·최종 승인 → 주차 편성 → 해당 편성의 강의 유인물 승인 → 인증 학습자
  수행·저장·리포트 확인. 이 점검에서 콘텐츠 생성·승인·DB 편성 쓰기는 수행하지 않았다.
- **지금 반드시 해결 / 로컬 수정 완료:** 리포트의 로컬 횟수 대체·조회 실패 은폐.
- **완성 전 해결 권장:** 다른 방향·모드와 실제 음성 호출 확인. 리포트의 과목별 귀속과
  과목 조회 실패 시 과거 mock 주차표를 쓰는 경계는 별도 확인 대상으로 남긴다.
- **후속 개선:** 기존 CSS·번들 크기 경고. 이번 연결 점검의 범위에 포함하지 않았다.

사용자 판단이 필요한 후속 범위는 첫 수업용 콘텐츠 확보·유료 검토 대상과 로컬 수정의 운영 반영이다.
교수자 최종 판정은 AI가 대행하지 않는다. 새 설계 결정은 만들지 않아 traceability·decision log는
갱신하지 않았고, `ITER-20260905-03`·`EVD-20260905-05`에 실측·수정·검증 경계를 남겼다.

근거: `docs/research-trail/evidence/2026-09-05-operational-flow-audit.json`.
원문 없는 집계·상태·후보 ID만 보존하며 실제 미션 본문과 인증 정보·학습자 응답은 포함하지 않는다.

## 사용자 승인 후 운영 반영 · 2026-09-05

- 사용자가 필요한 커밋·푸시·배포를 승인했다. 기능 `1241a8d64e17035666dcd5fe156689d06cd30d9c`를
  푸시하고 PR #79의 필수 CI `33972276467` 성공 뒤 정확한 head를 main에 병합했다.
- main merge `dc632f14863ea158dddf601242bc5da5f00a220f`, main CI `33972444473` 성공.
  `release:lineage`로 기능 커밋의 origin/main 포함을 확인했다.
- Railway 연동 GitHub deployment `6282164954` (`PRAGMA / production`)는 위 main SHA이며,
  2026-09-05 14:41:41 UTC에 `success` 상태가 됐다. production 직접 업로드·CI 우회는 없었다.
- 14:42:52 UTC 운영 홈·`/learner/records`·실제 `LearnerRecords-IE2aUSli.js` 응답이 모두 HTTP 200이었다.
  홈이 제공한 `index-CHxk3jM5.js`에서 연결된 번들을 찾아 로딩·오류·재시도 문구, 본인·완료행
  필터 유지, `practiceCount` fallback 부재를 확인했다. 인증 학습자 실저장은 이번 smoke에 포함하지 않았다.
- DB migration·Edge 함수·프롬프트 변경이 없어 해당 배포는 필요하지 않았다. 콘텐츠 생성·승인·
  편성도 변경하지 않았다. 앞선 운영 데이터 실측은 조회 당시의 값이며 배포 시점에 재집계한 값이 아니다.
- 이 절은 앞선 로컬 완료 경계를 갱신하는 배포 이력이다. 뒤따르는 기록 전용 커밋은 이 배포 증거를
  보존하며 기능 변경이나 별도의 학습자 종단 완료를 주장하지 않는다.
