# 2026-08-03 초대 대역 경계 오분류 보정

## 관찰

- `07d82bea…` core_v7·통역·중→한 초대 미션에서 규칙검사는 통과했지만 AI 품질점검이
  multi-judge 후보의 대역 오분류를 확인했다.
- `참석해 주시면 좋겠습니다`가 `too_pressuring`, 일정 조정 여지를 둔 초대가
  `too_ambiguous`로 저장됐다. 전자는 기존 카탈로그의 정상 자원
  `함께해 주시면 좋겠습니다`와 사실상 같은 희망형이다.
- 2026-08-02 표본에서도 분명한 일정 선택형 초대를 `too_ambiguous`로 붙인 사례가 있어,
  단일 우연보다 초대 대역 경계가 생성기에 충분히 명시되지 않은 문제로 판단했다.

## 변경

- `invitation_choice_commitment`를 `1.0`에서 `1.1`로 올렸다.
- 한국어·중국어 산출 방향의 조작적 정의에 다음 경계를 명시했다.
  - 참여 의무·기정사실화·거절 비용이나 반복 압박이 있을 때만 `too_pressuring`이다.
  - 행사와 참여 행위가 문맥에서 식별되고 수락 여부가 남으면 `within_band`이다.
  - 통상적 희망·환영형은 그 자체로 압박이 아니다.
  - 기존 문맥의 행사 정보를 반복하지 않거나 일정·불참 선택지를 준다는 이유만으로
    `too_ambiguous`가 되지 않는다.
- 이 경계를 고정하는 카탈로그 회귀 테스트를 추가했다.

## 검증

- `src/lib/pragma/targetFeatures.test.ts`: **6 pass**.
- 전체 Vitest: **239 pass / 6 skip**.
- `npm run typecheck` 통과.
- 변경 파일 ESLint와 `git diff --check` 통과.
- production build 통과(**1901 modules**).
- core prompt surface와 hash `07d82beaab49…`는 변경하지 않았다.
- PR #16을 merge commit `7fdd32b`으로 병합했다.
- Railway production 배포 `57c64116-a817-4047-baae-d53d43d22494`가 `SUCCESS`였고,
  운영 URL HTTP 200과 `/assets/targetFeatures-DpMM3B1Z.js`의 feature `v1.1`·한국어
  경계 예시 포함을 확인했다.

## 범위·후속 확인

- 구현·배포 단계에서는 DB·코어 생성계약·R1~R29·`policy_ver`·콘텐츠·검수 상태를
  변경하지 않았다.
- 기존 `invitation_choice_commitment v1.0` 미션을 소급 수정하지 않는다. `v1.1`은 배포 뒤
  새로 조립하는 미션부터 적용한다.
- 실제 생성 모델의 오분류 감소는 core_v7 초대 소수 재조립으로 확인한다.

## 운영 후속 검증 · core_v7 초대 소수 재조립

- 운영 관리자 조립 화면을 새로 열고 `07d82bea…`·통역·중→한·초대로 필터했다. 일치한
  5건은 기존 generated 3건과 조립 대기 코어 2건이었다. 기존 미션은 건드리지 않고 대기
  코어 2건만 조립했다.
- 일상 표본 1건은 최대 3회 재시도 뒤에도 `R5`로 저장 전 차단됐다. 실패물은 DB에
  저장되지 않았다.
- 직장 표본 1건은 시도 2회에 저장됐다. 운영 화면에서
  `invitation_choice_commitment v1.1`, `mission_v5_mpj4_minidiscourse_v4`, 콘텐츠 hash
  `ceee0f2a…`, AI 품질점검 통과를 확인했다.
- 이 저장본의 MPJ 상황문 4개는 서로 완전히 중복되지 않았고 DCT 상황문을 그대로 복제하지
  않았다. 각 3문장·120~128자로 R27 fail·warning 조건에 해당하지 않았다.
- multi-judge 후보의 저장 대역·글자 수는 `too_pressuring` 75자,
  `within_band` 100자, `too_ambiguous` 83자, `within_band` 82자,
  `too_pressuring` 77자였다. 저장 대역 그대로는 길이 범위가 겹쳐 R5를 통과한다.
- 그러나 83자 후보 `워크숍을 계획하고 있습니다. 참석 여부는 자유롭게 결정하세요`는 행사와
  참여 행위가 식별되고 수락 여부가 남아 `v1.1` 정의상 `within_band`다. 이를 바로잡으면
  부적절안 75·77자와 적절안 82·83·100자가 완전히 분리돼 R5 hard fail 조건이 된다.
- AI 품질점검은 위 대역 오분류를 탐지하지 못하고 통과시켰다. 따라서 이 미션은
  `reviewed`로 승격하지 않고 `generated`에 보류했다.

## 운영 검증 판정·범위

- 배포된 `v1.1` provenance 적용 자체는 확인됐지만, 생성기와 AI 품질점검의 경계 준수는
  확인되지 않았다. 운영 소수 표본 기준 저장 성공은 1/2였으나 인간 눈검사까지 통과한 사용
  가능 표본은 0/2다. 표본이 작으므로 빈도나 전체 성공률로 일반화하지 않는다.
- 이번 운영 검증으로 새 generated 미션 1건만 추가됐다. 기존 콘텐츠·검수 상태는 수정하지
  않았고, 실패물 저장·기존 v1.0 소급 수정·자동 reviewed 승격·DB 삭제는 하지 않았다.
- 코드 변경이나 자동 테스트 재실행은 없었다. 다음 진단에서는 생성기와 AI 품질점검이 각각
  같은 versioned 조작적 정의를 실제 입력으로 받는지 먼저 확인해야 한다.
