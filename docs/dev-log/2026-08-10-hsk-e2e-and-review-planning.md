# 2026-08-10 HSK 콘텐츠 연계 E2E와 독립 검토 계획

## 수행

- 운영 관리자 생성기에서 새 코어를 생성하고, 기존 입문·한→중 코어 한 건을 Full Mission으로 승격했다.
- 생성된 미션의 `hsk_lexical_audit`가 통합 검수 화면까지 전달되는지 확인했다.
- `/admin/corpus`가 최근 점검을 찾지 못하는 원인을 조사하고 로컬에서 조회 로직을 수정했다.
- 기존 Claude 독립 검토 스크립트·관리자 화면·생성계약과 현재 공식 API 사양을 대조했다.
- KCI 후보 학술지의 공식 투고 일정·분야를 확인하고 논문 2편의 분리 전략을 제안했다.

## HSK 운영 E2E 결과

- 새 미션: `처음 만난 조원에게 감사 인사 전하는 글`
- 조건: 감사 · PRAGMA 입문 · 한→중 · 번역
- 통합 검수 표시: HSK 참고 일치 71% · 교수자 확인 후보 14개
- 상세 수치: 점검 단어 49개 · 기준 내 확인 35개 · 후보 14개
- 미션은 `generated`로 저장됐으며 자동 `reviewed` 승격이나 강좌 배정은 하지 않았다.
- 새로 생성한 다른 코어의 Full Mission 시도 3회는 R27로 저장 전 차단됐다. HSK 실패가 아니라 기존
  결정론 규칙의 fail-closed 동작이다.

## 최근 점검 조회 결함과 수정

- 원인: 미션 승격은 오래된 코어 행의 `mission_content`를 갱신하지만 `created_at`을 바꾸지 않는다.
  화면은 최근 생성 행 40개만 조회해, 새 미션 audit가 있어도 해당 행이 범위 밖이면 누락됐다.
- 수정: mission이 있는 행과 최근 core 행을 분리 조회하고, 미션 `provenance.generated_at` 또는 코어
  `generation.generated_at`을 실제 audit 시각으로 사용해 최신 complete 결과를 선택한다.
- 회귀: 오래된 DB 행에 새 미션이 승격된 경우, core generation 시각, fallback 시각과 정렬을 3개
  단위 테스트로 고정했다.

## 검증

- `npm.cmd test -- src/lib/pragma/hskAuditSnapshot.test.ts`: 3/3 pass
- `npm.cmd run typecheck`: pass
- 변경 파일 ESLint: pass
- production build: 1,911 modules, pass
- localhost `/admin/corpus`: 8월 10일 오후 5:41 점검, 입문·한→중·HSK 1–4급 누적,
  49/35/14 및 후보 목록 표시 확인

## 독립 검토와 KCI 확인 사항

- Claude 배치는 이미 읽기 전용·동일 15축·구조화 출력·provenance·resume·token count로 구현돼 있다.
- 현재 공식 문서에서 `claude-opus-5`, `output_config.format`, effort와 token counting을 확인했다.
- 권고 범위는 동결 코어 전수 15축을 본 분석으로 유지하고, 미션은 MPJ·DCT 전용 축을 정의한 별도
  2차 검토로 분리하는 것이다. 사용자 확정 전 계약·배치를 변경하지 않았다.
- 공식 일정상 『중국문학』은 10월 15일 마감이고, 『중어중문학』 12월호는 발행 50일 전인
  11월 10일 마감이다. 하반기 『중국언어연구』 공지는 아직 확인되지 않아 확정 백업선으로 쓰지 않았다.

## 운영 DB의 HSK 감사 보급 현황

- 2026-08-10 읽기 전용 전수 조회 기준 `scenarios`는 1,391행이며, 방향별로 한→중 1,291행,
  중→한 100행이다.
- `mission_content`가 있는 행은 69행(`generated` 52, `reviewed` 17)이다. 이 가운데 mission
  `hsk_lexical_audit`는 `complete` 1행, 미존재 68행이다. 기존 mission 중 감사 보급 비율의 적격
  분모는 전체 1,391행이 아니라 mission 69행이므로 **1/69 = 1.45%**다.
- core audit는 전체 1,391행에서 0행이며 중→한 100행도 0행이다. 한→중 core는 원문이 한국어라
  설계상 core 단계 감사 대상이 아니지만, 중→한 구행에는 아직 소급 audit가 없다.
- 따라서 현재 증거가 지지하는 주장은 `새 mission 생성→audit 저장→통합 검수 소비`의 전향적 E2E가
  작동한다는 것까지다. 역사 콘텐츠 전체에 적용됐다는 주장은 할 수 없고, 소급 배치는 별도 승인 전
  실행하지 않았다.

## Claude Opus 5 독립 검토 스모크와 층화 6건 파일럿

- 로컬 ignored 파일 `.env.cross-vendor.local`에 Supabase·Anthropic 키를 저장하고 값은 로그나
  저장소에 출력하지 않았다. OpenAI 기준선 키는 비워 두고 `--no-baseline`으로 실행했다.
- 대상: run `core_zh_ko_1785926368500`의
  scenario `7476fd20-8f2b-427f-8e6e-f7a38fb8df78` 1건.
- 결과: API 성공 1/1, 15축 중 14축 pass, `learner_scene` fail, 전체 fail. Claude는 학생용
  `situation_ko`의 “부담을 감수할 수 있지만 거절할 권리”가 내부 평가 기준을 노출한다고 지적했다.
- Codex 정본 대조: 저장 원문과 생성계약의 R30·학생용 장면 규칙이 직접 충돌함을 확인했다.
  이는 독립 모델 지적의 계약 근거 확인이며 최종 인간 판정은 아니다. 해당 행은
  `core_v8_learner_scene_v1`, `needs_review`이며 상태를 자동 변경하지 않았다.
- 실측 사용량: 입력 8,081, 출력 2,407(그중 thinking 905), 캐시 0. 2026-08-10
  [공식 Opus 5 단가](https://platform.claude.com/docs/en/about-claude/models/whats-new-claude-4-8)
  (입력 $5/MTok, 출력 $25/MTok)로 계산한 이번 1회 비용은 약 **$0.1006**이다.
- 스모크 결과는 Git 제외 경로
  `cross-vendor-review-out/core_zh_ko_1785926368500__smoke-high/results.jsonl`에 보존했다.
- 승인 후 나머지 5건을 수준×모드 3×2, 일상·학업·직장 각 2건이 되도록 실행했다. 추가 호출은
  API 5/5 성공, 입력 40,629·출력 14,812였으며 모델 총평은 fail 3·warning 2였다.
- 6건 합계는 fail 4·warning 2·pass 0, 입력 48,710·출력 17,219(thinking 7,957)·캐시 0,
  공식 단가 환산 **$0.6740**이다.
- Codex 정본 대조에서는 5/6에서 통역 A/B/C 중첩, 반대 인접쌍·소유자 역전, R30 평가 기준
  노출 중 하나 이상의 계약 근거가 확인됐다. 나머지 거절 1건의 `burden` warning은 Claude가
  학습자 A의 업무 부담과 상대 B에 대한 R을 혼동해 제시 근거를 지지하지 않았다. 감사 화행의
  `R=high` warning은 조작 정의 확인 전 유보했다.
- 이는 최종 인간 판정이 아니며, 6건 모두 구세대 `core_v8` 계열·`needs_review` 목적 표집이다.
  신뢰도·타당도·현행 파이프라인 결함률이나 전체 콘텐츠 결함률의 증거로 사용하지 않는다.
- 추가 5건 원본은 Git 제외 경로
  `cross-vendor-review-out/core_zh_ko_1785926368500__pilot6-new5/results.jsonl`, Codex 대조표는
  `docs/research-trail/evidence/2026-08-10-claude-core-pilot6-adjudication.md`에 보존했다.

## AI 모델 간 독립 검토 도구 보정

- `--no-baseline` 실행을 불일치 `0/0`으로 표시하던 보고서를 독립 모델 판정·비통과 축 중심으로
  바꾸고, 기준선이 없으면 불일치율을 계산하지 않는다고 명시했다.
- token-count 요청에도 실제 호출과 동일한 `output_config.format` JSON schema와 effort를 포함했다.
  수정 전 5,633이던 단일 표본 사전 계측이 수정 후 8,081로 실제 입력 사용량과 정확히 일치했다.
- `--resume` 이후에도 저장된 `results.jsonl` 전체에서 사용량을 다시 합산하도록 보정했다.
- 검증: `node --check scripts/cross-vendor-review.mjs`, 해당 파일 `git diff --check`, 무료 단일
  `--count-only` 8,081 일치.

## 6건 층화 파일럿 비용 사전 계측

- 이미 실행한 고급·통역·요청·학업 1건을 포함해 수준×모드 3×2를 각 1건, 일상·학업·직장을
  각 2건으로 맞췄다. 화행은 요청·거절·감사·사과·반대 5종이다.
- 추가 후보 5건의 schema 포함 무료 계측은 합계 입력 40,629토큰(평균 8,126, 범위
  8,033~8,246)이었다.
- 실행 전 첫 스모크와 같은 출력량을 가정한 추가 5건 예상치는 약 $0.50이었다. 실제 추가 비용은
  출력량이 늘어 약 $0.5734, 6건 누적은 $0.6740이었다.

## 미실행

- Claude 나머지 18건 전수 확대, OpenAI 기준선 비교, 최종 인간 판정
- HSK 조회 수정의 commit·push·Railway 배포
- 새 미션의 사람 승인·강좌 배정
- 기존 69개 mission 또는 중→한 100개 core의 HSK 소급 감사

## HSK 기준–검수 연결과 용어 정리

- 관리자 사이드바를 `0 · 설계 기준`, `2 · 품질 검수·승인`으로 재구성하고 HSK 화면을
  `HSK 3.0 데이터셋`, 독립 검토 화면을 `AI 독립 검토`로 표시했다.
- corpus의 최근 complete 결과는 후보가 있으면 `/admin/review?hsk=candidates`를 열고, review는
  HSK 상태별 필터와 수준·누적 범위·점검 수치를 표시한다. 각 결과의 `적용 기준 보기`는 corpus의
  PRAGMA 입문·중급·고급 기준 앵커로 돌아간다.
- 사용자용 짧은 용어는 `AI 독립 검토`, 계약·연구 문서의 정식 용어는
  `AI 모델 간 독립 검토`로 통일했다. 학위논문 4.6.3 표제는
  `AI 모델 간 독립 검토의 활용과 한계`로 확정했으며 외부 논문 정본 파일은 수정하지 않았다.
- 검증: HSK 관련 단위 테스트 6/6, typecheck, 독립 검토 스크립트 syntax, diff check,
  production build 1,912 modules pass. localhost에서 후보 1건 필터와 입문 기준 역링크를 확인했다.
- 후속 화면 피드백을 반영해 H1을 `HSK 3.0 어휘 난이도 관리`로 바꾸고, 대표 어휘
  54개·펼치기·중복 수준 섹션·corpus의 원시 미일치 토큰 칩을 제거했다. 수준 매핑은 상단 DB 구성에,
  4단계 점검과 최근 49/35/14 결과·CTA는 하나의 운영 패널에 합쳤다. `검토 후보만 보기`는
  `어휘 확인 대상 보기`로 변경했다.
- 상단 LIVE 패널의 넓은 단일 캔버스는 1:1 두 열로 바꿨다. 왼쪽에는 입문→중급→고급 순서의
  왼쪽 정렬 누적 범위, 오른쪽에는 공식 HSK 출처와 생성 조건이 아닌 주제 참고자료 427개를 배치하고
  하단 중복 아코디언을 제거했다. localhost 1265×711에서 균형과 가로 넘침 없음을 확인했다.
- 최종 검증: HSK 관련 회귀 6/6, typecheck, 변경 파일 ESLint, `git diff --check`, production build
  1,909 modules pass. `14개 확인하기`가 `/admin/review?hsk=candidates`의 후보 1건으로 연결됐다.
  추가 UI 미감 조정은 다음 작업으로 남겼으며 커밋·푸시·배포는 수행하지 않았다.

## LOCK 전 HSK 감사 장치 30건 검증

- 최종 콘텐츠 품질 추정이 아니라 HSK 감사 장치의 수준·방향·모드별 작동 확인을 목적으로
  미승인·미감사 테스트 콘텐츠 30건을 식별 표집했다. LOCK 뒤 본생산 통계와 분리하고 이 테스트
  콘텐츠는 추후 폐기한다.
- 원안은 12개 `수준×방향×모드` 셀 각 2건 + 경계 사례 6건이었다. `중급×한→중×통역`의
  적격 행이 1건뿐이어서 기본 셀 23건 + 경계 사례 7건으로 조정하되 모든 셀을 포함했다.
- 기존 정본 HSK 감사 함수를 그대로 호출해 30/30 계약 검증, 30/30 DB 저장·재조회,
  30/30 콘텐츠 hash 불변을 확인했다. LLM 호출과 모델 비용은 없었고 `hsk_lexical_audit`만 기록했다.
- 처리된 고유 토큰 1,345개, 기준 내 1,053개, 추가 확인 후보 292개는 장치 작동 확인값이며
  콘텐츠 품질·수준 적합률·논문 최종 통계로 해석하지 않는다.
- Supabase가 review 조회 한 번에 1,000행만 반환해 새 결과 일부가 화면에서 누락되는 결함을 발견했다.
  `AdminReview`를 1,000행 단위 전체 페이지 조회로 바꾸고 mission의 새 13건과 core의 새 17건이
  각 HSK 후보 범위에 표시됨을 localhost에서 확인했다.
- 상세 증거: `docs/research-trail/evidence/2026-08-10-hsk-audit-validation-30.md`.
- 검증: 관련 Vitest 14/14, typecheck, 변경 파일 ESLint, production build 1,910 modules,
  script syntax와 저장 보고서 재검증 통과. 커밋·푸시·배포는 수행하지 않았다.

## HSK 결과 중심 관리자 화면 재설계

- 절차 설명이 첫 화면을 차지해 실제 49/35/14 결과와 30건 장치 시험이 약하게 보인다는 사용자·독립
  검토 피드백을 수용했다. H1은 `HSK 3.0 어휘 기준·점검`으로 바꿨다.
- Hero는 최근 콘텐츠 49개 전수 점검, HSK 1–4급 참고 일치 71%, 범위 확인 35개, 교수자 확인
  14개와 CTA를 하나의 결과 서사로 표시한다. 71%는 평가 점수·자동 통과율이 아닌 최근 한 사례의
  참고 대조 결과라고 화면에 명시했다.
- LOCK 전 검증은 품질 평균으로 표시하지 않고 `12개 조건 · 30건 · 감사 기록 30/30` 장치 작동
  증거로만 승격했다. 절차는 세로 설명에서 가로 한 줄로 압축했다.
- HSK 11,000개는 급수 구간별 비례 스택 막대로 바꾸고 5,400개 경계에 현재 PRAGMA 적용 상한
  눈금을 추가했다. 본문 최소 글자 크기는 12px로 올렸다.
- localhost 1265×711에서 결과 Hero와 DB 영역 시작이 한 viewport에 들어오고, 하단 막대 구간색·
  5,400 눈금·7–9급 미적용이 구분됨을 확인했다. CTA는 review의 HSK 후보 14건을 열었다.
- 관련 Vitest 14/14, typecheck, 변경 파일 ESLint와 production build 1,910 modules가 통과했다.
  커밋·푸시·배포는 수행하지 않았다.

### 설계 기준 우선 순서와 수준 대응 명료화

- `/admin/corpus`가 `0 · 설계 기준`에 속한다는 사용자 판단에 따라 페이지 순서를
  `HSK 3.0 어휘 DB 탑재·PRAGMA 수준 대응 → 실제 콘텐츠 점검 결과`로 바꿨다.
- PRAGMA 입문·중급·고급을 각각 HSK 1–4·1–5·1–6급 누적과 2,000·3,600·5,400개에 직접
  결합한 큰 카드로 표시했다. 균등 위치로 수준 대응을 암시하던 방식은 제거했다.
- HSK DB 구성은 1–4급 2,000·5급 신규 1,600·6급 신규 1,800·7–9급 신규 5,600개를 큰 수치로
  분리하고, 7–9급은 `DB 보관 · 현재 PRAGMA 미적용`으로 명시했다. 비례 막대의 2,000·3,600·
  5,400 경계에는 PRAGMA 입문·중급·고급 상한을 바로 위 HSK 색상 블록 끝과 정렬했다.
- localhost 1265×711에서 설계 기준과 다음 운영 결과 Hero의 연결을 확인했다.
- 최종 관련 Vitest 14/14, typecheck, 변경 파일 ESLint, production build 1,910 modules를 통과했고
  브라우저 콘솔 오류는 없었다. 기존 React Router 향후 버전 경고만 남아 있다.

## 작업 종료 인계 (2026-08-11)

- HSK 설계 기준 영역은 PRAGMA 입문·중급·고급 카드 하나씩에 HSK 누적 범위와
  2,000·3,600·5,400개를 결합했다. 카드 하단에는 1–4급 세부 수량, HSK 5급 신규 1,600개,
  HSK 6급 신규 1,800개를 보조로 표시한다.
- 같은 관계를 HSK→PRAGMA 방향으로 반복하던 별도 구간 카드와 상한 막대, 현재 점검에 쓰지 않는
  HSK 7–9급, 생성 조건이 아닌 HSK 주제 427개는 기본 화면에서 제거했다. 공식 출처는 최하단에 둔다.
- 운영 결과 영역에는 최근 저장 콘텐츠 한 건의 49개 전수 점검, 35개 HSK 참고 범위 확인,
  14개 교수자 확인 결과와 `/admin/review?hsk=candidates` CTA가 남아 있다.
- `12개 조건·시험 콘텐츠 30건·저장 30/30`은 장치 기능 검증이고, 49/35/14는 최근 콘텐츠 한 건의
  결과다. 두 증거가 한 화면에서 직관적으로 구별되지 않는다는 사용자 피드백을 확인했다.
- 다음 작업의 첫 항목은 30건 기능 검증 상태 줄을 주 화면에서 제거해 연구 증거에만 보존하고,
  최근 결과의 표제를 `최근 콘텐츠 점검 결과`처럼 단건 성격이 분명한 말로 정리하는 것이다.
  이 변경은 이번 종료 시점에는 구현하지 않았다.
- 커밋·푸시·배포는 수행하지 않았다. 현재 변경은 로컬 작업 트리에만 있다.

### 종료 검증과 보존

- 최종 UI 중복 제거 뒤 typecheck와 변경 관리자·HSK 파일 ESLint가 통과했다.
- HSK 화면 조회·스냅샷·페이지네이션 관련 표적 Vitest 8/8이 통과했다.
- 기능 브랜치에 세션 체크포인트를 커밋·푸시하되, 30건 상태 줄의 정보 구조가 미해결이므로
  실서비스 배포는 보류하기로 했다.

## 30건 기능 검증과 최근 단건 결과 분리 (2026-08-11)

- `/admin/corpus`에서 `12개 조건 · 시험 콘텐츠 30건 · 저장 30/30` 상태 줄을 제거했다. 30건의
  장치 기능 검증 증거는 `docs/research-trail/evidence/2026-08-10-hsk-audit-validation-30.md`와
  `EVD-20260810-04`에 그대로 보존했다.
- 운영 카드 표제를 `최근 콘텐츠 점검 결과`로 바꾸고 `최근 저장 콘텐츠 1건을 전수 대조했습니다`를
  명시했다. 49/35/14가 장치 시험 집계가 아니라 최근 저장 콘텐츠 한 건의 실제 결과임을 화면 안에서
  구별할 수 있다.
- localhost 1265×711 실데이터 화면에서 입문·한→중·HSK 1–4급 누적, 49개 전수 점검·35개 참고
  범위 확인·14개 교수자 확인을 확인했다. 30건·30/30 문구는 0건, 새 표제는 1건이었고
  `14개 확인하기`는 `/admin/review?hsk=candidates`를 유지했다.
- HSK 화면 조회·필터·페이지네이션 표적 Vitest 8/8, typecheck, `AdminCorpus.tsx` ESLint,
  production build 1,910 modules가 통과했다. 브라우저 오류는 없고 기존 React Router 향후 버전
  경고 2건만 남아 있다.
- 이번 변경은 아직 커밋·푸시·배포하지 않았다.

## 생성·검증 체계와 독립 검토 결과 화면 구현 (2026-08-11)

- 사이드바 `생성 규칙·프롬프트`를 `생성·검증 체계`, 화면 H1을
  `생성·검증 규칙과 프롬프트`로 바꿨다. 상단에 `검증① 결정론적 검사 → 검증② AI 품질점검 →
  교수자 최종 판단`의 세 층과 각 판단 지위를 배치했다.
- HSK lexical audit를 코어·미션 생성 프롬프트와 AI 품질점검 사이의 결정론적 검사로 노출했다.
  어휘 추출→PRAGMA 수준별 HSK 누적 headword exact match→결과 저장 흐름, 비차단 상태,
  확인하는 것과 판정하지 않는 것을 분리했다. exact match는 계산 방식이며 whitelist·수준 인증·
  자동 교체 판정이 아님을 화면 안에 명시했다.
- 자연어 프롬프트 원문·hash·지문 대조와 legacy 보관함은 보존했다. 현재 화면을 밀어내던 과거
  프롬프트 지문 9종·844건은 합계를 먼저 표시하고 세부 이력을 접는 방식으로 압축했다.
- `/admin/cross-vendor`의 `아직 실행하지 않았습니다` 상태를 실제 2026-08-10 Claude Opus 5
  파일럿 증거와 정렬했다. API 6/6, fail 4·warning 2, 2차 AI 정본 대조의 계약 근거 확인 5/6,
  비용 $0.6740과 행별 6건을 표시하되 최종 연구자 판정은 전부 pending으로 남겼다.
- 독립 검토 화면은 목적 표집·구세대 `core_v8`·전건 `needs_review`·OpenAI 기준선 미실행을
  결과 수치 바로 아래에 표시한다. 따라서 불일치율·정확도·신뢰도·현행 콘텐츠 결함률을 계산하거나
  주장하지 않으며, DB·콘텐츠 상태 변경과 실행 버튼도 두지 않았다.
- 검증: 전체 Vitest 316 pass·8 skip, typecheck, 변경 관리자 파일 ESLint, production build
  1,910 modules, `git diff --check`를 통과했다. localhost의 두 화면을 새 브라우저 탭에서 다시
  열어 콘솔 오류 0건을 확인했다.
- 최종 변경은 검증 완료 뒤 `codex/hsk3-reference-system` 브랜치에 커밋·푸시해 보존한다.
  운영 배포는 생성·DB·Edge 변경이 없는 관리자 UI 작업이지만, 기존 main·Railway 배포 승인
  게이트를 유지해 이번 wrap-up 범위에서는 수행하지 않는다.

## AI 생성 콘텐츠 품질관리 용어·화면 귀속 정리 (2026-08-11)

- 사이드바와 `/admin/prompt-harness` H1을 `AI 생성 콘텐츠 품질관리`로 통일했다. 상단 구조는
  `규칙 기반 검사 → 프롬프트 통제 기반 검토 → 교수자 검수·승인`으로 바꾸고, `생성·검증 하네스`,
  `결정론적 검사`, `AI 품질점검`, `사람 중심` 표기를 제거했다.
- 모델에 실제로 전송되는 지시문·모델 설정·출력 형식·버전·지문 영역은 `프롬프트·지문 관리`로
  명명했다. 프롬프트 목록의 검토 그룹과 두 항목도 `프롬프트 통제 기반 검토`, `코어 설계축 검토`로
  맞췄다. 내부 라우트 `prompt-harness`와 감사 기록 필드명은 변경하지 않았다.
- HSK 점검의 상세 박스를 `/admin/prompt-harness`에서 `/admin/corpus`로 옮겼다. corpus는 입력,
  DB 표제어 정확 일치 계산, 결과·정책 버전 저장과 교수자 검수 연결을 표시한다. 이 검사가 콘텐츠
  전체의 난이도가 너무 쉽거나 어려운지, 목록 밖 어휘가 부적절한지, 교체·재생성이 필요한지를
  판정하지 않는다고 명시했다.
- 최근 저장 콘텐츠 한 건의 49/35/14에서 14개는 `교수자 검수 후보`로 표시하고, 후보가 곧 수정
  필요를 뜻하지 않으며 고유명사·전문용어·분절 결과일 수 있음을 결과 바로 아래에 표시했다.
- `최근 콘텐츠`라는 추상 표제와 35/49 비율 막대만으로는 점검 대상과 수치의 의미를 알기 어렵다는
  피드백을 반영했다. 새 쿼리나 테이블을 만들지 않고 기존 `scenarios` 조회에 이미 저장된 제목·화행·
  수준·모드·언어 방향을 포함했다. 실제 사례를 `처음 만난 조원에게 감사 인사 전하는 글`·감사·
  PRAGMA 입문·번역·한→중·학습 미션 1건으로 먼저 식별한 뒤, `1건 → 문장부호·공백 제외 및 중복
  제거한 중국어 단어 49개 → HSK 목록 정확 일치 35개 + 교수자 검수 후보 14개`로 풀어 썼다.
- 35/49 막대와 71%는 합격률처럼 오독될 수 있어 제거했다. 35개는 목록 조회로 확인된 단어이며
  콘텐츠 전체의 난이도 합격을 뜻하지 않고, 14개도 실패가 아니라 검수 범위를 좁힌 후보임을
  결과 안에서 설명했다.
- localhost 실데이터에서 위 사례 제목·다섯 조건·1→49→35/14 서사, 상세 점검 방식과
  prompt-harness의 세 권한 층을 확인했다. 두 화면의 브라우저 오류는 0건이었다.
- 추가한 저장 축 보존 검사를 포함해 전체 Vitest 317 pass·8 skip(HSK 표적 13/13), typecheck,
  변경 화면·스냅샷 파일 ESLint, `git diff --check`, production build 1,910 modules가 통과했다. build의 prebuild가
  프롬프트 스냅샷 18종을 다시 생성해 표시 메타데이터와 원본 스크립트의 일치도 확인했다.
- 사이드바의 축약형 `AI 독립 검토`는 화면 H1·논문 용어와 같은 `AI 모델 간 독립 검토`로
  통일했다. HSK 점검 기록이 충분히 누적되면 현재 단건 사례와 분리된 수준·모드·언어 방향·화행별
  `누적 점검 기록 통계`를 후속 구현한다.
- 최종 변경은 검증 완료 뒤 `codex/hsk3-reference-system` 브랜치에 커밋·푸시해 보존한다.
  운영 배포는 생성·DB·Edge 변경이 없는 관리자 UI 작업이지만, 기존 main·Railway 배포 승인
  게이트를 유지해 이번 wrap-up 범위에서는 수행하지 않는다.
