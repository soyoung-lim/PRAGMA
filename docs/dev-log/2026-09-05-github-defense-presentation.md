# 2026-09-05 GitHub 심사용 소개와 공개 예시 진입

## 범위와 결정

- 단독 진행 적합: 공개 번들 예시 연결·소개 정비. 실제 학습 경로의 Auth/RLS, 생성계약, DB·Edge는 변경하지 않는다.
- 사용자는 심사위원의 첫 5분에 제품·기술 구조·검증 근거가 보이도록 요청했고, README 축약과 심사용 Release 준비를 수용했다. Zenodo는 공개 판본 확정 뒤 진행한다.
- /demo/mission이 RequireApproved를 거쳐 Google 로그인으로 이동하는 것을 운영 브라우저에서 확인했다.
- PublicMissionDemo는 기존 CanonicalMissionRunner와 CANONICAL_MISSION_PREVIEW만 사용한다. runtime·scenarioId를 전달하지 않아 DB 미션 조회·외부 AI 피드백·서버 수행 저장 경로가 열리지 않는다. 예시 피드백과 서버 비저장을 화면에 표시한다.
- 기존 로그인·교수자 승인 경로와 실제 수업 데이터는 유지한다. 홈페이지·아키텍처의 공개 체험 링크는 배포 환경 플래그와 분리한다.
- README는 소개·핵심 기능·기술 구성도·CI 근거로 축약했다. 날짜가 있는 main CI 근거를 연결하고 설치된 도구를 전체 E2E 검증처럼 제시하지 않는다.
- 아키텍처에서 계획/실적 구분이 없던 504개·45개와 실제 수업 운영 배지를 제거하고, 한 조건 변경 단계를 같은 화행의 다음 미션 수행으로 정렬했다.
- CLAUDE.md의 현행 MPJ4 안내와 삭제된 인수인계 경로를 정정했다. DEPLOY.md의 staging·학생 수 계획은 역사 기록임을 표시했다.

## 검증

- PublicMissionDemo 경계 테스트 및 기존 실행기 테스트 7개 통과, typecheck 통과.
- localhost 비로그인 브라우저에서 예시 장면·단서·수행 안내를 확인했다.
- 전체 테스트·운영 빌드·공개 main/배포 반영 결과는 PR 및 후속 기록에 연결한다.

## 기록 경계

- DEC-20260905-03에 공개 예시와 실제 수업의 분리를 기록한다.
- 공개 역사 메모의 문헌 선정 표현은 연구자의 원문 검토가 필요한 항목이다. 이번 변경에서 과거 이력을 다시 쓰지 않는다.
- 연구설계·프롬프트 동결본 변경 없음. 공개 체험은 실제 학습자 데이터 또는 전문가 타당화 증거가 아니다.
## 최종 로컬 검증

- 전체 728 tests 통과·9 skip(123파일 통과). typecheck·production build 1,974 modules 통과.
- 아키텍처 4개 뷰포트 카드·가로 넘침 0, 순환선 간격 4px. 1440×900 @2x 재캡처 육안 확인.
- 공개 예시 브라우저에서 도입 3단계 후 MJT 첫 판단 화면 진입 확인.
- 기존 CSS minify 경고 유지. 새 프롬프트·생성계약·운영 DB 변경 없음.

## 사용자 요청에 따른 최소 변경으로 축소

- PR #75 병합 뒤 사용자가 기존 버전을 최대한 유지하고 최소 변경만 하도록 요청했다.
- README는 PR #74 직후의 배너·스크린샷·설명 순서·상세 기술 스택을 복원했다. MJT3 선택 수와 피드백 이견 시 최초안 유지 설명 두 곳만 정정하고 이미지 캐시 쿼리를 갱신한다.
- 공개 예시 페이지·테스트·기술 안내 신설을 철회하고 App·Landing·CanonicalMissionRun은 PR #74 직후 코드로 복원했다. 기존 인증·데모 조건을 유지한다.
- 아키텍처의 카드·순환선 넘침과 준거 사례 표기는 PR #74 수정을 유지한다. 504개·45개는 목표·계획임을 표시하고, 상황 바꿔보기 설명만 현행 다음 미션 구조에 맞춘다.
- CLAUDE.md의 현행 문항 수·정본 경로 정정과 DEPLOY.md의 역사 기록 표시는 유지한다. Release·태그·Zenodo DOI는 생성하지 않았다.
- 앞선 728개 테스트와 공개 예시 확인은 PR #75의 역사적 검증이며, 축소한 현행 범위의 검증으로 대체하지 않는다. 공개 체험 결정 DEC-20260905-03은 철회 상태로 기록한다.
- 축소판 검증: 전체 727 tests 통과·9 skip, typecheck·production build(1,973 modules) 통과. App·Landing·CanonicalMissionRun이 PR #74 직후와 동일함을 diff로 확인했다.
- 이미지: 4개 뷰포트 카드·가로 넘침 0, 순환선 간격 4px. 1440×900 @2x, fullPage:false 재캡처와 육안 확인을 완료했다.

## README 기술 스택 표 가독성

- 사용자 캡처에서 좁은 첫 열 때문에 영역 이름이 두 줄로 분리되는 문제를 확인했다. 기술 스택 표를 첫 열 너비가 지정된 HTML 표로 바꾸고 AI 검토 흐름에 명시적 줄바꿈을 넣었다. 기술명·버전은 유지한다.
- 사용자 요청으로 README의 개발 현황 제목과 표 전체를 삭제했다. 앱 코드·연구설계·프롬프트·계약 변경은 없다.
- diff check 통과. GitHub 렌더링은 PR에서 확인하며, 문서 표시 변경이므로 별도 테스트를 추가하지 않는다. 연구 기록 갱신 대상은 아니다.
