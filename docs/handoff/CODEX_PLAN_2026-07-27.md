# PRAGMA 개발 계획 — 2026-07-27 확정 (Codex 실행용)

> **3자 분업**: Codex = `codex-0727` 코드·독립 커밋. 7/27 사용자 추가 승인으로 이 브랜치 push는 허용하되,
> 배포·db push·admin 로그인은 계속 **금지** ·
> 사용자 = 실행·승인·결정(admin 몫 전부) · Claude = 수요일(7/29) 복귀 후 검토·병합·배포.
> **마감(협상 불가)**: 7/29 코어 프롬프트 동결 → 7/29~30 500 배치(**7/30 초과 금지**) → 7/31 완성.
> 관련 정본: `CODEX_HANDOFF_2026-07-26.md`(환경·브랜치 규칙) · `CODEX_LOUNGE_BRIEF_2026-07-27.md`(라운지 스펙).

## 🗺️ Big Plan — 5 Steps

| Step | 날짜 | 한 줄 | 주인공 |
|---|---|---|---|
| **1. 실측 검증** | 7/27(월) | 20 스모크 + 100 분포 검산 + DB 저장왕복 — 동결 근거를 실측으로 확보 | 사용자(실행) + Codex(분석) |
| **2. 수정·준비 + 라운지 스켈레톤** | 7/28(화) | 스모크 결함 수정(**코어 프롬프트 수정 마지막 창구**) + 배치 리허설 + 라운지 UI 스켈레톤(목업) | Codex + 사용자(결정 2건) |
| **3. 동결** | 7/29(수) | Claude 복귀 → Codex 커밋 검토·병합·배포 → **코어 프롬프트 동결 선언** | Claude |
| **4. 500 배치** | 7/29 밤~7/30 | 본 배치(1~2시간) + 감사표 + 분포·지문 검산 | 사용자(admin) |
| **5. 완성 판정** | 7/31(금) | 완료 기준(0-g·46) 실측 검수 → 마감 선언, 8월 순서 확정 | Claude + 사용자 |

핵심 논리: 1→2가 동결의 근거를 만들고, 3이 동결하고, 4는 동결된 것만 찍어내고, 5는 확인만 한다.

---

## Step 1 — 실측 검증 (7/27)

| 작업 | 담당 | 완료 기준 |
|---|---|---|
| ① 20건 스모크 (`md file\PRAGMA_스모크_체크리스트_2026-07-27.md`) | **사용자**(admin) | 전 항목 통과 or 결함 목록화 |
| ② 100 분포 검산 | 사용자 실행 → **Codex** 분석 | 243셀 회전·수준·화행 분포 정상 |
| ③ DB 저장왕복 — 실 로그인 미션 1회 완주 → `learner_mission_logs`·`context_judgment` 기록 확인 | **사용자** + Codex(확인 쿼리 작성) | 로그 행 + jsonb 내용 정상 |
| ④ 소량 배치 리허설(20) — 멱등키·`prompt_snapshot_hash` 확인 | 사용자 + Codex | 해시 전건 동일 · 재실행 중복 0 |

⚠️ 고P 검사는 **정적 감사 3종 이미 통과**(7/26 밤, 코어 프롬프트 깨끗) — 재작업 금지. 산출물 감사는 미션 경로 복구 후(A+C안).
⚠️ 배치 실패 메시지 `permission denied for function save_generated_core` = **세션 만료**(코드 결함 아님) → 재로그인.

## Step 2 — 수정·준비 + 라운지 스켈레톤 (7/28)

**우선순위 A (코어 경로 — 항상 먼저)**

| 작업 | 담당 | 비고 |
|---|---|---|
| ① 스모크 결함 수정 | **Codex** | **코어 프롬프트 수정은 이날이 마지막.** 프롬프트 변경 커밋은 메시지에 `core-prompt` 명시(수요일 해시 재확인용) |
| ② 500 배치 운영 절차서 1장 | Codex | 직전 재로그인 · 실패 급증 시 중단→재로그인→재실행(멱등 안전) |
| ③ 🟠 결정 1: 수준별 통역 비율 GATE | **사용자** | 선택지 C(수준별 상한) 권고 · GPT 교차검증 권장. **판정 전 코드 변경 금지.** 편성기 기본값 사안이라 **500 배치를 막지 않음** — 못 정하면 8월 이월 |
| ④ 결정 2: 화용 초점 정의 6화행 | **사용자**(R14 사람 작성 정본) | 미션 승격용, **500 코어에는 불필요** → 마감 리스크 아님, 8월 이월 가능 |

**우선순위 B (A 완료 후 착수) — ☕ 라운지 UI 스켈레톤** *(2026-07-27 사용자 지시로 "여유 시"에서 정식 작업으로 승격. 지도교수 보고용 시연 가치)*

- **범위 = 신규 파일만 + 목업 데이터만 + DB 0** (frontend-first: 목업 → 사용자 눈검사 → 백엔드는 8월):
  1. 라우트 2개: `/learner/lounge`(허브) · `/learner/lounge/:corner` — `App.tsx`에 route 2줄 추가(기존 라우트 무변경)
  2. 허브 화면: 코너 3카드 + NEW/투표중 뱃지 + "미션과 무관, 기록되지 않아요" 문구 (스펙 = `CODEX_LOUNGE_BRIEF_2026-07-27.md` §1)
  3. 코너 3개 화면(하드코딩 샘플 각 1세트): 🎬 생생극장(유튜브 embed 1개 + 한 끗 1문항 + 통역 도전) · 😂 밈 배틀(밈 1 + 조건 카드 + 익명 캡션 목록 + 투표 UI, 전부 목업) · 💬 해독실(채팅 버블 5문항 + 눈치 체크, localStorage 완료 표시)
  4. 진입점: `LearnerHome` 하단 "☕ 라운지" 카드 1개 (**MissionRunV1 완료 화면 티저는 이번에 하지 않는다** — 동결 직전 코어 인접 파일 회피, 수요일 Claude가 병합 시 처리)
  5. 샘플 콘텐츠는 브리프 §7 백로그에서(脆皮打工人·520/666/233·精神状态良好 등) · §8 기각 목록 절대 준수(XP·리더보드·실시간 득표·자유입력 금지)
- 검증: `npm run typecheck` + 브라우저 E2E(허브→3코너 왕복, 콘솔 에러 0). 수준 분기는 v1 하드코딩(중급 고정)으로 충분.
- 커밋 분리: 라운지 커밋은 코어 수정 커밋과 섞지 말 것(수요일 선별 병합 대비). 필요 시 `codex-0727-lounge` 파생.

## Step 3 — 동결 (7/29 수, Claude 복귀)

1. `codex-0727` 전 커밋 선별 검토 → main 반영(문제 시 브랜치째 폐기 가능 구조 유지)
2. main push(자동배포) + edge 변경분 배포(`--use-api`) — 사용자 승인 하에
3. **동결 선언**: `/admin/prompt-harness` 배포본 해시 = 정본 해시 일치 확인 → 이후 코어 프롬프트 변경 = 계약 개정 사안
4. 라운지: 스켈레톤 병합 + 완료 화면 티저 1줄 추가 + untracked 문서 2건 커밋 정리
- **동결 조건**: Step 1 전 항목 해소. 미해결 항목은 "그것만 빼고 동결 가능한가"를 Claude가 판정(동결 자체를 미루지 않는다).

## Step 4 — 500 본 배치 (7/29 밤~7/30, 사용자)

- 배치 **직전 재로그인** → 실행(1~2시간) → 실패 건수 모니터링(급증 = 세션 만료 → 중단·재로그인·재실행)
- 배치 후 검산(Claude): ①500건 해시 전건 정본 일치 ②243셀 커버리지·분포 ③대시보드 5계수 ④감사표
- ⚠️ **7/30 초과 금지**(0-g·46)

## Step 5 — 완성 판정 (7/31)

1. 완료 기준(0-g·46) 항목별 실측 판정 — 과장 금지, 안 되는 것 구분 명시(보고 원칙)
2. 잔여 지위 확정: 기록 탭 · 관리자 예외 큐 · 표현 고착도 감사 → "완료/8월 이월" 명시
3. 8월 순서 확정: 라운지 백엔드(해독실→생생극장→밈배틀, DB·RLS) · 미션 승격 경로(화용 초점 6화행 후) · 통역 비율 GATE 반영

---

**전략 포인트**: ①코어 프롬프트 수정 창구 = 7/28로 고정(동결 미끄러짐 방지) ②사용자 결정 2건은 마감을 막지 않음(병목 아님) ③Codex는 격리 브랜치 — 사고 시 브랜치 폐기로 복구 ④라운지 스켈레톤은 신규 파일만이라 코어와 충돌 0, 지도교수 시연 가치 확보.
