# 중→한 30셀 코어 파일럿 실행 증거

## 실행 경계

- 날짜: 2026-08-05
- 계획: 중→한 30셀(핵심 3화행 18셀 + 확장 6화행 중급 12셀)
- 실행 ID: `core_zh_ko_1785926368500`
- 허용 범위: 코어 생성(C1), 저장된 코어의 AI 비평(C2), DB 저장·계보 확인
- 금지 유지: 미션 조립, `reviewed` 승격, 수업 편성, 배포, 기존 행 수정·삭제
- 실행 전 계획: `docs/operations/ZH_KO_30_CELL_PILOT_EVALUATION_PLAN_2026-08-05.md`

## 실행 시각과 중단

- 실행 직전 기록: `2026-08-05T10:40:45.097Z`
- 반복 길이 오류를 확인하고 중단 명령: `2026-08-05T10:41:52.486Z`
- 진행 중 호출 정착 완료: `2026-08-05T10:42:03.700Z`
- 중단 판단 시 화면 관찰값: 14/30 시도, 성공 11, 실패 3
- 중단 명령 뒤 이미 진행 중이던 호출까지 정착한 최종값: 22/30 시도, 저장 성공 17,
  규칙 실패 5, 미시도 8

중단 사유는 동일한 `effective_chars_v1` 원문 길이 규칙 위반이 세 셀에서 반복된 것이다.
이는 실행 전 계획 §6의 동일 계열 오류 반복 중지 기준을 적용한 결과다. 중단 명령이 전달되는
동안 동시 실행 풀에서 진행 중이던 호출은 완료됐으며, 새 큐 진행은 22번째 셀 뒤 멈췄다.

## C1 코어 규칙 결과

| 화행 | 수준 | 모드 | 도메인 | 요구 범위 | 실측 | 결과 |
|---|---|---|---|---:|---:|---|
| 거절 | 중급 | 통역 | 직장 | 40~60 | 70 | 너무 김, 저장 안 함 |
| 감사 | 중급 | 번역 | 일상 | 60~85 | 35 | 너무 짧음, 저장 안 함 |
| 요청 | 고급 | 번역 | 학업 | 80~110 | 59 | 너무 짧음, 저장 안 함 |
| 제안 | 중급 | 번역 | 학업 | 60~85 | 53 | 너무 짧음, 저장 안 함 |
| 제안 | 중급 | 통역 | 학업 | 40~60 | 39 | 너무 짧음, 저장 안 함 |

- C1 저장 성공률은 **17/22**다. 계획 30을 분모로 바꾸지 않는다.
- 단계 이탈은 규칙 실패 5셀과 중단 뒤 미시도 8셀로 따로 기록한다.
- 실패 5건은 모두 `source_text` 유효 글자 길이 위반이다. 언어 방향·화행·스키마 오류는
  화면 실패 목록에서 관찰되지 않았다.

## C2 AI 코어 비평

- 대상: C1을 통과해 이번 세션에 저장된 코어 17건
- 결과: `pass 17`, `warning 0`, `fail 0`, `호출 실패 0`
- 이 비평은 화면의 설명대로 감사 표시 전용이며 DB 저장·승격 게이트가 아니다.
- C2 17/17 pass를 사람 사용 가능 17/17로 해석하지 않는다. 생성기와 비평기의 공유 오분류
  가능성 때문에 H0 사람 판정은 별도로 남긴다.

## AI 보조 H0 사전검토 · 사람 판정 아님

관리자 `통합 검수·승인 > 코어 상태 확인`에서 이번 run의 17건을 모두 펼쳐 상황문, 중국어
원문, 선행 발화, P·D·R, 역할쌍, 길이 정책을 대조했다. 아래 분류는 박사님의 H0 판단을 돕는
AI 권고일 뿐이며 `core_human_verdict`가 아니다. 화면 순번은 생성 시각 역순이고, 계획 셀은
`generation_item_key`의 0-based index를 1-based로 바꾼 값이다.

| 화면 | 계획 셀 | 화행·수준·모드 | AI 권고 | 핵심 근거 |
|---:|---:|---|---|---|
| 1 | 20 | 사과·중급·통역 | 잠정 가능 | 서버 장애 인정–사과–수리 계획이 상황·화행·역할과 일치하고 중국어도 자연스럽다. |
| 2 | 17 | 감사·고급·번역 | 보류 | `珍惜您的指导` 결합이 어색하고, 감사문에 향후 도움 요청이 섞여 중심 화행이 번진다. |
| 3 | 19 | 사과·중급·번역 | 잠정 가능 | 납기 3일 지연, 불편 인정, 조정 계획이 동등한 거래처 사과 상황과 일치한다. |
| 4 | 18 | 감사·고급·통역 | 보류 | 처음 만나는 참여자에게 이미 핵심 기여를 했다고 감사하는 시간 관계가 불명확하고, 구어치고 감사 표현이 반복적·격식적이다. |
| 5 | 15 | 거절·고급·번역 | 기각 권고 | P·D·R은 낮은 부담인데 상황은 추가 업무 부담이 매우 크다고 서술하며, `合作提议`도 실제 추가 업무 요청보다 넓다. |
| 6 | 16 | 거절·고급·통역 | 기각 권고 | 콘텐츠 2차 사용 요청에 갑자기 공동개발 `源代码`를 거절 근거로 넣고, 학습자·중국어 화자 A·통역사의 역할이 중첩된다. |
| 7 | 14 | 요청·고급·통역 | 보류 | `课程负责人`가 학생 조의 보고서를 위해 `我们小组` 토론 참여를 요청해 책임자와 조원 역할 경계가 불명확하다. |
| 8 | 12 | 감사·중급·통역 | 기각 권고 | 상황문은 지금 ‘도움을 구하는’ 요청으로 설명하지만 원문은 이미 받은 데이터 정리에 감사한다. |
| 9 | 9 | 거절·중급·번역 | 잠정 가능 | 선행 요청, 자료수집 역할 거절, 일정 사유와 대안이 조장–조원 관계에 맞는다. |
| 10 | 7 | 요청·중급·번역 | 기각 권고 | 상황문은 학습자 자신이 만든 콘텐츠처럼 읽히지만 원문은 상대가 만든 콘텐츠(`您之前制作的内容`) 사용 허락을 요청한다. |
| 11 | 8 | 요청·중급·통역 | 잠정 가능 | 처음 만난 동등한 학생에게 데이터 분석 참여를 요청하며 선택권을 남겨 상황·P·D·R과 일치한다. |
| 12 | 6 | 감사·입문·통역 | 잠정 가능 | 선물과 축하에 대한 짧은 감사가 선후배 관계·구어 모드·입문 수준에 맞는다. |
| 13 | 4 | 거절·입문·통역 | 보류 | `承担那个角色`은 `担任那个角色` 또는 `承担那项任务`보다 어색하고, 상위 조교에게 쓰는 구어 화역도 다소 직접적이다. |
| 14 | 5 | 감사·입문·번역 | 보류 | 버디 관계에서 `合作`이 지나치게 업무적이며 감사문 안에 새 조언 요청이 섞인다. |
| 15 | 2 | 요청·입문·통역 | 기각 권고 | 에어컨 고장과 ‘조용한 휴식 공간’의 인과가 맞지 않고, 일반 투숙객–호텔 관리자를 지인 관계로 둔 장면도 부자연스럽다. |
| 16 | 3 | 거절·입문·번역 | 보류 | 역할 분담 요청을 `邀请`으로 받아 표현해 선행 화행과 어휘가 어긋나지만 거절 대상과 대안은 식별된다. |
| 17 | 1 | 요청·입문·번역 | 보류 | `请您能不能稍微注意一下`은 `能不能请您…` 또는 `请您…`보다 어색하며 상황문의 친숙도 표현도 미세하게 다르다. |

AI 보조 집계는 **잠정 가능 5, 보류 7, 기각 권고 5**다. 핵심은 C2의 `pass 17/17`과
상당한 불일치가 관찰됐다는 점이다. 다만 중국어 모어 화역과 연구 구인에 대한 최종 판정은
박사님이 각 행을 확인한 뒤 확정한다. 현재 DB 상태는 변경하지 않았다.

## 사용자 기본판정 채택 · 독립 H0로 산입하지 않음

2026-08-05 사용자는 항목별 브라우저 검토를 생략하고 “기본 판정으로 간다”고 결정했다.
이 결정에는 외부 AI 검토에서 제안한 계획 셀 #18의 보류→기각 조정도 포함한다.

- 조립 후보로 채택: **5건** — 계획 셀 `#20, #19, #9, #8, #6`
- 보류로 채택: **6건** — 계획 셀 `#17, #14, #4, #5, #3, #1`
- 기각으로 채택: **6건** — 계획 셀 `#18, #15, #16, #12, #7, #2`

이는 사용자가 AI 보조 분류를 **운영상 채택**한 결정이다. 사용자가 17개 원문을 독립적으로
내용 검토한 것은 아니므로 `independent_human_review=완료` 또는 교차 벤더 검토로 기록하지
않는다. 논문에서는 `사용자 채택 AI 보조 선별`로만 기술하며, C2와 독립적인 인간 적격률로
계산하지 않는다. 다섯 건도 자동 미션 조립·`reviewed` 승격 대상이 아니라, 호출 원장 확인과
10축 정적 실현 가능성 점검을 거칠 **운영 후보**다. DB 상태는 변경하지 않았다.

## DB·계보 확인

- 관리자 `학습 미션 조립` 화면에서 실행 ID를 필터링해 **17개** 저장 행을 확인했다.
- 관리자 `통합 검수·승인 > 코어 상태 확인`에서 같은 실행 ID의 **조건 결과 17건**을 확인했다.
- 17건은 모두 중→한 코어, `review_status=needs_review`이며 미션은 생성되지 않았다.
- 코어의 후보 릴리스: `pragma_content_candidate_20260804_03`
- 코어 prompt hash:
  `8e9b7ec87869d4c06093dfac96e911eec12ff4d30fb52a3249e48d56a3e09ab9`
- 2026-08-05 20:33~20:34 KST에 Supabase SQL Editor에서 `SELECT`만 실행해
  `public.llm_invocation_events`를 직접 대조했다. 코어는 실행 ID로, 비평은 같은 실행 ID의
  `scenario_id` 17개와 `operation='core_critic'`의 조인으로 집계했다.
- 원장 집계는 코어 이벤트 **35행**, 코어 고유 item **22개**, 저장 scenario **17개**,
  코어 비평 이벤트 **17행**이었다. 코어·비평 모두 HTTP 실패 0, 모델 fallback 0이었다.
- 호출 내역은 다음과 같다.

  | 단계 | attempt | 요청 모델 | 반환 모델 | 호출 | HTTP |
  |---|---:|---|---|---:|---|
  | `core_generate` | 1 | `gpt-4.1-mini` | `gpt-4.1-mini-2025-04-14` | 22 | 22건 모두 200·success |
  | `core_repair` | 2 | `gpt-4.1-mini` | `gpt-4.1-mini-2025-04-14` | 13 | 13건 모두 200·success |
  | `core_critic` | 1 | `gpt-4.1` | `gpt-4.1-2025-04-14` | 17 | 17건 모두 200·success |

  요청 별칭과 반환 문자열의 차이는 같은 모델 계열의 날짜 고정 snapshot 해석이며,
  `is_model_fallback=false`, `fallback_from=null`이었다. 이를 다른 모델로의 강등이나 fallback으로
  재분류하지 않는다. 코어 22개 item이 원장에 모두 대응해 호출 원장 누락은 관찰되지 않았다.

## 미시도 8셀 재개 · 2차 실행

사용자는 1차 실행의 길이 손실을 측정 결과로 유지한 채, 미시도 셀 `#23~#30`만 같은 실행
ID로 재개하는 것을 승인했다. 재개 대상은 초대·반대·칭찬·불만의 중급 번역/통역 8셀이며,
1차 실패 5셀은 다시 호출하지 않았다.

### 실행 계보 보완

재개 시점의 브라우저 저장소에는 1차 실행 ID가 남아 있지 않았고, 기존 선택 셀 실행 기능은
항상 새 실행 ID를 발급했다. 이 상태로 실행하면 한 파일럿의 1·2차 호출이 서로 다른 run으로
갈라진다. 따라서 관리자 화면에 다음 최소 기능을 추가한 뒤 재개했다.

- 현재 방향과 형식이 일치하는 기존 코어 실행 ID만 불러온다.
- 선택 셀을 현재 ID로 재개하거나 새 ID로 생성하는 동작을 분리한다.
- 현재 ID 재개 시 기존 저장 item을 읽어 중복 호출을 건너뛴다.
- 선택한 계획 셀의 원래 0-based item index를 유지해 `generation_item_key` 계보를 보존한다.

관련 구현은 `src/lib/pragma/coreRunIdentity.ts`,
`src/lib/pragma/coreRunIdentity.test.ts`, `src/pages/admin/AdminBatch.tsx`다.

### 2차 C1·C2 결과

- 대상: 계획 셀 `#23~#30` 8개
- C1 저장 성공: **7/8**
- C1 규칙 실패: **1/8** — `#27` 칭찬·중급·번역, `effective_chars_v1` 실측 47,
  요구 60~85, 저장 안 함
- C2 대상: 저장된 7건
- C2 결과: **pass 5, warning 0, fail 2, 호출 실패 0**
- C2 fail:
  - `#25` 반대·중급·번역: 선행 발화가 원문과 사실상 같은 반대 명제를 먼저 말해 인접쌍이
    성립하지 않음
  - `#26` 반대·중급·통역: 선행 발화와 원문이 같은 반대 입장을 반복해 화행·인접쌍이
    성립하지 않음

이번 C2는 명시적인 선행발화 반복 결함 2건을 포착했다. 이는 1차 저장 17건에서 관찰된
상황–원문 의미·역할·화역 결함을 전부 놓친 결과와 함께 해석해야 한다. C2의 유효성은 결함
유형에 따라 달랐으며, 자동 비평 pass를 사람 사용 가능으로 바꾸지 않는다.

### AI 보조 H0 사전 권고 · 미채택

2차 저장 7건에 대한 AI 보조 권고는 잠정 후보 4건(`#23, #24, #29, #30`), 보류 1건
(`#28`), 기각 2건(`#25, #26`)이다. `#28`은 상황문에서 학습자·화자·통역사 역할 경계가
불명확할 가능성이 있어 보류했다. 이 분류는 사용자가 아직 채택하지 않았고, 독립 사람 H0나
교차 벤더 검토가 아니다. DB 상태도 변경하지 않았다.

## 동일 run의 최종 원장 대조

2026-08-05 20:52 KST에 Supabase SQL Editor에서 같은 실행 ID의 최종 원장을 `SELECT`로
대조했다.

| 단계 | attempt | 요청 → 반환 모델 | 호출 | 실패 | fallback | HTTP |
|---|---:|---|---:|---:|---:|---|
| `core_generate` | 1 | `gpt-4.1-mini` → `gpt-4.1-mini-2025-04-14` | 30 | 0 | 0 | 30건 모두 200 |
| `core_repair` | 2 | `gpt-4.1-mini` → `gpt-4.1-mini-2025-04-14` | 15 | 0 | 0 | 15건 모두 200 |
| `core_critic` | 1 | `gpt-4.1` → `gpt-4.1-2025-04-14` | 24 | 0 | 0 | 24건 모두 200 |

- 계획 30셀 전부에 코어 최초 생성 호출이 대응해 미시도 셀은 0이다.
- C1 저장 scenario는 **24/30**, 최종 길이 실패는 **6/30**이다. 너무 짧음 5건, 너무 김
  1건이다.
- 같은 모델 repair는 15/30 item에서 호출됐다. HTTP 성공은 규칙 통과가 아니므로 repair
  호출률을 repair 성공률로 바꾸지 않는다.
- C2는 저장 24건을 대상으로 **pass 22, fail 2, warning 0, 호출 실패 0**이다.
- 코어·비평 호출의 HTTP 실패, 명시적 모델 fallback, 호출 원장 item 누락은 모두 0이다.
- DB에는 같은 실행 ID의 코어 24건만 있고 미션 생성·사람 검수 완료·상태 승격은 0이다.
- 릴리스와 prompt hash는 1차 실행과 동일한
  `pragma_content_candidate_20260804_03`,
  `8e9b7ec87869d4c06093dfac96e911eec12ff4d30fb52a3249e48d56a3e09ab9`다.

## 다음 게이트

1. 30셀 최초 시도와 호출 원장 대조는 완료했다. 길이 실패 6건은 측정 증거로 유지하며, 같은
   프롬프트의 확률적 재시도로 성공값을 덮어쓰지 않는다.
2. 1차 사용자 채택 운영 후보 5건과 2차 AI 잠정 후보 4건을 합치기 전에, 사용자가 2차 AI
   권고를 운영상 채택할지 별도로 결정한다. 채택해도 독립 사람 H0로 산입하지 않는다.
3. 조립 대상으로 정한 코어는 감사 v2의 10축 정적 실현 가능성 점검을 먼저 수행한다.
4. 미션 조립·오답 후보 생성·상태 승격은 각각 별도 승인 뒤 수행한다. 현재 결과는 ‘중→한
   30셀 코어 호출 완료’이며 ‘양방향 완성’이나 ‘54셀 검증’이 아니다.
