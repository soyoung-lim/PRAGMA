# FLAG · v55 `_02` Fable 교차검증 요청 대기

- 상태: `FLAGGED_NOT_SENT`
- 우선순위: 추가 프롬프트 수정·재생성 전
- 사유: Claude/Fable은 현재 논문 4.1 초안 집필 중이므로 즉시 요청하지 않는다.
- 대상 원본: `v55-interpreter-role-18-core-only.json`
- Codex 1차 판정: `HUMAN_REVIEW_6_ITEMS.md`

## 요청할 판정

1. K1의 `한국에서 온 교환학생`을 한국어 원발화자 표지로 인정할지, 국적과 언어를 분리해
   LV 보류/실패로 볼지 판정한다.
2. K8의 `당신=선배 멘토+통역사`와 Z5의 `학습자 A`/`당신 C` 분리를 각각 H1~H3 기준으로
   블라인드 판정한다.
3. K6의 사내 워크숍 추가, Z3의 향후 지원 요청 추가, Z9의 전달 누락·장비 조정 변경을 H6
   `통과/보류/실패`로 독립 판정한다.
4. v54/v55 자동 결과와 Codex 판정은 가리고 상황문·원문만 제시한다.

## 요청 시 함께 전달할 사실

- v55 `_02` 자동 전체 통과는 2/18이고 R16 실패는 15/18이다.
- `core_repair` 16회는 모두 `repair_v2`·동일 모델·success였지만 최종 콘텐츠 실패가 남았다.
- 콘텐츠 DB 저장·미션 생성·상태 승격은 0이다.
- 이 요청은 즉시 프롬프트 수정안을 고르는 것이 아니라 사람 판정 경계를 독립 검증하기 위한 것이다.
