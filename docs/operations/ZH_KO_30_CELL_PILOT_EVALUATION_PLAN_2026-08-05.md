# 중→한 30셀 혼합 파일럿 실행 전 판정 계획

- 확정일: 2026-08-05
- 상태: **첫 원격 생성 호출 전 고정**
- 관련 결정: `DEC-20260805-01`, `DEC-20260805-02`
- 성격: 내부 버전 기록이 있는 **실행 전 판정 계획**. 외부 등록소에 맡긴 정식 사전등록으로 부르지 않는다.
- 출처 정정: 첫 호출 전 논문 증거 `05_증거/PRAGMA_카탈로그_축구조_감사_v2_2026-08-03.md`
  (SHA256 `B2F30C1D0D22FFF94211E7FCF83A25042FD04FDD414708B621D419B5806B09FC`)를
  재확인해 Type A/B의 지위와 분모를 바로잡았다. 이 정정 전 원격 생성은 없었다.

## 1. 목적과 범위

이 파일럿은 중→한 콘텐츠를 곧바로 54셀 본배치로 확대하지 않고, 현재 생성·검수
워크플로우가 9화행에서 작동하는지 탐색적으로 점검한다. 학습 효과나 방향 간 우열을
검증하는 실험이 아니다.

고정 범위는 다음 30셀이다.

- 핵심 3화행(요청·거절·감사): 3수준 × 번역/통역 = 18셀
- 확장 6화행(사과·제안·초대·반대·칭찬·불만): 중급 × 번역/통역 = 12셀
- 방향: `zh_ko`만
- 셀당 계획 코어: 1건

이번 실행에서 하지 않는 것:

- 54셀 확대, ko_zh와 통과율 합산·우열 비교
- 프롬프트·모델·R규칙·오류 패턴 변경
- 생성물을 자동으로 `reviewed`로 승격
- 기존 콘텐츠 삭제·소급 수정, 학습자 배정, Railway 배포

## 2. 실행 직전 동결할 생성 조건

아래 값은 실행 버튼을 누르기 직전에 실제 코드·운영 Edge·호출 원장에서 다시 확인한다.
하나라도 다르면 실행을 멈추고 새 계획 버전 또는 변경 기록을 만든다. 서로 다른 조건의
결과는 같은 분모에 합치지 않는다.

| 항목 | 현재 저장소 기준 | 실행 시 기록 |
|---|---|---|
| content release | `pragma_content_candidate_20260804_03` | 실제 반환 ID |
| core prompt | `core_v8_learner_scene_v1` / `core_v8_learner_scene_v1_repair_v2` | 실제 prompt version·snapshot hash |
| mission prompt | `mission_v5_mpj4_minidiscourse_v5` 또는 `mission_v4_mpj4_dct1_context_v8` | 셀별 실제 값 |
| quality prompt | core `core_quality_v4`, mission `quality_v2` | 셀별 실제 값 |
| core model | 요청 `gpt-4.1-mini`, fallback 없음 | 반환 model·fallback 여부 |
| mission model | 요청 `gpt-4o`, fallback 없음 | 반환 model·fallback 여부 |
| critic model | 요청 `gpt-4.1`, fallback 없음 | 반환 model·fallback 여부 |
| 코드 | 실행 시 `main` HEAD | commit SHA |
| 운영 Edge | 실행 시 ACTIVE 버전 | version·bundle hash |
| 실행 식별자 | 신규 zh_ko 전용 run ID | run ID·시작/종료 시각 |

모델 fallback, 호출 원장 누락, content release 불일치, prompt hash 불일치가 한 건이라도 있으면
그 결과를 연구 분모에서 제외하고 원인을 먼저 확인한다. 실패를 성공으로 재분류하지 않는다.

## 3. 분석 단위와 분모

한 개의 통합 ‘성공률’을 만들지 않는다. 아래 단계를 각각 독립적으로 보고한다.

| 단계 | 분석 단위와 분모 | 기록값 |
|---|---|---|
| P0 계획 | 고정 계획 30셀 | 화행·수준·모드·plan index |
| C1 코어 생성 | 30셀 | 저장 성공/실패, R결과, repair, 오류 코드 |
| C2 코어 AI 점검 | 코어가 생성되어 점검 가능한 셀 | pass/warning/fail/호출 실패 |
| H0 코어 사람 선별 | 사람이 실제로 연 셀 | 적격/보류/기각 + 사유 |
| M1 미션 최초 성공 | H0 적격 코어 중 미션 조립 대상 | 1회차 규칙 non-fail 여부 |
| M3 미션 최종 성공 | 같은 대상, 최대 3회 | 3회 안 규칙 non-fail·저장 여부, 실제 시도 수 |
| Q 미션 AI 점검 | 규칙 non-fail 뒤 점검 응답이 있는 미션 | pass/warning/fail/호출 실패 |
| H 미션 사람 판정 | 사람이 실제로 검토한 generated 미션 | 사용 가능/보류/기각 + H1~H4 |
| R 상태 승격 | 사용자에게 별도로 승격 승인을 받은 미션 | reviewed 여부·검토자·시각 |

- 전송 오류에 대한 같은 요청의 backoff는 콘텐츠 생성 시도 수와 구분한다.
- M1과 M3을 함께 보고해 재시도로 가려진 최초 실패를 보존한다.
- 코어 실패로 미션을 조립하지 못한 셀은 미션 실패 분모에 억지로 넣지 않고 단계 이탈로 기록한다.
- 계획 수, 코어 생성 수, generated 수, 인간 사용 가능 수, reviewed 수를 서로 바꿔 쓰지 않는다.

## 4. 사람 판정 기준

### H0. 코어 적격

다음을 모두 만족하면 미션 조립 대상으로 삼는다.

1. 중국어 원문과 한국어 목표 방향이 맞고 언어 혼입이 없다.
2. 상황·역할·선행 발화·화행·P·D·R이 서로 모순되지 않는다.
3. 번역/통역 모드에 맞는 서면·구두 장면이며 원문이 해당 수준에서 수행 가능하다.
4. 중국어 원문이 자연스럽고 중심 명제·화행 의도가 식별된다.
5. 학습자 상황문이 평가 기준을 미리 답처럼 노출하지 않는다.

### H1~H4. 미션 사용 가능성

- **H1 불변항:** target·교정안·후보·추천안·참고안이 원문의 명제·의도·화행을 보존한다.
- **H2 대역 타당성:** 부적절성은 의미 손실이 아니라 해당 화용 초점의 과소·적정·과잉 차이다.
- **H3 한국어 화역:** 한국어 후보가 번역투·기계투가 아니며 상황에 맞는 자연스러운 화역이다.
- **H4 경계 사례:** 화용 지식 없이 제거할 수 있는 극단 후보가 판정 후보의 절반을 넘지 않는다.

최종 사람 판정:

- `사용 가능`: H1~H4 통과, 차단할 내용 결함 없음
- `보류`: 판단에 원어민·지도교수 확인이 필요하거나 경계가 확정되지 않음
- `기각`: H1/H2 위반, 화행·대역 오분류, 심각한 부자연성, 과업 불성립

`사용 가능` 판정도 자동 `reviewed` 승격 명령이 아니다. 상태 승격은 사용자가 별도로 결정한다.

## 5. 실행 전 탐색적 예상

논문 증거의 카탈로그 감사 v2는 `Type A/Type B`를 **서술적 분류**로 유지하되 안전성
분류로 쓰지 않으며, Type B의 과잉 실현과 조립 성공률 차이를 **미검증 가설**로 남긴다.
따라서 이를 새 화행 유형·새 구인·안전 보증으로 만들지 않고, 미션 조립 단계에서만 탐색적으로
기술한다. 현재 30셀의 고정 분모는 다음과 같다.

- Type B 18셀: 요청·거절 12셀 + 제안·초대(내부 키 `agreement`)·반대 6셀
- Type A 12셀: 감사 6셀 + 사과·칭찬·불만 6셀

실행 전에 고정하는 예상은 다음과 같다.

- E1. Type B의 미션 조립 성공률과 인간 사용 가능 비율은 Type A보다 높지 않을 것으로 예상한다.
- E2. 자동 규칙·AI 점검의 non-fail 비율은 인간 사용 가능 비율보다 높거나 같을 것으로 예상한다.
- E3. zh_ko 오류 힌트 수가 화행·방향별로 다르므로 ko_zh와의 통과율 차이를 방향 효과로 해석하지 않는다.

Type B 18셀과 Type A 12셀은 수준·화행·오류 힌트 구성이 다르다. 따라서 원자료 `n/N`과
사례를 기술할 뿐 유의성 검정, 인과 주장, 안전성 분류, ‘유형 검증’ 표현을 사용하지 않는다.
E1이 관찰되지 않아도 사후에 화행을 다른 집단으로 옮기거나 분모를 바꾸지 않는다. C1·C2·H0의
코어 결과만으로는 오답 대역 조립 가설인 E1을 평가하지 않는다.

## 6. 중지·진행 기준

다음 중 하나면 남은 원격 호출을 중지하고 보고한다.

- 코드·Edge·content release·prompt hash가 실행 계획과 불일치
- 요청 모델과 반환 모델 불일치 또는 fallback 발생
- 같은 스키마·언어 방향·화행 오류가 연속 2셀에서 반복
- 호출 원장 적재 실패, run/item 식별자 충돌, 저장 계보 불명
- 기존 `reviewed`·실험 잠금·학습 로그 참조 행에 영향을 줄 가능성 발견

30셀 코어 생성 완료가 미션 조립 자동 승인을 뜻하지 않는다. C1·C2·H0 결과를 먼저 보고한 뒤,
카탈로그 감사 v2 §4의 10축 정적 실현 가능성 점검(동일 명제·동일 화행 의도·실사용 가능한
경계 사례·gate1 예상·길이 비분리)을 거친 축만 별도 승인으로 미션 조립한다. 미션 조립 완료도
54셀 확대나 본배치 자동 승인을 뜻하지 않는다.

## 7. 결과 보고 형식

결과표는 최소한 다음 열을 가진다.

```text
plan_index, speech_act, level, mode, boundary_risk_group,
core_scenario_id, core_rule_result, core_quality_verdict, core_human_verdict,
mission_attempts, mission_rule_result, mission_quality_verdict,
h1, h2, h3, h4, human_usability, reviewed,
content_release_id, core_prompt_version, mission_prompt_version,
prompt_snapshot_hash, model_requested, model_returned, notes
```

보고서는 다음 순서로 작성한다.

1. 계획 30셀 대비 단계별 이탈 수
2. 화행·모드별 C1/M1/M3/Q/H 원자료 `n/N`
3. Type B 18셀과 Type A 12셀의 탐색적 기술 비교(미션 단계에서만)
4. 자동 non-fail이지만 인간 보류·기각인 불일치 사례
5. 54셀 확대·프롬프트 수정·오류 힌트 추가 여부에 대한 별도 후속 결정

## 8. 논문 서술 경계

- 3.3.5의 54셀은 ko_zh 전달 커버리지 체계로 유지한다.
- zh_ko 30셀은 **별도 범위의 탐색적 방향 검증**으로 서술한다.
- 방향을 54셀에 곱해 108셀이라고 쓰지 않는다.
- 규칙 통과율, AI 점검 결과, 인간 사용 가능률을 한 지표로 합치지 않는다.
- 결과가 나오기 전에는 ‘품질 확보’, ‘양방향 완성’, ‘가설 검증’이라고 쓰지 않는다.

## 9. 1차 실행 상태 · 2026-08-05

- 실행 ID `core_zh_ko_1785926368500`으로 코어 생성을 시작했다.
- 같은 `effective_chars_v1` 길이 위반이 반복되어 §6에 따라 중단했다.
- 최종 정착값은 계획 30셀 중 시도 22, C1 저장 성공 17, C1 규칙 실패 5, 미시도 8이다.
- 저장된 17건의 C2는 pass 17, warning 0, fail 0, 호출 실패 0이었다.
- DB 실행 ID 필터에서 17건, 후보 릴리스 `_03`, 현재 코어 지문 `8e9b7ec87869…`,
  `needs_review` 상태를 확인했다.
- 사용자는 항목별 직접 검토 대신 AI 보조 기본 분류를 운영상 채택했다(후보 5·보류 6·기각 6).
  따라서 이를 독립적인 H0 사람 검수 완료나 교차 벤더 검토로 산입하지 않는다.
- 원장 직접 조회에서 코어 고유 item 22개·이벤트 35행(최초 생성 22, 같은 모델 repair 13),
  저장 scenario 17개, 코어 비평 이벤트 17행을 확인했다. 요청 `gpt-4.1-mini`는 반환
  `gpt-4.1-mini-2025-04-14`, 요청 `gpt-4.1`은 반환 `gpt-4.1-2025-04-14`로 기록됐고,
  전 호출 HTTP 200·success, fallback 0이었다.
- 위 반환명은 요청 모델 계열의 날짜 고정 snapshot이며 모델 교체 fallback이 아니다. §6의
  ‘요청 모델과 반환 모델 불일치’는 다른 계열 반환이나 명시적 fallback을 중단 사유로 삼는다는
  뜻으로 사후 해석을 명시한다. 원문 기준을 바꾸거나 실패를 성공으로 재분류하지 않는다.
- 미션 조립은 아직 수행하지 않았다.
- 원자료는
  `docs/research-trail/evidence/2026-08-05-zh-ko-core-pilot.md`에 보존한다.

이 상태는 ‘30셀 완료’가 아니라 **중단 기준이 작동한 부분 실행**이다. 호출 원장 게이트는
통과했지만, 22개 중 13개가 repair를 사용하고도 최종 길이 실패 5개가 남았다. 남은 8셀이나
실패 5셀을 같은 계획으로 재개하려면 이 예상 손실을 측정 결과로 감수할지 별도 승인받는다.

## 10. 미시도 8셀 재개 결과 · 2026-08-05

- 사용자는 1차 길이 손실을 측정 결과로 유지한 채 미시도 `#23~#30`만 동일 실행 ID로
  재개하는 것을 승인했다. 1차 실패 5셀은 재시도하지 않았다.
- 브라우저 세션이 달라도 계보가 갈라지지 않도록 기존 run ID 불러오기와 ‘선택 셀·현재 ID
  재개’를 관리자 화면에 추가했다. 현재 방향과 ID 형식을 검사하고 원래 계획 item index를
  유지한다.
- 2차 8셀은 C1 저장 7, 길이 실패 1이었다. 새 실패는 `#27` 칭찬·중급·번역 47/60~85다.
- 2차 C2는 저장 7건 중 pass 5, fail 2, warning·호출 실패 0이었다. fail 2건(`#25`, `#26`)
  모두 선행 발화와 원문이 같은 반대 명제를 반복하는 인접쌍 결함이다.
- 동일 run의 최종 원장 집계는 최초 생성 30, 같은 모델 repair 15, 저장 scenario 24,
  코어 비평 24다. 모든 호출은 HTTP 200·success였고 fallback 0이다.
- 따라서 최종 C1은 저장 **24/30**, 길이 실패 **6/30**이며, C2는 **pass 22/24,
  fail 2/24**다. 계획 30셀은 모두 시도됐고 미시도는 0이다.
- 위 수치는 C1과 C2의 서로 다른 분모다. C2 22/24를 사람 사용 가능률로 표현하지 않는다.
- 2차 저장분의 AI 보조 권고는 잠정 후보 4·보류 1·기각 2였다. 그중 잠정 후보 4건을 내용만
  블라인드 제시한 뒤 Fable 교차 벤더 검토를 거쳐 사용자가 최종 판정했다. 결과는 `#29` 채택,
  `#23` 보류, `#24·#30` 기각이다. 사용자가 Fable 사유를 확인한 뒤 확정했으므로 사람 최종
  결정이지만 독립 블라인드 판정으로 쓰지 않는다.
- M1·M3·Q·H·R은 실행하지 않았다. 미션 조립 전에는 운영 후보 확정과 10축 정적 실현 가능성
  점검을 별도 게이트로 유지한다.

이 결과는 **중→한 30셀 코어 호출을 완료한 탐색적 파일럿**이다. 중→한 미션 9화행 완성,
한→중과의 통과율 비교, 54셀 방향 검증 또는 `reviewed` 승격을 뜻하지 않는다.

## 11. 10축 정적 실현 가능성 점검 · 2026-08-05

- 중→한 10개 target feature에 대해 동일 명제·동일 화행 의도를 유지한 과소·적정·과잉
  3안을 구성하고, Fable이 카탈로그 정본·2축 파일럿 5자 판정 종합·R5 코드·gate1 문구와
  대조했다.
- 교차검증 뒤 거절·감사·초대 문장을 수정하고, 사과·칭찬 대응을 보류에서 통과로,
  불만을 구조적 실패에서 예문 선택 오류로 교정했다. 칭찬하기의 3중 최상급도 두 겹으로
  줄였다.
- 최종 결과는 **10축 중 정적 통과 10, 보류 0, 실패 0**이다. 문서에 선언한 후보 30개의
  글자 수는 전부 실측과 일치했고, 3안 R5 시뮬레이션의 hard fail 예상은 0이다.
- 이 결과는 E1의 관찰값이 아니다. 실제 5후보 미션의 M1·M3·C2·H1~H4가 없으므로,
  Type A/B 조립 성공률이나 인간 사용 가능 비율을 계산하지 않는다.
- 1차 사용자 채택 운영 후보 5건은 정적 게이트를 통과했다. 2차 4건의 사람 최종 판정에서는
  `#29`만 채택됐고 `#23`은 보류, `#24·#30`은 기각됐다. 따라서 후속 탐색 조립 후보는 기존
  5건과 `#29`를 합친 6건이다. 기존 5건은 여전히 사용자 채택 AI 보조 선별이지 독립 H0가 아니다.
- 원자료는
  `docs/research-trail/evidence/2026-08-05-zh-ko-target-feature-static-feasibility.md`에 보존한다.
  미션 생성·DB 쓰기·프롬프트 변경·상태 승격은 수행하지 않았다.

## 12. 후속 4건 사람 최종 판정과 개발·테스트 콘텐츠 경계 · 2026-08-05

- 블라인드 A~D의 실제 키는 A=`#29`, B=`#24`, C=`#30`, D=`#23`이다.
- 사용자 최종 판정은 A 채택·B 기각·C 기각·D 보류다.
- B·C의 공통 기각 사유는 통역 화자와 학습자 통역사의 역할 중첩이다. 현재 자동 검사는
  두 언어와 `통역` 문자열 존재만 확인해 이 결함을 놓쳤고, C2도 두 건을 통과시켰다.
- D는 학습자 상황문에 `부담을 주지 않으면서도 정중하게`라는 목표 화용 기준이 노출돼 보류했다.
- 이 run뿐 아니라 현재 DB의 모든 생성 코어·미션은 조건 LOCK 전 개발·테스트 콘텐츠다.
  이 run의 조립 결과도 정식 생산 콘텐츠나 사람 사용 가능률로 해석하지 않는다.
- 현재 데이터의 수명과 LOCK 뒤 전량 재생산 절차는 `CONTENT_REFRESH_RUNBOOK.md`를 따른다.
  실제 삭제는 증거 보존과 참조 범위 확인 뒤 별도 사용자 승인으로 실행한다.

## 13. 확정 결함의 재발 방지 구현 · 2026-08-05

- B·C의 역할 중첩은 통역 장면을 `원발화자 A → 학습자 통역사 → 목표언어 청자 B`의 세
  참여자로 강제하는 생성·교정·R16 회귀 규칙으로 전환했다.
- D의 목표 화용 기준 노출은 학생용 상황문 검사와 R30으로 차단했다.
- 상황–원문 사건·행위자·대상 대응은 `scene_source_alignment` 품질점검 축으로 분리했다.
  이는 단순 문자열 일치가 아니며 최종 사람 H0를 대체하지 않는다.
- 새 버전은 `pragma_content_candidate_20260805_01`, `core_v9_interpreter_roles_v1`,
  `core_quality_v5`다. 이전 30셀 실행의 수치나 판정을 이 버전 결과로 소급 변경하지 않는다.
- 정적·회귀 검증은 완료했지만 새 Edge 실모델 표본은 별도 관찰값으로 기록한다.

## 14. 새 역할 가드의 무저장 운영 카나리 · 2026-08-05

- Edge v52 첫 6셀은 R검사 3/6이었다. 중→한 감사·통역의 정상 세 역할까지 특정 문구 부재로
  차단한 R16 과잉 규칙을 보정했다.
- v53 같은 6셀은 R검사 6/6이었지만, 통역 3건 중 2건은 사람이 읽으면 `학습자`가 청자/고객과
  통역사에 겹쳤다. 새 `participant_roles` 축도 세 건을 모두 pass로 읽었다.
- 따라서 R검사 6/6은 H0나 역할 계약 충족률이 아니다. A/B/C 블라인드 Fable 교차검증을
  다음 게이트로 두고, 그 전에는 추가 프롬프트·정규식 보정을 하지 않는다.
- 미션·콘텐츠 DB 저장·상태 승격은 수행하지 않았다. 현행 표본은 개발·테스트 증거다.
