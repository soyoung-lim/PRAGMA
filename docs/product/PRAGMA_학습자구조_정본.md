# PRAGMA 학습자 구조 정본

> 상태: 현재 정본. **이 문서는 구조·불변조건·완료 조건만 담는다.**
> 식별 규칙: 날짜 없는 이 경로만 현행이며, 날짜 판본은 `history/`의 역사 자료다.
> 테스트 수·배포 상태 같은 날짜 종속 사실은 `docs/handoff/ACTIVE_HANDOFF.md`가 정본이다.
> **마지막 코드 대조: 2026-08-17 · `mission_v6` 통합 작업트리** (미션 스키마·생성계약·학습자 실행 화면 대조)
> 델타 재대조 `35f0129`→`b2fde89`: 조언 문구 원칙을 §7.3 불변조건으로 확정
> (`08_작업관리\reviews\2026-08-06_정본3종_델타재대조_변경안.md`, 박사님 승인)
> 적용 범위: 학습자 정보구조, 수업·주차 흐름, 미션 수행, 학습 노트, 기록, 라운지, 연구윤리 경계
> 이전 문서: 2026-07-21 학습구조 문서는 결정 이력 보존용이며, 현재 동작과 충돌하면 이 문서가 우선한다.

## 1. 학습자 경험의 목적

PRAGMA의 학습자 화면은 검사 응시 화면이 아니라 수업과 연결된 학습 환경이다.

> 실제 상황에서 적절한 한·중 통번역 수행을 연습하고, 그 원리를 화용론적으로 이해하도록 돕는다.

정체성 기준(2026-08-06 확정, 정본 = 생성계약 §1 정체성 문장 2개): PRAGMA는 자유로운
외국어 화행 산출 도구가 아니라 **원문 제약 아래의 통번역 학습 워크플로우**다.
학습자 역할은 모드마다 다르다 — **번역 = 1인칭 발신자 관점(자기 발신 상황의 원문
제약형 번역), 통역 = 별도의 통역사**(원발화자 A와 청자 B 사이, P·D·R 준거는 A↔B).
이 비대칭은 의도된 설계다.

학습자에게는 상황과 Can-do 수행 목표를 먼저 보여준다.

**메타언어 노출 원칙 (2026-08-06 개정 — 「감춘다」에서 「시점을 통제한다」로):**

| 대상 | 규칙 |
|---|---|
| P·D·R 코드·라벨, 적절성 대역 코드, `target_feature` 정본명, 정답 방향 | **판단 전 노출 금지.** 상황문에서 추론하는 것이 과제이므로 노출하면 그 자체가 정답 단서가 된다(연구 타당도 사안) |
| 화행 명칭(요청·거절·감사…), 화용적 적절성 개념, 상황에 따라 표현이 달라지는 원리 | **명시적으로 가르친다.** 도입 아크·피드백·기록에서 학습자 언어로 노출한다 |

근거: 학습 설계가 이미 **귀납 후 명시**이고 도입 아크가 `장면 제시 → 차이 인식 → 원리
이해 → 적절성 판단`이다. 원리를 명시적으로 가르치기로 해 놓고 그 원리의 이름을 감추는
것은 모순이다. 주차 카드가 이미 「요청」·「감사」·「초대」로 화행을 보여주고 있으므로
범주는 노출하면서 명칭만 감추는 상태이기도 했다. 대상 학습자는 중급 통번역 수업
수강생이다.

**단, `목표 화용 요소`라는 용어 자체는 학습자 화면에 쓰지 않는다.** 그 자리는 카탈로그의
학습자 라벨(예: `부탁을 부드럽게 말하기`)이 이미 맡고 있고, 내용은 도입 아크에서 명시적으로
가르치므로 용어를 덧붙일 필요가 없다. 화행 명칭·화용 개념의 첫 소개는 화면이 아니라
**1주차 오리엔테이션과 수업 자료**에서 한다.

## 2. 최상위 정보구조

하단 메뉴는 3개다. **홈 탭은 폐지했다(2026-08-01)** — 수업 화면이 「이번 학습」 CTA와
9화행 지도를 갖게 되면서 홈에는 우회 링크만 남았고, 홈의 이월 조언이 이번 주 화행과
무관한 지난 화행을 나란히 보여 오해를 만들었기 때문이다. `/learner/home`은
`/learner/course`로 리다이렉트한다.

```text
수업 | 기록 | 라운지
```

| 메뉴 | 경로 | 역할 |
|---|---|---|
| 수업 | `/learner/course` | 학습자 착지 화면. 강좌·주차·배정 학습 진입 |
| 기록 | `/learner/records` | 학습자가 확인할 수 있는 수행 기록 |
| 라운지 | `/learner/lounge` | 미션과 분리된 저부담 쉬어가기 |

현재 원칙:

- 구 메뉴명 `과정`은 학습자 화면에서 `수업`으로 표시한다.
- `자유 연습` 준비중 타일을 표시하지 않는다.
- 학생이 500개 코어를 검색·추천받아 자율 선택하는 경로는 현재 논문·프로토타입 범위 밖이다.
- 셀프 학습 기능이 실제로 생기기 전에는 최상위 탭이나 자리표시자를 만들지 않는다.

## 3. 학습자 진입과 연구동의

### 3.1 학습 이용

- 승인된 학습자는 프로필 설정 후 수업 화면에 진입한다.
- 학습 이용과 연구 참여는 별개다.
- 연구 미동의 상태라도 동일한 학습 기회를 가져야 한다.

### 3.2 연구동의

- 프로필 온보딩에서 간단한 필수 연구동의 팝업을 받지 않는다.
- 기관 승인 설명문, 자료 범위, 보관·철회 절차가 준비된 별도 동의 흐름을 사용한다.
- 기존 동의 컬럼과 관리자 조회는 호환을 위해 남아 있을 수 있으나, 신규 온보딩은 동의 값을 학습 접근 게이트로 쓰지 않는다.
- 운영 로그가 있다는 사실만으로 연구 분석에 사용할 수 없다.

## 4. 수업·주차 구조

### 4.1 수업연계형 단일 경로

PRAGMA의 공식 학습 경로는 교강사가 편성·게시한 15주 수업이다.

```text
수업
→ 주차
→ Can-do와 예습 자료
→ 배정 미션
→ 복습 자료
→ 기록
```

- 공통 필수 미션은 주차의 핵심 수행이다.
- 선택 복습 미션이 있다면 교강사가 배정한 범위 안에서 제공한다.
- 학습자가 미검수 코어나 `generated` 미션을 직접 고르지 않는다.
- 학습자 실행 최소 조건은 `mission_status='reviewed'`다.

### 4.2 Can-do

Can-do는 상황·참여자·목적·수행·모드·상황 적절성 기준을 학습자 언어로 설명한다.

- 화행을 대체하지 않는다.
- 특정 완화 표현의 사용 자체를 성공 기준으로 고정하지 않는다.
- 입문·중급·고급을 CEFR·ACTFL 등급과 대응시키지 않는다.
- 교수자가 저장한 주차 Can-do를 우선 사용한다.
- 저장 Can-do가 없을 때만 결정론적 보조 가이드를 사용한다.

관련 원칙은 `docs/research/PRAGMA_CAN_DO_ALIGNMENT_PRINCIPLES_2026-07-28.md`를 따른다.

## 5. 주차 학습 노트

경로:

```text
/learner/course/week/:weekNo/note
```

학습 노트는 웹에서 읽고 브라우저 인쇄로 PDF 저장할 수 있다.

### 5.1 예습면

- 주차 Can-do
- 상황과 관계를 읽는 기준
- 일반적 화용 원리
- 미리 생각할 질문

### 5.2 복습면

- 활용 가능한 표현 자원
- 표현별 인상 비교
- 혼동하기 쉬운 판단 기준
- 예외·반례
- 자신의 표현에 대한 성찰

### 5.3 공개 규칙

```text
교수자 공개
OR
해당 주차 필수 미션 전체 완료
→ 복습면 공개
```

- 첫 제출이나 일부 미션 완료만으로 열지 않는다.
- 필수 미션이 없는 주차를 자동 완료로 간주하지 않는다.
- 해금 전 인쇄본에는 예습면만 포함한다.
- 해금 후 예습면과 복습면을 함께 포함한다.
- 노트 열람 자체를 학습 효과나 성취의 증거로 해석하지 않는다.

구현 메모:

- 학습 노트와 공개 제어 코드는 구현돼 있다.
- migration의 원격 적용 여부는 이 문서가 아니라 ACTIVE_HANDOFF·research-trail에서 확인한다.
  확인 없이 프로덕션 적용 완료라고 쓰지 않는다.

## 6. Full Mission 학습 흐름

학습자 정본 경로:

```text
/learner/practice
```

### 6.1 신규 Full Mission

신규 생성은 `mission_v6`만 사용한다. 과거 `mission_v1`~`mission_v5`의 의미는 바꾸지 않고 읽기 호환으로 보존한다.

```text
표현 감각 익히기
  MPJ1 Scale4: 이 상황에서 표현이 얼마나 적절한지 4점 판단
  → MPJ2 FixChoice: 상황과 상대에게 어떻게 들리는지 판단한 뒤 수정안 2개 선택
  → MPJ3 FixReview: 처음 쓴 문장을 고친 문장 3개를 비교하고, 이 상황에 여전히 어울리지 않는 문장 1개와 가장 큰 문제 1개 선택
  → MPJ4 MultiJudge: 과소·적정·과잉 후보 3개가 이 상황에서 어떻게 들리는지 각각 분류
독립 DCT1
  관계·부담을 확인한 뒤 새 사건을 번역 또는 통역
3층 피드백
  의미 / 문법·표현 / 목표 화용 특성
수정·재확인
  피드백을 반영해 문장을 고치거나, 고치지 않는 이유를 남겨 그대로 제출
  → 첫 수정본을 한 번 재확인
  → 필요하면 한 번 더 고친 뒤 종료
```

- MPJ 4개와 DCT 1회가 하나의 미션이다.
- 1부 완료는 미션 완료가 아니다.
- DCT 직전 맥락 확인은 채점하지 않고 완료 조건에도 넣지 않는다.
- 세 피드백 층을 모두 통과한 답안에는 수정을 강제하지 않는다.
- 수정이 필요한데 원문을 유지하려면 이유 또는 이미 남긴 이견이 필요하다.
- 공백·문장부호·기호만 바꾼 답은 수정으로 인정하지 않는다.
- 여러 층이 동시에 실패하면 한 번의 수정 목표는 `의미 전달 → 목표 화용 축 → 문법 정확성` 순으로 정하되, 세 층의 독립 판정과 피드백은 모두 보여 준다.
- 첫 수정본은 지정 목표 반영, 원래 의미 보존, 새 문제 발생 여부를 한 번만 확인한다. 부분 반영 또는 새 문제가 있으면 추가 수정 1회를 허용하되 두 번째 자동 재확인은 하지 않는다.
- MPJ의 비점수·수행 trace 성격은 스키마와 저장 데이터에만 보존한다. 학습자 화면에는 `참고 판정`, `비점수 활동`, `대역`, `trace` 같은 연구·분석 용어를 노출하지 않는다.
- 학생용 버튼은 `피드백을 반영해 문장 고치기`, `고치지 않고 그대로 제출하기`처럼 해야 할 행동을 직접 말한다.
- 선택지·후보 수는 수준별로 바꾸지 않는다. 난이도는 장면과 표현의 미묘함으로 조절한다.

### 6.2 legacy 호환

- 기존 `mission_v1/v2` MPJ5, `mission_v3/v4` MPJ4, 미니 담화 `mission_v5`는 계속 열 수 있다.
- 과거 행을 `mission_v6`로 소급 변환하거나 기존 스키마 버전의 의미를 바꾸지 않는다.
- 샘플·데모의 수행 방식 전환 단축은 실제 학습자의 완료 계약으로 일반화하지 않는다.

### 6.3 맥락과 전이

- P·D·R 코드는 학습자에게 그대로 노출하지 않는다.
- `역할/관계/부담` 같은 기계적 메타데이터 제목도 학습자에게 나열하지 않는다.
- 화행 명칭(요청·거절·감사…)은 노출한다 — 위 §1 메타언어 노출 원칙. 감추는 것은
  **판단의 단서가 되는 코드·라벨**이지 화용 개념 자체가 아니다.
- `목표 화용 요소`(코드의 `target_feature`)는 학습자 화면에서 용어로 쓰지 않고
  학습자 라벨로 표시한다. 화면 문구에 `화용 초점`·`목표 화용 축`을 쓰지 않는다.
- **번역 셀**의 상황문은 `나는 지금 누구에게 무엇을 하려 한다`는 1인칭 현재 장면으로 시작한다. 이어지는 문장에서 위계·권한, 접촉 이력·친밀도, 부탁이나 행위의 실제 부담이 자연스럽게 보여야 하며 문장마다 줄을 나눠 읽기 쉽게 표시한다.
- **통역 셀**의 상황문은 학습자를 통역사로 부르는 관점으로 서술한다. 원발화자 A의 1인칭(`저는`·`나는`) 서술과 학습자를 화행 당사자·청자로 만드는 서술은 금지다(생성계약 2026-08-06 절). 통역사가 판단하는 것은 자기 체면이 아니라 A↔B 관계에서 A의 화용적 힘을 등가하게 재현할 목표어 수단이다.
- `상대`에는 학습자가 마주한 상대의 역할·관계만 표시한다. 학습자 자신의 역할이나 `화자 → 상대` 도식은 함께 넣지 않는다.
- `첫인상 판단`은 소박한 규칙이 깨지는 적절한 대비 상황이다.
- `판단하고 고쳐보기`와 `교정본 검수`는 DCT와 같은 PDR의 서로 다른 사건이다.
- `여러 표현 분류`는 P·D·R 중 한 축만 바꾼 대비 사건이다.
- DCT는 같은 PDR의 새 사건으로 근접 전이를 연습한다.

주간 한 미션만으로 넓은 상황 전이를 달성했다고 표시하지 않는다. 학기 전체에서 PDR 조건을 순환하고 통합 과제 수행을 통해 별도로 확인한다.

## 7. 미션 화면과 스캐폴딩

### 7.1 상황 제시

- 관계와 장면은 자연어로 제시한다.
- P·D·R 원시 코드를 학습자 화면에 그대로 노출하지 않는다.
- 장면문은 상대·과업·접촉 이력·실제 부담처럼 학습자가 관찰할 수 있는 사실만 제시한다.
  기록 목적, 즉각 반응 요구 여부, 상대의 권리·의무·선택권, 필요한 완화·명료성처럼 답의
  방향을 알려 주는 평가 기준은 장면문에 노출하지 않는다.
- 상대 역할과 학습자와의 관계는 자연어 한 줄을 하나의 칩으로 보여 준다. `상대`와 `관계`를
  별도 태그로 쪼개 연구자용 분류표처럼 만들지 않는다.
- 상황 카드에는 `지금, 이 장면`이라는 진입 신호와 문장 위계를 두되, 별도의 역할·관계·부담 표를 만들지 않는다.
- 메신저 장면은 대화형, 이메일은 문서 초안형, 대면·전화는 발화형 시각 문법을 사용한다.
- 응답 화행에서 선행 발화가 필요하면 상대의 target-language 발화를 표시하되, 정답 표현을 미리 제공하지 않는다. 별도의 날짜 구분은 표시하지 않는다.
- UI용 `channel`은 연구·난이도 축이 아니라 장면을 정확히 표현하기 위한 메타다.
- 통역은 듣기·녹음·전사 확인의 오디오 프레임을 사용한다.
- 미션 상단은 target feature 정본명을 그대로 노출하지 않고 `화행 + 학습자가 수행할 행동 문장`으로 풀어 쓴다. 이 초점과 3단계 워크플로우는 사이트 헤더 아래에 고정해 긴 화면에서도 현재 위치를 유지한다.
- MPJ 4개 뒤 handoff는 `첫인상 판단 / 판단하고 고쳐보기 / 교정본 검수 / 여러 표현 분류`와 실제 응답에서 확인한 기준을 한 줄씩 정리한다. 다른 판단을 한 행은 이유를 수업에서 비교하도록 안내하되, 정답·오답 성적표는 만들지 않는다.
- 직접 번역·통역 단계는 SUMMARY 뒤에 상황과 산출 도구로 곧바로 진입하며, MPJ 내용을 일반 설명문으로 다시 반복하지 않는다.
- MPJ와 직접 표현 단계 모두 장면 카드와 실제 대화·산출 도구를 별도 패널로 분리한다. MPJ 대화 패널은 `표현 비교`, 판단 대상 말풍선은 `AI 번역 초안` 또는 `AI 통역 초안`으로 표시한다. 번역 화면의 단계명은 `번역하기`, 장면 신호는 `지금, 직접 번역할 장면`, 메신저 상태는 `메시지 작성 중`으로 표시한다. 장면 카드는 고정 최소 높이를 두지 않고 공통 패딩·행간·문장 간격을 사용해 내용에 맞는 높이로 표시한다.
- 한→중 번역에서 막힐 때 여는 힌트는 입력 말풍선 바로 위 우측의 작은 보조 행동으로 둔다. 기본은 접힘 상태이며, 펼칠 때는 산출을 막는 비화용적 내용 어휘·고유명사·전문용어 두 개만 한 줄로 보여 준다. MPJ에서 학습한 완화 표지나 정답 문장 구조는 제공하지 않고, 열람 여부와 최초 시각을 비채점 trace로 남긴다. 통역에는 힌트를 제공하지 않으며, 완성 답안은 제출 뒤에 공개한다.
- MPJ의 장면·메신저는 판단 대상인 문제 영역으로 가볍게 유지하고, 선지 카드는 네이비 상단선으로 `여기서부터 응답`임을 구분한다.

### 7.2 판단과 피드백

- 화면의 판단 안내는 상황에 따라 달라질 수 있으며 절대적 판결로 표시하지 않는다. 이 원칙을 연구 용어로 길게 고지하지 않고 피드백 문체에 반영한다.
- 의미, 명백한 문법 문제, 화용 초점을 분리한다.
- 의미층은 사실·조건·핵심 화행 내용의 보존을 확인하고, 강도·완화·선택권·명료성·범위 같은 목표 화용 자원의 변화는 상황 적절성 층에서만 다룬다. 이 경계는 요청·거절·감사·사과 등 화행에 관계없이 동일하다.
- “더 길게”, “더 완곡하게”를 일반 정답 규칙으로 가르치지 않는다.
- FixReview는 고치기 전 문장과 교정본 3개의 관계를 먼저 설명한 뒤, 문제가 남은 교정본 하나와 가장 큰 문제 하나를 차례로 묻는다.
- 실패 원인 선택지는 3개로 제한하고 모두 실제 표현과 상황에 결부하되, 핵심 원인과 동등하게 방어 가능해서는 안 된다.
- 피드백 화면은 내 답을 가장 선명하게 보여 준 뒤 `의미 전달 / 문법 정확성 / 상황 적절성`을 동등한 세 기준으로 제시한다. 번호는 쓰지 않으며, 각 카드는 상단의 기준·상태와 하단의 구체적 피드백을 안정된 2단 위계로 보여 준다.
- 주 피드백은 `살펴볼 문제 → 상대에게 들릴 수 있는 인상 → 표현을 바꿀 때 지킬 의미`의 순서로 읽히게 한다. 기존 3층 피드백의 내용을 사용하며 새 점수·판정층·추가 응답을 만들지 않는다.
- `AI`는 피드백 주 제목에서 반복 강조하지 않고 하단 참고 고지에만 남겨, 학습 흐름과 생성 주체 투명성을 함께 지킨다.
- 세 층 모두 통과하면 `이 표현으로 완료`를 주 행동으로, `다른 표현도 시도해보기`를 보조 행동으로 제공한다. 하나라도 수정이 필요하면 `한 번 다듬어보기`로 연결하고, 원문과 다른 수정안을 완료 조건으로 둔다.
- 참고 표현과 의견 남기기는 유지하되, 상세 판정 근거는 학습자 주 흐름에 노출하지 않는다.
- 일반 `담화 전체 확인`은 세 기준과 구체 피드백을 반복하므로 학습자 주 흐름에 표시하지
  않는다. 실제 초점 이탈 경고가 있을 때만 `추가로 확인할 부분`을 조건부로 제시한다.
- 수정 화면은 `이번 수정 목표 → 수정 전 → 수정 후 입력` 순서다. 수정 목표는 AI 피드백 문단을 반복하지 않고 판정 결과에서 도출한 한 줄 행동으로 제시한다.
- 완료 화면은 오늘 익힌 원리, 기존 피드백·재확인·이견에서 가져온 학습 흔적, 수정 전후와 바뀐 부분, 저장 상태와 다음 행동만 남긴다. 학습 흔적은 `이번에 확인한 점 / 수정하면서 지킨 뜻 / 수정 뒤 다시 본 점 / AI와 다르게 본 점`으로 정리하되, 이견이 없으면 해당 행을 만들지 않는다. 추가 수정 뒤 두 번째 자동 재확인을 하지 않은 경우에는 최종 문장을 확인한 것처럼 표시하지 않는다. 판정 결과가 없을 때만 일반 원칙 한 줄을 대체 문구로 사용하고, 표현 목록·라운지 진입을 완료 단계에서 반복하지 않는다.

### 7.3 주장 경계

- **조언 문구는 처방이 아니라 선택지로 제시한다 (불변조건).** 완료 화면의 한 줄 조언과
  수정 목표는 하나의 정답 행동을 지시하지 않고, 학습자가 택할 수 있는 대안적 방식으로
  쓴다(`…해 보세요` 아닌 `…하는 방식도 시도해 볼 수 있어요` 계열). 근거: 상황이 달라지면
  적절한 표현도 달라지므로 단일 처방은 이 연구의 구인과 어긋난다. MPJ가 후보 비교로
  단일 정답 인상을 피하는 것(`TRC-20260729-02`)과 같은 계열의 규칙이며, 화행별 조언
  10종 전부에 적용한다(`3f636f9`).
- 미션 점수 하나로 화용 능력 전체를 나타내지 않는다.
- 번역과 통역의 산출 결과는 매체와 처리 조건이 다른 수행 자료로 구분하여 기록·해석하며, 하나의 점수로 합산하지 않는다.
- 시도 횟수·힌트 감소를 성장 지표로 표시하지 않는다.
- 발음, 유창성, 성조 정확성을 채점하지 않는다.
- 실제 상호작용 능력이나 교육 효과를 자동으로 입증한다고 주장하지 않는다.
- MultiJudge 1·1·1은 세 방향을 대비하는 하나의 학습 세트다. 저장된 최종 분류 3개를 독립 문항 3개처럼 집계하지 않으며, 최초 선택 순서·변경·소거법 사용은 별도 관찰 없이는 추론하지 않는다.

## 8. 라운지

### 8.1 역할

라운지는 핵심 미션의 높은 인지부하를 완충하는 저부담 참여 지원 공간이다.

- 미션과 무관
- 점수·감점 없음
- 연구·학습 수행 기록에 미반영
- 별도 탭과 별도 시각 언어
- 미션 완료를 입장 조건으로 사용하지 않음

### 8.2 경로와 코너

```text
/learner/lounge
/learner/lounge/theater
/learner/lounge/meme
/learner/lounge/decoder
```

| 코너 | 현재 콘텐츠 |
|---|---|
| 생생극장 | 짧은 장면의 표현 뉘앙스 + 선택형 통역 도전 |
| 밈 배틀 | 번역 캡션 중 투표(서버 저장 없음) |
| 해독실 | 관계·채널이 있는 디지털 담화를 한국어 선택으로 해독 |

### 8.3 수준 정책

- 화면에서 입문·중급·고급 선택을 제공하지 않는다.
- 라운지는 하나로 열고 콘텐츠 안에서 표현 난도를 자연스럽게 변주한다.
- 내부 데이터에는 `difficulty` 메타만 남긴다.
- `difficulty`는 향후 순서·추천·교수자 배정에 활용할 수 있으나 현재 개인화 기능은 아니다.
- 프로필의 중국어 수준과 라운지를 자동 매핑하지 않는다.

### 8.4 현재 구현 범위

라운지는 학습자 하단 탭에 노출된 실화면이며 main 배포에 포함돼 있다. 다만 백엔드가 없다.

- 라운지 전용 DB·Edge·관리자 입력 화면 없음
- 밈 투표의 서버 저장 없음
- 생생극장 실제 영상 embed 없음
- 해독실 완료 표시는 기기 localStorage에만 저장
- XP, 배지 경제, streak, 리더보드, 개인화 추천 없음

해독실은 단순 숫자 뜻 퀴즈가 아니라 다음을 한 문항 안에서 연결한다.

```text
발신자·수신자·채널
→ 표현의 뜻과 태도
→ 관계에 맞는 한국어 번역 선택
→ 번역 이유
```

별도 `눈치 체크` 단계는 두지 않는다.

## 9. 기록

기록 화면은 학습자가 실제로 수행한 미션과 수정 이력을 확인하는 곳이다.

- 코어 수나 생성 성공 수를 학습 성취처럼 표시하지 않는다.
- 라운지 활동을 합산하지 않는다.
- 연구동의 상태를 학습 진행률로 표시하지 않는다.
- 기술적·탐색적 과정 자료와 학습자용 진행 표시를 구분한다.

## 10. 현재 완료·미완 범위

### 완료 (구조 층위)

- 3탭 학습자 IA (홈 폐지 — 2026-08-01)
- 수업연계형 강좌·주차 진입
- Can-do 보조 가이드
- 연구동의와 학습 접근 분리
- 주차 학습 노트 UI와 예습·복습 분리
- `mission_v6` MPJ4+독립 DCT1+수정 재확인 스키마·렌더 구현
- 9개 화행의 10개 승인 target feature별 MPJ SUMMARY, 요청 화행에 고정되지 않은 의미·문법·화용 피드백 경계, 의미→목표 화용 축→문법의 단일 수정 우선순위
- 수정 완료 게이트(원문과 실질 동일한 답으로 완료 불가)
- legacy `mission_v1/v2/v3/v4/v5` 렌더 호환
- 라운지 3코너 (탭 노출·배포 포함, 백엔드 없음)

테스트 수·배포 상태 등 검증 기준선은 ACTIVE_HANDOFF와 dev-log가 정본이다.

### 미완 또는 보류

- 정식 콘텐츠 확보 — 현행 모든 코어·미션은 LOCK 전 개발·테스트 콘텐츠이며,
  조건 LOCK 뒤 전량 재생산이 예정돼 있다(`docs/operations/CONTENT_REFRESH_RUNBOOK.md`)
- 라운지 실제 콘텐츠 파이프라인과 관리자 운영
- 셀프 학습 탭
- 학습 효과·전이·지연 효과 검증 (본 연구 범위 밖 — 주장하지 않음)

## 11. 변경 게이트

다음 변경은 먼저 영향 범위를 보고한다.

- 미션 단계·완료 조건 변경
- 학습자 로그 의미 변경
- 스캐폴딩 노출 시점 변경
- 연구동의와 학습 접근 재결합
- 라운지 활동을 연구·학습 기록에 편입
- 수준과 CEFR·ACTFL 직접 매핑
- DB schema, RLS, prompt hash, 배포가 필요한 기능

## 12. 관련 정본

- 현재 작업 상태(날짜 종속 사실의 정본): `docs/handoff/ACTIVE_HANDOFF.md`
- 생성·평가·저장 계약: `docs/contracts/PRAGMA_생성계약_정본.md`
- 관리자 구조: `docs/product/PRAGMA_관리자구조_정본.md`
- 제품·연구 정체성: `docs/research/PRAGMA_PRODUCT_RESEARCH_IDENTITY_2026-07-28.md`
- Can-do 원칙: `docs/research/PRAGMA_CAN_DO_ALIGNMENT_PRINCIPLES_2026-07-28.md`
