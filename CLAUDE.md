# L2 Pragmatic Translator

> ℹ️ PRAGMA 세션은 대개 OneDrive에서 열리며, 그때는 이 파일이 자동 로드되지 않는다.
> 그 경우의 작업 규칙 정본은 `C:\Users\cnkr\OneDrive\CLAUDE.md`(효율·흐름·모델 분업 포함).
> 이 파일은 레포에서 직접 작업할 때의 정본이며 아래 안전·git 규칙은 두 경우 모두 유효하다.

## 프로젝트 작업 원칙

### 작업 원칙

* 프론트와 Edge Function은 하나의 기능 단위로 보고 함께 수정할 수 있다.
* UI, 프론트 로직, Edge Function 코드, 타입, 테스트, 일반 버그 수정은 사전 승인 없이 진행한다.
* 수정 후 `npm run typecheck`는 항상 실행한다. 표적 `test`는 관련 있을 때, 전체 `build`는 **단계 완료 시에만** 실행한다(매 수정마다 build 금지 — 반복 비용). `git diff` 요약을 보고한다.
* DB schema, migration, RLS, Auth, production 배포, 데이터 삭제 또는 대량 수정은 반드시 사전 승인을 받는다.
* Edge Function 코드 편집은 사전 승인 없이 가능하지만, `supabase functions deploy` 등 실제 배포는 production 배포에 해당하므로 반드시 사전 승인을 받는다.
* 요청과 무관한 리팩터링은 하지 않는다.
* 작업 범위가 예상보다 커지거나 위험 변경이 필요해지는 순간 멈추고 보고한다.
* 버그를 코드 수준에서 해결할 수 없고 DB schema 또는 RLS 변경이 필요하다고 확인되는 순간 수정하지 말고 보고한다.
* 사용자 요청이 기존 코드 구조, 데이터 흐름 또는 프로젝트의 연구 목표와 충돌할 가능성이 있으면, 바로 수정하지 말고 충돌 지점과 영향을 먼저 보고한다.
* 요청대로 구현할 경우 기존 기능의 회귀, 기능 중복, 데이터 손실 또는 데이터 의미 변경 가능성이 있으면 수정 전에 위험과 대안을 보고한다.
* 코드 근거가 없는 사항을 사실처럼 추정하지 않는다. 구현을 위해 가정이 필요하면 그 가정을 명시하고, 위험도가 높은 가정이면 멈추고 사용자에게 보고한다.
* 완료 보고에서는 다음을 구분한다.
  * 구현한 것
  * 구현하지 않은 것
  * 아직 검증하지 못한 것
  * 변경으로 새로 생긴 위험 또는 기술 부채

### Git 운영

* 로컬 commit은 조건부로 자율 진행할 수 있다.
* 하나의 명확한 작업 단위가 완료되고 `typecheck`, `test`, `build`가 모두 통과한 경우에만 관련 변경을 묶어 commit한다.
* commit 전 변경 파일과 diff 범위가 사용자 요청과 일치하는지 확인한다.
* commit 메시지는 conventional commit 스타일을 사용한다.
* 여러 기능이나 관련 없는 변경을 하나의 commit에 섞지 않는다.
* push, merge, PR 생성은 반드시 사전 승인을 받는다.
* 검증이 실패하면 commit하지 않고 결과를 보고한다.
* 사용자가 "커밋하지 말라"고 지정하면 commit 없이 diff만 보고한다.
* 아직 push하지 않은 로컬 커밋은 상황에 따라 후속 수정 커밋으로 보완할 수 있다.
* 이미 원격에 push된 커밋을 되돌릴 때는 원칙적으로 `git revert`를 사용해 되돌림 내역을 새 커밋으로 남긴다.
* `git reset` 후 재push, history rewrite, `git push --force` 또는 `--force-with-lease`는 원격 이력을 변경하는 위험 작업이므로 반드시 사전 승인을 받는다.

### 작업 종료 절차

* 사용자가 "오늘 작업은 여기까지", "작업 종료", "마무리해줘" 등 작업 종료 의사를 명시하면 종료 절차를 수행한다.
* 먼저 현재 branch, `git status`, 당일 또는 현재 작업 세션의 commit 목록을 확인한다.
* 완료된 기능은 작업 단위별 commit으로 분리되어 있어야 하며, 여러 기능을 하루치 하나의 큰 commit으로 합치지 않는다.
* working tree가 clean이고 검증을 통과한 commit만 존재하면, 현재 branch를 origin에 push하기 전에 사용자에게 승인을 요청한다.
* 사용자의 승인을 받은 후에만 push한다.
* push 후 branch, 원격 최신 commit, 로컬·원격 동기 여부를 보고한다.
* 미완성 변경, 검증 실패 변경 또는 의도하지 않은 파일 변경이 남아 있으면 억지로 commit하거나 push하지 않고 현재 상태와 남은 작업을 보고한다.
* 사용자가 당일 push를 원하지 않으면 로컬 commit 상태만 유지하고 원격에는 반영하지 않는다.

## 연구·개발 기록 원칙

이 프로젝트는 한중 통번역 학습 워크플로우 웹앱 개발과 DDR 박사학위논문 작성을 함께 진행한다. 따라서 코드 변경뿐 아니라 중요한 설계 결정, 시행착오와 검증 근거를 추적 가능하게 남겨야 한다.

이 원칙은 개발을 방해하는 절대 규칙이 아니라, 논문에 필요한 기록이 유실되지 않도록 하기 위한 기본 운영 지침이다.

### 기록 문서

다음 문서를 연구 기록의 정본으로 사용한다.

```text
docs/research-trail/
├── 01_design_traceability.md
├── 02_decision_log.md
├── 03_iteration_log.md
└── 04_evidence_index.md
```

문서가 없다면 기존 저장소 구조와 문서 형식을 확인한 뒤 생성한다. 이미 존재한다면 새 문서를 중복 생성하지 말고 기존 문서를 갱신한다.

### 작업 종료 시 기록

의미 있는 개발 작업이 끝나면 다음을 수행한다.

1. 기존 `docs/dev-log` 형식을 확인하고, 실제로 조사·수정·검증한 사실만 간결하게 기록한다.
2. 이번 변경이 논문에 남길 만큼 중요한지 판단한다.
3. 연구적으로 중요한 변경인 경우에만 관련 `docs/research-trail` 문서를 갱신한다.
4. 네 문서를 매번 모두 수정하지 말고, 변경 성격과 관련된 문서만 갱신한다.
5. 하루 마지막에는 별도의 개발 일기를 새로 쓰기보다, 당일 중요 작업의 기록 누락 여부만 확인한다.

### Research trail 갱신 기준

다음 중 하나에 해당할 때 연구 기록을 남긴다.

* 학습설계 또는 연구 구성개념이 변경된 경우
* 핵심 학습 흐름이나 사용자 경험이 변경된 경우
* 생성계약, 핵심 데이터 구조 또는 평가 방식이 변경된 경우
* 중요한 설계 대안을 검토하고 채택·기각한 경우
* 구현 실패, 테스트 결과 또는 사용자 피드백으로 설계를 수정한 경우
* 논문에서 설계 근거 또는 반복 개발 과정의 증거로 활용할 수 있는 경우

단순 UI 간격, 색상, 오탈자, import 정리, 경미한 타입 오류나 동작 변화가 없는 리팩터링은 원칙적으로 `research-trail`에 기록하지 않는다. 필요한 경우 `dev-log`에만 기록한다.

### 문서별 역할

* `01_design_traceability.md`
  연구 문제·이론·설계 원리·웹앱 기능·생성 데이터·검증 방법 사이의 연결이 바뀔 때 갱신한다.

* `02_decision_log.md`
  중요한 설계 결정을 내리거나 유력한 대안을 채택·기각했을 때 문제, 대안, 결정, 근거와 관련 커밋을 기록한다.

* `03_iteration_log.md`
  의미 있는 반복 개발 단위별로 시작 문제, 변경 내용, 검증 결과, 예상과 달랐던 점과 다음 설계에 반영할 교훈을 기록한다.

* `04_evidence_index.md`
  Git 커밋·태그·릴리스, 테스트 결과, 스크린샷, 영상, 정본 문서 등 논문 근거로 활용할 증거가 새로 생겼을 때 위치를 기록한다.

### 기록 원칙

1. 중요한 결정은 가능한 한 해당 작업이 끝난 직후 기록하고, 나중에 기억에 의존해 복원하지 않는다.
2. 단순히 “무엇을 완료했다”보다 “왜 변경했는지, 어떤 대안을 검토했는지, 무엇으로 검증했는지”를 우선 기록한다.
3. 사소한 변경까지 연구 기록으로 만들지 않는다.
4. 구현 내용, 테스트 결과와 파일 경로는 실제 확인된 사실만 기록한다.
5. 설계 이유, 대안 또는 연구적 의미가 명확하지 않으면 추측하여 만들어내지 말고 `확인 필요`로 표시한다.
6. 기존 문서의 결정 번호, 반복 번호, 표 형식과 작성 방식을 우선 따른다.
7. 동일한 내용을 여러 문서에 반복하지 말고, 필요한 경우 관련 decision ID, iteration ID, 커밋 또는 파일 경로로 연결한다.

### 작업 종료 보고

작업 종료 시 다음을 간결하게 보고한다.

* 수행한 변경
* 실행한 검증
* 갱신한 `dev-log`
* 갱신한 `research-trail` 문서
* 연구 기록을 갱신하지 않았다면 그 이유
* 사용자의 판단이 필요한 `확인 필요` 항목

기록 작업 자체가 개발보다 커지지 않도록 한다. 핵심 목표는 모든 변경을 기록하는 것이 아니라, 논문에서 중요한 설계 논리와 시행착오가 유실되지 않게 하는 것이다.
